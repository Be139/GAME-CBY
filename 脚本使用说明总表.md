# 脚本使用说明总表

## 2026-07-26 HEARTH 第二套 UI 全系统结构、字幕与终端状态接口

> 本节是第二套 UI 本轮实现的最新覆盖说明。若后面的历史条目仍写着“字幕自动缩字/截断”、
> “教程常驻”或“终端内叠加全屏装饰框”，均以本节为准。正式对白稿、既有按键、住户剧情状态机
> 和七套 V2 功能 Wrapper 的使用方式没有改变。

### HearthUiThemeProfile.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthUiThemeProfile.cs`。
- 资产：`Assets/UI/HEARTH/V2/Profiles/Hearth_UiV2Theme.asset`；这是
  `ScriptableObject`，不挂载到 GameObject。也可通过
  `Assets > Create > Hearth > UI > V2 > Theme Profile` 创建其他主题变体。
- 职责：集中保存第二套 UI 的 Liberation Sans TMP 字体、终端深蓝黑/灰蓝/冷白/信息蓝、
  警告琥珀/成功绿/危险红，以及 2 px 规则线、普通键帽 `64 x 40`、宽键帽
  `96 x 40`、说话人 `22 px`、普通字幕 `28 px`、结局字幕 `30 px` 等公共设计值。
- Inspector 重点：`Primary Font Asset` 应拖入
  `Assets/TextMesh Pro/Resources/Fonts & Materials/LiberationSans SDF.asset`；
  颜色和尺寸应作为全局设计令牌调整，不要在每个 Presenter 中复制一套。
- 公开接口：`PrimaryFontAsset`、`TerminalBackground`、`TerminalPanelBackground`、
  `Secondary`、`Primary`、`Information`、`Warning`、`WarningHighlight`、
  `Success`、`Danger`、`RuleLineThickness`、`RegularKeycapSize`、
  `WideKeycapSize`、`SpeakerFontSize`、`SubtitleFontSize`、
  `EndingSubtitleFontSize`；均为只读运行时访问。
- 引用关系：`HearthUiV2SystemInstaller` 创建并绑定该资产；
  `HearthActionHintPresenter` 和教程生成器读取键帽、字体及颜色。当前
  `HearthUiV2Builder` 仍在 Editor 代码中使用同一套固定色值，后续可逐步改为直接读取本资产。
- Unity 使用步骤：先运行
  `Tools > Hearth > UI V2 > System > Install Profiles And Human Tutorial`；
  日常只在 Project 中编辑该 Profile。不要为了改一个色值重跑整套 V2 Prefab 重建。
- 最小测试：运行
  `Tools > Hearth > UI V2 > System > Validate Profiles And Human Tutorial`，
  应确认 Profile 存在且已绑定 Liberation Sans；Game View 中普通与宽键帽尺寸应有明显区分。
- 替换/扩展点：要制作高对比、色盲或其他终端主题时，复制该 Profile 并让 Presenter
  换引用即可；不要把动态文字或状态颜色烘焙进 PNG。

### HearthUiLayoutProfile.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthUiLayoutProfile.cs`。
- 资产：`Assets/UI/HEARTH/V2/Profiles/Hearth_UiV2Layout_1920x1080.asset`；
  不挂载到 GameObject。也可通过
  `Assets > Create > Hearth > UI > V2 > Layout Profile` 新建布局变体。
- 职责：以左上角为原点记录 1920×1080 设计坐标，统一安全区和 Human 身份、当前任务、
  地点、字幕、初始教程、动态 E 提示、终端顶栏、右上动作、内容、消息和底栏区域。
- 主要默认矩形：
  - 全局安全区 `48,40,1824,1000`，终端安全区 `96,64,1728,968`。
  - Human 身份 `64,48,432,96`，当前任务 `1408,48,448,104`，
    地点 `64,944,360,80`。
  - 字幕说话人 `480,748,960,32`，正文 `320,792,1280,96`，
    初始教程 `1136,928,720,96`，动态交互 `660,688,600,64`。
  - 终端顶栏 `120,72,1680,140`，右上动作 `1480,148,320,56`，
    内容 `120,232,1680,528`，消息 `320,790,1280,120`，
    底栏 `96,968,1728,64`。
- 公开接口：`ReferenceResolution`、`GetRegion(HearthUiLayoutRegion)`；
  `HearthUiReferenceRect` 提供 `Left/Top/Width/Height/Right/Bottom/Size`、
  `ToBottomLeftRect(...)` 和 `ApplyTopLeftAnchors(...)`。
- 引用关系：`HearthUiV2SystemInstaller` 用 `InitialHumanTutorial` 区域生成教程；
  后续 Human、Companion、Terminal Presenter 应逐步从该 Profile 取坐标。
- Unity 使用步骤：保持 CanvasScaler 为 `Scale With Screen Size / 1920 x 1080`；
  调整 Profile 后重新运行对应的定向安装/生成菜单，不要用场景缩放代替坐标修改。
- 最小测试：系统 Validator 应确认参考分辨率为 1920×1080；再检查 1920×1080、
  1280×720、2560×1440 Game View，锚点应随 CanvasScaler 缩放而不漂移。
- 替换/扩展点：新增 UI 区域时先扩展 `HearthUiLayoutRegion` 和
  `GetRegion(...)`，再让具体视图读取；不要在多个 Builder 中各写一套同名坐标。

### HearthUiStateCoordinator.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthUiStateCoordinator.cs`。
- 当前挂载对象：由 `HearthUiV2SystemInstaller` 自动挂在
  `Assets/Prefabs/UI/HearthHud/V2/HearthHudRoot_V2.prefab` 根对象；
  同一对象只允许一个。它只汇总 UI 状态，不接管 Human HUD、终端、字幕或剧情控制器。
- Inspector：
  - `Automatically Resolve Runtime State=true`，运行时自动读取真实状态。
  - `Apply Resolved State To Canvas Groups=false` 是当前正式安全值；六个 Layer
    CanvasGroup 暂不绑定，避免与现有页面控制器重复改 Alpha。
  - `Human Hud` 由 Installer 固定绑定；ViewSwitch、字幕播放器和关机挑战在 Play 时自动发现。
- 真实状态源：`HearthFirstPersonHudController.CurrentPageId` 与页面
  `FullscreenTakeover`、`HearthTvTerminalController.AnyTerminalOpen`、
  所有 `MinLoopSubtitlePlayer.IsPlaying`、正式 `ViewSwitchController`、
  `HearthPlayerControlLock.AnyControlsLocked`、`Time.timeScale`，以及所有
  `HearthShutdownChallenge.IsRunning`。
- 职责：只解析 `Persistent/Dialogue/Interaction/Terminal/Modal/Takeover`
  的显隐优先级，不读取键盘、不推进剧情、不反向调用控制器。优先级为
  `Takeover > Modal > Terminal > 普通 HUD`；普通状态下 Dialogue 可与 Persistent 同在，
  但 Dialogue、控制锁、暂停和非 Human 视角会抑制 Interaction。
- 安全处理：即使日后启用 CanvasGroup 应用，同一个 CanvasGroup 被误绑到多个层时也只会
  聚合应用一次，较高层的 Visible 不会被后续低层写回 Hidden；Validator 会报告重复绑定。
- 公开接口：`RequestedState`、`ResolvedState`、`HasResolvedState`、
  `ResolvedStateChanged`、`AutomaticallyResolveRuntimeState`、
  `AppliesResolvedStateToCanvasGroups`、`HumanHud`、`HasHumanHudBinding`、
  `HasDuplicateLayerBindings`、`ConfigureRuntimeSources(...)`、
  `SetRuntimeIntegration(...)`、`ResolveRuntimeSources()`、`RefreshRuntimeState()`、
  `Configure(...)`、`ApplyRequestedState(...)`、`SetLayerRequested(...)`、
  `Refresh()`，以及可独立测试的静态 `Resolve(...)`。
- 数据接口：`HearthUiVisibilityState.Gameplay` 是默认普通玩法状态；
  `GetLayer(...)`、`WithLayer(...)` 可生成和查询不可变式状态副本。
- 引用关系：`HearthInitialGameplayTutorialController` 优先查询其 `ResolvedState`；
  现有终端、字幕、页面、ViewSwitch、控制锁和关机挑战仍拥有各自执行权，协调器只读。
- Unity 使用步骤：运行
  `Tools > Hearth > UI V2 > System > Install Profiles And Human Tutorial`；
  不要手工给六层绑定现有页面自己管理的同一 CanvasGroup。只有未来拆出专用层根并完成全流程
  验收后，才把 `Apply Resolved State To Canvas Groups` 打开。
- 最小测试：Validator 确认 Coordinator 已启用、自动解析、绑定 Human HUD、保持被动模式，
  且教程引用同一实例；Play 中普通 Human 为 Persistent+Interaction，字幕抑制 Interaction，
  终端解析为 Terminal，Tab/选择页为 Modal，结局或关机挑战为 Takeover。
- 替换/扩展点：若以后接入统一 InputRouter，可监听 `ResolvedStateChanged` 切输入上下文；
  不要把输入处理或剧情推进塞进本脚本。本轮改变了运行接入与 Inspector 绑定，
  但没有改变任何既有控制器的挂载方式。

### HearthActionHintState.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthActionHintState.cs`。
- 类型：纯状态数据，不挂 GameObject、无 Inspector 组件。
- 职责：让剧情/输入状态机描述“现在允许显示哪些键位”，把状态所有权和视觉渲染分开。
  单个 `HearthActionHintItem` 保存 Key、Action、是否可用和是否使用宽键帽。
- 优先级枚举：`None < InitialTutorial < DynamicInteraction < Menu <
  ChoiceOrHold < Fullscreen < Takeover`。该数值供上层状态机仲裁；
  `HearthActionHintPresenter` 不会自动比较两个 State 的优先级。
- 公开接口：`HearthActionHintState.Hidden`、`Visible`、`Priority`、`ContextId`、
  `StatusMessage`、`ItemCount`、`GetItem(index)`；Item 提供 `KeyLabel`、
  `ActionLabel`、`Available`、`WideKeycap`。
- 引用关系：`HearthInitialGameplayTutorialController` 构造初始教程 State；
  未来动态 E、终端、Hold E、Modal/Takeover 都应各自从现有状态机提供 State。
- 使用步骤：上层控制器根据真实可执行动作创建 State，再调用 Presenter 的
  `Apply(state)`；不可执行动作可以保留并设 `Available=false`，不要靠删文字表达锁定。
- 最小测试：构造普通 E、宽 SPACE 和 disabled 三种 Item；Presenter 应分别使用
  `64×40`、`96×40` 和降低透明度，Hidden 应关闭整组。
- 替换/扩展点：需要手柄提示时扩展 Item 的设备/图标语义并由 Presenter 映射；
  不要把 Input.GetKey 写进本数据类。

### HearthActionHintPresenter.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthActionHintPresenter.cs`。
- 当前挂载对象：V2 Human Prefab 的
  `PersistentHudLayer/V2_InitialTutorialRoot`；也可以挂到终端、Hold 或 Takeover
  的独立提示根，但每个上下文应有自己的 Presenter。
- 必需组件/字段：根对象需要 `CanvasGroup`；每个 `HearthActionHintSlot` 要绑定
  Slot Root、Key TMP、Action TMP 和 Keycap Rect。可选 `Status Text`；
  `Theme Profile` 应拖 `Hearth_UiV2Theme.asset`。
- 职责：被动渲染一个 `HearthActionHintState`，负责显隐、文字、不可用透明度和
  64/96 px 键帽尺寸；不读取任何按键，也不决定教程/终端/模态谁优先。
- 公开接口：Presenter 的 `CurrentState`、`Configure(...)`、`Apply(...)`；
  Slot 的 `Configure(...)`、`Render(...)`、`Hide()`。
- 引用关系：初始教程控制器调用 Presenter；Theme Profile 提供键帽尺寸。
  未来应由 `HearthActionHintState` 的上层仲裁器调用，而不是借用字幕播放器。
- Unity 使用步骤：创建非交互 CanvasGroup 和若干 Slot，拖好 TMP/Rect 引用；
  调用 `Configure` 后默认 Hidden，再由状态机 `Apply` 当前 State。
- 最小测试：显示四个提示后，多余 Slot 应隐藏，CanvasGroup 不可交互且不阻挡 Raycast；
  SPACE 应自动使用宽键帽。
- 替换/扩展点：更换键帽视觉只替换 Slot 下的 Image/材质或 Theme 尺寸；
  保留 Presenter 的数据接口，避免改剧情调用。

### HearthTerminalViewState.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthTerminalViewState.cs`。
- 类型：终端视图数据，不挂 GameObject。每个 `HearthTvTerminalController` 内部拥有一个实例。
- 职责：统一暴露终端是否可见、住户 ID、页码/总页数、Before/After Tab、当前焦点、
  右上主动作、锁定状态、`PLEASE WAIT` 和 Esc 是否可用；它不读取输入、不执行动作。
- 固定动作标签：`ReviewArchivedEvent -> REVIEW ARCHIVED EVENT`、
  `EnterUnit -> ENTER UNIT`、`EnterHome -> ENTER HOME`，Custom 使用调用方标签。
- 公开只读接口：`Visible`、`TerminalId`、`PageIndex`、`PageCount`、
  `SelectedTab`、`FocusTarget`、`PrimaryActionType`、`PrimaryActionVisible`、
  `PrimaryActionLocked`、`PrimaryActionExecutable`、`PrimaryActionLabel`、
  `StatusMessage`、`CanExit`。
- 写入接口：`SetVisible`、`SetTerminalId`、`SetPage`、`SetNavigation`、
  `SetPrimaryAction`、`SetCanExit`；反序列化时会自动把页码限制在合法范围。
- 引用关系：只应由 `HearthTvTerminalController` 按真实运行状态更新；
  终端 Presenter 可订阅控制器的 `TerminalViewStateChanged`，但不能反向推进剧情。
- 使用步骤：不要在 Inspector 另建或挂载本类；从当前终端控制器读取
  `TerminalViewState`。剧情锁定时调用终端控制器
  `SetPrimaryActionInputEnabled(false)`，状态会保留动作并返回 `PLEASE WAIT`。
- 最小测试：锁定 17F01/02 时右上动作仍存在但不可执行，状态文本为
  `PLEASE WAIT`；17F03 为 `ENTER UNIT`，17F04 为 `ENTER HOME`。
- 替换/扩展点：新增动作先扩展 `HearthTerminalPrimaryActionType` 和标签映射，
  再在终端控制器中映射，避免按页面索引猜动作。

### HearthInitialGameplayTutorialController.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthInitialGameplayTutorialController.cs`。
- 挂载对象：由系统安装器自动挂在 V2 Human Prefab 的
  `PersistentHudLayer/V2_InitialTutorialRoot`，与 `Canvas`、`CanvasGroup`、
  `Image`、`HearthActionHintPresenter` 同对象。不要挂到字幕播放器。
- Inspector 重点：`Presenter`、`Canvas Group`、`Human Hud` 由安装器绑定；
  `Ui State Coordinator` 也由安装器绑定；View Switch Controller、Subtitle Players
  可留空并仅在协调器缺失时作为回退自动解析。
  `Effective Gameplay Seconds=10`、`Fade Out Seconds=0.35`；
  `Persist Completion` 决定是否写 PlayerPrefs。
- 职责：只累计“有效 Human Gameplay”时间。暂停、玩家控制锁、任一终端打开、
  非 Human/正在切换视角、Human 模态页面或正式字幕播放期间会暂停计时并隐藏提示；
  返回普通玩法后继续剩余时间，最后用 Unscaled Time 淡出。
- 当前教程内容：`WASD MOVE`、`MOUSE LOOK`、`E INTERACT`、`TAB MENU`；
  不提供错误的 `SPACE CONTINUE`。
- 公开接口：`ElapsedGameplaySeconds`、`IsRunning`、`Configure(...)`、
  `UiStateCoordinator`、`SetUiStateCoordinator(...)`、`RestartForPreview()`、
  静态 `ResetCompletionForCurrentRun()`。
- 引用关系：优先读取统一 `HearthUiStateCoordinator.ResolvedState`；只有协调器未安装或
  尚未完成首次解析时，才回退读取终端、控制锁、ViewSwitch、Human 页面和正式字幕。
  它只调用 Action Hint Presenter 显示，不会停止字幕或推进剧情。
- Unity 使用步骤：先运行系统 Installer；需要反复验收时运行
  `Tools > Hearth > UI V2 > System > Reset Initial Tutorial Completion`，
  再进入 Play。普通玩家流程不应每次开 Tab 都重置教程。
- 最小测试：普通 Human 玩法累计 10 秒后 0.35 秒淡出；在字幕、终端、Tab 菜单、
  TimeScale 0 或 Companion 视角中等待不应消耗计时，且低优先级教程不得盖住这些界面。
- 替换/扩展点：若改教程键位或增加手柄支持，修改上层 State/安装器生成的 Slot；
  不要把教程台词送入 `MinLoopSubtitlePlayer`。挂载方式为本轮新加，现有 Human
  HUD 控制器的挂载和 Inspector 使用方式无变化。

### HearthSubtitleStyleProfile.cs

- 路径：`Assets/Scripts/MinLoop/HearthSubtitleStyleProfile.cs`。
- 资产：`Assets/Data/MinLoop/UI/Hearth_SubtitleStyle.asset`，不挂 GameObject。
- 职责：统一正式对白的 `StandardDialogue / CenteredEpilogue / TimeCard` 布局，
  并提供 `Human / FieldUnit / Terminal` 三种宽度、底色、强调线和说话人颜色变体。
- 最新文字规则：普通说话人固定 `22 px`、正文固定 `28 px`、结局固定 `30 px`，
  TimeCard 正文 `34 px`；`bodyMaximumLines` 和 `maximumBodyHeightPixels` 只作为 QA
  预算，运行时不得按它们截断正式对白。此规则覆盖 2026-07-17 的自动缩字/截断旧说明。
- Inspector 重点：`Standard Dialogue` 负责普通对白；`Centered Epilogue`
  负责结局；`Human/Field Unit/Terminal` 负责上下文宽度和视觉温度。
  当前 Field Unit 宽度倍率 `0.84`，Terminal `0.76`，Human `1.0`。
- 公开接口：`TextColor`、`GetLayout(mode)`、`GetContext(context)`、
  `GetSpeakerFontSize(mode)`、`GetBodyFontSize(mode)`、`TimeCardFadeSeconds`、
  `ApplyProductionDefaults()`。
- 引用关系：所有正式 `MinLoopSubtitlePlayer` 共享本资产；
  `HearthSubtitleV2VisualBuilder` 只在明确执行
  `Apply Production Profile Defaults` 时才重置它，单纯重建 Visual Prefab 不覆盖 Profile。
- Unity 使用步骤：日常在 Project 中编辑共享资产；确需恢复本轮基线时运行
  `Tools > Hearth > UI V2 > Subtitles > Apply Production Profile Defaults`，
  然后检查所有住户长对白。
- 最小测试：使用当前最长人物对白和 Field Unit 对白；字号应保持固定、全文可见，
  背板随内容向上增长，不得出现穿线、截断或教程重叠。
- 替换/扩展点：新增说话环境应扩展 `HearthSubtitleContext` 和 `GetContext`；
  新增演出模式应扩展 Presentation Mode，不要复制新的字幕播放器。

### MinLoopSubtitlePlayer.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopSubtitlePlayer.cs`。
- 挂载对象：继续使用场景中现有正式 `MinLoopSubtitlePlayer` 对象，挂载方式无变化；
  本轮只为每个播放器绑定了明确的
  `HearthSubtitleVisualCanvas_V2/VisualRoot`，没有更改住户流程控制器的调用入口。
- 必需组件/字段：
  - `Style Profile` 拖 `Hearth_SubtitleStyle.asset`；Presentation/Default Context
    按用途选择，普通播放器可开启按 Speaker 自动识别。
  - `Visual Root/Layout Root/Backdrop/Accent Rule/Speaker/Body/CanvasGroup`
    应全部绑定 V2 VisualRoot；正式场景 `Create Fallback UI` 必须关闭。
  - 语音仍使用 `AudioSource` 和可选 `HearthAudioChannelSource`；
    `Subtitle Sorting Order` 应高于普通 HUD，当前正式播放器分别按场景需要使用
    8500 或更高顺序。
- 职责：只播放正式对白、语音和作者定义的 TimeCard。教程和操作提示必须使用
  Action Hint Presenter，不能调用本播放器的 `Stop/ShowLine` 抢占正在播放的对白。
- 最新排版：TMP 关闭 Auto Size，启用 Word Wrapping，固定字号，
  `maxVisibleLines=999`、`Overflow`；根据 `GetPreferredValues` 计算真实高度，
  Body、Speaker、Backdrop 和 2 px Accent Rule 一起向上适配。
- 上下文：可按 Speaker 自动识别 Terminal/Archive/Doorway/Synth Voice 为 Terminal，
  Field Unit/Companion/Mia 为 FieldUnit，其他使用 Human；也可显式设置。
- 公开接口：`IsPlaying`、`StyleProfile`、`PresentationMode`、`VisualRoot`、
  `ActiveContext`、两种 `PlaySequence(...)`、`PlayLines(...)`、
  `PlaySequenceAsset(...)`、`ShowLine(...)`、`SetPresentation(...)`、
  `SetSubtitleContext(...)`、`UseAutomaticSubtitleContext()`、`Hide()`、`Stop()`。
- 时长规则：有 VoiceClip 时继续按 Dialogue Line 的 Duration Mode 使用语音长度/
  尾长；无语音时才使用逐句或默认 Hold。正式对白资产和调用方式无变化。
- 引用关系：由现有 Lobby、17F01-04、Replay 和 Finale 控制器调用；
  `HearthSubtitleV2VisualBuilder` 负责可视根绑定，Style Profile 负责布局。
- Unity 使用步骤：运行
  `Tools > Hearth > UI V2 > Subtitles > Build And Bind Open Scene`，
  检查 Game View 后手动保存场景；日常修改对白仍走正式 Markdown 同步链路，
  不在 VisualRoot 中烘焙文字。
- 最小测试：运行
  `Tools > Hearth > UI V2 > Subtitles > Validate Open Scene`；
  应确认每个播放器有明确 VisualRoot、固定 TMP、Overflow 和关闭 fallback。
  Play 最长对白时全文完整、说话人名称正确、无 `SPACE CONTINUE`。
- 替换/扩展点：换字体/底板改共享 Profile 或 V2 Subtitle Prefab；
  换对白/语音仍改 `HearthDialogueSequence`，不要改 Presenter 视觉。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`；
  继续挂在 Lobby、17F01、17F02、17F03、17F04 每台终端的内容根，
  Canvas、页面、相机、玩家锁、音频和剧情 UnityEvent 的挂载使用方式无变化。
- 本轮职责扩展：在保留原输入/剧情执行权的同时维护一个只读
  `HearthTerminalViewState`，让视觉层能稳定读取页码、焦点、主动作和锁定状态。
- 新公开接口：`TerminalViewState` 和事件 `TerminalViewStateChanged`。
  既有 `OpenTerminal/CloseTerminal/ShowPage/RequestRobotReplay/RequestEnterUnit/
  RequestCustomPrimaryAction/ShowPostReplayChoicePage` 等接口保持兼容。
- 状态与动作映射：
  - `RequestReplay -> REVIEW ARCHIVED EVENT`。
  - `EnterUnit -> ENTER UNIT`。
  - 住户 ID `17F04` 固定暴露 `ENTER HOME`。
  - Custom 暴露自定义/确认动作。
- 锁定规则：剧情对白未结束时调用 `SetPrimaryActionInputEnabled(false)`；
  右上动作仍由 V2 视觉保持可见，ViewState 标记 Locked，
  Runtime Prompt 显示 `PLEASE WAIT`。恢复为 true 后，聚焦动作时显示
  `SPACE <动作>`。
- 输入规则不变：Tab 浏览，Left/Right（选择页可按既有 Up/Down 配置）移动焦点，
  Space 只执行当前可执行动作，Esc 退出；Presenter 不自行读键。
- 重要 Inspector：原 `Keyboard Hint/Focus/Runtime Prompt` TMP 应绑定 V2 Footer；
  `Primary Action`、`Replay Resident Id`、`Submit Primary Action From Current Page`
  和各 UnityEvent 必须继续由住户 Binder/Prefab 保持。相机仍须满足
  Terminal Hardware Root 所有权规则。
- 引用关系：继续依赖 `HearthPlayerControlLock`、`ViewSwitchController`、
  `MinLoopFlowController`、`HearthTerminalCameraTransition` 和住户 Flow；
  Human/Companion 输入读取 `AnyTerminalOpen`。ViewState 只是输出，不取代这些系统。
- Unity 使用步骤：不要手工移动主动作到最后一页；用 V2 Builder 的定向视觉刷新保持
  Before/After/右上动作在所有页面，然后运行住户 Binder/Validator 检查功能引用。
- 最小测试：五台终端分别打开、稳定、退出；17F01/02 的 Review、
  17F03 的 Enter Unit、17F04 的 Enter Home 在顶栏始终存在；
  Locked 时 Space 不推进且显示 `PLEASE WAIT`，解锁后原按键链路继续工作。
- 替换/扩展点：新终端 Presenter 订阅 `TerminalViewStateChanged`；
  不要从 TMP 文本或当前页对象反推剧情状态。挂载方式和既有 Inspector 引用没有改变。

### HearthUiV2Builder.cs

- 路径：`Assets/Editor/HearthUiV2Builder.cs`；Editor 工具，不挂 GameObject。
- 主要菜单：
  - `Tools > Hearth > UI V2 > Refresh Existing V2 Prefab Visuals`：
    日常定向刷新七套现有 V2 Prefab 的视觉树，保留功能根和序列化玩法绑定。
  - `Rebuild All V2 UI Assets`：从 Legacy 克隆七套 V2；会大范围重建，只能在明确需要
    全量重建并已备份/验证时使用，不能代替日常修复。
  - `Use V2 UI In Open Scene`、`Use Legacy UI In Open Scene`：
    带预检、引用重映射、Undo 和失败回滚的整套切换。
  - `Validate Open Scene UI` 及 Preview 子菜单：结构验证和 Play Mode 预览。
- 输出：Human、Companion、17F01、17F02、17F03、17F04、Lobby 共七个
  `Assets/Prefabs/UI/HearthHud/V2/` 下的 Prefab；根功能 Wrapper、场景剧情引用
  和输入控制器继续沿用原结构。
- 最新视觉规则：
  - 动态板、按钮、键帽和终端内容使用 Unity Image、2 px 规则线与 TMP，
    不再把 GeneratedParts 全屏框/大按钮框当通用容器。
  - Human 无常驻 E/TAB/SPACE Footer；交互提示和教程分时显示。
  - 实体电视是终端唯一外框，终端内容和每页不再叠第二、第三个全屏边框。
  - Household 所有页面同时保留 Before、After 和右上动作；Lobby 的
    `PLEASE WAIT/SPACE CLOSE TERMINAL` 由运行时 Flow 提供，不烘焙静态提示。
  - TMP 关闭 Auto Size/Truncate，使用真实文字区域和 Overflow。
- 依赖关系：需要七套 Legacy/V2 Prefab、`HearthUiThemeMarker`、
  Human/Companion/Terminal 控制器和 `HearthRuntimeTopologyTools`。
  切换仅复制白名单功能字段及必要无参剧情回调，不全量复制视觉 MonoBehaviour。
- 重要安全边界：定向视觉刷新会先删除目标 Prefab 中名称以 `V2_` 开头的旧视觉子树；
  `V2_InitialTutorialRoot` 也在此范围。因此每次运行
  `Refresh Existing V2 Prefab Visuals` 后，必须紧接着运行
  `System > Install Profiles And Human Tutorial`，再做两项 Validate。
- Unity 使用步骤：先保存场景并运行 Open Scene UI/Runtime Topology Validator；
  视觉修改优先跑 Refresh，不跑 Rebuild；随后重装教程、验证字幕、进入 Play 截图。
- 最小测试：七槽位唯一、一个正式 ViewSwitch、五台终端自有 Camera 和必要剧情回调
  全部通过；Human Tab 只有一个选中框，终端只有实体电视外框，Footer 在页面上方清晰可见。
- 替换/扩展点：新增页面先保持现有 Wrapper/页面 ID，再在 `Style...` 函数中替换
  `V2_` VisualRoot；若以后让 Builder 读取 Theme/Layout Profile，保持现有功能字段白名单
  和事务切换边界不变。

### HearthSubtitleV2VisualBuilder.cs

- 路径：`Assets/Editor/HearthSubtitleV2VisualBuilder.cs`；Editor 工具，不挂 GameObject。
- 输出 Prefab：
  `Assets/Prefabs/UI/HearthSubtitle/V2/HearthSubtitleVisualCanvas_V2.prefab`，
  结构为 1920×1080 Screen Space Overlay Canvas（默认排序 8500）下的
  `VisualRoot/Backdrop/AccentRule/Speaker/Body`。
- 菜单：
  - `Apply Production Profile Defaults`：唯一会明确重置共享 Subtitle Profile 的动作。
  - `Build Visual Prefab`：只重建无文字视觉 Prefab，不覆盖 Profile。
  - `Bind Open Scene Players`：为打开场景所有 Subtitle Player 实例化并绑定 VisualRoot。
  - `Build And Bind Open Scene`：执行构建、绑定和验证。
  - `Validate Open Scene`：检查明确根、TMP、CanvasGroup、固定字号、Overflow 和 fallback。
- 绑定行为：关闭旧生成根，写入 `visualRoot/layoutRoot/backdrop/accent/speaker/body/
  canvasGroup`，并把 `createFallbackUI` 设为 false；只标记场景 Dirty，不自动保存。
- 依赖关系：使用 `HearthDialoguePresentationBinder.EnsureSharedProfile()` 取得共享
  `HearthSubtitleStyleProfile`，向 `MinLoopSubtitlePlayer` 写引用；
  不改 `HearthDialogueSequence` 或正式 Markdown。
- Unity 使用步骤：需要新建或修复视觉根时运行 Build And Bind，检查 Game View 后手动保存；
  只改颜色/字号时编辑 Profile，不必重建 Prefab。
- 最小测试：Validator 输出所有播放器通过；Hierarchy 不再出现运行时临时 fallback Canvas，
  最长对白完整显示，VisualRoot 无固定对白文字。
- 替换/扩展点：更换字幕底板在该 Prefab 保留透明中心、Backdrop/Accent/Speaker/Body
  稳定名称；不要把教程或操作 CTA 放进此 Prefab。

### HearthUiV2SystemInstaller.cs

- 路径：`Assets/Editor/HearthUiV2SystemInstaller.cs`；Editor 工具，不挂 GameObject。
- 菜单：
  - `Tools > Hearth > UI V2 > System > Install Profiles And Human Tutorial`。
  - `Reset Initial Tutorial Completion`。
  - `Validate Profiles And Human Tutorial`。
- 职责：创建/维护 Theme 和 1920×1080 Layout Profile，绑定 Liberation Sans，
  在 `HearthHudRoot_V2.prefab` 根安装并绑定一个被动、自动解析的
  `HearthUiStateCoordinator`，再只在 Persistent 层重建
  `V2_InitialTutorialRoot`；不会替换 Human 功能根或剧情引用。
- 生成组件：教程根含 RectTransform、Image、独立排序 Canvas（8200）、
  CanvasGroup、Action Hint Presenter、Initial Tutorial Controller；
  子 Slot 全部使用无 Sprite 的平面 Image、2 px 规则线和 TMP。教程控制器会固定引用
  Prefab 根上的同一个 Coordinator。
- 依赖关系：需要 V2 Human Prefab、LiberationSans SDF、
  `HearthUiThemeProfile`、`HearthUiLayoutProfile`、`HearthUiStateCoordinator`
  和 Action Hint 两个运行时类。
- Unity 使用步骤：退出 Play Mode后运行 Install；尤其在 V2 Builder 的视觉 Refresh 后
  必须重跑一次。需要复测首生教程时运行 Reset，而不是删除 Prefs 或改脚本。
- 最小测试：Validate 应确认两个 Profile、字体、Coordinator 已启用并绑定 Human HUD、
  自动解析开启、CanvasGroup 直接应用关闭、教程引用同一 Coordinator、
  Presenter/Timer 均存在，且教程 Image 没有任何烘焙 Sprite；
  Play 中验证 10 秒有效计时和 0.35 秒淡出。
- 替换/扩展点：调整教程排布优先改 Layout/Theme 和 `BuildTutorial`；
  若改根名称，必须同步 V2 Builder 的清理/重装顺序和 Validator。

### HearthUiV2CriticalBindingRepair.cs

- 路径：`Assets/Editor/HearthUiV2CriticalBindingRepair.cs`；Editor 工具，不挂 GameObject。
- 菜单：`Tools > Hearth > UI V2 > Repair 17F01-02 Critical Bindings`。
- 职责：只修复 17F01、17F02 两台终端共六个关键引用：
  各自的 `MinLoopFlowController`、Human `PlayerInteraction` 和 Human Player Camera。
  它不重建 UI、不移动 Transform、不替换终端自有 Camera/Hardware Root。
- 识别规则：打开场景必须恰有一个 MinLoopFlowController；优先选择路径含
  `Player/Person Controller` 的 PlayerInteraction；终端同时要求
  `GetReplayResidentId()` 和对象名称都匹配 17F01/17F02，避免把 Lobby 误判为第一户。
- 安全边界：Play Mode 不运行；必要对象不唯一或缺失时在修改前终止；
  修改纳入一个 Undo Group，场景保存失败会回滚，不留下半套引用。
- 调用接口：内部只调用终端既有的 `SetMinLoopFlowController(...)`、
  `SetPlayerInteraction(...)`、`SetPlayerCamera(...)`，并记录 Prefab Instance Override。
- Unity 使用步骤：只有住户 Validator 报这六项丢失时才运行；运行后再执行
  17F01、17F02 Validate 和 Runtime Topology Validate，不要用整套 Builder 覆盖现场。
- 最小测试：两户静态验证通过；Play 中分别打开两台终端，使用 Human 进入前相机，
  退出后恢复移动/视角，Replay 仍交给唯一 MinLoop Flow。
- 替换/扩展点：如果以后需要修第三户或其他字段，应新增显式住户规则和预检；
  不要放宽成按第一个终端/第一个 Camera 猜测。

## 2026-07-12 玩家启动飞出楼层与 17F03 实体碰撞修复

### Hearth17F03MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F03MinimalLoopBinder.cs`。
- 菜单：`Tools / Hearth / Replay / Apply 17F03 Minimal Loop Setup`；检查菜单为 `Tools / Hearth / Replay / Validate 17F03 Minimal Loop Setup`。
- 根因：实体 `GameObject/ROBOT` 根节点曾带有默认 `BoxCollider`。该模型的导入缩放约为 65-100 倍，导致碰撞范围膨胀到约 `76 x 100 x 69m`，正式人类和机器人出生时都处于碰撞体内部，Unity 会把角色推出 17 楼。
- 当前结构：绑定工具会删除 `ROBOT` 根节点上的旧 `BoxCollider`，并生成两个职责分离的子物体：
  - `PhysicalBodyCollider_17F03`：启用的非 Trigger `BoxCollider`，尺寸贴合实体机器人，用来阻挡玩家穿过模型。
  - `InteractionVolume_17F03`：稍大的 Trigger `BoxCollider`，挂载 `Hearth17F03UnitInteractable`，只负责中心射线和 E 键检查交互。
- Inspector 调整：需要修改实体阻挡范围时，选中 `PhysicalBodyCollider_17F03` 修改 `BoxCollider/Center/Size`；需要修改 E 键命中范围时，选中 `InteractionVolume_17F03` 修改 `BoxCollider/Center/Size`。重新运行 Apply 菜单会按 Renderer 范围恢复自动计算值。
- 验证能力：Validate 菜单现在会检查根节点是否残留巨大碰撞体、两个子碰撞体的类型是否正确，以及正式 `Player/Person Controller`、`Player/Robot Controller` 是否在出生时嵌入任何阻挡碰撞体超过 `0.05m`。
- 依赖关系：`HearthCompanion17F03ReplayController` 继续控制实体 ROBOT 的整体显隐；`Hearth17F03UnitInteractable.SetAvailable(bool)` 只控制交互 Trigger 的启用时机，实体阻挡碰撞保持随 ROBOT 一起显隐。
- 公开接口：无新增运行时公开接口，原有 `Hearth17F03UnitInteractable` 接口和第三户剧情调用方式不变。
- 最小测试：无输入启动游戏 10 秒，玩家应留在 17 楼且可正常移动；父母台词结束后看向实体 ROBOT 才显示 E 提示；进入回放后实体 ROBOT 隐藏，其实体碰撞也随父物体一起消失。
- 本次 MCP 实测：无输入运行 `10.7s` 后，人类位置保持 `(-2318.584, 111.684, 591.006)`，机器人位置保持 `(-2313.054, 111.684, 593.819)`，两者速度均为零；`Time.timeScale = 1`、流程为 `Corridor`、玩家控制未锁定。
- 回归实测：17F01 胶囊交互仍为 `0.71m`；17F02 长按提示显示后持续存在；17F03 第三幕只有女儿激活，Animator Driver 能依次进入 `Walk -> Talking -> EnteringCode`，人类、正式机器人和三个参考控制器的占位胶囊 Renderer 均不可见。
- Console 无本次新增 Error；仍存在旧 Door Missing Script、第三方 Animator 状态、负缩放 BoxCollider 和中文 TMP fallback 警告，未纳入本次碰撞修复。

## 2026-07-11 共享交互提示与 17F03 第三幕动作修正

### HearthCompanion17F03ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F03ReplayController.cs`。
- 挂载对象：`MIN_LOOP_ROOT/ReplayRoom_17F03`。
- 本次根因修复：17F03 以前会在 `LateUpdate` 每帧刷新共享的 `HearthCompanionHoldPrompt`，即使 17F03 没有运行，也会把 17F01/17F02 已显示的按钮关掉。现在只有 `AwaitingDaughter`、`AwaitingMother` 两个 17F03 目标阶段能刷新该提示。
- 使用方式无变化：17F01 射线长度仍为 `0.71`；17F02 仍在倾诉结束并等待 `1.5s` 后显示长按提示。
- 第三幕动作顺序固定为：`Walk` 沿夜间路径移动 -> 抵达终点立即循环 `Talking` -> 夜间倾诉与关机前对白全部结束 -> 播放一次 `EnteringCode`。
- 正式剧情演员上的 `CityPeople.CityPeople` 自动随机动画组件会在运行前被关闭，避免它每隔一段时间随机切到走路、坐姿或其他动作。
- 新增 Inspector 字段 `Night Shutdown Lead In Sequence`。当前绑定：
  - `Assets/Data/MinLoop/Dialogues/17F03_NightShutdownLeadIn.asset`：陪伴单元回应与女儿说 `Enough.`，女儿继续保持 Talking。
  - `Assets/Data/MinLoop/Dialogues/17F03_NightShutdownAction.asset`：从 `Display opened` 开始，和 `EnteringCode` 同时启动。
- 可维护方式：要调整关机前台词或语音，修改 Lead In 资产；要调整输入代码时的系统字幕或语音，修改 Action 资产。流程等待每个 Dialogue Sequence 真正播放完成，不需要手算固定秒数。
- 最小测试：Play Mode 中第三幕女儿到终点后 Animator 状态应为 `Talking`；Lead In 播完前不进入 `EnteringCode`；Action 开始时才进入 `EnteringCode`。

### Hearth17F03MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F03MinimalLoopBinder.cs`。
- 菜单：`Tools / Hearth / Replay / Apply 17F03 Minimal Loop Setup`。
- 本次修改：绑定正式演员时关闭其 `CityPeople` 自动动画，设置 Animator 为 `Always Animate`，并维护上述两个关机 Dialogue Sequence。
- 本次修改：正式 `Player/Person Controller` 和 `Player/Robot Controller` 内名为 `Capsule` 的占位 Renderer 都会关闭；Collider、移动、相机和交互组件不受影响。17F01 小男孩的 `Capsule Mesh (1)` 位于独立交互层级，不会被这条规则处理。
- 参考控制器 `Person Controller (1)`、`Robot Controller (4)`、`Robot Controller (5)` 仍由 `HearthEditorOnlyReferenceModel` 保证 Play Mode 中 Renderer 隐藏、Collider 关闭。
- 最小测试：第三幕只显示女儿正式演员；第一/第二幕演员隐藏；米娅和三个参考控制器的胶囊都不可见。

### 本轮运行验证

- 17F01：`Capsule Mesh (1)` 中心射线命中成立后，长按提示在多个刷新帧中持续显示。
- 17F02：`17F02_02` 的长按提示显示后持续存在，不再被空闲的 17F03 控制器关闭。
- 17F03：Animator 已分别进入 `Walk`、`Talking`、`EnteringCode`，三个状态对应的骨骼姿态不同；正式女儿的 `CityPeople` 为 disabled。
- 视觉检查：夜间终点机位下女儿显示 Talking 姿态，第一/第二幕演员与人类/参考控制器胶囊均未出现。
- Unity 脚本编译无新增 Error。Console 中仍有旧 `Door` Missing Script、其他非正式演员的第三方 Animator 报错和负缩放 BoxCollider 报错，未由本次修改新增。

## 2026-07-11 三户交互提示与射线判定修复

### 本次修改的脚本

- `Assets/Scripts/MinLoop/HearthCompanionReplayInteractable.cs`
- `Assets/Scripts/MinLoop/HearthCompanion17F02ReplayController.cs`
- `Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudController.cs`
- `Assets/Scripts/MinLoop/Hearth17F03UnitInteractable.cs`
- `Assets/Editor/Hearth17F03MinimalLoopBinder.cs`
- 场景绑定：`Assets/Scenes/SampleScene.unity`

### 17F01 小男孩胶囊交互

- 挂载对象：`MIN_LOOP_ROOT/ReplayRoom_17F01/RuntimeInteractables/Capsule Mesh (1)`。
- 必需组件：`CapsuleCollider`、`HearthCompanionReplayInteractable`。Renderer 可以关闭；Game 里不需要显示胶囊，Scene 视图通过 Gizmo 查看范围。
- `HearthCompanionReplayInteractable` 现在显式保存 `Interaction Collider`，并由 `SetAvailable(bool)` 同时控制逻辑状态和 Collider 的启用状态。
- 流程未开放时 Collider 关闭；卧室字幕和额外等待结束后 Collider 自动重新启用；完成靠近交互后再次关闭，避免重复触发。
- 当前 `Max Distance` 保持用户已经调好的 `0.71`，本次没有放大射线长度。
- 公开接口：`SetAvailable(bool)`、`CanInteract(Transform, Camera)`、`NotifyConfirmed()`、`InteractionCollider`、`IsAvailable`。
- 最小测试：进入 17F01 回放，字幕未结束时不显示提示；结束后走到床边允许范围并让准星命中 `Capsule Mesh (1)`，应显示长按框；完成后进入床边安抚页。

### 17F02 倾诉结束后的长按提示

- `HearthCompanionHudController` 新增 `ShowCurrentHoldPrompt()`：它会重新读取当前 `HearthCompanionHudSceneData` 的按键、文案和时长，而不是只把旧对象设为可见。
- `HearthCompanion17F02ReplayController` 在女主倾诉字幕完成并等待 `Bedroom Prompt Delay After Confide Seconds` 后调用该方法。
- 当前 17F02 卧室交互仍使用 `17F02_02` 的 `Hold Seconds = 1.5`；完成长按后 `bedroomAcknowledged` 才会成立，剧情才继续。
- 调整入口：在 `17F02_02` Scene Data 中改 `Hold Prompt Text`、`Hold Key`、`Hold Seconds`；在流程控制器中改字幕后等待时间。
- 使用方式有变化：以后剧情延迟后重新显示长按提示，应调用 `ShowCurrentHoldPrompt()`，不要只调用 `SetHoldPromptVisible(true)`。

### 17F03 实体陪伴单元交互

- 挂载对象：实体 `ROBOT` 子物体 `InteractionVolume_17F03`。
- 必需组件：`BoxCollider (Is Trigger)`、`Hearth17F03UnitInteractable`。
- 根因修复：实体模型父级带有大倍率缩放，绑定工具现在把目标世界尺寸换算成 Collider 的局部尺寸，避免交互体膨胀到几十米并把玩家包在 Collider 内。
- `Hearth17F03MinimalLoopBinder` 会在 `HearthHudRoot/InteractionPromptLayer/PlayerInteractionPrompt` 创建并绑定人类视角提示，最终由 `Player/Person Controller/PlayerInteraction` 控制显隐。
- `InteractionPromptLayer` 每次绑定时都会移到 `HearthHudRoot` 的最上层，避免提示虽然处于 Active 状态却被后面的全屏 HUD 层遮住。
- 交互开放时机保持不变：第一幕父母对话全部完成后才调用 `SetAvailable(true)`；检查开始、回放开始或流程取消时自动关闭。
- 公开接口：`Hearth17F03UnitInteractable.SetAvailable(bool)`、`SetInteractionCollider(Collider)`、`IsAvailable`、`InteractionCollider`、`HearthCompanion17F03ReplayController.OpenUnitInspection()`。
- 绑定工具会把 `Interaction Collider` 明确写入 Inspector，不再只依赖 Play Mode 的 `Awake` 临时查找。
- 调整 UI：选中 `HearthHudRoot/InteractionPromptLayer/PlayerInteractionPrompt`，可修改 `RectTransform`、背景 `Image` 和子物体 `InteractionText`；重新运行绑定菜单会恢复工具默认布局。
- 自动绑定菜单：`Tools / Hearth / Replay / Apply 17F03 Minimal Loop Setup`。
- 验证菜单：`Tools / Hearth / Replay / Validate 17F03 Minimal Loop Setup`。
- 最小测试：父母台词结束前实体 ROBOT 无提示；结束后准星命中 `InteractionVolume_17F03`，显示 `E INSPECT COMPANION UNIT`；按 E 进入故障检查界面。

### 本轮运行验证

- 17F01：`0.71` 射线命中胶囊，长按提示显示；完成后 Collider 关闭并进入 `BedsideSoothing`。
- 17F02：`17F02_02` 重新应用 `1.5s` 长按配置；完成后确认状态成立且提示隐藏。
- 17F03：交互体世界尺寸约为 `0.77 x 1.43 x 1.01`，中心射线可以命中，`PlayerInteraction` 能找到 `Hearth17F03UnitInteractable` 并显示提示。
- `Time.timeScale = 1`，本次没有改游戏速度。
- Unity 脚本编译无新增 Error；当前场景仍有旧门 Missing Script、第三方 CityPeople 动画状态和负缩放 BoxCollider 报错，和本次三户交互修复无关，已列入后续场景清理项。

## 2026-07-07 17F02 机器人参考控制器可见性修复

- 修改脚本：`Assets/Editor/Hearth17F02MinimalLoopBinder.cs`。
- 追加修正：`Assets/Scripts/MinLoop/HearthCompanion17F02ReplayController.cs` 新增 `Bedroom Start Camera Anchor`，解决只同步机器人身体、不同步卧室开场相机视角的问题。
- 本次调整只影响 17F02 参考机器人，不改变正式运行时机器人控制逻辑。
- 正式可操作机器人仍然是 `Player/Robot Controller`。
- `Player/Robot Controller (2)` 和 `Player/Robot Controller (3)` 现在作为编辑器里的位置/朝向/相机参考体保留可见：
  - `(2)` 用于生成 `Anchor_Robot_17F02_BedroomStart` 和 `Anchor_Robot_17F02_BedroomStartCamera`。
  - `(3)` 用于生成 `Anchor_Robot_17F02_LivingRoomTerminal` 和 `Anchor_Robot_17F02_LivingRoomTerminalCamera`。
- 这两个参考体会自动挂 `HearthEditorOnlyReferenceModel`，编辑器里可见、可移动、可旋转；Play Mode 时会隐藏 Renderer、关闭 Collider 和控制脚本，避免挡路或出现多个机器人。
- Inspector 使用方式：
  1. 需要调 17F02 机器人出生/第三幕站位时，直接移动或旋转 `Player/Robot Controller (2)` / `(3)`。
  2. 需要调画面时，展开对应参考体下面的 `Robot First Person Camera`，移动或旋转 Camera。
  3. 调好后运行 `Tools / Hearth / Replay / Apply 17F02 Minimal Loop Setup`。
  4. 程序会把参考体和参考 Camera 复制到 `MIN_LOOP_ROOT/Anchors` 下的正式锚点。
- 使用方式有变化：以前绑定工具会把 `(2)/(3)` 根物体设为 inactive；现在不再关闭根物体，只关闭它们的相机、AudioListener、移动、视角和交互脚本，并通过 `HearthEditorOnlyReferenceModel` 保证运行时不可见、不碰撞。
- 最小测试：在 Scene 里应能看到并选中 `Player/Robot Controller (2)` 和 `(3)` 的胶囊体；进入 Play 后游戏里不应出现额外机器人，正式流程仍由 `Player/Robot Controller` 被传送到对应锚点。17F02 卧室开场时，正式机器人相机应使用 `Anchor_Robot_17F02_BedroomStartCamera`，第三幕使用 `Anchor_Robot_17F02_LivingRoomTerminalCamera`。

> 最新覆盖提醒（2026-07-05）：本项目已清理一批高置信度旧脚本：`TerminalInteractable`、`DialogueSystem`、`DialogueTrigger`、`DialogueLine`、`IDialogueSource`、`EventTemplpate`、`HearthHudDemoController`。前面历史段落里如果仍提到它们，请以本文档的“2026-07-05 高置信度旧脚本清理记录”为准；正式 TV 终端入口使用 `HearthTvTerminalInteractable`，正式字幕使用 `MinLoopSubtitlePlayer` / 关卡专用字幕数据，正式玩家 HUD 使用 `HearthHudRoot`，正式机器人 HUD 使用 `HearthCompanionHudRoot`。

> 历史覆盖提醒（2026-06-30）：`MinLoopObjectivePresenter`、`MinLoopRobotHudPresenter`、`MinLoopTrustPresenter` 已经退役并从项目删除。前面历史段落里如果仍提到它们，请以后面的“2026-06-30 黑框清理、机器人 HUD 时序与 17F01 交互修正”为准。

本文档记录本项目中由 Codex 新增、修改或需要重点说明的 Unity / C# 脚本。

以后每次新增或修改脚本后，都必须同步更新本文档。记录重点不是解释每一行代码，而是让使用者知道脚本应该挂到哪里、Inspector 怎么拖引用、和哪些对象或脚本配合、如何测试功能是否生效。

## 先看这里：最小循环脚本总地图

这份总表下面的单个脚本说明很长，不建议从头一条一条手动读完。现在 17F-01 最小循环应该按“系统入口 -> 玩家交互 -> 流程推进 -> 复盘表现 -> 返回终端 -> 测试检查”来理解。

最短理解方式：

```text
玩家看向物体按 E
-> PlayerInteraction 找到可交互脚本
-> ResidentTerminalFlow / ComfortActionInteractable / SmartDoorController 等脚本响应
-> MinLoopFlowController 改变当前阶段
-> 终端 UI、视角切换、复盘、信任度、指引、灯光、音效跟着阶段变化
-> MinLoopSceneValidator 和 MinLoopDebugHotkeys 用来检查与快速测试
```

### 第一层：真正的最小循环主链路

这组脚本是 17F-01 垂直切片的主干。以后如果只想跑通流程，优先看这一组。

| 脚本 | 简要作用 | 应该和谁一起用 |
|---|---|---|
| `MinLoopTypes.cs` | 定义流程阶段、A/B 处置选择、字幕行等基础类型。 | 不用挂载，其他最小循环脚本会自动使用它。 |
| `MinLoopFlowController.cs` | 总流程导演，决定现在处于走廊、刷卡、摘要、复盘、安抚、早晨、A/B、下一户中的哪一步。 | 和终端、视角切换、复盘、信任度、阶段表现脚本一起用。 |
| `ResidentTerminalFlow.cs` | 让 17F-01 门口终端按 `E` 后进入最小循环流程。 | 需要 `PlayerInteraction` 和 `MinLoopFlowController`。 |
| `MinLoopTerminalPresenter.cs` | 显示刷工牌、住户摘要、昨夜事件、A/B 处置、下一户指引这些终端页面。 | 由 `MinLoopFlowController` 调用，可配合正式 UI 或占位 UI。 |
| `ViewSwitchController.cs` | 在 Mia/玩家视角与陪伴单元视角之间切换，并做黑场淡入淡出。 | 由 `MinLoopFlowController` 调用，也可以按 `R` 手动测试。 |
| `ReplaySequenceController.cs` | 管理儿童房复盘流程：噩梦醒来、喊“妈妈”、等待安抚、早晨父母对话、结束复盘。 | 需要 `ComfortActionInteractable`、`SimpleActorCueController`、`MinLoopSubtitlePlayer`。 |
| `ComfortActionInteractable.cs` | 床边唯一安抚操作点，玩家按 `E` 后通知复盘继续。 | 需要 `ReplaySequenceController` 和 Collider。 |
| `TrustStateController.cs` | 记录信任度数值，A/B 处置后加减分。 | 由 `MinLoopFlowController` 调用，结果显示给正式 HUD 或终端处置/历史页面。 |

### 第二层：玩家控制与通用交互

这组负责“玩家能不能看到、走动、按 E、开门、触发反馈”。

| 脚本 | 简要作用 | 应该和谁一起用 |
|---|---|---|
| `PlayerInteraction.cs` | 玩家屏幕中心射线检测，看到可交互物体时显示提示，按 `E` 调用目标。 | 所有实现 `IInteractable` 的脚本。 |
| `IInteractable1.cs` / `IInteractable` | 可交互接口，表示这个物体能被 `PlayerInteraction` 按 `E` 调用。 | 不用挂载，终端、门、反馈、安抚点会实现它。 |
| `FirstPersonLook.cs` | 第一人称视角旋转控制，额外提供强制同步视角角度的方法。 | 被 `ViewSwitchController` 和角色控制器使用。 |
| `TerminalInteractable.cs` | 已删除。旧版/通用空白终端入口，功能已被正式 TV 终端入口替代。 | 以后不要再挂载；TV 使用 `HearthTvTerminalInteractable`，旧最小循环终端测试可看 `ResidentTerminalFlow`。 |
| `TerminalUIController.cs` | 通用终端面板开关、冻结玩家、关闭按钮、内容挂载点。 | 可以作为 `MinLoopTerminalPresenter` 的外层 UI 管理，也可单独测试终端。 |
| `SmartDoorController.cs` | 通用门控制，支持滑动门、旋转门，能按 `E` 开关。 | 配合 `PlayerInteraction`，也可被 `SmartDoorTriggerZone` 自动触发。 |
| `SmartDoorTriggerZone.cs` | 靠近门自动开、离开后自动关的触发区。 | 配合 `SmartDoorController` 和 Trigger Collider。 |
| `InteractionFeedbackController.cs` | 播放一次音效、灯光闪烁、材质高亮，可做电梯按钮、刷卡哔声、提示反馈。 | 可被 `MinLoopFlowController`、`MinLoopStageCueController` 或玩家按 `E` 触发。 |

### 第三层：UI 与信息展示

这组是“让玩家看懂现在发生了什么”。第一版可以先用自动生成的占位 UI，正式 UI 以后再拖进去。

| 脚本 | 简要作用 | 应该和谁一起用 |
|---|---|---|
| `MinLoopObjectivePresenter.cs` | 已删除。旧占位目标 UI，曾用于左上角当前目标提示。 | 正式玩家 HUD 已接管目标/地点/任务信息，不再使用旧黑框占位。 |
| `MinLoopRobotHudPresenter.cs` | 已删除。旧占位机器人 HUD，曾用于时间、心率、噩梦判定。 | 正式机器人 HUD 使用 `HearthCompanionHudRoot` 及其子 View。 |
| `MinLoopTrustPresenter.cs` | 已删除。旧占位信任度 UI。 | 信任度通过正式 HUD 浮字、终端处置页和历史页展示。 |
| `MinLoopSubtitlePlayer.cs` | 播放字幕和可选语音，例如孩子“妈妈”、父母早晨对话。 | 由 `ReplaySequenceController` 调用。 |
| `MinLoopWorldGuideMarker.cs` | 场景里的发光指引点、文字和距离显示。 | 常放在终端、安抚点、下一户指引位置。 |

### 第四层：场景表现与复盘演员

这组不是流程必需，但它让流程“看起来像一个游戏片段”。

| 脚本 | 简要作用 | 应该和谁一起用 |
|---|---|---|
| `SimpleActorCueController.cs` | 控制孩子/父母模型的简单动作、看向目标、占位姿势或 Animator Trigger。 | 由 `ReplaySequenceController` 或阶段 Cue 调用。 |
| `MinLoopStageObjectActivator.cs` | 按流程阶段显示/隐藏某些物体，例如终端指引、安抚点指引、下一户指引。 | 监听 `MinLoopFlowController`。 |
| `MinLoopStageAnchorController.cs` | 按阶段把 Mia 或 Companion 放到指定位置，例如复盘起点、回终端位置。 | 配合 `ViewSwitchController` 和流程阶段。 |
| `MinLoopStageCueController.cs` | 阶段变化时触发门、动画、音效、反馈或 UnityEvent。 | 监听 `MinLoopFlowController`，适合做“某阶段发生一件事”。 |
| `MinLoopLightingStateController.cs` | 按阶段切换走廊暖光、昨夜冷光、早晨柔光等灯光状态。 | 监听 `MinLoopFlowController`。 |
| `MinLoopAudioStateController.cs` | 按阶段切换环境声，例如走廊、夜晚儿童房、早晨。 | 监听 `MinLoopFlowController`。 |

### 第五层：工具、自动挂载与测试

这组主要给我和你做搭建、自动绑定、检查、快速测试用。它们不是玩家体验本身。

| 脚本 | 简要作用 | 使用时机 |
|---|---|---|
| `MinLoopSetupChecklistWindow.cs` | Unity 菜单里的最小循环清单窗口，里面有创建骨架、自动绑定、检查按钮。 | 你想快速搭建或检查时打开。 |
| `MinLoopSceneSkeletonCreator.cs` | 自动创建 `MIN_LOOP_ROOT`、管理器、锚点、指引、灯光、音频、占位对象。 | 新场景或场景缺骨架时使用。 |
| `MinLoopSceneAutoBinder.cs` | 按对象命名自动把引用拖到脚本字段里，减少手动 Inspector 拖拽。 | 对象摆好并按命名规则命名后使用。 |
| `MinLoopSceneValidator.cs` | Play 或手动调用时检查最小循环核心引用是否缺失。 | 每次绑定后、测试前使用。 |
| `MinLoopSceneValidatorEditor.cs` | 给 Validator 提供 Inspector 检查按钮。 | 选中 Validator 时使用。 |
| `MinLoopDebugHotkeys.cs` | Play Mode 快捷键测试：F1 重置，F2 终端，F3 刷卡，F4 复盘，F5 安抚，F6/F7 A/B，F8 下一户。 | 测流程时使用，正式演示可关闭。 |

### 第六层：当前不属于 17F-01 主流程的历史/辅助脚本

这些脚本不要优先拿来搭最小循环，除非任务明确说要做城市生成、广告牌、旧对话或外部门包修复。

| 脚本/条目 | 简要作用 | 当前建议 |
|---|---|---|
| `CityLotAssetPlacer.cs` | 城市场景批量放置楼体资产。 | 与 17F-01 最小循环无关，先不动。 |
| `CityBuildingPlacementSlot.cs` | 记录城市楼体槽位信息。 | 与 17F-01 最小循环无关，先不动。 |
| `CityLotAssetPlacerEditor.cs` | 城市楼体放置工具的 Editor 面板。 | 与 17F-01 最小循环无关，先不动。 |
| `CityFacadeBillboardPlacer.cs` | 给城市建筑外立面生成广告牌占位。 | 与 17F-01 最小循环无关，先不动。 |
| `CityBillboardPlacementSlot.cs` | 记录广告牌槽位信息。 | 与 17F-01 最小循环无关，先不动。 |
| `CityFacadeBillboardPlacerEditor.cs` | 广告牌工具的 Editor 面板。 | 与 17F-01 最小循环无关，先不动。 |
| `Free Wood Door Pack 命名空间修复` | 修复导入门包时命名空间/类名冲突。 | 只在门包脚本冲突时需要知道。 |
| `DialogueSystem.cs` | 已删除。旧版通用对话面板系统，会生成大黑底字幕框。 | 正式字幕使用 `MinLoopSubtitlePlayer` 或关卡专用字幕数据。 |
| `DialogueTrigger.cs` | 已删除。旧版看向 NPC/物体按 `E` 触发对话。 | 正式交互由关卡流程控制器决定，不再用旧触发器。 |
| `DialogueLine.cs` | 已删除。旧版对话行数据。 | 新字幕数据使用 `MinLoopSubtitleLine` 或关卡 ScriptableObject。 |
| `IDialogueSource.cs` | 已删除。旧版对话触发接口。 | 不再作为剧情接口。 |
| `EventTemplpate.cs` | 已删除。早期 UnityEvent 模板脚本。 | 不再作为项目模板。 |

## 17F-01 最小循环的推荐使用顺序

不要按脚本文档顺序做。推荐按下面顺序搭：

1. 先用 `MinLoopSetupChecklistWindow` 创建或确认 `MIN_LOOP_ROOT` 骨架。
2. 把 Mia 控制器、Companion 控制器、17F-01 终端、儿童房、孩子、父母、床边安抚点、门、灯光、音效摆到场景里。
3. 按推荐命名，例如 `Player_Mia_Controller`、`Companion_Controller`、`Terminal_17F01`、`Child_Actor`、`ComfortAction_Bedside`。
4. 运行 `MinLoopSceneAutoBinder` 自动绑定大部分引用。
5. 运行 `MinLoopSceneValidator` 检查缺什么。
6. Play Mode 测主链路：看向终端按 `E` -> 刷工牌 -> 摘要 -> 调出昨夜事件 -> 黑场切 Companion -> 安抚 -> 回终端 -> 选 A/B -> 下一户。
7. 如果某一步卡住，先用 `MinLoopDebugHotkeys` 跳到对应阶段确认是“流程问题”还是“场景资源/引用问题”。

## 以后你可以这样描述任务，我就能更快挂载

你不需要说脚本名。你只要说清楚“哪个物体 + 想变成什么功能 + 触发方式 + 是否先用占位”。下面这些句子可以直接复制改对象名：

```text
请把场景里的 [Terminal_17F01] 接成 17F-01 最小循环终端：玩家看向它按 E 后，进入刷工牌、住户摘要、调出昨夜事件流程。

请把 [Player_Mia_Controller] 和 [Companion_Controller] 接到视角切换系统：R 可以测试切换，终端调复盘时自动切到 Companion，复盘结束自动回 Mia。

请把 [ComfortAction_Bedside] 接成儿童房复盘里的唯一安抚点：只有复盘等待安抚阶段能按 E，按完后进入早晨父母对话。

请把 [Child_Actor] 接入复盘表现：没有 Animator 就先用占位姿势，有 Animator 就用 Trigger 播放睡觉、惊醒、被安抚、早晨状态。

请把 [Lobby_GlassDoor] 做成自动滑动玻璃门：玩家靠近打开、离开关闭，带开门音效。

请把 [Door_17F01] 做成可交互旋转房门：玩家看向按 E 开关。

请把 [Elevator_Button_17F] 做成按钮反馈：玩家按 E 后发光并播放叮咚声，但先不接正式电梯系统。

请把这些正式 UI 对象接到终端/HUD/目标/信任度显示上，保留原来的占位流程不变。

请帮我检查当前场景的最小循环绑定：先自动绑定，再告诉我缺哪些对象或引用。
```

如果你想让我直接挂载，最好给我这四类信息：

1. 对象名字：例如 `Terminal_17F01`、`Child_Actor`、`Door_17F01`。
2. 功能目的：终端、安抚点、门、指引、音效反馈、角色动作、UI。
3. 触发方式：看向按 `E`、靠近自动触发、流程阶段自动触发、按钮点击。
4. 第一版要求：占位能跑即可，还是要接正式模型/正式 UI/正式音效。

## 哪些脚本应该一起用

| 目标 | 必用脚本 | 可选增强 |
|---|---|---|
| 跑通 17F-01 主流程 | `MinLoopFlowController`、`ResidentTerminalFlow`、`MinLoopTerminalPresenter`、`ViewSwitchController`、`ReplaySequenceController`、`ComfortActionInteractable`、`TrustStateController`、`PlayerInteraction` | `MinLoopSubtitlePlayer`、正式 `HearthHudRoot`、正式 `HearthCompanionHudRoot` |
| 做终端 | `ResidentTerminalFlow`、`MinLoopTerminalPresenter`、Collider、`PlayerInteraction` | `TerminalUIController`、`InteractionFeedbackController` |
| 做儿童房复盘 | `ReplaySequenceController`、`ComfortActionInteractable`、`SimpleActorCueController`、`MinLoopSubtitlePlayer` | `MinLoopLightingStateController`、`MinLoopAudioStateController`、`MinLoopStageCueController` |
| 做 Mia/Companion 切换 | `ViewSwitchController`、两个角色控制器、两个 Camera、两个移动/视角脚本 | HumanCanvas、RobotCanvas、黑场 UI |
| 做门 | `SmartDoorController`、Collider | `SmartDoorTriggerZone`、`InteractionFeedbackController` |
| 做按钮/刷卡/电梯叮咚 | `InteractionFeedbackController`、Collider | Renderer 高亮、Light 闪烁、AudioSource |
| 做阶段指引 | `MinLoopStageObjectActivator`、`MinLoopWorldGuideMarker` | 自定义箭头模型、正式 UI 提示 |
| 做快速检查 | `MinLoopSceneValidator`、`MinLoopSceneValidatorEditor` | `MinLoopSetupChecklistWindow`、`MinLoopDebugHotkeys` |

## 哪些脚本是测试/工具用的

测试和工具脚本主要是：

- `MinLoopDebugHotkeys.cs`：Play Mode 里用 F1-F8 跳流程，测试用，正式演示前可以关掉。
- `MinLoopSceneValidator.cs`：检查缺引用，开发期常用，正式游戏里可以保留但不需要显示给玩家。
- `MinLoopSceneValidatorEditor.cs`：只在 Unity Editor 里提供按钮。
- `MinLoopSetupChecklistWindow.cs`：只在 Unity Editor 菜单里使用。
- `MinLoopSceneSkeletonCreator.cs`：只在 Unity Editor 里创建骨架。
- `MinLoopSceneAutoBinder.cs`：只在 Unity Editor 里自动拖引用。
- 城市放置和广告牌相关脚本：当前不是 17F-01 最小循环测试重点。

## 目前脚本优化计划

当前优先目标不是继续加新脚本，而是让现有脚本更好用、更容易验证。

| 阶段 | 目标 | 做法 | 是否马上做 |
|---|---|---|---|
| 1 | 让你能看懂和让我能快速挂载 | 在总表顶部加入脚本地图、组合关系、任务描述模板。 | 当前已做。 |
| 2 | 让场景挂载变快 | 优先使用 `MinLoopSceneAutoBinder` 和统一命名，而不是让你手动拖几十个字段。 | 下一次具体挂载时做。 |
| 3 | 先验证再重构 | 先用 `MinLoopSceneValidator`、`MinLoopDebugHotkeys` 和 Unity Console 找出真正卡点。 | 需要 Unity 场景验证时做。 |
| 4 | 拆长脚本 | 优先拆 `MinLoopSceneAutoBinder.cs`、`MinLoopSceneSkeletonCreator.cs`、`MinLoopTerminalPresenter.cs`。 | 等主流程能跑后再做，避免边拆边坏。 |
| 5 | 补测试边界 | 后续可加 Assembly Definition 和 EditMode/PlayMode 测试，降低改脚本时的风险。 | 主流程稳定后做。 |

第一轮最建议的优化不是删脚本，而是“少让你碰脚本”。也就是：你告诉我对象名和目标行为，我负责判断该用哪些现有脚本、哪些字段要拖、哪些地方可以占位。

## 记录模板

新增脚本说明时，使用下面结构：

```text
## 脚本名

- 路径：
- 脚本作用：
- 挂载对象：
- 必需组件：
- Inspector 字段：
- 公开接口：
- 关联脚本：
- 使用步骤：
- 测试方式：
- 后续扩展：
- 最近更新：
```

## TerminalInteractable.cs

- 删除状态：已于 2026-07-05 从项目中删除，同时删除 `Assets/TerminalInteractable.cs.meta`。
- 原路径：`Assets/TerminalInteractable.cs`
- 原脚本作用：旧版通用终端入口，只会调用 `TerminalUIController.Open()` 打开空白终端 UI。
- 删除原因：正式 TV 终端已经使用 `HearthTvTerminalInteractable + HearthTvTerminalController`；17F01 早期最小循环测试入口使用 `ResidentTerminalFlow`。保留这个脚本会让一个 TV 或终端同时存在两个入口，后续容易出现重复打开或错误流程。
- 场景处理：当前清理没有发现必须保留的运行时挂载；`HearthTvTerminalPrefabBuilder` 中对该脚本的兼容禁用逻辑也已移除，避免删除脚本后编译失败。
- 后续替代：
  - TV 终端：使用 `Assets/Scripts/UI/HearthHud/HearthTvTerminalInteractable.cs`。
  - 旧最小循环终端测试：使用 `ResidentTerminalFlow.cs`。
  - 通用 UI 开关：如以后仍需要，可基于 `TerminalUIController.cs` 新写一个明确命名的入口，不恢复旧脚本。
- 最近更新：高置信度旧脚本清理；使用方式改为“不可再挂载”。

## TerminalUIController.cs

- 路径：`Assets/Scripts/UI/TerminalUIController.cs`
- 脚本作用：管理终端 UI 的打开、关闭、玩家控制冻结、内容挂载点，以及未来正式 UI Prefab 的接入。
- 挂载对象：推荐挂在 `HumanCanvas`、`UIManager` 或一个专门的终端 UI 管理物体上。
- 必需组件：
  - 推荐场景中有 `Screen Space - Overlay` Canvas。
  - 如果手动搭 UI，需要一个空白 Panel、一个关闭 Button、一个 `contentRoot` 空物体。
  - 如果要冻结玩家，需要引用 `FirstPersonMovement`、`FirstPersonLook`、`PlayerInteraction`，以及玩家 Rigidbody。
- Inspector 字段：
  - `Terminal Panel`：终端 UI 的外层 Panel。打开时显示，关闭时隐藏。
  - `Close Button`：右上角关闭按钮。脚本会自动绑定点击关闭，不需要在 Button 的 OnClick 里手动加。
  - `Content Root`：未来正式 UI 内容的挂载点。后续 UI Prefab 会实例化到这里。
  - `Initial Content Prefab`：可选。启动时自动放入 `Content Root` 的内容 Prefab。当前第一版可以留空。
  - `Create Fallback UI`：如果没有手动拖 Panel、Button 或 Content Root，运行时自动生成一个空白 Panel 和右上角关闭按钮。默认开启。
  - `Create Event System If Missing`：打开终端时如果场景中没有 `EventSystem`，自动创建一个，避免 UI Button 无法点击。默认开启。
  - `Lock Player While Open`：打开终端 UI 时是否冻结玩家移动、视角和交互。默认开启。
  - `Player Movement`：拖入玩家身上的 `FirstPersonMovement`。
  - `Player Look`：拖入玩家相机或视角控制物体上的 `FirstPersonLook`。
  - `Player Interaction`：拖入玩家身上的 `PlayerInteraction`。
  - `Player Rigidbody`：拖入玩家 Rigidbody，用于打开 UI 时清空当前速度。
- 公开接口：
  - `Open()`：打开终端 UI，显示 Panel，释放鼠标，冻结玩家。
  - `Close()`：关闭终端 UI，隐藏 Panel，恢复鼠标状态和玩家控制。
  - `SetContent(GameObject contentPrefab)`：清空当前内容，并把指定 Prefab 实例化到 `Content Root`。
  - `ClearContent()`：清空当前由 `SetContent()` 创建的内容。
  - `IsOpen`：只读属性，表示终端 UI 当前是否打开。
  - `ContentRoot`：只读属性，提供给其他脚本获取内容挂载点。
- 关联脚本：
  - 可被自定义终端入口调用 `Open()`；旧调用方 `TerminalInteractable` 已删除。
  - 打开时调用 `PlayerInteraction.SetInteractionEnabled(false)` 禁止继续交互。
  - 关闭时恢复 `FirstPersonMovement`、`FirstPersonLook`、`PlayerInteraction` 原本启用状态。
- 使用步骤：
  1. 在 `HumanCanvas` 或 `UIManager` 上添加 `TerminalUIController`。
  2. 如果已经手动搭好 UI，把空白 Panel 拖到 `Terminal Panel`。
  3. 把右上角关闭按钮拖到 `Close Button`。
  4. 在 Panel 内新建一个空物体作为内容挂载点，拖到 `Content Root`。
  5. 把玩家的移动、视角、交互和 Rigidbody 分别拖到对应字段。
  6. 如后续新建通用终端入口脚本，可在该入口脚本中引用这个控制器；旧 `TerminalInteractable` 已删除。
- 测试方式：
  - 调用 `Open()` 或通过终端按 `E` 打开页面。
  - 页面中央出现空白 Panel。
  - Panel 右上角有关闭按钮。
  - 如果场景中原本没有 `EventSystem`，打开终端后会自动生成，按钮仍可点击。
  - 打开后玩家不能移动、不能转头、不能继续触发其他交互。
  - 点击关闭按钮后恢复玩家控制。
  - 按 `Esc` 后恢复玩家控制。
- 后续扩展：
  - 正式 UI 做好后，可以把正式 UI Prefab 放到 `Initial Content Prefab`，或由其他脚本调用 `SetContent()` 动态放入。
  - 如果要完全手工设计 UI，可以关闭 `Create Fallback UI`，并手动绑定 Panel、Button、Content Root。
  - 如果后续做多个终端页面，可以保留同一个控制器，按终端类型调用不同的 `SetContent()`。
- 最近更新：使用方式有变化。新增 `Create Event System If Missing` 字段；打开终端时会自动补齐 `EventSystem + StandaloneInputModule`，避免占位 UI 或正式 UI 的按钮在空场景里无法点击。原有挂载方式不变。

## PlayerInteraction.cs

- 路径：`Assets/Scripts/Interactions/PlayerInteraction.cs`
- 脚本作用：从玩家屏幕中心发射射线，检测实现了 `IInteractable` 的物体，显示交互提示，并在按键时触发交互。新版使用 `RaycastNonAlloc` 和提示状态缓存，减少每帧分配和重复 UI 刷新。
- 挂载对象：玩家对象，或玩家控制系统中负责第一人称交互的物体。
- 必需组件：
  - 需要能引用玩家主 Camera。
  - 如果要显示交互提示，需要场景中有提示 UI 物体和 TMP 文本。
  - 可交互物体需要 Collider，并实现 `IInteractable`。
- Inspector 字段：
  - `Main Camera`：玩家第一人称相机。若留空，启动时会尝试使用 `Camera.main`。
  - `Interaction Range`：交互检测距离，默认 `3.0`。
  - `Interact Key`：交互按键，默认 `E`。
  - `Interaction Layers`：交互射线检测的 LayerMask，默认全部层。
  - `Trigger Interaction`：是否命中 Trigger Collider。默认 `Collide`，可用于交互热区。
  - `Search Parent For Interactable`：Collider 在子物体、脚本在父物体时向父级查找 `IInteractable`。建议开启。
  - `Find Camera If Missing`：相机为空时自动查找自身、子物体或 `Camera.main`。
  - `Ui Interaction`：交互提示 UI 根物体。
  - `Ui Interaction Text`：交互提示文本。
  - `Fallback Description`：目标返回空描述时显示的兜底文案，默认 `E 交互`。
  - `Refresh Prompt Every Frame`：是否每帧刷新提示文案。默认关闭，通常没必要。
  - `Description Refresh Interval`：目标不变时提示文案的刷新间隔，默认 `0.1` 秒。
- 公开接口：
  - `InteractionEnabled`：只读属性，表示当前交互是否启用。
  - `CurrentInteractable`：当前屏幕中心命中的交互对象。
  - `HasCurrentTarget`：是否命中交互对象。
  - `InteractKey`：读取当前交互按键。
  - `SetInteractionEnabled(bool enabled)`：启用或禁用交互。禁用时会隐藏交互提示。
  - `SetInteractionCamera(Camera camera)`：切换当前交互射线使用的相机，并清空旧目标。
  - `ForceRefreshPrompt()`：强制刷新当前目标提示文案。
- 关联脚本：
  - 检测所有实现 `IInteractable` 的脚本，包括 `ResidentTerminalFlow`、`HearthTvTerminalInteractable`、`SmartDoorController`、`InteractionFeedbackController`、`ComfortActionInteractable`。
  - 被 `TerminalUIController` 调用，用于打开 UI 时暂停交互，关闭 UI 时恢复交互。
  - 被 `ViewSwitchController` 调用 `SetInteractionCamera()`，切换 Mia / Companion 相机后同步交互射线相机。
- 使用步骤：
  1. 确认玩家对象上有 `PlayerInteraction`。
  2. 把玩家相机拖到 `Main Camera`，或确保相机带有 `MainCamera` 标签。
  3. 把交互提示 UI 根物体拖到 `Ui Interaction`。
  4. 把提示文字 TMP 组件拖到 `Ui Interaction Text`。
  5. 设置合适的 `Interaction Range`。
  6. 保持 `Search Parent For Interactable` 开启，方便 Collider 放子物体、脚本挂父物体。
  7. 给可交互物体添加 Collider 和实现 `IInteractable` 的脚本。
- 当前场景挂载：
  - `SampleScene` 中已将 `PlayerInteraction` 挂到 `First Person Controller`。
  - `Main Camera` 引用场景里的 `Main Camera`。
  - `Ui Interaction` 引用 `HumanCanvas/Panel`。
  - `Ui Interaction Text` 引用 `HumanCanvas/Panel/description`。
  - `HumanCanvas/Panel` 默认关闭，只有射线命中实现 `IInteractable` 的物体时才显示。
- 测试方式：
  - 进入 Play Mode。
  - 玩家没有看向可交互物体时，底部 `E` 提示不显示。
  - 玩家看向可交互物体时，提示 UI 出现。
  - 玩家移开视角时，提示 UI 隐藏。
  - 按 `E` 后触发目标物体的 `Interact()`。
  - 门打开/关闭后，提示文案会在刷新间隔内更新，例如从 `E 开门` 变为 `E 关门`。
  - 打开终端 UI 后，提示隐藏且不会继续触发其他交互。
- 后续扩展：
  - 如果以后需要交互优先级、不同按键或交互图标，可以在这个脚本中扩展检测结果和提示 UI。
  - 如果可交互脚本挂在父物体而 Collider 在子物体，当前逻辑已经支持通过父级查找 `IInteractable`。
- 最近更新：使用方式有变化但兼容旧字段。升级为 `RaycastNonAlloc` 检测，新增交互按键、LayerMask、Trigger 命中策略、父级查找开关、相机自动查找、提示文案缓存和 `SetInteractionCamera()`；旧的 `Main Camera`、`Interaction Range`、`Ui Interaction`、`Ui Interaction Text` 挂载方式继续可用。

## ViewSwitchController.cs

- 路径：`Assets/Scripts/MinLoop/ViewSwitchController.cs`
- 脚本作用：在 Mia / Human 第一人称控制器和陪伴单元 / Companion 第一人称控制器之间切换视角和控制权。默认按 `R` 触发，切换时先渐黑，黑屏中切换相机和控制脚本，再渐亮到新视角。
- 挂载对象：推荐在场景中新建空物体 `ViewSwitchController` 或 `ViewModeController`，把脚本挂在这个空物体上。不要直接挂到某一个玩家控制器上。
- 必需组件：
  - 场景中需要已经放好两个角色控制器：一个作为 Mia / Human，一个作为陪伴单元 / Companion。
  - 每个控制器至少需要一个 Camera。
  - 如果要让该视角可移动，需要对应的 `FirstPersonMovement` 和 `FirstPersonLook`。
  - 如果该视角需要交互提示和 `E` 交互，可选拖入 `PlayerInteraction`。
  - 如果希望切换时清掉移动惯性，可选拖入对应 Rigidbody。
  - 淡入淡出可以由脚本自动生成 UI，也可以手动准备一个全屏黑色 Image + CanvasGroup。
- Inspector 字段：
  - `Switch Key`：切换按键，默认 `R`。
  - `Starting Mode`：初始视角。当前建议保持 `Human`，也就是 Play Mode 后先控制 Mia。
  - `Human`：Mia / 玩家控制器配置。
    - `Root Object`：拖入 Mia / 玩家控制器根物体。
    - `View Camera`：拖入 Mia 视角的 Camera。
    - `Movement`：拖入 Mia 控制器上的 `FirstPersonMovement`。
    - `Look`：拖入 Mia 相机或视角物体上的 `FirstPersonLook`。
    - `Interaction`：可选，拖入 Mia 控制器上的 `PlayerInteraction`。
    - `Rigidbody`：可选，拖入 Mia 控制器上的 Rigidbody。
    - `Canvas`：可选，拖入 HumanCanvas。切到 Human 时显示，切走时隐藏。
  - `Companion`：陪伴单元 / 机器视角控制器配置，字段含义同 `Human`。`Canvas` 可选拖入 RobotCanvas。
  - 如果某个模式同时拖了 `View Camera` 和 `Interaction`，脚本会自动调用 `PlayerInteraction.SetInteractionCamera()`，把交互射线相机切到本模式的 `View Camera`。
  - `Create Fade Overlay`：没有手动拖淡入淡出 UI 时，运行时自动创建一个全屏黑色遮罩。默认开启。
  - `Fade Canvas Group`：可选。手动准备淡入淡出遮罩时，拖入它的 CanvasGroup。
  - `Fade Image`：可选。手动准备淡入淡出遮罩时，拖入全屏黑色 Image。
  - `Fade Color`：遮罩颜色，默认黑色。
  - `Fade Out Duration`：渐黑时间，默认 `0.35` 秒。
  - `Black Hold Duration`：全黑停顿时间，默认 `0.1` 秒。
  - `Fade In Duration`：渐亮时间，默认 `0.35` 秒。
  - `Use Unscaled Time`：是否用不受 Time.timeScale 影响的时间做淡入淡出。默认开启。
  - `Current Mode`：运行时当前视角，只用于观察状态。
- 公开接口：
  - `ToggleView()`：在 Human 和 Companion 之间来回切换。当前按 `R` 就调用它。
  - `SwitchToHuman()`：切回 Mia / Human 视角，后续剧情复盘结束时可直接调用。
  - `SwitchToCompanion()`：切到陪伴单元 / Companion 视角，后续终端“调出昨夜事件”时可直接调用。
  - `SwitchTo(ViewSwitchController.ViewMode targetMode)`：按指定模式切换。
  - `IsSwitching`：只读属性，表示当前是否正在渐黑/切换/渐亮。
  - `CurrentMode`：只读属性，表示当前处于 Human 还是 Companion。
- 关联脚本：
  - 直接启用/禁用 `FirstPersonMovement`、`FirstPersonLook`。
  - 通过 `PlayerInteraction.SetInteractionEnabled(bool)` 启用/禁用交互。
  - 可与后续 `InspectionFlowController` 或终端流程脚本连接：终端按钮调用 `SwitchToCompanion()`，复盘结束调用 `SwitchToHuman()`。
- 使用步骤：
  1. 在 Hierarchy 新建空物体，命名为 `ViewSwitchController`。
  2. 给它添加 `ViewSwitchController.cs`。
  3. 在 `Human` 区域拖入 Mia / 玩家控制器根物体、Camera、`FirstPersonMovement`、`FirstPersonLook`、可选 `PlayerInteraction`、Rigidbody 和 HumanCanvas。
  4. 在 `Companion` 区域拖入陪伴单元控制器根物体、Camera、`FirstPersonMovement`、`FirstPersonLook`、可选 `PlayerInteraction`、Rigidbody 和 RobotCanvas。
  5. 保持 `Starting Mode = Human`。
  6. 第一版可以保持 `Create Fade Overlay` 开启，不需要手动搭淡入淡出 UI。
  7. 进入 Play Mode，按 `R` 测试切换。
- 测试方式：
  - Play Mode 后，只有 Human / Mia 的 Camera、移动、视角和交互启用。
  - Companion 的 Camera 不渲染，移动和视角不响应。
  - 按 `R` 后画面渐黑，黑屏中切到 Companion Camera，然后渐亮。
  - 切换后 WASD 和鼠标只控制 Companion，Mia 不会继续滑动。
  - 再按 `R` 能用同样流程切回 Mia。
  - 快速连续按 `R` 不会卡黑屏，也不会出现两个 Camera 同时渲染。
  - 某些可选字段为空，例如 `Interaction`、`Canvas`、`Rigidbody`，不会报错；只是对应功能不被切换。
- 后续扩展：
  - 接入最小循环时，不需要再用 `R` 作为正式入口，可以由终端 UI 或流程控制器调用 `SwitchToCompanion()`。
  - 如果后续需要三种以上视角，可把当前两个 `ViewRig` 扩展成列表。
  - 如果以后改用 Cinemachine，可以保留淡入淡出和控制权切换逻辑，只替换 Camera 启用方式。
- 最近更新：使用方式无变化。交互相机同步改为调用 `PlayerInteraction.SetInteractionCamera()`，切换视角时会清空旧交互目标；此前 R 键双视角切换、自动黑场淡入淡出、控制权切换和后续流程调用接口继续保留。

## FirstPersonLook.cs

- 路径：`Assets/Mini First Person Controller/Scripts/FirstPersonLook.cs`
- 脚本作用：Mini First Person Controller 的第一人称视角控制脚本，负责鼠标控制相机俯仰和角色水平旋转。本次只补公开同步接口，用于阶段锚点传送后同步内部视角累计值。
- 挂载对象：Mia / Companion 第一人称控制器的相机或视角物体，沿用 Mini First Person Controller 原本挂载方式。
- 必需组件：需要能引用父级 `FirstPersonMovement` 所在角色 Transform。原脚本 `Reset()` 会自动获取。
- Inspector 字段：
  - `Character`：角色根 Transform。
  - `Sensitivity`：鼠标灵敏度。
  - `Smoothing`：鼠标平滑值。
- 公开接口：
  - `ForceLookAngles(float yaw, float pitch)`：强制设置内部 yaw/pitch 累计值，并立即应用到角色和相机。
  - `ForceLookFromCurrentTransforms()`：从当前角色/相机 Transform 反推 yaw/pitch，并同步内部累计值。
- 关联脚本：
  - 被 `MinLoopStageAnchorController` 调用，用于传送到 Anchor 后避免下一帧被旧鼠标累计值拉回旧朝向。
  - 被 `ViewSwitchController` 启用/禁用，控制当前可操作视角。
- 使用步骤：
  1. 保持 Mini First Person Controller 原挂载方式不变。
  2. 阶段锚点规则里如果 `First Person Look` 已自动绑定，不需要手动调用。
  3. 如果手动传送角色，也可以在传送后调用 `ForceLookFromCurrentTransforms()` 同步视角状态。
- 测试方式：
  - 先把视角转到一个明显角度，再触发 `MinLoopStageAnchorController` 传送。
  - 传送后镜头应对齐 Anchor/规则指定方向，不应下一帧弹回传送前的旧角度。
- 后续扩展：如果后续替换为其他第一人称控制器，可保留 `MinLoopStageAnchorController`，只把视角同步部分改接新控制器的公开方法。
- 最近更新：使用方式无变化。新增 `ForceLookAngles()` 和 `ForceLookFromCurrentTransforms()`，给阶段锚点传送后同步视角状态使用。

## 已清理旧试验脚本

- `Assets/CityLotBuildingGenerator.cs`：已删除。旧脚本只生成方块预览楼，不再作为城市资产摆放方案使用。
- `Assets/Editor/CityLotBuildingGeneratorEditor.cs`：已删除。对应旧 Inspector 按钮同步移除。
- 场景旧物体 `CityGenerator` 和 `Generated_City_Preview`：已从 `SampleScene` 清理。
- 后续城市建筑生成只使用 `CityLotAssetPlacer`、`CityBuildingPlacementSlot` 和 `CityLotAssetPlacerEditor`。

## 2026-07-05 高置信度旧脚本清理记录

本次清理目标是删除“已经被正式系统替代、没有有效场景/Prefab 依赖、后续继续保留反而容易误用”的旧脚本。删除前已检查脚本内容、引用关系和场景挂载，并通过 Unity MCP 移除了 `SampleScene` 中旧的 `DialogueSystem` / `DialogueTrigger` 物体。

| 已删除脚本 | 原功能 | 删除原因 | 当前替代入口 |
|---|---|---|---|
| `Assets/TerminalInteractable.cs` | 旧版空白终端入口，按 `E` 调用 `TerminalUIController.Open()`。 | 正式 TV 终端和最小循环终端已经拆分，继续保留会造成同一终端多个入口。 | `HearthTvTerminalInteractable` / `ResidentTerminalFlow` |
| `Assets/DialogueSystem.cs` | 旧版黑底对话面板，会自动生成 `Dialogue Canvas`。 | 正式字幕已经改为无黑底居中字幕和关卡字幕队列，旧面板会遮挡画面。 | `MinLoopSubtitlePlayer` / 关卡专用字幕数据 |
| `Assets/DialogueTrigger.cs` | 旧版看向物体按 `E` 触发 `DialogueSystem`。 | 当前剧情交互必须由关卡阶段控制，不再使用独立旧触发器。 | `HearthCompanion17F01ReplayController` / `HearthCompanion17F02ReplayController` |
| `Assets/DialogueLine.cs` | 旧版对话行数据，只服务 `DialogueSystem`。 | 旧对话系统删除后无独立用途。 | `MinLoopSubtitleLine` 或后续 `ReplayConfig` 数据 |
| `Assets/IDialogueSource.cs` | 旧版对话触发接口。 | 旧触发链路删除后无独立用途。 | 关卡流程公开方法和 UnityEvent |
| `Assets/Scripts/EventTemplpate.cs` | 早期 UnityEvent 练习模板。 | 没有引用，且命名拼写错误，后续不应作为模板。 | 需要事件时直接在正式脚本暴露 UnityEvent |
| `Assets/Scripts/UI/HearthHudDemoController.cs` | 早期运行时 HUD 全页预览，会硬编码生成临时 HUD。 | 正式 HUD/TV/机器人 HUD 已拆分；旧 Demo 容易产生黑框、重复信任度和占位 UI。 | `HearthHudRoot` / `HearthTvTerminalController` / `HearthCompanionHudController` |

本次同步修改：

1. `Assets/Editor/HearthTvTerminalPrefabBuilder.cs` 不再直接引用已删除的 `TerminalInteractable`，避免编译错误。
2. `SampleScene` 中旧 `DialogueSystem` 和 `DialogueTrigger` 物体已移除，避免 Missing Script。
3. 使用方式变化：以上脚本都不可再挂载；如果历史文档或旧制作清单仍提到它们，应按本节为准。

## CityLotAssetPlacer.cs

- 路径：`Assets/CityLotAssetPlacer.cs`
- 脚本作用：根据 `dikuai` 内的真实地块 Mesh 生成高密度写字楼街区资产摆放结果。正式建筑 Prefab 会先读取自身真实包围盒，再按地块高度档、退线、间距和真实占地判断能否放进地块。默认优先使用 `hazelwoodloft` 高层资产；`JapaneseCity` 只作为少量辅助楼体，低矮房屋会被过滤，少数中等高度且比例合适的日式楼体允许 XYZ 等比放大后使用。
- 挂载对象：推荐挂在场景中的空物体 `CityAssetPlacer` 上。旧的 `CityGenerator` 已清理，不再使用。
- 必需组件：
  - 场景中需要存在名为 `dikuai` 的地块模型。
  - `dikuai` 下需要有 `Group20`、`Group51`、`Group47`、`Group29`、`Group25`、`Group106`、`Group113`、`Group39`、`Group109` 等地块子物体。
  - 地块子物体需要有 MeshFilter 和 Renderer，脚本会从 Mesh 三角面读取地块平面轮廓。
  - 如果要摆放正式建筑，需要提前把购买/导入的建筑 Prefab 拖到 `Building Prefabs`。当前优先使用 `Assets/hazelwoodloft/CITY_DATA_NEW/new_prefabs/prefabs_day_buildings_skyscrapers`，并只少量混入 `Assets/JapaneseCity/Prefabs/Buildings` 中高度和比例合格的楼体。
- Inspector 字段：
  - `Lot Root`：拖入场景里的 `dikuai`。也可以点击 `Find dikuai Lot Root` 自动查找。
  - `Lot Root Name`：默认 `dikuai`，用于自动查找地块根物体。
  - `Target Lot Names`：要生成的地块名。当前默认 9 个测试地块：`Group20`、`Group51`、`Group47`、`Group29`、`Group25`、`Group106`、`Group113`、`Group39`、`Group109`。
  - `Generated Root Name`：默认 `CityAssets_AllTestLots_HeightControlled`。所有新生成的槽位、正式建筑 Prefab 和广告牌挂点都会放在这个父物体下。旧的 `CityAssets_AllTestLots_NoScale` 不会被自动清理，方便对比前后效果。
  - `Random Seed`：控制随机结果。相同地块、相同参数、相同资产列表会生成稳定布局。
  - `Clear Before Generate`：生成前是否清掉同名旧父级 `CityAssets_AllTestLots_HeightControlled`。默认开启。
  - `Edge Setback`：地块边缘退界距离，默认 `1.5` 米。第一版采用偏满的城市街区效果。
  - `Building Gap`：正式建筑之间的基础间距，默认 `3` 米。
  - `Large Building Extra Gap`：大体量或较高建筑之间的视觉间距，默认 `8` 米。
  - `Huge Building Extra Gap`：超高层或超大占地建筑之间的视觉间距，默认 `12` 米。
  - `Large Footprint Threshold` / `Huge Footprint Threshold`：判断大楼和超大楼的占地阈值，默认 `45` 米和 `60` 米。
  - `Lot Area Per Building`：估算每个地块目标建筑数量的密度参数，默认 `3600`。数值越小，尝试摆放越密。
  - `Max Buildings Per Lot`：每个地块最多摆放多少栋，默认 `90`，防止超大地块生成过多。
  - `Max Placement Attempts Per Lot`：每个地块最多试探多少次位置，默认 `1200`。地块形状越不规则，可能需要越高。
  - `Internal Road Width`、`Block Size Range`、`Occupancy Chance`、`Footprint Width Range`、`Building Height Range`：主要服务旧的占位楼/调试模式。正式 Prefab 原尺寸摆放时，不再生成可见内部路网，也不靠这些字段决定正式建筑尺寸。
  - `Use Height Planning Bands`：是否启用按地块分高度档的正式规则。默认开启。
  - `Global Minimum Building Height`：正式写字楼街区的全局最低高度，默认 `70` 米。
  - `Fallback Minimum Building Height`：当某地块因高度档太严格导致没有可用楼体时，最低只放宽到 `60` 米。
  - `Transition Lot Height Range`：小/过渡地块高度档，默认 `70-180` 米。
  - `Dense Lot Height Range`：中高密地块高度档，默认 `90-240` 米。
  - `Core Lot Height Range`：核心大地块高度档，默认 `120-320` 米。
  - `Dense Lot Area Threshold` / `Core Lot Area Threshold`：根据地块面积判断使用哪个高度档的阈值。
  - `Lot Height Overrides`：按地块单独指定高度规则的表。表中规则优先级高于自动面积分档；没有写进表的地块继续使用 `Transition/Dense/Core` 自动规则。
    - `Lot Name`：地块名，例如 `Group20`、`Group29`。
    - `Height Mode`：高度模式。`AutoByArea` 表示继续自动分档；`LowRiseDistrict` 约 `60-120m`；`MidRiseDistrict` 约 `80-180m`；`HighRiseDistrict` 约 `120-260m`；`LandmarkCore` 约 `180-360m`；`Custom` 使用下面的自定义高度。
    - `Min Height` / `Max Height`：只在 `Custom` 模式下作为自定义高度范围使用，例如 `70-150m`。如果最大值小于最小值，脚本会自动交换成有效范围。
  - `Building Prefabs`：购买或导入的建筑 Prefab 列表。正式模式会读取每个 Prefab 的真实 X/Z 占地和 Y 高度。
  - `Billboard Prefabs`：广告牌 Prefab 列表。可以放霓虹广告牌、屏幕、灯箱等。
  - `Min Building Prefab Height`：建筑候选的基础最低高度，默认 `70` 米。`JapaneseCity` 的中等高度候选会先进入临时池，再按等比放大规则判断是否可用。
  - `Min Building Prefab Footprint Edge`：建筑候选的最低占地最长边，默认 `6` 米。太小的物件不会被当作建筑主体摆放。
  - `Max Japanese City Share`：每个地块中 `JapaneseCity` 楼体最多占比，默认 `0.1`，也就是不超过约 10%。
  - `Allow Japanese City Uniform Upscale`：是否允许少数 `JapaneseCity` 楼体做 XYZ 等比放大。默认开启。
  - `Japanese City Upscale Source Min Height`：允许放大的日式楼体原始最低高度，默认 `45` 米。20/30 米级低矮房屋不会被放大。
  - `Japanese City Upscale Max Scale`：日式楼体最大等比放大倍率，默认 `1.8`。
  - `Japanese City Scaled Min/Max Footprint Edge`：日式楼体放大后占地最长边必须落在约 `18-62` 米范围内，避免比例和 hazelwoodloft 高层资产差太远。
  - `Japanese City Upscaled Target Max Height`：日式楼体放大后的目标高度上限，默认 `140` 米，避免把低层材质楼强行拉成超高层。
  - `Use Building Prefabs When Available`：有建筑 Prefab 时是否优先用 Prefab。默认开启。
  - `Create Placeholders When No Prefab`：没有建筑 Prefab 时是否生成占位建筑。默认开启。
  - `Prefab Fit Mode`：正式建筑默认保持 `KeepPrefabScale`。hazelwoodloft 和合格的原尺寸高层不会被缩放；只有符合 Japanese City 专用规则的楼体会做 XYZ 等比放大。
  - `Prefab Height Scale Range`：正式模式下保持 `1-1`，不用于改变真实建筑高度。
  - `Min Prefab Scale` / `Max Prefab Scale`：只服务旧缩放调试模式，不参与正式高度档生成。
  - `Create Road Placeholders`：是否生成内部道路占位块。默认关闭；当前需求不生成 `InternalRoad` 可见路块。
  - `Create Billboard Anchors`：是否给每个建筑生成广告牌挂点。
  - `Create Visible Billboard Placeholders`：没有广告牌 Prefab 时是否先显示一个发光广告牌占位。当前默认关闭，只保留挂点，避免测试建筑摆放时被临时广告牌干扰。
  - `Road Segment Length`：道路占位分段长度。分段是为了让不规则地块边缘处不要出现一整条道路穿出地块太多。
- 公开接口：
  - `FindLotRoot()`：自动查找场景中的 `dikuai`。
  - `GeneratePlacement()`：生成资产摆放结果。正式 Prefab 模式下会按原尺寸读取、筛选、摆放和贴地。
  - `ClearPlacement()`：删除 `CityAssets_AllTestLots_HeightControlled`。
  - `AutoFillDefaultBuildingPrefabs()`：在 Unity Editor 中自动扫描默认资产路径，把 `hazelwoodloft` 高层和 `JapaneseCity` 中低层建筑 Prefab 填入 `Building Prefabs`。
  - `SetBuildingPrefabs(GameObject[] prefabs)`：通过代码替换建筑 Prefab 池。
  - `SetBillboardPrefabs(GameObject[] prefabs)`：通过代码替换广告牌 Prefab 池。
  - `LotRoot`、`TargetLotNames`、`BuildingPrefabs`、`BillboardPrefabs`：允许其他工具脚本读取或设置核心输入。
- 关联脚本：
  - 生成的每个建筑槽位会自动添加 `CityBuildingPlacementSlot`，用于记录这个槽位属于哪个地块、占地大小、高度和广告牌挂点。
  - `CityLotAssetPlacerEditor` 给它提供 Inspector 按钮。
  - 与玩家控制、终端 UI、交互系统无直接关系。
- 使用步骤：
  1. 在 Hierarchy 中新建空物体，命名为 `CityAssetPlacer`。
  2. 给它添加 `CityLotAssetPlacer`。
  3. 点击 `Find dikuai Lot Root`，或手动把 `dikuai` 拖到 `Lot Root`。
  4. 确认 `Target Lot Names` 中是 9 个测试地块。
  5. 点击 `Auto Fill Imported Building Prefabs`，自动把 `hazelwoodloft` 高层 Prefab 和 `JapaneseCity` 中低层 Prefab 填进 `Building Prefabs`；如果想更精细控制，也可以手动拖 Prefab。
  6. 确认 `Use Height Planning Bands` 开启，`Global Minimum Building Height` 为 `70`，`Max Japanese City Share` 为 `0.1`。
  7. 如果某些地块要单独控制高度，在 `Lot Height Overrides` 中添加规则，例如 `Group20 = MidRiseDistrict`、`Group29 = LandmarkCore`、`Group51 = Custom 70-150m`。
  8. 如果有广告牌 Prefab，把广告牌 Prefab 拖进 `Billboard Prefabs`。
  9. 点击 `Generate Asset Placement Rules`，脚本会生成 `CityAssets_AllTestLots_HeightControlled`，并在 9 个地块下按高度档摆放建筑。
- 测试方式：
  - 点击生成后，场景中出现 `CityAssets_AllTestLots_HeightControlled`。
  - 其下出现 `AssetPlacement_Group20`、`AssetPlacement_Group51`、`AssetPlacement_Group47`、`AssetPlacement_Group29`、`AssetPlacement_Group25`、`AssetPlacement_Group106`、`AssetPlacement_Group113`、`AssetPlacement_Group39`、`AssetPlacement_Group109`。
  - 默认不出现 `InternalRoad` 可见内部道路。
  - hazelwoodloft 正式建筑实例的 `localScale` 保持 Prefab 原始值；`JapaneseCity` 只有在满足专用规则时允许 XYZ 等比放大。
  - 每个地块 Console 会输出高度档，例如 `transition 70-180m`、`override mid-rise 80-180m`、`override landmark 180-360m`，以及主资产候选数、Japanese City 候选数、Japanese City 实际使用数量和等比放大数量。
  - 在 `Lot Height Overrides` 中指定过的地块必须按对应高度范围筛选；没有指定过的地块保持自动分档。
  - 建筑底部贴合地块表面；即使 Prefab 轴心不在几何中心，脚本也会用真实渲染包围盒中心对齐槽位中心。
  - 建筑不压出地块边界，建筑之间有基本空隙。
  - 建筑朝向与地块主要边缘方向平行。
  - 如果某个地块因为真实资产尺寸限制只能放较少建筑，Console 会输出对应地块的放置数量和拒绝原因统计。
  - 原始 `dikuai` 和各个 Group 不被修改。
- 后续扩展：
  - 如果正式资产导入后比例差异很大，优先检查资产本身单位和导入 Scale Factor；只有 Japanese City 辅助楼体允许有限等比放大。
  - 如果建筑太密，增大 `Building Gap`、`Large Building Extra Gap`、`Huge Building Extra Gap`，或增大 `Lot Area Per Building`。
  - 如果建筑太稀，减小 `Lot Area Per Building`，或补充更多小占地建筑 Prefab。
  - 如果某片地块要做低区、中区、高区或地标区，优先在 `Lot Height Overrides` 里加规则，不需要复制多个生成器。
  - 如果确实需要把内部路网显示出来做调试，可以临时开启 `Create Road Placeholders`。
  - 如果需要更精确的不规则地块裁剪，可后续接入 Clipper2 等多边形裁剪库，把当前采样式判断升级为真正的多边形内缩和切割。
- 最近更新：使用方式有变化。新增 `Lot Height Overrides` 地块高度规则表，可以对单个 Group 指定 `LowRiseDistrict`、`MidRiseDistrict`、`HighRiseDistrict`、`LandmarkCore` 或 `Custom` 高度范围；旧的 `CityGenerator` 预览生成器已删除，正式城市生成只使用 `CityAssetPlacer`。
- 当前场景整理：此前多批 `CityAssets_Batch_...` 生成父级已整合到 `CityBuildings_ByHeight` 下，按实际高度分为 `Buildings_High_180mPlus`、`Buildings_Mid_120_179m`、`Buildings_Low_Under120m` 三个父级；旧批次父级已清理，便于统一管理。

## CityBuildingPlacementSlot.cs

- 路径：`Assets/CityBuildingPlacementSlot.cs`
- 脚本作用：记录每一个由 `CityLotAssetPlacer` 生成的建筑槽位信息，包括所属地块、槽位编号、目标占地、目标高度、朝向、内容挂载点和广告牌挂点。它相当于每个建筑位置的“身份证”和替换接口。
- 挂载对象：不需要手动挂载。`CityLotAssetPlacer` 生成每个 `BuildingSlot_地块名_编号` 时会自动添加。
- 必需组件：无额外必需组件。脚本依赖生成出来的 `Content` 子物体作为建筑内容挂载点。
- Inspector 字段：
  - `Lot Name`：该槽位所属地块，例如 `Group20`。
  - `Slot Index`：该地块内的槽位编号。
  - `Footprint Size`：该槽位建议的建筑占地尺寸。
  - `Target Height`：该槽位建议的建筑高度，主要用于占位楼和广告牌挂点高度。
  - `Facing Angle`：该槽位沿地块边缘估算出的朝向角度。
  - `Content Root`：建筑 Prefab 或占位楼挂载的位置。
  - `Billboard Anchor`：广告牌建议挂载点。
- 公开接口：
  - `ClearContent()`：清空当前槽位里的建筑内容。
  - `ReplaceWithPrefab(GameObject prefab)`：清空当前内容，并把指定 Prefab 放入 `Content Root`。
  - `LotName`、`SlotIndex`、`FootprintSize`、`TargetHeight`、`FacingAngle`、`ContentRoot`、`BillboardAnchor`：只读属性，供后续工具脚本读取。
  - `Configure(...)`：由 `CityLotAssetPlacer` 生成槽位时写入槽位信息。
- 关联脚本：
  - 由 `CityLotAssetPlacer` 自动创建和配置。
  - 后续如果做“单独替换某一栋楼”“手动锁定某个建筑”“给某个槽位指定广告牌”，可以基于这个脚本扩展。
- 使用步骤：
  1. 正常情况下不需要手动添加。
  2. 生成后在 `CityAssets_AllTestLots_HeightControlled/AssetPlacement_GroupXX/BuildingSlot_...` 上可以看到这个组件。
  3. 如果只想替换某个槽位的建筑，可以选中该槽位，通过后续工具或代码调用 `ReplaceWithPrefab()`。
- 测试方式：
  - 生成资产摆放规则后，每个 `BuildingSlot_...` 上都有 `CityBuildingPlacementSlot`。
  - `Content Root` 指向该槽位下的 `Content`。
  - `Billboard Anchor` 指向该槽位下的 `BillboardAnchor_Front`。
- 后续扩展：
  - 可以增加“锁定此槽位不重生”“指定建筑类型”“指定广告牌类型”“是否为沿街主楼”等字段。
  - 可以配合自定义 Editor 做单槽位替换按钮。
- 最近更新：新增建筑槽位记录组件和 Prefab 替换接口。

## CityLotAssetPlacerEditor.cs

- 路径：`Assets/Editor/CityLotAssetPlacerEditor.cs`
- 脚本作用：给 `CityLotAssetPlacer` 的 Inspector 增加操作按钮和提示说明，方便在 Unity 里一键查找地块、生成资产摆放规则、清除生成结果。
- 挂载对象：不需要手动挂载。它是 Editor 脚本，只要项目中存在 `CityLotAssetPlacer`，选中挂载该脚本的物体时自动生效。
- 必需组件：
  - 需要项目中存在 `CityLotAssetPlacer.cs`。
  - 文件必须放在 `Assets/Editor` 文件夹下。
- Inspector 字段：无独立字段。
- 公开接口：无运行时公开接口。
- 关联脚本：
  - 只服务 `CityLotAssetPlacer`。
- 使用步骤：
  1. 选中带有 `CityLotAssetPlacer` 的 `CityAssetPlacer`。
  2. 点击 `Find dikuai Lot Root` 自动绑定地块根物体。
  3. 点击 `Auto Fill Imported Building Prefabs` 自动收集当前已导入的建筑资产。
  4. 点击 `Generate Asset Placement Rules` 生成原尺寸建筑摆放结果和建筑槽位。
  5. 点击 `Clear Generated Asset Placement` 删除生成结果。
- 测试方式：
  - 选中 `CityAssetPlacer` 后能看到四个按钮。
  - 点击自动收集按钮后，`Building Prefabs` 会填入默认资产包中的候选建筑。
  - 点击生成按钮后场景出现 `CityAssets_AllTestLots_HeightControlled`。
  - 点击清除按钮后生成结果被删除。
- 后续扩展：
  - 可以继续加“只生成道路”“只生成广告牌挂点”“扫描资产文件夹并自动填入 Prefab 列表”等按钮。
- 最近更新：使用方式无变化。Inspector 提示语已改为高度档高层摆放说明；按钮仍用于查找 `dikuai`、自动填入已导入建筑 Prefab、生成/清除地块资产摆放结果。

## CityFacadeBillboardPlacer.cs

- 路径：`Assets/CityFacadeBillboardPlacer.cs`
- 脚本作用：在已经生成好的城市建筑立面上批量生成空白广告牌占位。它参考东京、秋叶原、高密度商业街区和赛博朋克城市的“大小混合、部分覆盖立面、偏发光屏幕”的广告牌摆放方式；广告牌位置和大小仍由本脚本负责，图片与循环动画内容改由 `CityBillboardContentDistributor` 统一分配。
- 挂载对象：推荐新建空物体 `CityBillboardPlacer`，把脚本挂在这个空物体上。先不要挂到建筑 Prefab 或地块模型上。
- 必需组件：
  - 场景里需要先有 `CityLotAssetPlacer` 生成的建筑结果，例如 `CityAssets_Batch_...` 或 `CityAssets_AllTestLots_HeightControlled`。
  - 生成出来的建筑槽位上需要有 `CityBuildingPlacementSlot`。
  - 建筑内容下需要有 Renderer，脚本通过 Renderer 的包围盒判断建筑立面宽度和高度。
- Inspector 字段：
  - `Use Generated City Slots`：是否扫描 `CityLotAssetPlacer` 生成的建筑槽位。当前广告屏需求只针对你手动挑出的 `BUILDING`，所以场景里的 `CityBillboardPlacer` 已设置为关闭。
  - `Generated City Roots`：要扫描的城市生成父级。可以手动拖入 `CityAssets_Batch_...`，也可以点 `Find Generated City Roots` 自动查找。
  - `Generated City Root Name Hints`：自动查找用的名字片段。默认会找 `CityAssets_Batch_` 和 `CityAssets_AllTestLots_HeightControlled`。
  - `Use Manual Building Roots`：是否扫描手动指定的普通模型父级。当前场景已开启，用于只扫描 `BUILDING`。
  - `Manual Building Roots`：手动挑选出来、需要生成广告屏的建筑父级。当前场景中拖入的是 `BUILDING`。
  - `Manual Building Root Name Hints`：自动查找手动父级时使用的名字，默认 `BUILDING`。点击 `Find Manual BUILDING Roots` 可以自动绑定。
  - `Include Inactive Buildings`：是否扫描未激活物体，默认开启。
  - `Parent Mode`：广告牌生成后的父级方式。默认 `UnderSharedRoot`，会统一放到 `CityBillboards_Generated` 下，方便整体选择、隐藏、删除和批量编辑；`UnderBuildingSlot` 才会在每个建筑槽位下创建 `Billboards` 子物体。旧组件会自动迁移一次到 `UnderSharedRoot`。
  - `Shared Billboard Root Name`：选择 `UnderSharedRoot` 时使用的统一父级名称。当前场景已设置为 `CityBillboards_BUILDING`，表示只服务 `BUILDING` 里挑出来的建筑。
  - `Clear Before Generate`：生成前是否清掉目标范围内旧的广告牌。默认关闭，避免你已经手动调整过的广告牌在再次运行工具时被删掉。如果想完全重来，可以先点 `Clear Generated Facade Billboards`。
  - `Random Seed`：控制随机结果。相同建筑、相同参数、相同随机种子会得到稳定广告牌分布。
  - `Building Use Chance`：每栋建筑被放广告牌的概率，默认约 `0.38`。数值越高，广告牌越密。
  - `Min/Max Billboards Per Building`：每栋建筑最多生成多少块广告牌。默认 `0-2`，避免每栋楼都贴满。
  - `Min Building Height`：低于该高度的建筑不生成广告牌，默认 `60m`。
  - `Min Facade Width`：立面宽度太小则跳过，默认 `10m`。
  - `Size Mode`：广告牌尺寸模式。`Mixed` 会混合小牌、中牌、大牌和横向立面带；也可以强制用 `Small`、`Medium`、`Large`、`FacadeBand`。
  - `Vertical Sign Chance`：生成竖向招牌的概率，默认 `0.22`。
  - `Facade Band Chance`：生成横向大屏/立面带广告牌的概率，默认 `0.18`。
  - `Absolute Width/Height Range`：广告牌绝对尺寸范围，避免过小或过大。
  - `Max Facade Width Coverage`：广告牌最多覆盖当前立面宽度的比例，默认 `0.92`，不会把整面墙横向全部盖死。
  - `Max Building Height Coverage`：广告牌最多覆盖建筑高度的比例，默认 `0.42`，不会做成整栋楼满高外皮。
  - `Vertical Position Range`：广告牌在建筑高度上的分布范围，默认偏中上部。
  - `Facade Offset`：广告牌离立面的外凸距离，默认 `0.42m`，减少和建筑墙面穿模。
  - `Minimum Top/Bottom Clearance`：广告牌距离楼顶和地面的最小空隙。
  - `Avoid Duplicate Facade Per Building`：同一栋楼尽量不要在同一面重复放太多广告牌，默认开启。
  - `Billboard Prefabs`：后续如果你买了霓虹灯箱、LED 屏幕、广告牌框架等 Prefab，可以拖到这里。
  - `Use Billboard Prefabs When Available`：是否直接混用上面的广告牌 Prefab。当前默认关闭，因为第一版只需要空白占位。
  - `Create Placeholder Surface`：是否生成空白广告牌面板，默认开启。
  - `Placeholder Base/Emission Color`：空白面板的底色和发光颜色，只作为临时视觉占位。
  - `Placeholder Emission Intensity`：临时面板发光强度。
  - `Add Collider`：是否给广告牌保留 Collider。默认关闭，避免影响玩家碰撞和射线交互。
  - `Skip Inactive Building Slots`：是否跳过被隐藏或禁用的建筑槽位。默认开启，避免你手动隐藏某栋楼后工具还在它原来的位置生成广告牌。
  - `Require Active Renderer`：是否要求建筑 Renderer 处于启用和可见层级中。默认开启，避免对已经隐藏的模型生成广告牌。
  - `Cleanup Orphan Billboards Before Generate`：当 `Clear Before Generate` 关闭时，生成前是否先清理失去建筑来源的孤儿广告牌。默认开启。
  - `Skip Buildings With Existing Billboards`：是否跳过已经生成过广告牌的建筑。默认开启，避免脚本运行多次后同一栋楼不断叠加新的广告牌。
- 公开接口：
  - `FindGeneratedCityRoots()`：自动查找场景中符合命名的城市生成父级。
  - `FindManualBuildingRoots()`：自动查找场景中的 `BUILDING` 等手动建筑父级。
  - `GenerateBillboards()`：在目标城市建筑上生成空白广告牌占位。
  - `ClearBillboards()`：清理目标范围内由该工具生成的广告牌。
  - `ClearOrphanBillboards()`：只清理建筑已删除、建筑内容为空或 Renderer 已隐藏导致失去依附对象的广告牌。
  - `SetBillboardPrefabs(GameObject[] prefabs)`：通过代码替换广告牌 Prefab 池。
  - `GeneratedCityRoots`：允许其他工具脚本读取或设置要扫描的城市父级。
- 关联脚本：
  - 读取 `CityBuildingPlacementSlot` 来知道每栋建筑属于哪个地块、槽位编号和建筑内容根节点。
  - 也可以直接读取 `Manual Building Roots` 下的普通建筑模型，不要求这些模型带 `CityBuildingPlacementSlot`。
  - 生成的每块广告牌会自动添加 `CityBillboardPlacementSlot`，用于记录它挂在哪栋楼、在哪个立面、尺寸是多少、后续内容挂点在哪里；重复运行时，`CityFacadeBillboardPlacer` 会读取这些记录，跳过已经有广告牌的建筑。
  - 新生成的 `BlankBillboardSurface` 会自动添加 `CityBillboardContentController`，因此不需要再逐块手动挂图片/视频控制脚本。
  - `CityBillboardContentDistributor` 可以读取这些槽位，批量分配图片和循环动画；这不会改变广告牌的位置、尺寸或所属建筑。
  - `CityFacadeBillboardPlacerEditor` 给它提供 Inspector 按钮。
- 使用步骤：
  1. 先用 `CityLotAssetPlacer` 生成城市建筑。
  2. 在 Hierarchy 新建空物体，命名为 `CityBillboardPlacer`。
  3. 给 `CityBillboardPlacer` 添加 `CityFacadeBillboardPlacer`。
  4. 如果要给所有脚本生成建筑加广告牌，开启 `Use Generated City Slots` 并点击 `Find Generated City Roots`。
  5. 如果只给你手动挑出来的建筑加广告牌，关闭 `Use Generated City Slots`，开启 `Use Manual Building Roots`，把 `BUILDING` 拖进 `Manual Building Roots`，或点击 `Find Manual BUILDING Roots`。
  6. 保持 `Parent Mode = UnderSharedRoot`，广告牌会统一生成到 `Shared Billboard Root Name` 指定的父级下；当前场景使用 `CityBillboards_BUILDING`。
  7. 保持 `Use Billboard Prefabs When Available` 关闭时，会生成可直接接收图片/视频的空白屏幕；正式框架 Prefab 仍可后续替换。
  8. 调整 `Building Use Chance`、`Max Billboards Per Building`、`Size Mode`、`Vertical Sign Chance` 等参数来控制疏密和风格。
  9. 保持 `Skip Buildings With Existing Billboards` 开启。脚本再次运行时，已经有广告牌的建筑会被跳过。
  10. 需要真正生成时，点击 `Generate Blank Facade Billboards`。
  11. 如果想完全重来，点击 `Clear Generated Facade Billboards`，调整参数后重新生成。
- 测试方式：
  - 生成前确认场景中已有城市建筑父级，且建筑槽位上存在 `CityBuildingPlacementSlot`；如果只生成 `BUILDING` 广告屏，则确认 `Manual Building Roots` 指向 `BUILDING`。
  - 点击生成后，场景中出现或复用统一父级 `CityBillboards_Generated`，广告牌都在这个父级下。
  - 每个 `Billboard_...` 上都有 `CityBillboardPlacementSlot`。
  - 每块实际屏幕表面都有 `CityBillboardContentController`；如果旧槽位没有 Renderer，内容管理器的 `Prepare Existing Billboard Screens` 会在原位补齐显示面。
  - 连续点击两次生成时，已经有广告牌的建筑不会继续叠加新的广告牌；Console 会输出跳过数量。
  - 广告牌位于建筑外立面，不应大量漂浮在楼体内部。
  - 广告牌尺寸有大有小，包含少量竖向牌和横向大屏，但不会覆盖整栋建筑高度。
  - 如果某个建筑槽位下的建筑模型被删除，或建筑 Renderer 被禁用，再次生成时不会在该位置生成广告牌。
  - 如果先生成广告牌再删除建筑，点击 `Clear Orphan Facade Billboards` 可以清理失去建筑来源的广告牌。
  - 关闭 `Add Collider` 时，广告牌不会额外阻挡玩家或交互射线。
  - 点击清理按钮后，生成的广告牌被删除，原建筑和地块不被修改。
- 后续替换或扩展：
  - 批量图片和短动画不要再逐块手工换材质，优先使用 `CityBillboardContentDistributor` 的两个媒体池和重新分配按钮。
  - 单独指定某一块屏幕时，可直接调用该表面上的 `CityBillboardContentController.SetImage()` 或 `SetVideo()`。
  - 如果要换成正式广告牌 Prefab，把 Prefab 拖入 `Billboard Prefabs`，再开启 `Use Billboard Prefabs When Available`。
  - 如果想让广告牌更贴合复杂墙面，可后续改成 URP Decal Projector 方案；当前版本优先用独立面板，便于管理和替换。
- 最近更新：使用方式有变化。新生成的屏幕表面会自动带 `CityBillboardContentController`，可以被统一内容分配器直接接管；广告牌的共享父级、重复生成跳过和孤儿清理方式保持不变。
- 当前场景配置：`CityBillboardPlacer` 已设置为只扫描 `BUILDING`，不扫描整理后的 `CityBuildings_ByHeight`；广告牌统一生成到 `CityBillboards_BUILDING`；每个 `BUILDING` 子建筑默认尝试生成 `1-2` 个广告牌；重复运行时跳过已有广告牌的建筑。现有 26 个广告牌槽位已经全部具备媒体显示面和内容控制器。

## CityBillboardPlacementSlot.cs

- 路径：`Assets/CityBillboardPlacementSlot.cs`
- 脚本作用：记录每一块由 `CityFacadeBillboardPlacer` 生成的广告牌信息，包括所属建筑槽位、所属地块、建筑槽位编号、所在立面、广告牌尺寸、高度位置、表面根节点和内容挂载点。
- 挂载对象：不需要手动挂载。`CityFacadeBillboardPlacer` 生成 `Billboard_...` 时会自动添加。
- 必需组件：无额外必需组件。脚本依赖生成出来的 `SurfaceRoot` 和 `ContentRoot` 子物体。
- Inspector 字段：
  - `Building Slot`：该广告牌挂在哪个 `CityBuildingPlacementSlot` 上。
  - `Source Building Root`：该广告牌挂在哪个普通建筑模型根物体上。用于 `BUILDING` 这种手动父级里的普通模型。
  - `Lot Name`：所属地块名，例如 `Group20`。
  - `Building Slot Index`：所属建筑槽位编号。
  - `Facade Side`：挂在建筑的 `Front`、`Back`、`Left` 或 `Right` 立面。
  - `Billboard Size`：广告牌宽高尺寸。
  - `Height On Building`：广告牌中心点在建筑局部高度上的位置。
  - `Surface Root`：空白广告牌面板或正式广告牌 Prefab 的表面根节点。
  - `Content Root`：后续放置文字、灯箱框架或自定义广告 Prefab 的挂载点。普通图片和视频现在直接显示在 `Surface Root` 的屏幕上。
- 公开接口：
  - `SetContent(GameObject contentPrefab)`：把正式广告内容 Prefab 放到 `ContentRoot`。
  - `ClearContent()`：清空 `ContentRoot` 下的内容。
  - `SetSurfaceMaterial(Material material)`：把广告牌表面材质替换成指定材质。
  - `BuildingSlot`、`SourceBuildingRoot`、`LotName`、`BuildingSlotIndex`、`FacadeSide`、`BillboardSize`、`HeightOnBuilding`、`SurfaceRoot`、`ContentRoot`：只读属性，供后续工具或剧情系统读取。
  - `Configure(...)`：由 `CityFacadeBillboardPlacer` 生成广告牌时写入信息。
- 关联脚本：
  - 由 `CityFacadeBillboardPlacer` 自动创建和配置。
  - 如果广告牌来自脚本生成建筑，会依赖 `CityBuildingPlacementSlot` 来知道广告牌和建筑之间的关系。
  - 如果广告牌来自 `BUILDING` 这类手动父级，则通过 `Source Building Root` 记录普通模型根物体。
- 使用步骤：
  1. 正常情况下不需要手动添加。
  2. 生成广告牌后，选中任意 `Billboard_...` 可以查看该组件记录的尺寸和立面信息。
  3. 后续正式广告框架或文字 Prefab 做好后，把它放入 `ContentRoot`，或通过代码调用 `SetContent()`；图片/视频则交给 `CityBillboardContentController`。
- 测试方式：
  - 每块由工具生成的广告牌都带有 `CityBillboardPlacementSlot`。
  - `Building Slot` 指向对应建筑槽位。
  - `Surface Root` 指向空白广告牌面板。
  - `Content Root` 存在且为空，方便后续替换。
- 后续替换或扩展：
  - 可以增加“广告牌类型”“是否播放视频”“广告内容 ID”“是否夜间发光”等字段。
  - 可以配合剧情系统或时间系统，按章节替换某些广告牌内容。
- 最近更新：使用方式有变化。`Content Root` 继续用于自定义 Prefab；图片和短视频改由 `Surface Root` 上的媒体控制器显示。原有 `Source Building Root`、挂载位置和公开接口不变。

## CityBillboardContentController.cs

- 路径：`Assets/Scripts/Environment/City/CityBillboardContentController.cs`
- 脚本作用：控制单块广告牌屏幕显示一张图片，或播放一个 Unity `VideoClip`。视频默认静音、自动循环，适合几秒到约 10 秒的短动画广告。
- 挂载对象：挂在实际带 Renderer 的广告牌表面，例如 `Billboard_.../SurfaceRoot/BlankBillboardSurface`。正常不需要手动挂载：新广告牌由 `CityFacadeBillboardPlacer` 自动添加，旧广告牌由内容分配器的准备按钮自动补齐。
- 必需组件：
  - 同一 GameObject 必须有 Renderer，脚本使用 `[RequireComponent(typeof(Renderer))]` 保证这一点。
  - 分配视频后会自动添加 Unity 内置 `VideoPlayer`，不需要手动创建 RenderTexture。
- Inspector 字段：
  - `Target Renderer`：要显示媒体的屏幕 Renderer，自动绑定同一物体上的 Renderer。
  - `Content Kind`：当前是 `None`、`Image` 或 `Video`，通常由分配器自动填写。
  - `Image Texture`：当前分配的图片。
  - `Video Clip`：当前分配的短动画视频。
  - `Play On Awake`：进入 Play Mode 后是否自动播放，默认开启。
  - `Loop Video`：视频播完后是否重复播放，默认开启。
  - `Mute Video`：视频是否静音，默认开启，避免城市里多块广告屏同时发声。
  - `Pause When Invisible`：摄像机看不到该屏幕时是否暂停视频，默认开启，用于减少不必要的视频解码开销。
- 公开接口：
  - `Configure(Renderer renderer)`：绑定目标屏幕 Renderer。
  - `SetImage(Texture texture)`：把单块屏幕切换为指定图片。
  - `SetVideo(VideoClip clip)`：把单块屏幕切换为指定短视频，并按当前循环设置播放。
  - `ClearContent()`：清空当前图片或视频分配。
  - `Play()`、`Pause()`、`Stop()`：控制当前视频播放状态。
  - `ApplyAssignedContent()`：重新应用 Inspector 中已经记录的内容。
  - `ContentKind`、`ImageTexture`、`VideoClip`、`TargetRenderer`：供其他脚本读取当前状态。
- 关联脚本：
  - 由 `CityFacadeBillboardPlacer` 给新广告牌自动添加。
  - 由 `CityBillboardContentDistributor` 批量查找、配置和切换图片/视频。
  - 它不负责广告牌的位置和尺寸，因此重新分配媒体不会移动任何广告牌。
- 使用步骤：
  1. 一般不需要单独操作，先在统一内容管理器中填入图片和视频资源。
  2. 如果只想改某一块，选中该广告牌的 `BlankBillboardSurface`，直接拖入 `Image Texture` 或通过其他脚本调用 `SetImage()` / `SetVideo()`。
  3. 进入 Play Mode 测试视频循环；编辑状态只保存分配，不会持续播放视频。
- 测试方式：
  - 图片分配后屏幕显示对应 Texture，不创建独立材质副本。
  - 视频分配后同物体自动出现 `VideoPlayer`，`Is Looping` 开启且默认无声音。
  - 视频播放结束后重新从头播放。
  - 关闭或看不到屏幕时视频暂停，再次可见时继续。
  - 清空内容后 `VideoPlayer` 停止，屏幕恢复空白占位状态。
- 后续替换或扩展：
  - 可以增加亮度、色温、UV 裁切和保持原始宽高比等单屏参数。
  - 如果将来需要直播流或网页内容，可在这里增加 URL / RenderTexture 模式；当前版本只接收导入 Unity 的 Texture 和 VideoClip。
- 最近更新：新增脚本，负责单块屏幕的图片显示、短视频静音循环和不可见暂停。

## CityBillboardContentDistributor.cs

- 路径：`Assets/CityBillboardContentDistributor.cs`
- 脚本作用：统一管理某个广告牌父级下的全部屏幕，从多个图片和多个动画资源中随机选择，并按可调整权重重新分配。默认按“图片多、动画少”解释为 `图片 : 动画 = 7 : 3`。
- 挂载对象：当前场景挂在 `CityBillboardPlacer` 上，与 `CityFacadeBillboardPlacer` 共用同一个管理物体。不要把它挂到每块广告牌上。
- 必需组件：
  - 目标父级下的每块广告牌需要有 `CityBillboardPlacementSlot` 和 `Surface Root`。
  - 屏幕原本没有 Renderer 时，本脚本会在原槽位、原位置补建 `BlankBillboardSurface_Media`，不会新增广告牌槽位或移动广告牌。
  - 图片要以 Unity Texture 导入；动画要以 Unity `VideoClip` 导入。
- Inspector 字段：
  - `Billboard Root`：要管理的广告牌统一父级。当前指向 `CityBillboards_BUILDING`。
  - `Billboard Root Name`：自动查找父级时使用的名字，默认 `CityBillboards_BUILDING`。
  - `Include Inactive Billboards`：是否同时准备未激活的广告牌槽位，默认开启。
  - `Media Surface Material`：补建缺失屏幕时复用的表面材质。留空时优先复用已有广告牌材质。
  - `Repaired Surface Thickness`：补建屏幕的厚度，默认 `0.28m`。
  - `Image Contents`：图片资源池，可放多张 PNG/JPG 导入后的 Texture。
  - `Animation Contents`：短动画资源池，可放多个几秒到约 10 秒的 VideoClip。
  - `Image Weight`：图片权重，默认 `7`。
  - `Animation Weight`：动画权重，默认 `3`。因此默认约 70% 是图片、30% 是循环动画。
  - `Use Exact Whole Set Ratio`：开启时按整个父级精确计算数量；当前 26 块屏幕在 7:3 下会分配约 18 张图片屏和 8 个动画屏。关闭时每块独立按概率抽取，最终数量可能有波动。
  - `Random Seed`：控制本次随机顺序和具体资源选择。
  - `Advance Seed After Redistribute`：每次重新分配后自动增加随机种子，默认开启，因此重复点击会得到新的组合。
  - `Last Billboard/Image/Animation Count`：显示上一次分配的实际统计结果。
- 公开接口：
  - `FindBillboardRoot()`：按名称查找统一广告牌父级。
  - `PrepareExistingBillboards()`：给现有广告牌补齐显示面和单屏控制器，但不分配媒体。
  - `RedistributeAll()`：按当前资源池、比例和随机种子重新分配全部广告牌内容，不改变位置和尺寸。
  - `SetWeights(float imageWeight, float animationWeight, bool redistributeImmediately)`：由其他脚本调整图片/动画比重，并可立即重新分配。
  - `ClearAllContent()`：清空全部媒体分配，保留广告牌位置、槽位和屏幕表面。
  - `BillboardRoot`：允许其他脚本设置目标父级。
  - `ImageWeight`、`AnimationWeight`、`ImageShare`：读取当前权重和换算后的图片占比。
- 关联脚本：
  - 读取 `CityBillboardPlacementSlot` 确定哪些对象是真正的广告牌槽位。
  - 自动创建或调用 `CityBillboardContentController` 执行单屏内容切换。
  - 不重新运行 `CityFacadeBillboardPlacer`，所以不会在已有建筑上重复生成广告牌。
- 使用步骤：
  1. 把广告图片导入 Unity；把短动画文件导入后确认 Inspector 中类型为 `VideoClip`。
  2. 选中 `CityBillboardPlacer`，找到 `CityBillboardContentDistributor`。
  3. 把所有图片拖入 `Image Contents`，把所有动画拖入 `Animation Contents`。
  4. 保持默认 `Image Weight = 7`、`Animation Weight = 3`，或填入新的比例，例如 `8:2`、`6:4`。
  5. 旧广告牌第一次使用时点 `Prepare Existing Billboard Screens`；当前场景已经准备完成。
  6. 点击 `Redistribute Images And Animations`，所有屏幕会按比例随机获得内容。
  7. 以后调整权重或替换资源后，再点同一个重新分配按钮即可；不需要重新生成广告牌。
- 测试方式：
  - 当前 `CityBillboards_BUILDING` 下有 26 个槽位，并已准备 26 个 `CityBillboardContentController`。
  - 资源池为空时只给出提示，不删除广告牌、不改变现有内容。
  - 同时存在图片和视频且权重为 7:3 时，统计结果应接近或等于 18 张图片、8 个动画。
  - 调整为 5:5 后重新分配，图片/动画数量应接近各 13 个。
  - 连续重新分配只改变媒体内容，不新增广告牌、不改变广告牌 Transform。
  - Unity Console 无红色脚本错误。
- 后续替换或扩展：
  - 可以增加分区资源池，例如不同地块使用不同广告内容。
  - 如果动画数量和分辨率变多，可增加玩家距离分级、同时播放上限或共享 RenderTexture 优化。
- 最近更新：新增脚本。当前场景已经挂载到 `CityBillboardPlacer` 并绑定 `CityBillboards_BUILDING`；默认权重为图片 7、动画 3。项目当前没有发现可直接作为广告内容的视频文件，因此只完成接口和 26 块屏幕准备，尚未执行媒体随机分配。

## CityBillboardContentDistributorEditor.cs

- 路径：`Assets/Editor/CityBillboardContentDistributorEditor.cs`
- 脚本作用：给内容分配器增加查找父级、准备旧屏幕、重新分配媒体和清空媒体四个 Inspector 按钮。
- 挂载对象：不需要手动挂载；文件必须保留在 `Assets/Editor` 下。
- 必需组件：项目中存在 `CityBillboardContentDistributor.cs`。
- Inspector 字段：无独立字段。
- 公开接口：无运行时公开接口。
- 关联脚本：只服务 `CityBillboardContentDistributor`。
- 使用步骤：选中 `CityBillboardPlacer` 后，在内容分配器底部使用四个按钮。
- 测试方式：按钮只处理内容和缺失显示面；不会调用广告牌位置生成器，不会改动建筑。
- 后续替换或扩展：可增加“只重新分配选中广告牌”“按地块分配”“导出分配清单”等按钮。
- 最近更新：新增 Editor 面板，使用方式见 `CityBillboardContentDistributor.cs`。

## CityFacadeBillboardPlacerEditor.cs

- 路径：`Assets/Editor/CityFacadeBillboardPlacerEditor.cs`
- 脚本作用：给 `CityFacadeBillboardPlacer` 的 Inspector 增加查找城市父级、生成空白广告牌和清理生成结果的按钮；顶部提示现在也会说明图片/视频应交给 `CityBillboardContentDistributor`。
- 挂载对象：不需要手动挂载。它是 Editor 脚本，只要项目中存在 `CityFacadeBillboardPlacer`，选中挂载该脚本的物体时自动生效。
- 必需组件：
  - 需要项目中存在 `CityFacadeBillboardPlacer.cs`。
  - 文件必须放在 `Assets/Editor` 文件夹下。
- Inspector 字段：无独立字段。
- 公开接口：无运行时公开接口。
- 关联脚本：
  - 只服务 `CityFacadeBillboardPlacer`。
- 使用步骤：
  1. 选中带有 `CityFacadeBillboardPlacer` 的 `CityBillboardPlacer`。
  2. 点击 `Find Generated City Roots` 自动绑定城市建筑父级。
  3. 点击 `Find Manual BUILDING Roots` 自动绑定 `BUILDING` 手动建筑父级。
  4. 点击 `Generate Blank Facade Billboards` 生成空白广告牌占位。
  5. 如果你删除或隐藏了部分建筑，点击 `Clear Orphan Facade Billboards` 只清理失去建筑来源的广告牌。
  6. 点击 `Clear Generated Facade Billboards` 清理全部生成的广告牌。
- 测试方式：
  - 选中 `CityBillboardPlacer` 后能看到五个按钮。
  - 自动查找按钮能把 `CityAssets_Batch_...` 等父级填入 `Generated City Roots`。
  - 手动父级查找按钮能把 `BUILDING` 填入 `Manual Building Roots`。
  - 生成按钮能在建筑立面上创建 `Billboard_...`。
  - 孤儿清理按钮只删除已经没有有效建筑 Renderer 的广告牌。
  - 清理按钮只删除广告牌，不删除建筑。
- 后续扩展：
  - 可以继续加“只给选中地块生成广告牌”“按地块设置广告牌密度”“按高度过滤广告牌”等按钮。
- 最近更新：使用方式无变化。按钮和挂载方式不变，只更新了 Inspector 提示，说明新屏幕会自动带媒体控制器，图片/视频通过统一内容分配器管理。

## Free Wood Door Pack 命名空间修复

- 路径：
  - `Assets/Free Wood Door Pack/Script/Door.cs`
  - `Assets/Free Wood Door Pack/Script/CameraOpenDoor.cs`
- 脚本作用：这是导入门资产包自带的开门脚本。原资产包使用了 `namespace DoorScript`，会和项目里已有的全局类 `DoorScript` 撞名，导致 Unity 报 `CS0101` 编译错误。
- 本次修改：将资产包内部命名空间从 `DoorScript` 改为 `FreeWoodDoorPack`，并同步修改 `CameraOpenDoor.cs` 中对 `Door` 组件的引用。
- 挂载对象：使用方式无变化。仍然按资产包原本方式挂在对应门和相机/玩家检测物体上。
- 必需组件：使用方式无变化。`Door.cs` 仍需要挂载对象带 `AudioSource`。
- Inspector 字段：使用方式无变化。字段仍为原资产包自带字段，例如 `open`、`smooth`、`openDoor`、`closeDoor`、`DistanceOpen`、`text`。
- 公开接口：使用方式无变化。`Door.OpenDoor()` 仍然用于切换开门/关门。
- 关联脚本：`CameraOpenDoor.cs` 现在调用 `FreeWoodDoorPack.Door`，避免和项目已有 `DoorScript` 类冲突。
- 测试方式：
  - Unity 刷新后 Console 不再出现 `The namespace '<global namespace>' already contains a definition for 'DoorScript'`。
  - 如果后续使用这个门包自带的门，按原资产包方式测试按 `E` 开门/关门。
- 后续替换或扩展：
  - 如果这个门包只作为素材库，不使用它自带脚本，也可以保持当前命名空间修复，不影响模型和材质使用。
- 最近更新：修复命名空间冲突。使用方式无变化。

## MinLoopTypes.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopTypes.cs`
- 脚本作用：定义最小循环通用数据类型，包括流程阶段 `MinLoopStage`、A/B 处置枚举 `MinLoopDispositionChoice`、流程事件 `MinLoopStageEvent`、处置事件 `MinLoopDispositionEvent`、字幕行数据 `MinLoopSubtitleLine`。
- 挂载对象：不需要挂载。这是供其他脚本引用的数据类型文件。
- 必需组件：无。
- Inspector 字段：无独立 Inspector。`MinLoopSubtitleLine` 会作为列表元素显示在 `ReplaySequenceController` 中。
- 公开接口：
  - `MinLoopStage`：表示当前流程处于走廊、刷卡、摘要、复盘、等待安抚、处置等阶段。
  - `MinLoopDispositionChoice`：表示最终选择 A 或 B。
  - `MinLoopStageEvent`：UnityEvent 包装类型，参数为 `MinLoopStage`，用于让 UI/音效/任务指引监听流程阶段变化。
  - `MinLoopDispositionEvent`：UnityEvent 包装类型，参数为 `MinLoopDispositionChoice, int, int`，依次表示选择、当前信任度、实际变化量。
  - `MinLoopSubtitleLine`：包含字幕延迟、说话人、正文、停留时间和可选语音。
- 关联脚本：被 `MinLoopFlowController`、`MinLoopStageObjectActivator`、`MinLoopObjectivePresenter`、`ReplaySequenceController`、`MinLoopSubtitlePlayer`、`MinLoopTerminalPresenter`、`TrustStateController` 引用。
- 使用步骤：无需手动操作，只要文件存在即可。
- 测试方式：Unity 编译无报错；其他最小循环脚本能在 Inspector 中显示对应枚举和字幕列表。
- 后续扩展：如果后续做 17F-02/17F-03，可继续在这里扩展公共枚举，但不要把单户剧情内容写进这里。
- 最近更新：使用方式无变化。新增 `MinLoopStageEvent` 和 `MinLoopDispositionEvent`，供 `MinLoopFlowController` 暴露阶段变化和 A/B 处置结果事件。

## MinLoopFlowController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopFlowController.cs`
- 脚本作用：17F-01 最小循环的总流程控制器。负责从终端刷卡开始，依次推进住户摘要、切换陪伴单元视角、启动儿童房复盘、回到终端、提交 A/B 处置、显示信任度变化和下一户指引。
- 挂载对象：推荐新建空物体 `MIN_LOOP_ROOT/FlowManagers/MinLoopFlowController`。
- 必需组件：无，但场景中应有终端展示、视角切换、复盘和信任值脚本。
- Inspector 字段：
  - `Terminal Presenter`：拖入挂有 `MinLoopTerminalPresenter` 的对象。
  - `View Switch Controller`：拖入场景中的 `ViewSwitchController`。
  - `Replay Sequence Controller`：拖入儿童房复盘对象上的 `ReplaySequenceController`。
  - `Trust State Controller`：拖入 `TrustStateController`。
  - `Auto Find Missing References`：第一版可开启，脚本会自动找场景中的对应组件。
  - `Reset Flow On Start`：第一版建议开启，Play 后从走廊状态开始。
  - `Terminal Open Feedback`：可选，拖入一个 `InteractionFeedbackController`，打开终端刷卡页时播放提示音或发光。
  - `Access Card Feedback`：可选，拖入刷工牌反馈对象，点击“刷工牌”后播放。
  - `Replay Request Feedback`：可选，拖入调出昨夜事件反馈对象，进入复盘前播放。
  - `Disposition Submit Feedback`：可选，拖入处置提交反馈对象，选择 A/B 后播放。
  - `Stage Changed`：可选 UnityEvent。流程阶段变化时触发，参数为当前 `MinLoopStage`。
  - `Disposition Applied`：可选 UnityEvent。玩家选择 A/B 后触发，参数为选择、当前信任度、实际变化量。
- 公开接口：
  - `BeginTerminalInspection()`：终端交互入口，通常由 `ResidentTerminalFlow.Interact()` 调用；如果当前已经处于刷卡、摘要、处置或完成阶段，会重新打开对应页面，而不是强制从头开始。
  - `ConfirmAccessCard()`：刷工牌确认，正式 UI 的按钮可以直接调用。
  - `RequestReplayFromTerminal()`：从终端调出昨夜事件。
  - `NotifyReplayComfortReady()`：复盘脚本通知流程进入等待安抚阶段。
  - `NotifyComfortActionPerformed()`：唯一安抚操作完成。
  - `NotifyMorningReviewStarted()`：进入第二天早晨父母对话。
  - `NotifyReplayCompleted()`：复盘结束，切回终端。
  - `ChooseDispositionA()` / `ChooseDispositionB()`：A/B 处置按钮调用。
  - `ContinueToNextResident()`：显示下一户指引。
  - `ResetFlow()`：重置最小循环。
  - `CurrentStage`：给调试或其他流程读取当前阶段。
  - `StageChanged`：给后续 UI、音效或任务指引监听流程阶段变化。
  - `DispositionApplied`：给后续 UI、数据记录或音效监听 A/B 处置结果。
- 关联脚本：
  - 调用 `MinLoopTerminalPresenter` 显示终端内容。
  - 调用 `ViewSwitchController.SwitchToCompanion()` 和 `SwitchToHuman()`。
  - 调用 `ReplaySequenceController.BeginReplay()`。
  - 调用 `TrustStateController.ApplyChoice()`。
  - 被 `ResidentTerminalFlow` 和正式 UI 按钮调用。
- 使用步骤：
  1. 新建空物体 `MinLoopFlowController`。
  2. 添加 `MinLoopFlowController`。
  3. 把终端展示器、视角切换器、复盘控制器、信任控制器拖到对应字段。
  4. 在 17F-01 门口终端上使用 `ResidentTerminalFlow` 指向这个流程控制器。
  5. Play 后看向终端按 `E`，流程应从刷工牌开始。
- 测试方式：从终端进入，能依次看到刷卡、摘要、切换陪伴单元、复盘、回终端、A/B、信任变化、下一户；在刷卡页或摘要页关闭终端后再次按 `E`，应恢复当前阶段页面，不应错误回退到前一页。
- 后续扩展：后续多户巡检时，可以把硬编码 17F-01 内容拆成数据表，再让该流程控制器读取不同住户数据；正式任务 UI 可直接监听 `StageChanged`，不用反复改主流程。
- 最近更新：使用方式有变化。优化终端重开逻辑：玩家在刷卡页或摘要页关闭终端后再次按 `E`，会按当前流程阶段恢复对应页面，不再强制回到刷工牌开头；此前 `Stage Changed`、`Disposition Applied` 和 4 个可选反馈字段继续保留。

## MinLoopStageObjectActivator.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopStageObjectActivator.cs`
- 脚本作用：根据 `MinLoopFlowController` 的当前阶段自动显示或隐藏对象。用于终端入口指引、安抚点提示、下一户方向箭头、阶段性高亮、占位模型显隐等，不把这些表现写死在主流程里。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/FlowManagers/MinLoopStageObjectActivator`。
- 必需组件：无。需要场景中有 `MinLoopFlowController`。
- Inspector 字段：
  - `Flow Controller`：拖入 `MinLoopFlowController`。骨架工具会自动绑定。
  - `Find Flow Controller On Awake`：第一版可开启，没拖时自动查找。
  - `Apply Current Stage On Enable`：启用脚本时立刻按当前阶段刷新所有规则。建议开启。
  - `Rules`：阶段显隐规则数组。
  - `Rules / Label`：规则备注名，例如 `下一户指引`。
  - `Rules / Active Stages`：在哪些 `MinLoopStage` 阶段视为匹配。
  - `Rules / Invert Match`：反选匹配结果。开启后，不在这些阶段时显示。
  - `Rules / Target Object`：需要整体显示/隐藏的 GameObject。常用做法是把箭头、光圈、提示牌作为它的子物体。
  - `Rules / Renderers`：可选，只开关渲染器，不改 GameObject 激活状态。
  - `Rules / Colliders`：可选，只开关 Collider。
  - `Rules / Behaviours`：可选，只开关某些脚本组件。
- 公开接口：
  - `ApplyCurrentStage()`：按当前流程阶段立即刷新所有规则。
  - `ApplyStage(MinLoopStage stage)`：按指定阶段刷新所有规则，可被事件或调试按钮调用。
  - `SetFlowController(MinLoopFlowController controller)`：运行时重新绑定流程控制器，并刷新当前状态。
- 关联脚本：
  - 监听 `MinLoopFlowController.StageChanged`。
  - 使用 `MinLoopStage` 枚举。
  - 由 `MinLoopSceneSkeletonCreator` 自动创建并写入默认规则。
  - 由 `MinLoopSceneValidator` 检查是否存在。
- 使用步骤：
  1. 使用 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建骨架。
  2. 骨架会生成 `MIN_LOOP_ROOT/FlowManagers/MinLoopStageObjectActivator`。
  3. 骨架会生成 `MIN_LOOP_ROOT/Guides/Guide_Terminal_17F01`、`Guide_ComfortAction`、`Guide_NextResident_17F02`。
  4. 把实际箭头、光圈、发光提示模型分别放到这些 Guide 父物体下面。
  5. 默认规则会让终端指引只在 `Corridor` 显示，安抚指引只在 `WaitingForComfort` 显示，下一户指引只在 `Complete` 显示。
- 测试方式：
  - Play 后初始走廊阶段，`Guide_Terminal_17F01` 应显示。
  - 进入等待安抚阶段后，`Guide_ComfortAction` 应显示。
  - 处置完成并进入下一户阶段后，`Guide_NextResident_17F02` 应显示。
  - 不匹配当前阶段的指引父物体应自动隐藏。
- 后续扩展：
  - 如果后续加 17F-02/17F-03，可以继续在 `Rules` 中添加不同阶段或新枚举对应的对象。
  - 如果正式 UI 需要随阶段出现，也可以把 UI 根物体拖到 `Target Object`。
  - 如果只想控制门禁 Collider 或某个光源脚本，不要拖 `Target Object`，改拖 `Colliders` 或 `Behaviours`。
- 最近更新：新增脚本。用于把“下一户指引、终端高亮、安抚提示”等阶段性表现从主流程中解耦出来，监听流程阶段事件自动显隐。

## MinLoopStageAnchorController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopStageAnchorController.cs`
- 脚本作用：根据 `MinLoopFlowController` 的当前阶段，把 Mia 或陪伴单元传送到指定 Anchor。用于保证开场、进入儿童房复盘、复盘结束回到终端时，角色站位稳定，不依赖演示前手动把角色摆到完全正确的位置。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/FlowManagers/MinLoopStageAnchorController`。
- 必需组件：无。需要场景中有 `MinLoopFlowController`；如果要移动角色，需要把 Mia/Companion 根物体和对应 Anchor 拖入规则。
- Inspector 字段：
  - `Flow Controller`：拖入 `MinLoopFlowController`。骨架工具会自动绑定。
  - `Find Flow Controller On Awake`：没拖流程控制器时自动查找。第一版建议开启。
  - `Apply Current Stage On Enable`：启用时按当前阶段刷新站位。建议开启。
  - `Rules`：阶段站位规则数组。
  - `Rules / Label`：规则备注名，例如 `Mia 初始走廊站位`、`陪伴单元复盘起点`、`Mia 回到终端站位`。
  - `Rules / Active Stages`：哪些 `MinLoopStage` 阶段触发这条传送规则。
  - `Rules / Target Root`：要移动的角色根物体，例如 `Player_Mia_Controller` 或 `Companion_Controller`。
  - `Rules / Anchor`：目标站位空物体，例如 `Anchor_Mia_TerminalReturn`。
  - `Rules / Target Rigidbody`：目标角色 Rigidbody，可选；自动绑定工具会尽量填。
  - `Rules / Target Character Controller`：目标角色 CharacterController，可选；移动前会临时禁用再恢复。
  - `Rules / First Person Look`：目标角色的 `FirstPersonLook`，用于传送后同步视角内部累计值。
  - `Rules / Look Transform`：通常拖角色相机 Transform；开启重置视角时会把它的本地旋转归零。
  - `Rules / Apply Position`：是否应用 Anchor 位置。
  - `Rules / Apply Rotation`：是否应用 Anchor 朝向。
  - `Rules / Yaw Only`：只应用 Y 轴朝向，避免误改第一人称相机俯仰。第一版建议开启。
  - `Rules / Reset Look Local Rotation`：传送后重置相机本地旋转，避免切换后镜头歪着。
  - `Rules / Sync First Person Look`：传送后调用 `FirstPersonLook.ForceLookFromCurrentTransforms()`，避免下一帧被旧视角累计值拉回。
  - `Rules / Clear Rigidbody Velocity`：传送前后清空 Rigidbody 速度，避免切换后继续滑动。
  - `Only First Matching Rule Per Stage`：同一阶段只执行第一条匹配规则。第一版默认可关闭。
- 公开接口：
  - `ApplyCurrentStage()`：按当前流程阶段执行匹配锚点规则。
  - `ApplyStage(MinLoopStage stage)`：按指定阶段执行匹配锚点规则。
  - `SetFlowController(MinLoopFlowController controller)`：运行时重新绑定流程控制器。
  - `ApplyRuleNow(int ruleIndex)`：手动执行某条规则，便于测试。
  - `HasRules` / `RuleCount`：给检查器读取是否已配置规则。
- 关联脚本：
  - 监听 `MinLoopFlowController.StageChanged`。
  - 与 `ViewSwitchController` 分工：本脚本只处理位置/朝向；`ViewSwitchController` 只处理黑场、相机和控制权。
  - 调用 `FirstPersonLook.ForceLookFromCurrentTransforms()` 同步第一人称视角内部状态。
  - 由 `MinLoopSceneSkeletonCreator` 自动创建，并生成默认 Anchor 空物体。
  - 由 `MinLoopSceneAutoBinder` 自动把 Mia/Companion 根物体和 Anchor 填入规则。
  - 由 `MinLoopSceneValidator` 检查是否存在和是否有规则。
- 使用步骤：
  1. 使用 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建骨架。
  2. 骨架会生成 `MIN_LOOP_ROOT/FlowManagers/MinLoopStageAnchorController`。
  3. 骨架会生成 `MIN_LOOP_ROOT/Anchors/Anchor_Mia_CorridorStart`、`Anchor_Companion_ReplayStart`、`Anchor_Mia_TerminalReturn`、`Anchor_NextResident_17F02`。
  4. 把 `Anchor_Mia_CorridorStart` 移到 17F 走廊开场位置。
  5. 把 `Anchor_Companion_ReplayStart` 移到儿童房复盘第一人称起点。
  6. 把 `Anchor_Mia_TerminalReturn` 移到 17F-01 终端前方。
  7. 运行 `Auto Bind 17F-01 Scene References`，自动把 Mia/Companion 根物体填到锚点规则里。
- 测试方式：
  - Play 后重置到 `Corridor` 时，Mia 应出现在 `Anchor_Mia_CorridorStart`。
  - 点击调出昨夜事件后，切换到 Companion 前，Companion 应被放到 `Anchor_Companion_ReplayStart`。
  - 复盘结束回到终端前，Mia 应被放到 `Anchor_Mia_TerminalReturn`。
  - 传送后角色不应继续滑动，镜头不应残留奇怪俯仰。
- 后续扩展：
  - 如果后续做 17F-02/17F-03，可继续添加每户门口终端、每户复盘起点和下一户导向 Anchor。
  - 如果某阶段只想改朝向不改位置，可关闭对应规则的 `Apply Position`。
  - 如果需要过场移动而不是瞬移，可在这个脚本外接 Timeline 或 Cinemachine；本脚本保留为最小循环的稳定站位兜底。
- 最近更新：新增脚本。阶段锚点控制器已接入骨架、自动绑定和检查器；用于在流程关键阶段稳定 Mia/陪伴单元站位。

## MinLoopStageCueController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopStageCueController.cs`
- 脚本作用：根据 `MinLoopFlowController` 的当前阶段触发一次性表现 Cue。用于把“等待安抚时床边亮一下/响一下、早晨回顾开始时提示、下一户指引出现时提示、阶段开门、角色 Animator Trigger、一次性音效”等表现从主流程脚本中拆出来。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/FlowManagers/MinLoopStageCueController`。
- 必需组件：无。需要场景中有 `MinLoopFlowController`；如果要触发反馈、开门、动画或音效，需要在规则里拖对应的 `InteractionFeedbackController`、`SmartDoorController`、`Animator` 或 `AudioSource`。
- Inspector 字段：
  - `Flow Controller`：拖入 `MinLoopFlowController`。骨架工具会自动绑定。
  - `Find Flow Controller On Awake`：没拖流程控制器时自动查找。第一版建议开启。
  - `Apply Current Stage On Enable`：启用时按当前阶段触发匹配 Cue。默认可关闭，避免进场时重复播一次。
  - `Rules`：阶段 Cue 规则数组。
  - `Rules / Label`：规则备注名，例如 `安抚点就绪反馈`、`早晨回顾反馈`、`下一户指引反馈`。
  - `Rules / Active Stages`：哪些 `MinLoopStage` 阶段触发这条 Cue。
  - `Rules / Trigger Once`：同一轮流程中是否只触发一次。第一版建议开启。
  - `Rules / Delay Seconds`：进入阶段后延迟多少秒触发。
  - `Rules / Feedback Objects`：拖入要调用 `PlayFeedback()` 的 `InteractionFeedbackController`。
  - `Rules / Door Cues`：拖入要控制的 `SmartDoorController`，动作可选 `Open`、`Close`、`Toggle`、`SnapOpen`、`SnapClosed`、`Lock`、`Unlock`。
  - `Rules / Animator Cues`：拖入 Animator 和 Trigger 名。脚本会先检查 Animator 里是否存在这个 Trigger，避免拼错时报错。
  - `Rules / Audio Cues`：拖入一次性或直接播放的 `AudioSource` / `AudioClip`。
  - `Rules / Enable Objects` / `Disable Objects`：随 Cue 立即开关的物体。
  - `Rules / Enable Behaviours` / `Disable Behaviours`：随 Cue 立即开关的脚本组件。
  - `Rules / Cue Event`：可选 UnityEvent，后续可直接绑定你自己的脚本方法。
  - `Reset Triggered Rules On Corridor`：回到 `Corridor` 时重置已触发状态，并清掉未执行的延迟 Cue。第一版建议开启。
- 公开接口：
  - `ApplyCurrentStage()`：按当前流程阶段执行匹配 Cue。
  - `ApplyStage(MinLoopStage stage)`：按指定阶段执行匹配 Cue。
  - `SetFlowController(MinLoopFlowController controller)`：运行时重新绑定流程控制器。
  - `ExecuteRuleNow(int ruleIndex)`：手动立即执行某条规则，便于测试。
  - `ResetTriggeredRules()`：重置 `Trigger Once` 的触发记录。
  - `HasRules` / `RuleCount`：给检查器读取是否已配置规则。
- 关联脚本：
  - 监听 `MinLoopFlowController.StageChanged`。
  - 调用 `InteractionFeedbackController.PlayFeedback()` 播放提示音/发光。
  - 可调用 `SmartDoorController` 的开关门和锁定接口。
  - 可触发角色或门的 `Animator` Trigger。
  - 由 `MinLoopSceneSkeletonCreator` 自动创建，并写入 3 条默认反馈规则。
  - 由 `MinLoopSceneAutoBinder` 自动补齐流程引用和默认规则。
  - 由 `MinLoopSceneValidator` 检查是否存在和是否有规则。
- 使用步骤：
  1. 使用 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建骨架。
  2. 骨架会生成 `MIN_LOOP_ROOT/FlowManagers/MinLoopStageCueController`。
  3. 骨架会生成 `Feedback_ComfortReady`、`Feedback_MorningReview`、`Feedback_NextResidentGuide` 三个默认反馈对象。
  4. 默认规则会在 `WaitingForComfort`、`MorningReview`、`Complete` 阶段分别触发这三个反馈对象。
  5. 如果要让 17F-01 房门在复盘阶段打开，在对应规则的 `Door Cues` 里拖门对象并选择 `Open`。
  6. 如果要让孩子/父母在某阶段做动作，在对应规则的 `Animator Cues` 里拖 Animator 并填写 Trigger。
- 测试方式：
  - Play 后进入 `WaitingForComfort`，`Feedback_ComfortReady` 应播放一次。
  - 进入 `MorningReview`，`Feedback_MorningReview` 应播放一次。
  - 选择 A/B 后进入 `Complete`，`Feedback_NextResidentGuide` 应播放一次。
  - 快速反复跳阶段时，不应重复触发已经触发过的 `Trigger Once` 规则；回到 `Corridor` 后规则会重置。
- 后续扩展：
  - 正式动画做好后，把规则里的占位反馈换成 Animator Trigger 即可，不需要改主流程。
  - 如果门改成 Animator 门，可用 `Animator Cues`；如果继续用脚本门，可用 `Door Cues`。
  - 如果后续有“终端弹出后播 UI 音”“父母对话结束门外灯亮”等表现，都可以加规则解决。
- 最近更新：新增脚本。阶段 Cue 控制器已接入骨架、自动绑定、检查器和搭建清单；用于统一管理阶段性一次性反馈、门、动画、音效和事件。

## MinLoopLightingStateController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopLightingStateController.cs`
- 脚本作用：根据 `MinLoopFlowController` 的当前阶段自动切换灯光、环境光、雾效和可选氛围物体。用于让 17F 走廊/终端、昨夜儿童房复盘、第二天早晨回顾在第一版里也有清楚的时间和情绪差异。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/FlowManagers/MinLoopLightingStateController`。
- 必需组件：无。需要场景中有 `MinLoopFlowController`；如果要控制实际灯光，需要把场景里的 `Light` 拖入规则。
- Inspector 字段：
  - `Flow Controller`：拖入 `MinLoopFlowController`。骨架工具会自动绑定。
  - `Find Flow Controller On Awake`：没拖流程控制器时自动查找。第一版建议开启。
  - `Apply Current Stage On Enable`：启用时按当前阶段刷新灯光。建议开启。
  - `Rules`：阶段灯光规则数组。
  - `Rules / Label`：规则备注名，例如 `走廊/终端暖光`、`昨夜复盘冷光`、`早晨回顾柔光`。
  - `Rules / Active Stages`：哪些 `MinLoopStage` 阶段启用这条规则。
  - `Rules / Lights`：要控制的 Light 数组。
  - `Rules / Set Light Enabled` / `Light Enabled`：是否按阶段开关灯。
  - `Rules / Set Light Color` / `Light Color`：是否切换灯光颜色。
  - `Rules / Set Light Intensity` / `Light Intensity`：是否切换灯光强度。
  - `Rules / Set Light Range` / `Light Range`：是否切换灯光范围。
  - `Rules / Active Objects`：可选，随阶段显示/隐藏的氛围物体，例如窗外晨光片、夜间体积雾占位。
  - `Rules / Behaviours`：可选，随阶段开关的脚本组件。
  - `Rules / Apply Ambient Color` / `Ambient Color`：是否同步修改 `RenderSettings.ambientLight`。
  - `Rules / Apply Fog` / `Fog Enabled` / `Fog Color` / `Fog Density`：是否同步修改场景雾效。
  - `Rules / Transition Seconds`：灯光、环境光、雾效过渡时间，默认 0.35 秒左右。
  - `Disable Unmatched Rule Lights`：当前阶段不匹配的规则灯光是否自动关闭。第一版建议开启。
  - `Capture Scene Ambient On Awake`：启动时记录原始环境光/雾效。
  - `Restore Scene Ambient On Disable`：脚本禁用时恢复原始环境光/雾效。
- 公开接口：
  - `ApplyCurrentStage()`：按当前流程阶段立即刷新灯光。
  - `ApplyStage(MinLoopStage stage)`：按指定阶段刷新灯光，可被调试工具或流程事件调用。
  - `SetFlowController(MinLoopFlowController controller)`：运行时重新绑定流程控制器。
  - `RestoreSceneAmbient()`：恢复脚本启动时记录的环境光和雾效。
  - `HasRules` / `RuleCount`：给检查器读取是否已配置规则。
- 关联脚本：
  - 监听 `MinLoopFlowController.StageChanged`。
  - 使用 `MinLoopStage` 枚举。
  - 由 `MinLoopSceneSkeletonCreator` 自动创建并写入 3 条默认灯光规则。
  - 由 `MinLoopSceneAutoBinder` 自动补齐流程引用和默认规则。
  - 由 `MinLoopSceneValidator` 检查是否存在和是否有规则。
- 使用步骤：
  1. 使用 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建骨架。
  2. 骨架会生成 `MIN_LOOP_ROOT/FlowManagers/MinLoopLightingStateController`。
  3. 骨架会生成 `MIN_LOOP_ROOT/Lighting/Light_Corridor_Warm`、`Light_Replay_Night`、`Light_Morning_Soft`。
  4. 默认规则会让走廊/终端阶段使用暖光，昨夜复盘阶段使用冷夜光，早晨回顾阶段使用柔光。
  5. 后续放入正式灯光后，把正式 `Light` 拖进对应规则的 `Lights`，或直接替换默认 Light 对象。
- 测试方式：
  - Play 后初始走廊阶段，`Light_Corridor_Warm` 应开启，复盘和早晨灯应关闭。
  - 调出昨夜事件后，灯光应过渡到 `Light_Replay_Night` 的冷色。
  - 进入 `MorningReview` 后，应切到 `Light_Morning_Soft`。
  - `Validate Scene Setup` 应提示检测到阶段灯光控制器，且灯光规则存在。
- 后续扩展：
  - 正式打光时可以保留这个脚本，只把规则里的占位 Light 换成正式 Light。
  - 如果场景有多个房间，可给每个房间建多盏灯，并拖入同一条规则的 `Lights` 数组。
  - 如果后续做 17F-02/17F-03，可继续增加阶段枚举或复制规则组。
- 最近更新：使用方式有变化。新增阶段灯光控制器并接入骨架、自动绑定和检查器；脚本支持 Light、环境光、雾效和对象/脚本显隐，阶段变化时可做平滑过渡。

## MinLoopAudioStateController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopAudioStateController.cs`
- 脚本作用：根据 `MinLoopFlowController` 的当前阶段自动播放、停止和淡入淡出环境声/氛围声。它负责走廊底噪、昨夜儿童房夜间氛围、早晨回顾环境声这类“持续声音”，不负责刷卡哔声、按钮声等一次性反馈。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/FlowManagers/MinLoopAudioStateController`。
- 必需组件：无。需要场景中有 `MinLoopFlowController`；如果要实际出声，需要把 `AudioClip` 放到规则的 `Fallback Clip` 或对应 `AudioSource.clip`。
- Inspector 字段：
  - `Flow Controller`：拖入 `MinLoopFlowController`。骨架工具会自动绑定。
  - `Find Flow Controller On Awake`：没拖流程控制器时自动查找。第一版建议开启。
  - `Apply Current Stage On Enable`：启用时按当前阶段刷新音频。建议开启。
  - `Rules`：阶段音频规则数组。
  - `Rules / Label`：规则备注名，例如 `走廊/终端环境声`、`昨夜复盘环境声`、`早晨回顾环境声`。
  - `Rules / Active Stages`：哪些 `MinLoopStage` 阶段启用这条规则。
  - `Rules / Audio Sources`：要控制的 `AudioSource` 数组。
  - `Rules / Fallback Clip`：如果 `AudioSource.clip` 为空，可使用这里的 Clip。
  - `Rules / Assign Fallback Clip If Missing`：当 AudioSource 没有 Clip 时，是否自动把 `Fallback Clip` 填进去。
  - `Rules / Loop`：是否循环播放。环境声通常开启。
  - `Rules / Target Volume`：该阶段目标音量。
  - `Rules / Set Spatial Blend` / `Spatial Blend`：是否设置 2D/3D 声音混合。第一版环境声通常用 0。
  - `Rules / Set Pitch` / `Pitch`：是否设置音高。
  - `Rules / Restart On Enter`：进入阶段时是否从头播放。环境声通常关闭。
  - `Rules / Stop When Unmatched`：离开阶段时是否淡出并停止。第一版建议开启。
  - `Rules / Fade In Seconds` / `Fade Out Seconds`：淡入淡出时间。
  - `Rules / Active Objects`：可选，随音频阶段显示/隐藏的氛围物体。
  - `Rules / Behaviours`：可选，随音频阶段开关的脚本组件。
  - `Stop Audio On Disable`：脚本禁用时是否立即停止所有受控音频。
- 公开接口：
  - `ApplyCurrentStage()`：按当前流程阶段刷新音频。
  - `ApplyStage(MinLoopStage stage)`：按指定阶段刷新音频，可被流程事件或调试工具调用。
  - `SetFlowController(MinLoopFlowController controller)`：运行时重新绑定流程控制器。
  - `StopAllAudio()`：按各规则淡出并停止全部受控环境声。
  - `HasRules` / `RuleCount`：给检查器读取是否已配置规则。
- 关联脚本：
  - 监听 `MinLoopFlowController.StageChanged`。
  - 使用 `MinLoopStage` 枚举。
  - 与 `InteractionFeedbackController` 分工：本脚本负责持续环境声；`InteractionFeedbackController` 负责刷卡、按钮、终端确认等一次性音效。
  - 由 `MinLoopSceneSkeletonCreator` 自动创建并写入 3 条默认音频规则。
  - 由 `MinLoopSceneAutoBinder` 自动补齐流程引用和默认规则。
  - 由 `MinLoopSceneValidator` 检查是否存在和是否有规则。
- 使用步骤：
  1. 使用 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建骨架。
  2. 骨架会生成 `MIN_LOOP_ROOT/FlowManagers/MinLoopAudioStateController`。
  3. 骨架会生成 `MIN_LOOP_ROOT/Audio/Audio_Corridor_Ambience`、`Audio_Replay_Night_Ambience`、`Audio_Morning_Ambience`。
  4. 第一版没有正式音频 Clip 也能运行，不会报错。
  5. 后续放入正式音频后，把走廊底噪拖到 `Audio_Corridor_Ambience` 的 `AudioSource.clip`，把夜间儿童房氛围拖到 `Audio_Replay_Night_Ambience`，把早晨环境声拖到 `Audio_Morning_Ambience`。
- 测试方式：
  - Play 后初始走廊阶段，如果 `Audio_Corridor_Ambience` 有 Clip，应淡入播放。
  - 调出昨夜事件后，走廊环境声应淡出，`Audio_Replay_Night_Ambience` 应淡入。
  - 进入 `MorningReview` 后，夜间环境声应淡出，早晨环境声应淡入。
  - `Validate Scene Setup` 应提示检测到阶段音频控制器，且音频规则存在。
- 后续扩展：
  - 正式混音时可以保留这个脚本，只调整每条规则的 `Target Volume`、淡入淡出时间和 AudioSource。
  - 如果某个阶段要同时播放空调声、远处电梯声和房间底噪，可把多条 AudioSource 放进同一规则的 `Audio Sources` 数组。
  - 如果后续做 17F-02/17F-03，可复制规则或增加新阶段。
- 最近更新：新增脚本。阶段音频控制器已接入骨架、自动绑定和检查器；用于最小循环环境声淡入淡出，不替代一次性交互反馈音效。

## MinLoopWorldGuideMarker.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopWorldGuideMarker.cs`
- 脚本作用：世界空间阶段指引标记。用于 `Guide_Terminal_17F01`、`Guide_ComfortAction`、`Guide_NextResident_17F02` 等父物体，在没有正式箭头/光圈资源时自动生成一个发光点、文字标签、距离文字和轻微浮动/缩放动效，让玩家能看懂下一步去哪里或按哪里。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/Guides/Guide_Terminal_17F01`、`Guide_ComfortAction`、`Guide_NextResident_17F02`。
- 必需组件：无。第一版可自动生成占位视觉；如果使用正式模型，可把正式模型拖到 `Visual Root` 或关闭 `Create Fallback Visual`。
- Inspector 字段：
  - `Marker Label`：显示文字，例如 `前往 17F-01 终端`、`床边安抚操作`、`下一户 17F-02`。
  - `Marker Color`：发光点和灯光颜色。
  - `Label Color`：文字颜色。
  - `Visual Root`：指引视觉根物体。自动占位会生成 `Generated Guide Marker`。
  - `Label Text`：正式或占位 TMP 文本。
  - `Marker Renderer`：发光点 Renderer。脚本使用 MaterialPropertyBlock 改颜色，不会改共享材质。
  - `Marker Light`：可选点光。
  - `Target Camera`：指引朝向的 Camera；不拖时会自动找 `Camera.main`。
  - `Create Fallback Visual`：没有正式视觉时自动生成发光球、文字和灯光。第一版建议开启。
  - `Visual Local Offset`：指引视觉相对父物体的本地偏移。
  - `Marker Local Scale`：占位发光点大小。
  - `Label Font Size` / `Label Rect Size`：占位文字大小。
  - `Face Camera`：是否始终朝向玩家相机。
  - `Find Camera If Missing`：没有手动指定相机时自动查找。
  - `Bob` / `Bob Amplitude` / `Bob Frequency`：上下浮动。
  - `Pulse` / `Pulse Scale` / `Pulse Frequency`：轻微缩放脉冲。
  - `Show Distance`：是否在文字下方显示玩家相机到指引点的距离。第一版建议开启。
  - `Distance Refresh Interval`：距离刷新间隔。默认约 `0.12` 秒，避免每帧重写 TMP 文本。
- 公开接口：
  - `SetLabel(string value)`：运行时修改指引文字。
  - `SetMarkerColor(Color value)`：运行时修改颜色。
  - `SetTargetCamera(Camera camera)`：指定朝向相机。
  - `SetShowDistance(bool value)`：运行时开关距离显示。
  - `RefreshVisuals()`：刷新文字、颜色和引用。
  - `MarkerLabel`：读取当前文字。
- 关联脚本：
  - 通常由 `MinLoopStageObjectActivator` 显示/隐藏它所在的 Guide 父物体。
  - 由 `MinLoopSceneSkeletonCreator` 自动创建到 3 个默认 Guide 对象上。
  - 由 `MinLoopSceneAutoBinder` 自动补组件和默认文字。
  - 由 `MinLoopSceneValidator` 检查是否存在。
- 使用步骤：
  1. 使用 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建骨架。
  2. 骨架会给 3 个 Guide 对象自动添加 `MinLoopWorldGuideMarker`。
  3. 把 Guide 父物体放到终端、安抚点、下一户方向附近。
  4. 第一版不需要放任何箭头模型，Play 后会自动生成发光点和文字。
  5. 后续正式箭头/光圈资源做好后，把正式视觉拖到 `Visual Root`，或关闭 `Create Fallback Visual`。
- 测试方式：
  - 初始走廊阶段只显示 `Guide_Terminal_17F01` 的发光文字。
  - 等待安抚阶段只显示 `Guide_ComfortAction`。
  - 完成阶段显示 `Guide_NextResident_17F02`。
  - 指引文字应面向玩家相机，并有轻微浮动或缩放。
  - `Show Distance` 开启时，文字下方应显示类似 `3.5 m` 的距离，移动时距离会低频刷新。
- 后续扩展：
  - 正式美术可以替换为箭头、地面光圈、门牌高亮或粒子效果；仍可保留这个脚本只负责文字/朝向/脉冲。
  - 多户流程后，可由流程控制器调用 `SetLabel()` 动态改成不同户号。
- 最近更新：使用方式有变化。新增距离显示：`Show Distance` 开启时会在指引文字下方显示玩家相机到指引点的米数，并按 `Distance Refresh Interval` 低频刷新；此前自动生成发光点、文字、灯光、朝向相机、浮动和脉冲动效继续保留。

## MinLoopObjectivePresenter.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopObjectivePresenter.cs`
- 脚本作用：显示当前最小循环目标提示。它监听 `MinLoopFlowController` 的阶段变化和 A/B 处置结果，在屏幕上提示玩家下一步该做什么，例如前往终端、刷工牌、执行安抚、选择 A/B、前往下一户。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/UI/MinLoopObjectivePresenter`。
- 必需组件：无。需要场景中有 `MinLoopFlowController`。如果使用正式 UI，目标文本建议使用 TextMeshPro。
- Inspector 字段：
  - `Flow Controller`：拖入 `MinLoopFlowController`。骨架工具会自动绑定。
  - `Find Flow Controller On Awake`：第一版可开启，没拖时自动查找。
  - `Refresh On Enable`：启用时立即刷新当前目标。建议开启。
  - `Objective Root`：正式当前目标 UI 根物体，可选。脚本会显示/隐藏它。
  - `Title Text`：正式 UI 的目标标题 TMP 文本。
  - `Body Text`：正式 UI 的目标说明 TMP 文本。
  - `Canvas Group`：可选，用于同步 alpha。
  - `Create Fallback UI`：没有正式 UI 时自动创建左上角目标提示。第一版建议开启。
  - `Fallback Panel Color`：自动占位目标面板颜色。
  - `Objective Overrides`：可选，按阶段覆盖默认目标文案。
  - `Objective Overrides / Stage`：要覆盖的流程阶段。
  - `Objective Overrides / Visible`：该阶段是否显示目标提示。
  - `Objective Overrides / Title` / `Body`：该阶段显示的标题和正文。
- 公开接口：
  - `RefreshCurrentObjective()`：按当前流程阶段刷新目标提示。
  - `ShowStage(MinLoopStage stage)`：显示指定阶段的默认或覆盖目标。
  - `SetFlowController(MinLoopFlowController controller)`：运行时重新绑定流程控制器。
- 关联脚本：
  - 监听 `MinLoopFlowController.StageChanged` 和 `DispositionApplied`。
  - 使用 `MinLoopStage` 和 `MinLoopDispositionChoice`。
  - 由 `MinLoopSceneSkeletonCreator` 自动创建并绑定流程控制器。
  - 由 `MinLoopSceneValidator` 检查是否存在。
- 使用步骤：
  1. 使用 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建骨架。
  2. 骨架会生成 `MIN_LOOP_ROOT/UI/MinLoopObjectivePresenter`。
  3. 第一版无需拖正式 UI，Play 后会自动生成左上角目标提示。
  4. 后续正式 UI 上传后，把目标 UI 根物体拖到 `Objective Root`，标题 TMP 文本拖到 `Title Text`，正文 TMP 文本拖到 `Body Text`。
  5. 如需改某个阶段的文字，在 `Objective Overrides` 添加一项并选择对应阶段。
- 测试方式：
  - Play 后走廊阶段显示“当前目标 / 前往 17F-01 门口终端”。
  - 刷工牌、摘要、复盘、等待安抚、A/B 处置等阶段切换时，目标提示会自动变化。
  - 选择 A/B 后，会显示处置已提交和信任度变化文本；继续到下一户后显示 17F-02 指引。
- 后续扩展：
  - 正式任务 UI、任务列表、语音提示都可以监听同一套阶段事件。
  - 多户流程后，可以把默认文案改为数据驱动，或用 `Objective Overrides` 针对不同住户覆写。
- 最近更新：新增脚本。用于给最小循环提供当前目标提示，占位 UI 自动生成，正式 UI 可直接绑定 TMP 文本。

## MinLoopRobotHudPresenter.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopRobotHudPresenter.cs`
- 脚本作用：显示陪伴单元第一人称视角的机器 HUD。它监听 `MinLoopFlowController.StageChanged`，在接入复盘、孩子噩梦、等待安抚、安抚中、早晨反馈和回传终端阶段显示时间、心率、系统判定和当前机器指令。第一版不需要正式 UI，也能自动生成占位 HUD。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/UI/MinLoopRobotHudPresenter`。
- 必需组件：无。需要场景中有 `MinLoopFlowController`。如果使用正式 UI，文字建议使用 TextMeshPro。
- Inspector 字段：
  - `Flow Controller`：拖入 `MinLoopFlowController`。骨架工具会自动绑定。
  - `Find Flow Controller On Awake`：第一版可开启，没拖时自动查找。
  - `Refresh On Enable`：启用时立即刷新当前 HUD。建议开启。
  - `HUD Root`：正式机器 HUD 根物体，可选。脚本会显示/隐藏它。
  - `Time Text`：显示时间，例如 `02:47`。
  - `Heart Rate Text`：显示心率或记录类型，例如 `心率 124`。
  - `Status Text`：显示系统判定，例如 `噩梦可能性高`。
  - `Instruction Text`：显示机器当前指令，例如 `执行唯一安抚操作`。
  - `Accent Image`：可选，用于显示蓝色/橙色状态条。
  - `Canvas Group`：可选，用于同步 alpha。
  - `Create Fallback UI`：没有正式 UI 时自动创建左下角机器 HUD。第一版建议开启。
  - `Fallback Parent Canvas`：可选。拖入 `RobotCanvas` 或 `CompanionCanvas` 后，占位 HUD 会生成在这个 Canvas 下。
  - `Fallback Panel Color`：自动占位 HUD 背景色。
  - `Normal Accent Color`：普通状态颜色。
  - `Alert Accent Color`：噩梦/等待安抚等警示状态颜色。
  - `Hide Outside Companion Replay`：非复盘阶段是否隐藏。默认开启。
  - `Stage Overrides`：可选，按阶段覆盖默认机器 HUD 文案和颜色。
- 默认显示内容：
  - `SwitchingToCompanion`：`02:47 / 接入昨夜记录 / 正在切换至陪伴单元第一人称视角`。
  - `CompanionReplay`：`02:47 / 心率 124 / 噩梦可能性高 / 观察儿童醒来后的第一反应`。
  - `WaitingForComfort`：`02:48 / 心率 132 / 儿童呼唤家属 / 执行唯一安抚操作`。
  - `Comforting`：`02:49 / 心率 104 / 安抚执行中 / 等待心率回落`。
  - `MorningReview`：`07:36 / 晨间记录 / 父母反馈回放`。
  - `ReturningToTerminal`：`07:38 / 回传完成 / 复盘结束`。
- 公开接口：
  - `RefreshCurrentHud()`：按当前流程阶段刷新机器 HUD。
  - `ShowStage(MinLoopStage stage)`：显示指定阶段的默认或覆盖 HUD。
  - `SetFlowController(MinLoopFlowController controller)`：运行时重新绑定流程控制器。
  - `HasBoundUI`：检查是否有正式或占位根 UI 和关键文字。
  - `CanCreateFallbackUI`：检查是否允许生成占位 UI。
- 关联脚本：
  - 监听 `MinLoopFlowController.StageChanged`。
  - 使用 `MinLoopStage`。
  - 由 `MinLoopSceneSkeletonCreator` 自动创建并绑定流程控制器。
  - 由 `MinLoopSceneAutoBinder` 自动绑定流程控制器和 `RobotCanvas` / `CompanionCanvas`。
  - 由 `MinLoopSceneValidator` 检查是否存在。
- 使用步骤：
  1. 使用 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建骨架。
  2. 骨架会生成 `MIN_LOOP_ROOT/UI/MinLoopRobotHudPresenter`。
  3. 第一版无需拖正式 UI，Play 后进入陪伴单元复盘阶段会自动生成机器 HUD。
  4. 如果场景已有 `RobotCanvas`，运行 `Auto Bind 17F-01 Scene References` 后会自动填到 `Fallback Parent Canvas`。
  5. 后续正式 UI 上传后，把机器 HUD 根物体拖到 `HUD Root`，把时间、心率、判定、指令 TMP 文本分别拖到对应字段。
- 测试方式：
  - 走廊和终端阶段机器 HUD 默认不显示。
  - 点击调出昨夜事件并切到 Companion 后，左下角出现机器 HUD。
  - 孩子噩梦/等待安抚阶段，HUD 显示 `02:47/02:48`、心率和噩梦判定。
  - 安抚后 HUD 变为安抚中/心率回落语义。
  - 回到终端处置后 HUD 隐藏。
- 后续扩展：
  - 正式机器 UI、扫描线、心率曲线、设备状态灯都可以绑定到这个脚本或监听同一流程阶段。
  - 如果后续复盘变成更复杂时间轴，可增加公开方法让 `ReplaySequenceController` 逐句推送心率和状态，而不是只按阶段显示。
- 最近更新：新增脚本。用于补齐陪伴单元视角的机器 HUD，占位 UI 自动生成，正式 RobotCanvas 可直接绑定 TMP 文本和状态条。

## MinLoopTrustPresenter.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopTrustPresenter.cs`
- 脚本作用：显示住户信任度 UI。它监听 `TrustStateController.TrustChanged` 和 `MinLoopFlowController.DispositionApplied`，显示当前信任度、最近一次 A/B 选择造成的变化量，并可驱动正式 UI 的文本和 Slider。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/UI/MinLoopTrustPresenter`。
- 必需组件：无。需要场景中有 `TrustStateController`；如果要显示 A/B 后的实际变化量，推荐同时绑定 `MinLoopFlowController`。
- Inspector 字段：
  - `Trust State Controller`：拖入 `TrustStateController`。骨架工具会自动绑定。
  - `Flow Controller`：拖入 `MinLoopFlowController`。骨架工具会自动绑定。
  - `Find References On Awake`：第一版可开启，没拖时自动查找。
  - `Refresh On Enable`：启用时立即刷新当前信任度。建议开启。
  - `Trust Root`：正式信任度 UI 根物体，可选。脚本会显示/隐藏它。
  - `Value Text`：正式 UI 的当前信任度 TMP 文本。
  - `Delta Text`：正式 UI 的变化量 TMP 文本，例如 `+8`、`-3`。
  - `Label Text`：正式 UI 标题文本，可选。
  - `Trust Slider`：正式 UI 进度条，可选。脚本会自动把 Slider 范围设为 0-100，再写入当前信任度。
  - `Canvas Group`：可选，用于同步 alpha。
  - `Create Fallback UI`：没有正式 UI 时自动创建右上角信任度提示。第一版建议开启。
  - `Fallback Panel Color`：自动占位信任度面板颜色。
  - `Label`：标题文案，默认 `住户信任度`。
  - `Value Format`：数值格式，默认 `{0}`。
  - `Delta Format`：变化量格式，默认 `{0}{1}`，第一个参数是正号前缀，第二个参数是变化量。
  - `Show Only After Disposition`：开启后，只有 A/B 产生变化后才显示；默认关闭，开局也显示当前值。
- 公开接口：
  - `Refresh()`：从信任控制器读取当前值并刷新 UI。
  - `SetTrustValue(int trustValue)`：外部直接写入当前信任度显示。
  - `SetReferences(TrustStateController trustController, MinLoopFlowController flow)`：运行时重新绑定信任控制器和流程控制器。
- 关联脚本：
  - 监听 `TrustStateController.TrustChanged`。
  - 监听 `MinLoopFlowController.DispositionApplied`。
  - 由 `MinLoopSceneSkeletonCreator` 自动创建并绑定信任控制器和流程控制器。
  - 由 `MinLoopSceneValidator` 检查是否存在。
- 使用步骤：
  1. 使用 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建骨架。
  2. 骨架会生成 `MIN_LOOP_ROOT/UI/MinLoopTrustPresenter`。
  3. 第一版无需拖正式 UI，Play 后会自动生成右上角信任度提示。
  4. 后续正式 UI 上传后，把信任度 UI 根物体拖到 `Trust Root`，数值 TMP 文本拖到 `Value Text`，变化量 TMP 文本拖到 `Delta Text`。
  5. 如果正式 UI 有进度条，把 Slider 拖到 `Trust Slider`。
- 测试方式：
  - Play 后右上角显示初始信任度。
  - 选择 A 后显示正向变化量，例如 `+8`。
  - 选择 B 后显示负向变化量，例如 `-3`。
  - 如果信任度接近上下限，变化量应显示钳制后的实际变化量。
- 后续扩展：
  - 正式 UI 可以换成环形进度、仪表盘或终端内嵌条，只要把文本/Slider 拖入即可。
  - 多户流程后，可以按住户切换不同 `TrustStateController` 或把信任值改成数据表驱动。
- 最近更新：新增脚本。用于给最小循环提供独立信任度显示，占位 UI 自动生成，正式 UI 可直接绑定 TMP 文本和 Slider。

## MinLoopDebugHotkeys.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopDebugHotkeys.cs`
- 脚本作用：Play Mode 测试热键脚本。用于不依赖正式 UI 或完整走位，快速推进最小循环关键节点：重置、打开终端、刷工牌、调出复盘、模拟安抚、选择 A/B、进入下一户。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/FlowManagers/MinLoopDebugHotkeys`。
- 必需组件：无。需要场景中有 `MinLoopFlowController`；如果要用 `F5` 模拟安抚，需要有 `ReplaySequenceController`。
- Inspector 字段：
  - `Flow Controller`：拖入 `MinLoopFlowController`。骨架工具会自动绑定。
  - `Replay Sequence Controller`：拖入 `ReplaySequenceController`。骨架工具会自动绑定。
  - `Find References On Awake`：第一版可开启，没拖时自动查找。
  - `Enable Hotkeys`：是否启用热键。正式演示不想误触时可关闭。
  - `Editor Or Development Only`：开启后只在 Unity Editor 或 Development Build 生效。默认开启。
  - `Reset Key`：默认 `F1`，重置最小循环。
  - `Open Terminal Key`：默认 `F2`，触发终端入口。
  - `Confirm Access Card Key`：默认 `F3`，模拟刷工牌。
  - `Request Replay Key`：默认 `F4`，调出昨夜事件复盘。
  - `Perform Comfort Key`：默认 `F5`，模拟执行安抚。
  - `Choose A Key`：默认 `F6`，选择 A。
  - `Choose B Key`：默认 `F7`，选择 B。
  - `Next Resident Key`：默认 `F8`，继续到下一户。
- 公开接口：
  - `ResetFlow()`：调用总流程重置。
  - `BeginTerminalInspection()`：调用终端入口。
  - `ConfirmAccessCard()`：模拟刷工牌。
  - `RequestReplay()`：调出复盘。
  - `PerformComfort()`：调用复盘控制器执行安抚。
  - `ChooseDispositionA()` / `ChooseDispositionB()`：选择 A/B。
  - `ContinueToNextResident()`：显示下一户。
  - `SetHotkeysEnabled(bool value)`：运行时启用或关闭热键。
- 关联脚本：
  - 调用 `MinLoopFlowController` 的公开方法。
  - 调用 `ReplaySequenceController.PerformComfortAction()`。
  - 由 `MinLoopSceneSkeletonCreator` 自动创建并绑定引用。
  - 由 `MinLoopSceneValidator` 检查是否存在。
- 使用步骤：
  1. 使用 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建骨架。
  2. 骨架会生成 `MIN_LOOP_ROOT/FlowManagers/MinLoopDebugHotkeys`。
  3. Play Mode 下可用 `F1-F8` 快速测试流程节点。
  4. 正式录屏或演示不想误触时，关闭 `Enable Hotkeys`。
- 测试方式：
  - `F1` 后流程回到走廊状态。
  - `F2-F4` 可快速打开终端、刷工牌、进入复盘。
  - 复盘等待安抚时，`F5` 可模拟安抚。
  - 回到 A/B 页面后，`F6` 或 `F7` 可提交处置。
  - `F8` 后显示下一户指引。
- 后续扩展：
  - 如果后续增加更多住户或剧情节点，可以继续补 F9/F10 或改成 Debug 面板按钮。
  - 打正式包前可关闭 `Enable Hotkeys`，或者保留默认 `Editor Or Development Only`。
- 最近更新：新增脚本。用于最小循环 Play Mode 快速跳测，不参与正式玩法逻辑。

## ResidentTerminalFlow.cs

- 路径：`Assets/Scripts/MinLoop/ResidentTerminalFlow.cs`
- 脚本作用：让 17F-01 门口终端成为最小循环入口。玩家看向终端并按 `E` 时，调用 `MinLoopFlowController.BeginTerminalInspection()`。
- 挂载对象：`Terminal_17F01`，或终端模型上带 Collider 的父物体。
- 必需组件：
  - 终端物体或子物体需要 Collider。
  - 玩家身上需要 `PlayerInteraction`。
  - 场景中需要 `MinLoopFlowController`。
- Inspector 字段：
  - `Fallback Description`：找不到流程控制器时显示的默认交互提示。
  - `Flow Controller`：拖入 `MinLoopFlowController`。
  - `Find Controller On Awake`：第一版可开启，自动找流程控制器。
- 公开接口：
  - `Interact()`：由 `PlayerInteraction` 调用，开始终端流程。
  - `GetDescription()`：根据当前流程阶段返回交互提示。
  - `SetFlowController(MinLoopFlowController controller)`：运行时指定流程控制器。
- 关联脚本：
  - 实现 `IInteractable`。
  - 被 `PlayerInteraction` 射线检测。
  - 调用 `MinLoopFlowController`。
- 使用步骤：
  1. 选中 `Terminal_17F01`。
  2. 确认终端或子物体有 Collider。
  3. 添加 `ResidentTerminalFlow`。
  4. 拖入 `MinLoopFlowController`。
  5. 不要再添加旧 `TerminalInteractable`；该脚本已删除。若同一物体已经有正式 TV 入口 `HearthTvTerminalInteractable`，需要确认它和 `ResidentTerminalFlow` 不会同时响应同一次 `E`。
- 测试方式：玩家看向终端，提示显示 `E 刷工牌`；按 `E` 后打开最小循环终端页面。
- 后续扩展：如果每一户都有独立终端，可以每个终端挂一个类似脚本，或后续扩展成读取住户 ID 的版本。
- 最近更新：使用方式无变化。增强运行时引用自动查找；如果 Awake 时没找到 `MinLoopFlowController`，玩家交互或读取提示时会再查找一次。

## MinLoopTerminalPresenter.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopTerminalPresenter.cs`
- 脚本作用：显示最小循环终端内容，包括刷工牌、住户摘要、A/B 处置、信任变化和下一户指引。第一版会自动生成占位 UI，后续可替换成正式 UI。
- 挂载对象：推荐挂在 `HumanCanvas`、`UIManager` 或 `MIN_LOOP_ROOT/UI/MinLoopTerminalPresenter`。
- 必需组件：
  - 推荐场景中有 `TerminalUIController`，用于打开/关闭终端并冻结玩家。
  - 如果没有 `TerminalUIController`，脚本会生成临时 Canvas、Panel 和右上角关闭按钮，并可通过 `Gameplay Behaviours To Disable` 临时关闭玩家移动/视角脚本。
  - 如果已经绑定 `TerminalUIController`，冻结和恢复玩家控制权由 `TerminalUIController` 负责，避免两个脚本同时记录控制状态导致关闭终端后玩家仍被禁用。
- Inspector 字段：
  - `Terminal UI`：拖入现有 `TerminalUIController`。
  - `Find Terminal UI On Awake`：第一版可开启。
  - `Create Fallback UI`：没有正式 UI 时开启。
  - `Create Event System If Missing`：打开终端时如果场景中没有 `EventSystem`，自动创建一个，避免按钮点不了。
  - `Disable Gameplay Behaviours When Open`：打开终端时是否临时禁用下方列表里的玩家控制脚本。没有 `TerminalUIController` 时建议开启。
  - `Gameplay Behaviours To Disable`：可选，拖入当前 Mia 控制器上的 `FirstPersonMovement`、`FirstPersonLook`、`PlayerInteraction` 等组件；仅在没有 `TerminalUIController` 时作为兜底冻结列表使用，终端关闭时会恢复原本启用状态。
  - `Access Title/Body`：刷工牌页面文案。
  - `Summary Title/Body`：住户摘要页面文案。
  - `Disposition Title/Body`：处置选择页面文案。
  - `Option A Label` / `Option B Label`：A/B 按钮文案。
  - `Next Resident Body`：下一户指引文案。
  - `Use Bound UI When Assigned`：如果已拖入正式 UI 文本和按钮，优先使用正式 UI；未拖完整时继续使用自动占位 UI。
  - `Bound UI Root`：正式终端 UI 的根物体，可选。使用正式 UI 时会自动显示/隐藏。
  - `Bound Title Text`：正式 UI 标题 TMP 文本。
  - `Bound Body Text`：正式 UI 正文 TMP 文本。
  - `Bound Primary Button` / `Bound Primary Button Text`：正式 UI 主按钮和按钮文字，用于“刷工牌”“调出昨夜事件”“查看下一户指引”等单按钮页面。
  - `Bound Secondary Button` / `Bound Secondary Button Text`：正式 UI 第二按钮，用于 A/B 处置页面的 B 选项。
  - `Bound Close Button` / `Bound Close Button Text`：正式 UI 关闭按钮，可选；脚本会绑定为关闭终端。
- 公开接口：
  - `ShowAccessCard(UnityAction confirmAction)`：显示刷工牌页。
  - `ShowResidentSummary(UnityAction replayAction)`：显示住户摘要页。
  - `ShowDispositionChoices(UnityAction chooseAAction, UnityAction chooseBAction)`：显示 A/B 选择。
  - `ShowDispositionResult(MinLoopDispositionChoice choice, int trustValue, int delta, UnityAction continueAction)`：显示处置结果和信任度。
  - `ShowNextResident(UnityAction closeAction)`：显示下一户指引。
  - `Close()`：关闭终端。
  - `HasTerminalUIController`：读取是否绑定了 `TerminalUIController`。
  - `HasSingleButtonBoundUI`：读取正式 UI 的标题、正文、主按钮是否绑定完整。
  - `HasChoiceBoundUI`：读取正式 UI 的 A/B 页面是否绑定完整。
  - `HasAnyBoundUIAssigned`：读取是否拖入过任何正式 UI 字段。
  - `CanCreateFallbackUI`：读取是否允许创建占位 UI。
  - `CanCreateEventSystemIfMissing`：读取是否允许自动创建 `EventSystem`。
  - `HasGameplayBehavioursToDisable`：读取是否配置了终端打开时要临时禁用的玩家控制组件。
- 关联脚本：
  - 被 `MinLoopFlowController` 调用。
  - 可复用 `TerminalUIController.ContentRoot`。
- 使用步骤：
  1. 在 UI 管理对象上添加 `MinLoopTerminalPresenter`。
  2. 将已有 `TerminalUIController` 拖到 `Terminal UI`。
  3. 第一版可以直接使用默认中文文案和自动占位 UI。
  4. 如果暂时不使用 `TerminalUIController`，把 Mia 当前启用的 `FirstPersonMovement`、`FirstPersonLook`、`PlayerInteraction` 拖到 `Gameplay Behaviours To Disable`。
  5. 如果已经使用 `TerminalUIController`，优先把 Mia 控制脚本和 Rigidbody 拖到 `TerminalUIController`，`MinLoopTerminalPresenter` 的冻结列表只当备用记录。
  5. 后续正式 UI 上传后，如果 UI 是整体终端面板，可继续让 `TerminalUIController` 负责打开/冻结玩家。
  6. 把正式 UI 的标题、正文、主按钮、A/B 第二按钮、关闭按钮拖到 `Bound UI Optional` 对应字段。
  7. 保持 `Use Bound UI When Assigned` 开启。字段拖完整后，脚本会优先使用正式 UI；字段不完整时自动回退到占位 UI。
- 测试方式：流程控制器调用后，能看到刷工牌、摘要、A/B、结果和下一户指引页面；按钮点击能推进流程；没有手动 EventSystem 的场景里按钮也能点击；没有 `TerminalUIController` 时，兜底终端面板右上角应有关闭按钮；如果没有 `TerminalUIController` 但配置了 `Gameplay Behaviours To Disable`，打开终端时玩家不会继续移动或转头，关闭后恢复；如果有 `TerminalUIController`，关闭终端后 Mia 控制权应由 `TerminalUIController` 正常恢复。
- 后续扩展：正式 UI 完成后，可以保留这些公开方法，只绑定正式 TMP_Text 和 Button；如果后续页面更多，可继续扩展绑定字段或拆成专门 UI View。
- 最近更新：使用方式有变化。兜底终端 UI 新增右上角关闭按钮，并修正终端控制权冻结职责：绑定 `TerminalUIController` 时不再由 `MinLoopTerminalPresenter` 额外禁用玩家控制，避免关闭终端后 Mia 仍无法移动；没有 `TerminalUIController` 时仍可通过 `Gameplay Behaviours To Disable` 做兜底冻结。此前正式 UI 可选绑定模式、自动补 `EventSystem` 继续保留。

## ReplaySequenceController.cs

- 路径：`Assets/Scripts/MinLoop/ReplaySequenceController.cs`
- 脚本作用：控制儿童房昨夜事件复盘。流程为孩子睡眠状态、噩梦醒来并看门口、说“妈妈”、等待玩家唯一安抚、孩子重新睡下并再次看门口、播放第二天父母对话，最后通知总流程回终端。
- 挂载对象：推荐新建空物体 `ReplayRoom_17F01/ReplaySequenceController`。
- 必需组件：
  - 场景中需要 `MinLoopFlowController`。
  - 推荐有 `MinLoopSubtitlePlayer`。
  - 推荐摆放一个 `ComfortActionInteractable` 作为唯一安抚操作点；如果暂时没摆，开启兜底时会运行时自动生成。
  - 可选 `SimpleActorCueController` 控制孩子、母亲、父亲。
- Inspector 字段：
  - `Flow Controller`：拖入 `MinLoopFlowController`。
  - `Subtitle Player`：拖入 `MinLoopSubtitlePlayer`。
  - `Comfort Action`：拖入床边或夜灯上的 `ComfortActionInteractable`。
  - `Create Fallback Comfort Action If Missing`：没有拖 `Comfort Action` 时，运行时自动创建一个兜底安抚点。第一版建议开启。
  - `Fallback Comfort Action Local Position`：兜底安抚点相对 `ReplaySequenceController` 物体的本地位置。
  - `Fallback Comfort Action Collider Size`：兜底安抚点的 BoxCollider 大小。
  - `Child Actor`：拖入孩子对象上的 `SimpleActorCueController`。
  - `Mother Actor` / `Father Actor`：拖入父母占位人物，可选。
  - `Door Look Target`：拖入儿童房门口方向的空物体。
  - `Initial Delay`：进入复盘后，孩子开始惊醒前的等待。
  - `Wait Before Door Look After Wake`：孩子播放惊醒 Cue 后，再看向门口前的等待。默认 `0.35` 秒，用于让无 Animator 的占位姿势先完成一点。
  - `Wait After Door Look`、`Wait After Comfort`、`Child Second Door Look Hold`：复盘节奏时间。
  - `Seed Default Lines If Empty`：第一版建议开启，自动填充默认字幕。
  - `Pre Comfort Lines`、`Comfort Lines`、`Morning Lines`：三段字幕和可选语音。
- 公开接口：
  - `BeginReplay(MinLoopFlowController owner)`：由总流程调用，开始复盘。
  - `PerformComfortAction()`：由唯一安抚交互调用。
  - `CancelReplay()`：重置复盘状态。
  - `IsRunning` / `IsWaitingForComfort`：给调试或其他脚本读取。
  - `HasComfortAction`：读取当前是否已绑定或生成安抚点。
  - `CanCreateFallbackComfortAction`：读取是否允许自动生成兜底安抚点。
- 关联脚本：
  - 调用 `MinLoopSubtitlePlayer.PlayLines()`。
  - 调用 `ComfortActionInteractable.SetAvailable()`。
  - 调用 `SimpleActorCueController` 的显隐、动画 Trigger 和 LookAt。
  - 通知 `MinLoopFlowController` 当前进度。
- 使用步骤：
  1. 在儿童房复盘根物体下新建 `ReplaySequenceController`。
  2. 添加脚本并拖入流程控制器、字幕播放器、唯一安抚点、孩子对象和门口看向目标。
  3. 第一版不用配置字幕，保持 `Seed Default Lines If Empty` 开启即可。
  4. 如果后面有临时配音，把音频拖到各字幕行的 `Voice Clip`。
- 测试方式：从终端调出昨夜事件后，能进入复盘；孩子看向门口并出现“妈妈”；安抚点出现；按 `E` 安抚后继续播放孩子睡下和父母早晨对话；如果没有手动摆 `ComfortActionInteractable` 但开启兜底，运行时应生成 `Generated_ComfortAction_Bedside`。
- 后续扩展：后续可以把三段字幕改成 ScriptableObject 或 JSON 数据；也可以把简单朝向替换成 Timeline/Animator 动画。
- 最近更新：使用方式有变化。新增兜底安抚点：未绑定 `Comfort Action` 且开启 `Create Fallback Comfort Action If Missing` 时，会运行时生成 `Generated_ComfortAction_Bedside`、BoxCollider 和自动发光提示，避免复盘停在等待安抚阶段；此前 `Wait Before Door Look After Wake`、开始复盘时重新解析引用、重复复盘前取消旧复盘、时间参数限制为非负数的逻辑继续保留。

## ComfortActionInteractable.cs

- 路径：`Assets/Scripts/MinLoop/ComfortActionInteractable.cs`
- 脚本作用：复盘中唯一的玩家操作点。只有 `ReplaySequenceController` 进入等待安抚阶段时才启用，玩家按 `E` 后通知复盘继续。
- 挂载对象：推荐挂在 `ComfortAction_Bedside`、`NightLight_ComfortAction` 或床边一个带 Collider 的空物体。
- 必需组件：
  - 当前物体或子物体需要 Collider。
  - 陪伴单元控制器需要 `PlayerInteraction`。
  - 场景中需要 `ReplaySequenceController`。
- Inspector 字段：
  - `Interaction Description`：交互提示，默认 `执行安抚操作`。
  - `Available On Start`：第一版应关闭，避免玩家提前触发。
  - `Sequence Controller`：拖入 `ReplaySequenceController`。
  - `Visual Root`：可选，拖入按钮提示模型或高亮物体；可用来随可交互状态显隐。没有拖时可自动生成占位发光球。
  - `Create Fallback Visual If Missing`：没有正式提示模型时自动生成小发光球和点光。第一版建议开启。
  - `Fallback Visual Color`：自动占位视觉颜色。
  - `Fallback Visual Local Position`：自动占位视觉相对安抚点的本地位置。
  - `Fallback Visual Scale`：自动占位视觉大小。
  - `Fallback Light Intensity` / `Fallback Light Range`：自动点光强度和范围。
- 公开接口：
  - `Interact()`：被 `PlayerInteraction` 调用。
  - `GetDescription()`：返回交互提示。
  - `SetAvailable(bool value)`：复盘脚本用来启用/禁用交互 Collider 和可见标记。
  - `SetSequenceController(ReplaySequenceController controller)`：运行时指定复盘控制器。
  - `IsAvailable`：读取当前是否可交互。
- 关联脚本：
  - 实现 `IInteractable`。
  - 调用 `ReplaySequenceController.PerformComfortAction()`。
  - 被 `ReplaySequenceController` 控制可用状态。
- 使用步骤：
  1. 在孩子床边新建空物体 `ComfortAction_Bedside`。
  2. 添加 Collider，建议先用 BoxCollider。
  3. 添加 `ComfortActionInteractable`。
  4. 拖入 `ReplaySequenceController`。
  5. 如果做了一个小图标或夜灯高亮，拖到 `Visual Root`。
  6. 如果还没有正式模型，保持 `Create Fallback Visual If Missing` 开启，脚本会自动生成一个小发光球。
- 测试方式：复盘开始时床边不可交互；孩子说完关键台词后，床边出现提示或自动发光球；按 `E` 后提示消失，流程继续。
- 后续扩展：后续可把唯一操作改成按钮 UI、靠近床边、播放白噪音或开夜灯，但仍建议通过 `SetAvailable()` 控制开放时机。
- 最近更新：使用方式有变化。新增自动占位视觉：未拖 `Visual Root` 时会生成小发光球和点光，并随安抚点可用状态显示/隐藏；此前启动顺序容错、启用/禁用交互时自动缓存 Collider、玩家交互时重新查找 `ReplaySequenceController` 的逻辑继续保留。

## MinLoopSubtitlePlayer.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopSubtitlePlayer.cs`
- 脚本作用：播放最小循环字幕和可选语音。没有正式 UI 时自动生成底部字幕条。
- 挂载对象：推荐挂在 `MinLoop_UI_Canvas`、`UIManager` 或 `MIN_LOOP_ROOT/UI/MinLoopSubtitlePlayer`。
- 必需组件：
  - 如使用正式 UI，需要一个字幕 Panel、说话人 TMP_Text、正文 TMP_Text。
  - 可选 AudioSource，用于播放对应字幕行语音。
- Inspector 字段：
  - `Subtitle Panel`：正式字幕面板根物体。
  - `Speaker Text`：显示说话人。
  - `Body Text`：显示字幕正文。
  - `Canvas Group`：可选，用于显隐。
  - `Create Fallback UI`：第一版建议开启。
  - `Force Subtitle Canvas Sorting`：建议开启。开启后字幕面板会自动添加独立 Canvas，避免被黑屏、机器人 HUD 或其他高层 UI 盖住。
  - `Subtitle Sorting Order`：字幕 Canvas 的排序层级，当前推荐 `7600`。17F02 黑屏层默认是 `7000`，因此字幕会显示在黑屏上方。
  - `Default Hold Seconds`：字幕行未设置停留时间时的默认时长。
  - `Use Unscaled Time`：是否不受 Time.timeScale 影响。
  - `Audio Source`：可选，播放语音。
- 公开接口：
  - `PlaySequence(IList<MinLoopSubtitleLine> lines)`：开始播放字幕序列。
  - `PlayLines(IList<MinLoopSubtitleLine> lines)`：协程版本，可被其他协程 `yield return`。
  - `ShowLine(string speaker, string text)`：直接显示一行字幕。
  - `Hide()`：停止并隐藏。
  - `Stop()`：停止当前字幕序列。
  - `IsPlaying`：读取是否正在播放。
- 关联脚本：
  - 被 `ReplaySequenceController` 调用。
  - 使用 `MinLoopSubtitleLine` 数据。
- 使用步骤：
  1. 新建或选中 UI 对象，添加 `MinLoopSubtitlePlayer`。
  2. 第一版可不拖任何 UI，保持 `Create Fallback UI` 开启。
  3. 如果有正式 UI，把 Panel、说话人文本、正文文本拖入字段。
  4. 如果有配音，给对象加 AudioSource 并拖入 `Audio Source`。
- 测试方式：复盘脚本播放字幕时，底部出现说话人和正文；字幕按顺序自动切换并隐藏。
- 后续扩展：后续可加入打字机效果、头像、语音波形或跳过按钮，但保留 `PlayLines()` 供复盘流程等待字幕结束。
- 最近更新：使用方式有变化。字幕面板现在可强制使用独立 Canvas 排序，默认 `Subtitle Sorting Order = 7600`，用于解决 17F02 黑屏开场时字幕被 `Hearth17F02ReplayBlackout` 盖住的问题。停止字幕时仍会同步停止 AudioSource；默认字幕停留时间会被限制为非负数。

## SimpleActorCueController.cs

- 路径：`Assets/Scripts/MinLoop/SimpleActorCueController.cs`
- 脚本作用：给孩子、父母等人物提供第一版可用的简单表现接口，包括显隐、播放 Animator Trigger、看向某个目标点，以及没有 Animator 时用本地位移/旋转/缩放做占位姿势。适合先演出“睡下、惊醒、被安抚、早晨出现”。
- 挂载对象：孩子、母亲、父亲等人物模型根物体，或它们的控制空物体。
- 必需组件：无。若需要动画，人物或子物体上需要 Animator。
- Inspector 字段：
  - `Animator`：拖入人物 Animator，可选。
  - `Look Root`：要旋转看向目标的 Transform；不填则使用自身 Transform。
  - `Visible Root`：要显隐的人物根物体；不填则使用自身。
  - `Sleep Trigger`、`Nightmare Trigger`、`Comforted Trigger`、`Morning Trigger`：Animator Trigger 名称。
  - `Use Fallback Poses When No Animator`：没有 Animator，或 Animator 里没有对应 Trigger 参数时，是否启用占位姿势。第一版建议开启。
  - `Apply Fallback Poses Even With Animator`：即使有 Animator 也同时套用占位姿势。正式动画阶段通常关闭。
  - `Pose Root`：要做占位姿势的 Transform。建议拖人物模型根或一个专门的姿势父物体。
  - `Capture Initial Pose On Awake`：Play 时记录初始姿势，便于隐藏/重置时恢复。
  - `Reset Pose When Hidden`：隐藏人物时恢复初始姿势。
  - `Sleep Pose`：无 Animator 时的睡眠姿势，本地位置/旋转/缩放和过渡时间。
  - `Nightmare Wake Pose`：无 Animator 时的噩梦惊醒姿势。
  - `Comforted Pose`：无 Animator 时被安抚后的睡下姿势。
  - `Morning Pose`：无 Animator 时早晨/站立/出现姿势。
  - `Look Horizontal Only`：第一版建议开启，人物只在水平面转向。
- 公开接口：
  - `SetVisible(bool isVisible)`：显示或隐藏人物。
  - `PlaySleep()`、`PlayNightmareWake()`、`PlayComforted()`、`PlayMorning()`：播放对应 Trigger；没有 Animator 时会播放对应占位姿势。
  - `PlayTrigger(string triggerName)`：播放自定义 Trigger；如果 Animator 或 Trigger 参数不存在，会安静跳过，不向 Console 抛 Trigger 缺失错误。
  - `ResetToInitialPose()`：恢复 Play 时记录的初始姿势。
  - `StopPose()`：停止当前占位姿势过渡。
  - `SetAnimatorBool(string parameterName, bool value)`：设置 Animator Bool。
  - `LookAt(Transform target)` / `LookAtPosition(Vector3 worldPosition)`：让人物看向目标。
  - `CaptureSleepPoseFromCurrent()`、`CaptureNightmareWakePoseFromCurrent()`、`CaptureComfortedPoseFromCurrent()`、`CaptureMorningPoseFromCurrent()`：把当前 `Pose Root` 姿势记录到对应 Pose，可从组件右上角 Context Menu 调用。
- 关联脚本：
  - 被 `ReplaySequenceController` 调用。
- 使用步骤：
  1. 给孩子模型或占位物添加 `SimpleActorCueController`。
  2. 如果有 Animator，拖到 `Animator`；没有也可以先不拖。
  3. 将孩子头部/根物体拖到 `Look Root`，第一版可直接用根物体；如果有头部骨骼，优先拖头部或上半身。
  4. 将要做占位姿势的父物体拖到 `Pose Root`，第一版也可以直接用根物体。
  5. 没有 Animator 时，调整 `Sleep Pose`、`Nightmare Wake Pose`、`Comforted Pose`、`Morning Pose` 的本地旋转/位置；也可以手动摆好模型后用 Context Menu 记录。
  6. 在 `ReplaySequenceController` 的 `Child Actor` 字段拖入这个组件。
  7. 父母同理，可拖到 `Mother Actor`、`Father Actor`；父母第一版只需要 `Morning Pose`。
- 测试方式：
  - 复盘开始时孩子显示并进入睡眠姿势。
  - 惊醒时孩子从睡眠姿势过渡到惊醒姿势，然后看向门口。
  - 执行安抚后孩子回到睡下姿势。
  - 父母早晨段显示出来并播放 Morning Trigger 或 Morning Pose。
  - 如果 Animator Trigger 配对正确，会触发正式动画；如果没有 Animator，或 Animator Controller 里没有对应 Trigger，也能看到占位姿势变化。
- 后续扩展：后续正式动画完成后，只需要统一 Animator Trigger 名，并关闭或保留 `Apply Fallback Poses Even With Animator`；复盘流程脚本不需要改。
- 最近更新：使用方式有变化。新增无 Animator / 缺少 Trigger 时的占位姿势系统、初始姿势重置、Context Menu 捕捉 Pose，以及 `StopPose()`/`ResetToInitialPose()` 公开接口。

## TrustStateController.cs

- 路径：`Assets/Scripts/MinLoop/TrustStateController.cs`
- 脚本作用：记录并更新最小循环中的信任度。A 选项默认提高信任度，B 选项默认小幅降低信任度。
- 挂载对象：推荐新建空物体 `MIN_LOOP_ROOT/FlowManagers/TrustStateController`。
- 必需组件：无。
- Inspector 字段：
  - `Starting Trust`：初始信任度，默认 50。
  - `Min Trust` / `Max Trust`：信任度范围。
  - `Option A Delta`：选择 A 的变化值，默认 +8。
  - `Option B Delta`：选择 B 的变化值，默认 -3。
  - `Trust Changed`：信任度变化事件，可选绑定 UI 更新。
  - `Current Trust`、`Last Delta`：运行时观察值。
- 公开接口：
  - `ResetTrust()`：重置信任度。
  - `ApplyChoice(MinLoopDispositionChoice choice)`：根据 A/B 应用变化并返回当前信任度。
  - `SetTrust(int value)`：直接设置信任度。
  - `CurrentTrust`：读取当前信任度。
  - `LastDelta`：读取上次变化值。
  - `TrustChanged`：读取信任度变化 UnityEvent，给 `MinLoopTrustPresenter` 或其他 UI 监听。
- 关联脚本：
  - 被 `MinLoopFlowController` 调用。
  - 结果传给 `MinLoopTerminalPresenter` 显示。
  - 被 `MinLoopTrustPresenter` 监听，用于刷新独立信任度 UI。
- 使用步骤：
  1. 新建空物体 `TrustStateController`。
  2. 添加脚本。
  3. 根据需要调整 A/B 的信任度变化。
  4. 拖到 `MinLoopFlowController` 的 `Trust State Controller` 字段。
- 测试方式：在终端选择 A 后显示正变化；选择 B 后显示负变化；当前信任度不超过最小/最大范围。
- 后续扩展：后续可把信任度拆成家庭信任、系统信任、家属依赖度等多个数值。
- 最近更新：使用方式无变化。新增只读属性 `TrustChanged`，方便 `MinLoopTrustPresenter` 等脚本用代码监听信任度变化；原有 Inspector 事件和信任度计算方式不变。

## SmartDoorController.cs

- 路径：`Assets/Scripts/Interactions/SmartDoorController.cs`
- 脚本作用：通用门控制脚本。支持滑动门、旋转门、滑动加旋转三种模式；实现 `IInteractable`，玩家看向带 Collider 的门并按 `E` 可开关。适合大厅玻璃滑门、17F-01 房门、临时测试门。
- 挂载对象：推荐挂在门的父物体或门交互 Collider 所在物体，例如 `Door_17F01`、`Lobby_GlassDoor_Interactable`。
- 必需组件：
  - 挂载物体或子物体需要 Collider，供 `PlayerInteraction` 射线命中。
  - 如果要播放声音，挂载物体需要 AudioSource，或把其他 AudioSource 拖入字段。
  - 如果门板和交互 Collider 分离，需要把实际移动/旋转的门板拖到 `Moving Root`。
- Inspector 字段：
  - `Closed Description`：门关闭时的交互提示，默认 `E 开门`。
  - `Open Description`：门打开时的交互提示，默认 `E 关门`。
  - `Locked Description`：锁定时的提示。
  - `Can Toggle`：是否允许开关切换；如果只允许打开一次，可关闭。
  - `Locked`：是否锁定。锁定时交互只播放锁定反馈。
  - `Moving Root`：真正移动/旋转的门板 Transform。不填则使用当前物体。
  - `Motion Mode`：`Slide` 为平移门，`Rotate` 为旋转门，`SlideAndRotate` 为同时移动和旋转。
  - `Capture Closed State On Awake`：第一版建议开启。Play 时自动把当前门板位置/旋转记为关闭状态。
  - `Closed Local Position` / `Closed Local Euler Angles`：关闭状态数据。通常不用手填，开启上面字段会自动记录。
  - `Open Local Position Offset`：打开时相对关闭位置的偏移。玻璃滑门常用，例如 X 方向 `1.6`。
  - `Open Local Euler Offset`：打开时相对关闭旋转的角度。普通门常用，例如 Y 方向 `90`。
  - `Move Duration`：开关耗时。
  - `Motion Curve`：开关缓动曲线。
  - `Start Open`：Play 后是否默认打开。
  - `Auto Close` / `Auto Close Delay`：开门后是否自动关门。
  - `Audio Source`、`Open Clip`、`Close Clip`、`Locked Clip`：开门、关门、锁门声音。
  - `Opened`、`Closed`、`Lock Rejected`：UnityEvent，可用于触发额外动画、灯光、剧情。
- 公开接口：
  - `Interact()`：由 `PlayerInteraction` 调用。
  - `GetDescription()`：返回当前交互提示。
  - `Open()`、`Close()`、`Toggle()`：给按钮、剧情或 UnityEvent 调用。
  - `SetLocked(bool value)`：运行时锁定/解锁。
  - `SnapOpen()`、`SnapClosed()`：瞬间切到开/关状态。
  - `IsOpen`、`IsMoving`、`IsLocked`：读取当前状态。
- 关联脚本：
  - 实现 `IInteractable`，被 `PlayerInteraction` 检测。
  - 可被 `MinLoopFlowController`、按钮事件、终端流程或 UnityEvent 调用。
  - 可被 `SmartDoorTriggerZone` 调用，实现玩家靠近自动开门、离开自动关门。
- 使用步骤：
  1. 选中门的交互父物体，例如 `Door_17F01`。
  2. 添加 Collider，或确认子物体已有 Collider。
  3. 添加 `SmartDoorController`。
  4. 如果门板是子物体，把门板拖到 `Moving Root`。
  5. 玻璃滑门：`Motion Mode = Slide`，调整 `Open Local Position Offset`。
  6. 普通房门：`Motion Mode = Rotate`，调整 `Open Local Euler Offset`。
  7. 如需声音，加 AudioSource 并拖入开关门音效。
- 测试方式：
  - Play 后玩家看向门，出现 `E 开门`。
  - 按 `E` 后门平滑移动或旋转到打开位置。
  - 再按 `E` 门关闭。
  - 锁定后按 `E` 不开门，只播放锁定声音/事件。
  - Auto Close 开启时，门打开后会按延迟自动关闭。
- 后续扩展：
  - 如果后续门要用 Animator，可保留该脚本作为交互入口，在 `Opened/Closed` 事件中调用 Animator Trigger，或另写 Animator 专用门脚本替换运动部分。
  - 如果后续做门禁权限，可由终端流程调用 `SetLocked(false)` 解锁。
- 最近更新：使用方式有变化。新增 `SmartDoorTriggerZone` 后，`SmartDoorController` 可继续作为门本体控制脚本，也可被自动门触发区调用；原有按 `E` 开门、阶段 Cue 开门和 UnityEvent 开门方式继续保留。

## SmartDoorTriggerZone.cs

- 路径：`Assets/Scripts/Interactions/SmartDoorTriggerZone.cs`
- 脚本作用：自动门触发区脚本。玩家或指定对象进入 Trigger 区域时调用 `SmartDoorController.Open()`，区域清空后延迟调用 `Close()`。适合大厅玻璃门、走廊感应门、只想靠近自动打开的门。
- 挂载对象：推荐挂在门旁边的触发区空物体上，例如 `Lobby_GlassDoor_TriggerZone`。最简单做法是把它作为门对象的子物体。
- 必需组件：
  - 触发区物体需要 Collider，并勾选 `Is Trigger`。
  - 如果没有 Collider，保持 `Create Box Collider If Missing` 开启，Play 后会自动补一个 BoxCollider。
  - 门本体需要挂 `SmartDoorController`。
- Inspector 字段：
  - `Target Door`：要控制的 `SmartDoorController`。如果触发区是门的子物体，可不拖，脚本会自动找父级门。
  - `Open On Enter`：对象进入触发区时是否开门。
  - `Close On Exit`：触发区清空后是否关门。
  - `Close Delay`：离开后延迟多久关门，建议玻璃门 `0.25` 到 `0.6`。
  - `Allowed Layers`：允许触发的 Layer。第一版可保持默认。
  - `Required Tag`：可选，只允许指定 Tag 触发，例如 `Player`。
  - `Required Root`：可选，只允许某个角色根物体或其子物体触发。
  - `Ignore Trigger Colliders`：忽略其他 Trigger Collider，减少误触发。
  - `Create Box Collider If Missing`：没有 Collider 时自动补 BoxCollider。
  - `Auto Mark Collider As Trigger`：自动把 Collider 设为 Trigger。
  - `Fallback Box Size`：自动补 BoxCollider 时的触发区大小。
  - `Enter Feedback` / `Exit Feedback`：进入/清空区域时播放的反馈，可拖 `InteractionFeedbackController`。
  - `Zone Entered` / `Zone Emptied`：UnityEvent，可继续接灯光、音效或其他剧情脚本。
- 公开接口：
  - `SetTargetDoor(SmartDoorController door)`：运行时指定目标门。
  - `ForceOpen()` / `ForceClose()`：外部强制开门/关门。
  - `ClearOccupants()`：清空当前触发记录。
  - `RefreshReferences()`：重新查找父级门和触发 Collider。
  - `IsOccupied`、`OccupantCount`：读取当前触发区内是否有人。
  - `HasConfiguredDoor`、`HasUsableTriggerCollider`、`CanCreateBoxColliderIfMissing`：给检查器读取。
- 关联脚本：
  - 调用 `SmartDoorController.Open()` / `Close()`。
  - 可播放 `InteractionFeedbackController` 作为进出门提示音或灯光。
  - `MinLoopSceneValidator` 会检查自动门触发区是否能找到目标门、是否有 Trigger Collider 或开启了自动创建。
  - `MinLoopSceneAutoBinder` 会把触发区列表写入检查器，并在触发区是门子物体或场景只有一个门时尽量自动填 `Target Door`。
- 使用步骤：
  1. 门本体挂 `SmartDoorController`，玻璃门建议 `Motion Mode = Slide`。
  2. 在门旁新建空物体 `Lobby_GlassDoor_TriggerZone`，放到玩家经过门前会穿过的位置。
  3. 给触发区挂 `SmartDoorTriggerZone`。
  4. 如果触发区不是门的子物体，把门本体拖到 `Target Door`。
  5. 调整 `Fallback Box Size` 或手动 BoxCollider 大小，让玩家靠近门时会进入触发区。
- 测试方式：
  - Play 后玩家走进触发区，玻璃门自动打开。
  - 玩家离开触发区，等待 `Close Delay` 后门自动关闭。
  - 多个角色/Collider 同时在触发区内时，直到区域清空才关门。
  - 如果设置了 `Required Tag` 或 `Required Root`，只有符合条件的对象会触发。
- 后续扩展：
  - 如果需要门禁权限，可由终端流程或阶段 Cue 调用 `SmartDoorController.SetLocked(false)` 后再允许触发区开门。
  - 如果换成 Animator 门，仍可保留 `SmartDoorTriggerZone`，让它调用挂了 Animator 逻辑的门控制入口。
- 最近更新：新增自动门触发区脚本，用于大厅玻璃门靠近自动开、离开自动关。

## InteractionFeedbackController.cs

- 路径：`Assets/Scripts/Interactions/InteractionFeedbackController.cs`
- 脚本作用：通用交互反馈脚本。可以播放一次音效、让灯光亮一下、让模型材质发光一下；也实现 `IInteractable`，可直接作为电梯按钮、刷卡面板、终端提示按钮使用。
- 挂载对象：推荐挂在按钮、刷卡区、终端屏幕、门禁面板或一个专门的反馈空物体上，例如 `Elevator_Button_17F`、`Terminal_17F01_AccessFeedback`。
- 必需组件：
  - 如果玩家要直接看向它按 `E`，物体或子物体需要 Collider。
  - 如果要播声音，需要 AudioSource。
  - 如果要发光，需要拖入 Renderer 或 Light；不拖时会自动找子物体中的 Light 和 Renderer。
- Inspector 字段：
  - `Can Interact`：是否允许玩家交互。
  - `Interaction Description`：交互提示。
  - `Play Feedback On Interact`：玩家按 `E` 时是否自动播放反馈。
  - `One Shot`：是否只播放一次。
  - `Cooldown`：反馈冷却，避免快速连点刷屏。
  - `Audio Source`、`Feedback Clip`、`Volume`、`Pitch Range`：音效配置。电梯按钮可拖入叮咚声；终端可拖入蜂鸣声。
  - `Target Lights`、`Light Color`、`Light Intensity`：灯光反馈。
  - `Target Renderers`、`Pulse Color`、`Emission Intensity`、`Set Base Color`：材质发光反馈。脚本使用 MaterialPropertyBlock，不会在运行时复制材质。
  - `Pulse Duration`：亮起持续时间。
  - `Stay Lit After Feedback`：反馈后是否保持亮起。
  - `Feedback Started`、`Feedback Completed`：UnityEvent，可继续触发门、终端流程或动画。
- 公开接口：
  - `Interact()`：由 `PlayerInteraction` 调用。
  - `GetDescription()`：返回交互提示。
  - `PlayFeedback()`：由按钮、终端流程或其他脚本直接调用。
  - `SetInteractable(bool value)`：启用/禁用交互。
  - `ResetOneShot()`：重置一次性播放状态。
  - `SetStayLit(bool value)`：运行时切换是否保持亮起。
  - `CanInteract`：读取当前是否可交互。
- 关联脚本：
  - 实现 `IInteractable`，可被 `PlayerInteraction` 检测。
  - `MinLoopFlowController` 可选引用它，用于终端打开、刷工牌、调出复盘、提交处置时播放反馈。
  - 可通过 UnityEvent 调用 `SmartDoorController.Open()` 或其他公开方法。
- 使用步骤：
  1. 选中电梯按钮、刷卡区或终端反馈物体。
  2. 添加 `InteractionFeedbackController`。
  3. 添加 AudioSource，并拖到 `Audio Source`；把叮咚/蜂鸣音拖到 `Feedback Clip`。
  4. 如需发光，把按钮模型 Renderer 拖到 `Target Renderers`，或把小灯 Light 拖到 `Target Lights`。
  5. 如果它只是被流程调用，不需要玩家直接按，关闭 `Play Feedback On Interact` 或不加 Collider。
  6. 如果要接到最小循环，把它拖到 `MinLoopFlowController` 的对应反馈字段。
- 测试方式：
  - 玩家看向按钮按 `E`，能听到声音并看到发光。
  - 快速连按时会受到 `Cooldown` 限制。
  - `One Shot` 开启时只触发一次，调用 `ResetOneShot()` 后可再次触发。
  - 从终端流程刷工牌、调出事件、提交处置时，对应反馈对象能播放。
- 后续扩展：
  - 正式 UI 做好后，可继续用这个脚本专门处理声音和发光，不负责 UI 逻辑。
  - 如果需要更复杂的按钮动画，可在 `Feedback Started` 或 `Feedback Completed` 里绑定 Animator Trigger。
- 最近更新：使用方式无变化。新增通用音效/发光反馈脚本，用于电梯按钮、刷卡、门禁和终端提示反馈；补充空数组保护，外部事件提前调用显隐恢复时不会触发空引用。

## MinLoopSceneValidator.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopSceneValidator.cs`
- 脚本作用：17F-01 最小循环场景挂载检查器。用于在你摆好场景资源后，一次性检查总流程、终端、视角切换、复盘、安抚点、字幕、信任度、门和反馈对象是否缺关键组件。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/MinLoopSceneValidator` 或 `MIN_LOOP_ROOT/FlowManagers/MinLoopSceneValidator`。
- 必需组件：无。它是检查工具，不直接驱动游戏流程。
- Inspector 字段：
  - `Validate On Start`：Play 后是否自动检查。第一版建议开启。
  - `Include Inactive Objects`：是否检查未激活物体。第一版建议开启，因为 RobotCanvas、Companion 控制器等可能默认关闭。
  - `Log Success Details`：是否在 Console 中列出通过项。调试时开启；嫌日志太长可关闭。
  - `Flow Controller`：可选拖入 `MinLoopFlowController`；不拖会自动查找。
  - `Terminal Flow`：可选拖入 `ResidentTerminalFlow`。
  - `Terminal Presenter`：可选拖入 `MinLoopTerminalPresenter`。
  - `View Switch Controller`：可选拖入 `ViewSwitchController`。
  - `Stage Object Activators`：可选拖入 `MinLoopStageObjectActivator`，用于检查阶段性指引/高亮显隐脚本是否存在。
  - `Replay Sequence Controller`：可选拖入 `ReplaySequenceController`。
  - `Comfort Action`：可选拖入 `ComfortActionInteractable`。
  - `Subtitle Player`：可选拖入 `MinLoopSubtitlePlayer`。
  - `Robot HUD Presenter`：可选拖入 `MinLoopRobotHudPresenter`，用于检查陪伴单元视角是否有机器 HUD。
  - `Objective Presenter`：可选拖入 `MinLoopObjectivePresenter`，用于检查当前目标提示是否存在。
  - `Trust Presenter`：可选拖入 `MinLoopTrustPresenter`，用于检查独立信任度 UI 是否存在。
  - `Trust State Controller`：可选拖入 `TrustStateController`。
  - `Debug Hotkeys`：可选拖入 `MinLoopDebugHotkeys`，用于检查是否存在 Play Mode 快速测试入口。
  - `Stage Anchor Controller`：可选拖入 `MinLoopStageAnchorController`，用于检查 Mia/陪伴单元阶段站位锚点是否存在且有规则。
  - `Stage Cue Controller`：可选拖入 `MinLoopStageCueController`，用于检查阶段反馈、开门、动画、音效等一次性 Cue 是否存在且有规则。
  - `Lighting State Controller`：可选拖入 `MinLoopLightingStateController`，用于检查阶段灯光/氛围控制是否存在且有规则。
  - `Audio State Controller`：可选拖入 `MinLoopAudioStateController`，用于检查阶段环境声/氛围声控制是否存在且有规则。
  - `Guide Markers`：可选拖入 `MinLoopWorldGuideMarker`，用于检查默认世界指引是否存在。
  - `Doors`：可选拖入场景内所有 `SmartDoorController`。
  - `Door Trigger Zones`：可选拖入场景内所有 `SmartDoorTriggerZone`，用于检查自动门触发区。
  - `Feedback Objects`：可选拖入电梯按钮、刷卡反馈等 `InteractionFeedbackController`。
- 公开接口：
  - `ValidateSceneSetup()`：执行检查并在 Console 输出报告。
  - `ResolveReferences()`：自动查找场景中的关键脚本。
  - `LastErrorCount`：读取上次检查的必须项错误数量。
  - `LastWarningCount`：读取上次检查的建议项数量。
- 关联脚本：
  - 读取 `MinLoopFlowController`、`ResidentTerminalFlow`、`MinLoopTerminalPresenter`、`ViewSwitchController`、`MinLoopStageObjectActivator`、`MinLoopStageAnchorController`、`MinLoopStageCueController`、`MinLoopLightingStateController`、`MinLoopAudioStateController`、`ReplaySequenceController`、`ComfortActionInteractable`、`MinLoopSubtitlePlayer`、`MinLoopRobotHudPresenter`、`MinLoopObjectivePresenter`、`MinLoopTrustPresenter`、`TrustStateController`、`MinLoopDebugHotkeys`、`MinLoopWorldGuideMarker`、`SmartDoorController`、`SmartDoorTriggerZone`、`InteractionFeedbackController`。
  - 不会修改这些脚本的状态，只做检查和日志输出。
- 使用步骤：
  1. 在场景中创建空物体 `MinLoopSceneValidator`。
  2. 添加 `MinLoopSceneValidator`。
  3. 保持 `Validate On Start` 和 `Include Inactive Objects` 开启。
  4. 可以不手动拖字段，点击 `Resolve References` 自动查找；如果场景里有多个同类对象，再手动指定。
  5. 点击 Inspector 里的 `Validate Scene Setup`，或进入 Play Mode 自动检查。
- 测试方式：
  - Console 输出 `[MinLoopSceneValidator] 17F-01 最小循环场景检查`。
  - 如果缺少必须脚本或 Collider，会以 `[必须]` 列出并用 Error 日志输出。
  - 如果只是缺少门、按钮反馈、字幕等建议项，会以 `[建议]` 列出并用 Warning 日志输出。
  - 错误数为 0 时，说明最小循环的核心挂载条件基本满足。
- 后续扩展：
  - 后续如果加入 17F-02/17F-03，可在这个脚本中增加住户 ID、多个终端、多个复盘房间的检查。
  - MCP 挂载完成后，也可以用它作为验收脚本快速确认场景引用是否完整。
- 最近更新：使用方式有变化。新增 `SmartDoorTriggerZone` 自动门触发区检查；安抚点检查支持 `ReplaySequenceController` 运行时兜底安抚点：如果没有手动摆放 `ComfortActionInteractable` 但开启自动生成，会降级为建议提醒而不是必须错误；此前 `Stage Cue Controller`、`Stage Anchor Controller`、`Audio State Controller`、`Lighting State Controller`、`Guide Markers`、`Robot HUD Presenter`、`Debug Hotkeys`、`Trust Presenter`、`Objective Presenter`、`Stage Object Activators`、终端按钮 `EventSystem` 兜底和终端打开时玩家控制冻结方案检查继续保留。

## MinLoopSceneValidatorEditor.cs

- 路径：`Assets/Editor/MinLoopSceneValidatorEditor.cs`
- 脚本作用：给 `MinLoopSceneValidator` 的 Inspector 增加三个按钮：`Resolve References`、`Auto Bind Scene References` 和 `Validate Scene Setup`，方便不用进入 Play Mode 也能自动接线和检查挂载。
- 挂载对象：不需要手动挂载。它是 Editor 脚本，只要放在 `Assets/Editor` 下就会在选中 `MinLoopSceneValidator` 时自动生效。
- 必需组件：
  - 项目中需要存在 `MinLoopSceneValidator.cs`。
  - 项目中需要存在 `MinLoopSceneAutoBinder.cs`，用于 Inspector 中的一键自动绑定按钮。
  - 文件必须放在 `Assets/Editor` 目录下，避免被打进运行时代码。
- Inspector 字段：无独立字段。它只扩展 `MinLoopSceneValidator` 的 Inspector。
- 公开接口：无运行时公开接口。
- 关联脚本：
  - 服务 `MinLoopSceneValidator`。
  - 调用 `MinLoopSceneAutoBinder.AutoBindSceneReferences()` 自动绑定场景引用。
- 使用步骤：
  1. 选中挂有 `MinLoopSceneValidator` 的物体。
  2. 在 Inspector 中点击 `Resolve References` 自动填充引用。
  3. 如果已经按命名规则摆好 Mia、Companion、孩子、父母、门口目标点等对象，点击 `Auto Bind Scene References`。
  4. 点击 `Validate Scene Setup` 输出场景检查报告。
- 测试方式：
  - 选中 `MinLoopSceneValidator` 后能看到三个按钮。
  - 点击按钮后 Console 有对应检查输出。
- 后续扩展：
  - 后续可以增加“一键选中缺失对象”“复制检查报告”等编辑器辅助功能。
- 最近更新：使用方式有变化。新增 `Auto Bind Scene References` 按钮，可从检查器直接调用最小循环自动绑定工具。

## MinLoopSceneAutoBinder.cs

- 路径：`Assets/Editor/MinLoopSceneAutoBinder.cs`
- 脚本作用：编辑器自动绑定工具。用于在你摆好 17F-01 最小循环场景资源并按约定命名后，自动把 Mia、陪伴单元、终端、复盘、孩子/父母演员、门口目标点、指引物体、门、自动门触发区、反馈对象等引用填到对应脚本字段里，减少手动拖拽。
- 挂载对象：不需要手动挂载。它是 Editor 工具，通过 Unity 顶部菜单或 `MinLoopSceneValidator` Inspector 按钮使用。
- 必需组件：
  - 文件必须放在 `Assets/Editor` 下。
  - 项目中需要存在最小循环运行时脚本和编辑器骨架脚本。
  - 推荐先运行 `Tools / Min Loop / Create 17F-01 Scene Skeleton` 创建 `MIN_LOOP_ROOT`。
- Inspector 字段：无。它通过场景对象命名和已挂脚本类型自动查找。
- 菜单入口：
  - Unity 顶部菜单：`Tools / Min Loop / Auto Bind 17F-01 Scene References`
  - 或选中 `MinLoopSceneValidator` 后点击 Inspector 中的 `Auto Bind Scene References`。
- 识别的关键对象名：
  - `MIN_LOOP_ROOT`
  - `Player_Mia_Controller`、`Mia_Controller`、`Mia_FirstPersonController`、`Player_Mia`、`Mia`
  - `Companion_Controller`、`CompanionUnit_Controller`、`Robot_Controller`、`Companion_FirstPersonController`、`Robot`
  - `RobotCanvas`、`Robot_Canvas`、`CompanionCanvas`、`Companion_Canvas`
  - `Terminal_17F01_Interactable`、`Terminal_17F01`
  - `ReplaySequenceController`
  - `ComfortAction_Bedside`
  - `Child_Actor`
  - `Mother_Actor`
  - `Father_Actor`
  - `DoorLook_Target`、`DoorLookTarget`、`Child_DoorLook_Target`
  - `Guide_Terminal_17F01`
  - `Guide_ComfortAction`
  - `Guide_NextResident_17F02`
  - `Anchor_Mia_CorridorStart`
  - `Anchor_Companion_ReplayStart`
  - `Anchor_Mia_TerminalReturn`
  - `Anchor_NextResident_17F02`
  - `Light_Corridor_Warm`
  - `Light_Replay_Night`
  - `Light_Morning_Soft`
  - `Audio_Corridor_Ambience`
  - `Audio_Replay_Night_Ambience`
  - `Audio_Morning_Ambience`
  - `Feedback_TerminalOpen`
  - `Feedback_AccessCard`
  - `Feedback_ReplayRequest`
  - `Feedback_DispositionSubmit`
  - `Feedback_ComfortReady`
  - `Feedback_MorningReview`
  - `Feedback_NextResidentGuide`
- 自动绑定内容：
  - `MinLoopFlowController`：绑定终端展示器、视角切换器、复盘控制器、信任度控制器和 4 个反馈对象。
  - `ResidentTerminalFlow`：绑定总流程控制器。
  - `MinLoopTerminalPresenter`：绑定 `TerminalUIController`，并把 Mia 的 `FirstPersonMovement`、`FirstPersonLook`、`PlayerInteraction` 填入 `Gameplay Behaviours To Disable` 作为无 `TerminalUIController` 时的兜底冻结列表。
  - `TerminalUIController`：绑定 Mia 的移动、视角、交互和 Rigidbody，用于打开终端时冻结玩家。
  - `ReplaySequenceController`：绑定总流程、字幕播放器、唯一安抚点、孩子/父母演员和 `DoorLook_Target`；如果暂时没有唯一安抚点，复盘控制器可运行时生成兜底安抚点。
  - `ComfortActionInteractable`：绑定复盘控制器，并尝试把 `ComfortAction_Visual`、`ComfortActionVisual` 或 `Guide_ComfortAction` 作为可见提示物。
  - `ViewSwitchController`：自动填写 Human/Mia 和 Companion/Robot 两套 Root、Camera、FirstPersonMovement、FirstPersonLook、PlayerInteraction、Rigidbody、Canvas。
  - `PlayerInteraction`：把各自视角的 Camera 填到 `mainCamera`。
  - `MinLoopRobotHudPresenter`：绑定总流程，并优先把 `RobotCanvas` / `CompanionCanvas` 作为占位 HUD 的父 Canvas。
  - `MinLoopObjectivePresenter`、`MinLoopTrustPresenter`、`MinLoopDebugHotkeys`：绑定各自需要的流程/信任度/复盘引用。
  - 正式 UI 自动识别：如果场景里存在按约定命名的 `Terminal_BoundUI`、`Objective_Root`、`RobotHUD_Root`、`Trust_Root` 及其子对象，会自动填入终端、目标提示、机器 HUD、信任度 UI 的 Bound UI 字段。
  - `MinLoopStageObjectActivator`：补齐 3 条默认指引规则，不删除你的自定义规则。
  - `MinLoopStageAnchorController`：绑定总流程，并补齐 Mia 初始走廊站位、陪伴单元复盘起点、Mia 回到终端站位 3 条默认锚点规则。
  - `MinLoopStageCueController`：绑定总流程，并补齐安抚点就绪、早晨回顾、下一户指引 3 条默认 Cue 规则。
  - `MinLoopLightingStateController`：绑定总流程，并补齐走廊/终端暖光、昨夜复盘冷光、早晨回顾柔光 3 条默认规则。
  - `MinLoopAudioStateController`：绑定总流程，并补齐走廊/终端环境声、昨夜复盘环境声、早晨回顾环境声 3 条默认规则。
  - `MinLoopWorldGuideMarker`：给 3 个默认 Guide 对象补世界指引标记和默认文字。
  - `SmartDoorTriggerZone`：如果触发区是门子物体，或场景里只有一个 `SmartDoorController`，会尽量自动填 `Target Door`。
  - `MinLoopSceneValidator`：刷新检查器引用、门列表、自动门触发区列表、阶段锚点、阶段 Cue、灯光控制器、音频控制器和反馈列表。
- 自动补组件：
  - 如果 `Child_Actor`、`Mother_Actor`、`Father_Actor` 找得到但没有 `SimpleActorCueController`，会给这些命名对象补上该组件。
  - 如果终端入口或安抚点没有任何 Collider，会补一个 BoxCollider。
  - 如果 `Guide_Terminal_17F01`、`Guide_ComfortAction`、`Guide_NextResident_17F02` 找得到但没有 `MinLoopWorldGuideMarker`，会自动补上。
  - 不会移动、删除或重命名你的模型。
- 公开接口：
  - `AutoBindSceneReferences()`：执行一次自动绑定，可由菜单、清单窗口或检查器按钮调用。
- 关联脚本：
  - 调用 `MinLoopSceneSkeletonCreator`，在缺少 `MIN_LOOP_ROOT` 时可询问是否先创建骨架。
  - 可被 `MinLoopSetupChecklistWindow` 和 `MinLoopSceneValidatorEditor` 调用。
  - 读取并写入最小循环各运行时脚本的 Inspector 引用。
- 使用步骤：
  1. 打开 17F-01 最小循环场景。
  2. 点击 `Tools / Min Loop / Create 17F-01 Scene Skeleton`。
  3. 把 Mia 控制器命名为 `Player_Mia_Controller`，把陪伴单元控制器命名为 `Companion_Controller`。
  4. 把孩子、父母、门口目标点按上面对象名放到场景中。
  5. 点击 `Tools / Min Loop / Auto Bind 17F-01 Scene References`。
  6. 查看 Console 自动绑定报告：`[绑定]` 和 `[已存在]` 代表已处理，`[需要处理]` 代表必须补，`[可稍后]` 代表第一版可先跳过。
  7. 再运行 `Validate Scene Setup` 做最终检查。
- 最小测试方式：
  - 自动绑定后，`ViewSwitchController` 的 Human 和 Companion 字段应能看到对应 Root、Camera 和控制脚本。
  - `ReplaySequenceController` 应能看到 `Comfort Action`、`Child Actor` 和 `Door Look Target`。
  - 如果已经按推荐命名摆放正式 UI，`MinLoopTerminalPresenter`、`MinLoopObjectivePresenter`、`MinLoopRobotHudPresenter`、`MinLoopTrustPresenter` 应能看到对应 UI 根物体、文本、按钮、图片或 Slider 字段。
  - `MinLoopRobotHudPresenter` 应能看到 `Flow Controller`；如果场景中有 `RobotCanvas`，`Fallback Parent Canvas` 应自动填入。
  - `MinLoopStageAnchorController` 应能看到 `Flow Controller`，并有 Mia 初始、陪伴单元复盘、Mia 回终端三条规则。
  - `MinLoopStageCueController` 应能看到 `Flow Controller`，并有安抚点就绪、早晨回顾、下一户指引三条规则。
  - `MinLoopLightingStateController` 应能看到 `Flow Controller`，并有走廊/终端、昨夜复盘、早晨回顾三条规则。
  - `MinLoopAudioStateController` 应能看到 `Flow Controller`，并有走廊/终端、昨夜复盘、早晨回顾三条规则。
  - `MinLoopTerminalPresenter` 的 `Gameplay Behaviours To Disable` 可包含 Mia 的移动、视角、交互脚本，用于无 `TerminalUIController` 时的兜底冻结。
  - Console 报告不应出现必须处理的 Mia/Companion 控制器缺失。
- 后续扩展：
  - 后续 Unity MCP 可用时，可以按同一命名约定让 MCP 摆资源，再运行这个工具统一接线。
  - 如果增加 17F-02/17F-03，可复制此工具并替换对象名规则。
- 最近更新：使用方式有变化。自动绑定工具新增正式 UI 命名识别：可按 `Terminal_BoundUI`、`Objective_Root`、`RobotHUD_Root`、`Trust_Root` 等名称自动填终端、目标提示、机器 HUD、信任度 UI 字段；也会收集 `SmartDoorTriggerZone` 到检查器，并尽量自动填自动门触发区的 `Target Door`；同时会把缺失的唯一安抚点视为可稍后处理，包含 `ReplaySequenceController` 和 `MinLoopSceneValidator` 的安抚点引用，因为 `ReplaySequenceController` 已支持运行时生成兜底安抚点；此前 `MinLoopStageCueController`、`MinLoopStageAnchorController`、`MinLoopAudioStateController`、`MinLoopLightingStateController`、`MinLoopWorldGuideMarker`、`MinLoopRobotHudPresenter`、按命名约定自动填核心脚本引用、补演员 Cue 和必要 Collider 的能力继续保留。

## MinLoopSceneSkeletonCreator.cs

- 路径：`Assets/Editor/MinLoopSceneSkeletonCreator.cs`
- 脚本作用：编辑器菜单工具，用于一键创建 17F-01 最小循环标准场景骨架。它会在当前场景生成 `MIN_LOOP_ROOT`、`FlowManagers`、`UI`、`ReplayRoom_17F01`、`Interactions`、`Guides`、`Anchors`、`Lighting`、`Audio` 等空物体，并自动挂上核心脚本、阶段 Cue 和部分引用。
- 挂载对象：不需要手动挂载。它是 Editor 工具，通过 Unity 顶部菜单使用。
- 必需组件：
  - 项目中需要存在最小循环运行时脚本：`MinLoopFlowController`、`TrustStateController`、`ViewSwitchController`、`MinLoopStageObjectActivator`、`MinLoopStageAnchorController`、`MinLoopStageCueController`、`MinLoopLightingStateController`、`MinLoopAudioStateController`、`MinLoopWorldGuideMarker`、`MinLoopDebugHotkeys`、`MinLoopTerminalPresenter`、`MinLoopSubtitlePlayer`、`MinLoopRobotHudPresenter`、`MinLoopObjectivePresenter`、`MinLoopTrustPresenter`、`ReplaySequenceController`、`ComfortActionInteractable`、`ResidentTerminalFlow`、`InteractionFeedbackController`、`MinLoopSceneValidator`。
  - 文件必须放在 `Assets/Editor` 下。
- Inspector 字段：无。
- 公开接口：无运行时公开接口。
- 菜单入口：
  - Unity 顶部菜单：`Tools/Min Loop/Create 17F-01 Scene Skeleton`
- 自动创建的层级：
  - `MIN_LOOP_ROOT`
  - `MIN_LOOP_ROOT/FlowManagers/MinLoopFlowController`
  - `MIN_LOOP_ROOT/FlowManagers/TrustStateController`
  - `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`
  - `MIN_LOOP_ROOT/FlowManagers/MinLoopStageObjectActivator`
  - `MIN_LOOP_ROOT/FlowManagers/MinLoopStageAnchorController`
  - `MIN_LOOP_ROOT/FlowManagers/MinLoopStageCueController`
  - `MIN_LOOP_ROOT/FlowManagers/MinLoopLightingStateController`
  - `MIN_LOOP_ROOT/FlowManagers/MinLoopAudioStateController`
  - `MIN_LOOP_ROOT/FlowManagers/MinLoopDebugHotkeys`
  - `MIN_LOOP_ROOT/UI/MinLoopTerminalPresenter`
  - `MIN_LOOP_ROOT/UI/MinLoopSubtitlePlayer`
  - `MIN_LOOP_ROOT/UI/MinLoopRobotHudPresenter`
  - `MIN_LOOP_ROOT/UI/MinLoopObjectivePresenter`
  - `MIN_LOOP_ROOT/UI/MinLoopTrustPresenter`
  - `MIN_LOOP_ROOT/ReplayRoom_17F01/ReplaySequenceController`
  - `MIN_LOOP_ROOT/ReplayRoom_17F01/ComfortAction_Bedside`
  - `MIN_LOOP_ROOT/Interactions/Terminal_17F01_Interactable`
  - `MIN_LOOP_ROOT/Interactions/Feedback_TerminalOpen`
  - `MIN_LOOP_ROOT/Interactions/Feedback_AccessCard`
  - `MIN_LOOP_ROOT/Interactions/Feedback_ReplayRequest`
  - `MIN_LOOP_ROOT/Interactions/Feedback_DispositionSubmit`
  - `MIN_LOOP_ROOT/Interactions/Feedback_ComfortReady`
  - `MIN_LOOP_ROOT/Interactions/Feedback_MorningReview`
  - `MIN_LOOP_ROOT/Interactions/Feedback_NextResidentGuide`
  - `MIN_LOOP_ROOT/Guides/Guide_Terminal_17F01`
  - `MIN_LOOP_ROOT/Guides/Guide_ComfortAction`
  - `MIN_LOOP_ROOT/Guides/Guide_NextResident_17F02`
  - `MIN_LOOP_ROOT/Anchors/Anchor_Mia_CorridorStart`
  - `MIN_LOOP_ROOT/Anchors/Anchor_Companion_ReplayStart`
  - `MIN_LOOP_ROOT/Anchors/Anchor_Mia_TerminalReturn`
  - `MIN_LOOP_ROOT/Anchors/Anchor_NextResident_17F02`
  - `MIN_LOOP_ROOT/Lighting/Light_Corridor_Warm`
  - `MIN_LOOP_ROOT/Lighting/Light_Replay_Night`
  - `MIN_LOOP_ROOT/Lighting/Light_Morning_Soft`
  - `MIN_LOOP_ROOT/Audio/Audio_Corridor_Ambience`
  - `MIN_LOOP_ROOT/Audio/Audio_Replay_Night_Ambience`
  - `MIN_LOOP_ROOT/Audio/Audio_Morning_Ambience`
  - `MIN_LOOP_ROOT/MinLoopSceneValidator`
- 自动挂载和引用：
  - `MinLoopFlowController` 会自动引用终端展示、视角切换、复盘、信任度和 4 个反馈对象。
  - `MinLoopStageObjectActivator` 会自动引用 `MinLoopFlowController`，并写入 3 条默认阶段规则。
  - `MinLoopStageAnchorController` 会自动引用 `MinLoopFlowController`，并写入 3 条默认锚点规则；目标角色根物体需要运行自动绑定或手动拖入。
  - `MinLoopStageCueController` 会自动引用 `MinLoopFlowController`，并写入安抚点就绪、早晨回顾、下一户指引 3 条默认 Cue 规则。
  - `MinLoopLightingStateController` 会自动引用 `MinLoopFlowController`，并写入走廊/终端暖光、昨夜复盘冷光、早晨回顾柔光 3 条默认规则。
  - `MinLoopAudioStateController` 会自动引用 `MinLoopFlowController`，并写入走廊/终端环境声、昨夜复盘环境声、早晨回顾环境声 3 条默认规则。
  - 3 个默认 Guide 对象会自动挂 `MinLoopWorldGuideMarker` 并写入默认文字。
  - `MinLoopRobotHudPresenter` 会自动引用 `MinLoopFlowController`。
  - `MinLoopObjectivePresenter` 会自动引用 `MinLoopFlowController`。
  - `MinLoopTrustPresenter` 会自动引用 `TrustStateController` 和 `MinLoopFlowController`。
  - `MinLoopDebugHotkeys` 会自动引用 `MinLoopFlowController` 和 `ReplaySequenceController`。
  - `ResidentTerminalFlow` 会自动引用 `MinLoopFlowController`。
  - `ReplaySequenceController` 会自动引用流程控制器、字幕播放器和安抚点。
  - `ComfortActionInteractable` 会自动引用 `ReplaySequenceController`。
  - `MinLoopSceneValidator` 会自动引用核心脚本、阶段激活器、阶段锚点、阶段 Cue、灯光、音频和反馈对象。
  - `Terminal_17F01_Interactable` 和 `ComfortAction_Bedside` 会自动添加 BoxCollider。
- 使用步骤：
  1. 打开要制作最小循环的 Unity 场景。
  2. 点击顶部菜单 `Tools/Min Loop/Create 17F-01 Scene Skeleton`。
  3. 场景中会出现或更新 `MIN_LOOP_ROOT`。
  4. 把你摆好的 Mia 控制器、Companion 控制器、终端模型、儿童房模型、孩子和父母模型逐步拖到对应脚本字段。
  5. 选中 `MIN_LOOP_ROOT/MinLoopSceneValidator`，点击 `Validate Scene Setup` 检查还缺什么。
- 测试方式：
  - 点击菜单后，Hierarchy 中出现标准层级。
  - Console 输出已创建/更新 17F-01 最小循环场景骨架。
  - 选中 `MinLoopSceneValidator` 后可以直接运行检查。
  - 重复点击菜单不会无限创建重复的同名子物体，而是复用已有同名对象并补齐脚本。
- 后续扩展：
  - 后续 Unity MCP 可用时，可以先运行这个工具创建骨架，再由 MCP 把你摆好的具体模型拖入字段。
  - 如果后续做 17F-02/17F-03，可复制这个工具逻辑，生成不同住户编号的骨架。
- 最近更新：使用方式有变化。骨架现在会创建 `MinLoopStageCueController` 和 `Feedback_ComfortReady`、`Feedback_MorningReview`、`Feedback_NextResidentGuide` 三个默认阶段反馈对象；此前 `Anchors` 分组、4 个默认 Anchor 空物体、`MinLoopStageAnchorController` 默认规则、`Audio` 分组、3 个默认占位 AudioSource、`MinLoopAudioStateController` 默认规则、`Lighting` 分组、3 盏默认占位灯、`MinLoopLightingStateController` 默认规则、3 个默认 Guide 对象挂 `MinLoopWorldGuideMarker`、`MinLoopRobotHudPresenter`、`MinLoopDebugHotkeys`、信任度显示、当前目标提示、`Guides` 层级和阶段显隐规则继续保留。

## MinLoopSetupChecklistWindow.cs

- 路径：`Assets/Editor/MinLoopSetupChecklistWindow.cs`
- 脚本作用：编辑器清单窗口，用于显示 17F-01 最小循环搭建步骤、必须摆放的对象、核心字段拖拽说明、自动绑定入口、阶段锚点、阶段 Cue、阶段灯光、阶段音频、门/反馈/UI 接入提示和最小验收流程。它还提供按钮直接创建骨架、自动绑定引用、运行场景检查、复制清单。
- 挂载对象：不需要手动挂载。它是 EditorWindow，通过 Unity 顶部菜单打开。
- 必需组件：
  - 项目中需要存在 `MinLoopSceneSkeletonCreator`、`MinLoopSceneAutoBinder` 和 `MinLoopSceneValidator`。
  - 文件必须放在 `Assets/Editor` 下。
- Inspector 字段：无。
- 公开接口：无运行时公开接口。
- 菜单入口：
  - Unity 顶部菜单：`Tools/Min Loop/Open Setup Checklist`
- 窗口按钮：
  - `Create / Update Skeleton`：调用 `MinLoopSceneSkeletonCreator.CreateSceneSkeleton()` 创建或更新标准层级。
  - `Auto Bind References`：调用 `MinLoopSceneAutoBinder.AutoBindSceneReferences()`，按约定对象名自动填写核心引用。
  - `Validate Scene`：查找 `MinLoopSceneValidator` 并运行 `ValidateSceneSetup()`；如果没有检查器，会询问是否先创建骨架。
  - `Refresh Checklist`：刷新窗口内文字清单。
  - `Copy Checklist`：把当前清单复制到系统剪贴板。
- 使用步骤：
  1. 点击 `Tools/Min Loop/Open Setup Checklist`。
  2. 点击 `Create / Update Skeleton` 创建 `MIN_LOOP_ROOT`。
  3. 按窗口内清单摆放 Mia、Companion、终端、儿童房、孩子、父母、门、锚点、阶段 Cue、灯光、音频和反馈对象。
  4. 按清单命名后点击 `Auto Bind References` 自动拖一批脚本引用。
  5. 点击 `Validate Scene` 检查还缺什么。
  6. 如果需要发给我或自己记录，点击 `Copy Checklist`。
- 测试方式：
  - 菜单能打开窗口。
  - 点击 `Create / Update Skeleton` 后场景出现或更新 `MIN_LOOP_ROOT`。
  - 点击 `Auto Bind References` 后 Console 输出 `[MinLoopSceneAutoBinder]` 自动绑定报告。
  - 点击 `Validate Scene` 后 Console 输出检查报告。
  - 点击 `Copy Checklist` 后剪贴板中有完整搭建清单。
- 后续扩展：
  - 后续可以把清单拆成可勾选项，或增加“一键选中对应对象”的按钮。
  - Unity MCP 可用后，这个窗口里的层级和字段名可以作为 MCP 自动挂载的目标说明。
- 最近更新：使用方式有变化。窗口清单新增正式 UI 推荐命名和自动绑定说明、`MinLoopWorldGuideMarker` 距离显示说明、`SmartDoorTriggerZone` 自动门触发区搭建说明，以及 `ReplaySequenceController` 自动生成兜底安抚点 `Generated_ComfortAction_Bedside` 的说明，并保留 `ComfortActionInteractable` 自动占位发光球说明；此前 `MinLoopStageCueController` 阶段 Cue 和 `Feedback_ComfortReady` / `Feedback_MorningReview` / `Feedback_NextResidentGuide` 默认反馈说明、`MinLoopStageAnchorController` 阶段锚点和默认 Anchor 说明、`MinLoopAudioStateController` 阶段音频和默认三个占位 AudioSource 说明、`MinLoopLightingStateController` 阶段灯光和默认三盏占位灯说明、`MinLoopWorldGuideMarker` 默认世界指引说明、`SimpleActorCueController` 占位 Pose 用法、`Wait Before Door Look After Wake`、`MinLoopRobotHudPresenter` 机器 HUD 说明、`Auto Bind References` 按钮、自动绑定步骤和演员 Cue 自动补组件说明继续保留。

## HearthHudDemoController.cs

- 删除状态：已于 2026-07-05 从项目中删除，同时删除 `Assets/Scripts/UI/HearthHudDemoController.cs.meta`。
- 原路径：`Assets/Scripts/UI/HearthHudDemoController.cs`
- 原脚本作用：早期 HUD 原型预览控制器，会在运行时生成一套临时 HUD、终端页、机器人复盘页、信任浮字和组件总览。
- 删除原因：正式玩家 HUD、正式 TV 终端和正式机器人 HUD 已经拆分为 Prefab + 专用控制器。旧 Demo 以运行时硬编码方式生成 UI，容易和正式 HUD 同时出现，造成黑框、重复信任度、错位占位 UI 等问题。
- 后续替代：
  - 玩家第一人称 HUD：`Assets/Prefabs/UI/HearthHud/HearthHudRoot.prefab` 与 `HearthFirstPersonHudController`。
  - TV 终端：`Assets/Prefabs/UI/HearthHud/Terminals/Terminal_17Fxx.prefab` 与 `HearthTvTerminalController`。
  - 陪伴单元 HUD：`Assets/Prefabs/UI/HearthCompanionHud/CompanionHudRoot.prefab` 与 `HearthCompanionHudController`。
- 最近更新：高置信度旧脚本清理；使用方式改为“不可再挂载”。

## HEARTH HUD 正式 UI 重做总览

- 生成结果：
  - 正式根 Prefab：`Assets/Prefabs/UI/HearthHud/HearthHudRoot.prefab`
  - 24 个页面 Prefab：`Assets/Prefabs/UI/HearthHud/Pages/Slide01_*.prefab` 到 `Slide24_*.prefab`
  - TV 终端页面 Prefab：`Assets/Prefabs/UI/HearthHud/TerminalPages/TerminalSlide01_*.prefab` 到 `TerminalSlide18_*.prefab`
  - TV 终端组 Prefab：`Assets/Prefabs/UI/HearthHud/Terminals/Terminal_17F01.prefab`、`Terminal_17F02.prefab`、`Terminal_17F03_Alert.prefab`
  - PPT / 文档参考：`UI参考资料/HEARTH-HUD.pptx`、`UI参考资料/已为您沟通_UI说明书_for_Unity_v1.md`
  - TV 终端 PPT 参考：`UI参考资料/HEARTH-Night-Rounds-Master.pptx`
  - 组件 PNG：`Assets/Resources/UI/HearthHudComponents`
- 制作方式：
  - 使用 Unity 顶部菜单 `Tools/Hearth HUD/Rebuild Formal HUD Prefabs` 一键从 PPT 1-24 页读取坐标、形状、颜色和英文文字。
  - 使用 Unity 顶部菜单 `Tools/Hearth HUD/Rebuild TV Terminal UI From Master PPT` 一键生成世界空间 TV 终端页面。
  - 选择 TV 物体后，可用 `Tools/Hearth HUD/TV Terminal/Apply 17F-01 To Selected TV`、`Apply 17F-02 To Selected TV` 或 `Apply 17F-03 Alert To Selected TV` 标准化终端。
  - PPT 坐标按 20 x 11.25 inch 转成 1920 x 1080 UI 坐标，Unity 里统一使用左上角锚点。
  - 25-40 页导出的 PNG 已批量设置为 Sprite、透明边缘、关闭 Mipmap；由于这些 PNG 很多带烘焙文字，正式 UI 里优先使用 PPT 形状 + TMP 文本重建，避免文字重复。
  - TV 终端黑色 PPT 背景不直接烘进页面，生成器会改用 `TerminalScreenGlass` 半透明暗屏层。
- 最小验收：
  - Unity Console 没有新增 HEARTH HUD 编译错误。
  - `HearthHudRoot.prefab` 内含 24 个 `HearthHudPage`、1 个 `HearthHudController`、1 个 `HearthHoldToActButton`。
  - 每个 `Terminal_17Fxx` 终端组内含 6 个 `HearthHudPage`、1 个 `HearthTvTerminalController`，默认只有 1 页显示。
  - Play 后可用 `[` / `]` 翻页，数字键快速跳关键页面，Tab 在终端页切换预览 Tab，Slide 6 可长按 E 完成回放。

## HearthHudTypes.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthHudTypes.cs`
- 脚本作用：定义 HEARTH HUD 的公共枚举和页面元数据，包括 1-24 页页面 ID、HUD 状态、门口终端 Tab、按钮动作类型和页面显示信息。
- 挂载对象：不需要挂载，是供其他脚本引用的数据类型文件。
- 必需组件：无。
- Inspector 字段：无独立 Inspector；`HearthHudPageMetadata` 会被构建器内部使用。
- 公开接口：`HearthHudPageId`、`HearthHudState`、`HearthDoorwayTab`、`HearthHudButtonActionType`、`HearthHudPageMetadata`。`HearthHudButtonActionType` 现在包含 `CloseTerminal`，供 TV 终端关闭按钮使用。
- 引用关系：被 `HearthHudController`、`HearthTvTerminalController`、`HearthHudPage`、`HearthHudButtonAction`、`HearthDoorwayTerminalPanel`、`HearthHudPrefabBuilder` 和 `HearthTvTerminalPrefabBuilder` 使用。
- Unity 使用步骤：无需手动操作，保持脚本存在即可。
- 测试方法和验收标准：Unity 编译通过；其他 HEARTH HUD 脚本的枚举字段能在 Inspector 正常显示。
- 后续扩展：新增 HUD 页面、状态或终端 Tab 时从这里扩展枚举。
- 最近更新：使用方式有变化。新增 `CloseTerminal` 按钮动作；原有枚举值保持不变。

## HearthHudPage.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthHudPage.cs`
- 脚本作用：每一个 PPT 页面 Prefab 的页面标识和显隐控制器，记录该页是否显示常驻 HUD、当前 HUD 状态、时间、任务和字幕。
- 挂载对象：自动挂在每个页面 Prefab 根对象，例如 `Slide03_DoorwaySummary`。
- 必需组件：同对象需要 `RectTransform`；脚本会自动确保存在 `CanvasGroup`。
- Inspector 重要字段：`Page Id`、`Show Persistent Hud`、`Hud State`、`Clock Text`、`Show Task`、`Task Text`、`Subtitle Text`、`Canvas Group`、`Doorway Terminal Panel`。
- 公开接口：`Configure(...)`、`SetDoorwayTerminalPanel(...)`、`Show()`、`Hide()`、`SetVisible(bool)`，以及页面信息只读属性。
- 引用关系：由 `HearthHudController` 或 `HearthTvTerminalController` 搜集和切换；由 `HearthHudPrefabBuilder`、`HearthTvTerminalPrefabBuilder` 自动创建和配置。
- Unity 使用步骤：优先用菜单生成；若手动做新页面，根对象添加 `RectTransform`、`CanvasGroup` 和 `HearthHudPage`，再设置对应页面 ID 和 HUD 元信息。
- 测试方法和验收标准：`HearthHudController.ShowPage(pageId)` 或 `HearthTvTerminalController.ShowPage(pageId)` 能显示该页并隐藏上一页；常驻 HUD 状态或 TV 页面状态随控制器变化。
- 后续扩展：页面级转场、页面进入事件、数据源绑定可从这里增加。
- 最近更新：使用方式有变化。现在也被世界空间 TV 终端复用，页面本身不用改挂载方式。

## HearthHudPersistentView.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthHudPersistentView.cs`
- 脚本作用：控制常驻 HUD 层，包括左上工号/状态、右上时间/任务、底部字幕和信任度浮字。
- 挂载对象：`HearthHudRoot.prefab/PersistentHudLayer/PersistentHudView`。
- 必需组件：同对象需要 `RectTransform`；子对象需要 `Image`、`TMP_Text`、`CanvasGroup`。
- Inspector 重要字段：`Root`、`Worker Badge Image`、`Status Dot Image`、`Status Text`、`Worker Name Text`、`Clock Text`、`Task Root`、`Task Text`、`Subtitle Root`、`Subtitle Text`、`Subtitle Canvas Group`、`Trust Root`、`Trust Delta Text`、`Trust Canvas Group`、状态色和脉冲参数。
- 公开接口：`ConfigureBindings(...)`、`SetVisible(bool)`、`SetHudState(HearthHudState)`、`SetClock(string)`、`SetTask(bool,string)`、`SetSubtitle(string)`、`ShowTrustDelta(int|string)`。
- 引用关系：由 `HearthHudController` 驱动；按钮或剧情系统可以通过控制器间接调用。
- Unity 使用步骤：把 `HearthHudRoot.prefab` 放入场景或由后续 UI 管理器实例化；需要手动替换字体或颜色时，在该组件 Inspector 里改 TMP / Image 引用或色值。
- 测试方法和验收标准：切换 Slide 1、7、13、23 时，状态分别显示 Active、Dormant、Alert、Warning；调用 `ShowTrustDelta(+1)` 时出现并自动淡出。
- 后续扩展：如果要换中文字体，可先创建 TMP Dynamic Font Asset，再拖到这些 TMP_Text 上或设置 TMP 默认字体。
- 最近更新：新增脚本。修复隐藏常驻 HUD 时设置字幕不再启动 inactive 对象上的协程，使用方式无变化。

## HearthHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthHudController.cs`
- 脚本作用：HEARTH HUD 总控制器，统一管理页面切换、常驻 HUD 状态、字幕、信任度浮字、机器人回放返回页和调试翻页。
- 挂载对象：`HearthHudRoot.prefab` 根对象。
- 必需组件：同对象需要 `Canvas`、`CanvasScaler`、`GraphicRaycaster`；场景中需要 `EventSystem`，脚本可自动创建。
- Inspector 重要字段：`Canvas`、`Canvas Scaler`、五个 Layer、`Persistent View`、`Pages`、`Starting Page`、`Show Starting Page On Start`、`Create Event System If Missing`。
- 公开接口：`ShowPage(HearthHudPageId/int)`、`ShowNextPage()`、`ShowPreviousPage()`、`SetHudState(...)`、`SetSubtitle(...)`、`ShowTrustDelta(...)`、`SelectDoorwayTab(...)`、`ShowRobotReplay(returnPage)`、`CompleteRobotReplay()`、`RefreshPageListFromChildren()`。
- 引用关系：驱动 `HearthHudPage`、`HearthHudPersistentView`、`HearthHoldToActButton`、`HearthHudButtonAction`。
- Unity 使用步骤：把 `Assets/Prefabs/UI/HearthHud/HearthHudRoot.prefab` 放进场景；正式剧情脚本可直接调用 `ShowPage`、`ShowRobotReplay`、`SetSubtitle`、`ShowTrustDelta`。
- 测试方法和验收标准：Prefab 抽查应包含 24 页；`ShowPage(13)` 显示 Alert 终端；`ShowRobotReplay(Slide18AlertDisposition)` 后长按 E 能回 Slide 18。
- 后续扩展：接入 `MinLoopFlowController` 时，不再让预览键盘驱动，而是在剧情节点调用这些公开方法。
- 最近更新：新增脚本。

## HearthDoorwayTerminalPanel.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthDoorwayTerminalPanel.cs`
- 脚本作用：预留给门口终端的合并式 Tab 面板，支持住户概要、Acquisition、Family Log、Trust Trend、Inspection History 内容层切换。
- 挂载对象：未来可挂在某个终端页面的内容根对象，例如 `DoorwayTerminalPanel`。
- 必需组件：每个 Tab 按钮需要 `Button`；每个内容层是普通 `GameObject`；可选 `TMP_Text` 和 `Image` 用于高亮。
- Inspector 重要字段：`Tabs`、`Default Tab`、`Bind Buttons On Awake`、Tab 激活/非激活文字色和高亮色。
- 公开接口：`ConfigureTabs(...)`、`BindButtons()`、`SelectTab(...)`、`SelectResidentSummary()`、`SelectAcquisition()`、`SelectFamilyLog()`、`SelectTrustTrend()`、`SelectInspectionHistory()`。
- 引用关系：可由 `HearthHudController.SelectDoorwayTab` 间接驱动；当前首版页面级 Tab 切换主要由 `HearthHudButtonAction.ShowPage` 实现。
- Unity 使用步骤：如果后续要把 Slides 13-17 合并成同一个页面，在终端根对象添加该脚本，把五个 Tab 按钮和五个内容层拖入 `Tabs`。
- 测试方法和验收标准：每个 Tab 点击后，对应内容层显示，其余内容层隐藏，文字/高亮颜色变化。
- 后续扩展：接真实住户数据时，可以把每个内容层里的 TMP_Text 暴露成数据绑定字段。
- 最近更新：新增脚本。

## HearthHoldToActButton.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthHoldToActButton.cs`
- 脚本作用：处理“长按 E 完成动作”的 UI 交互，支持键盘和鼠标按住，显示进度条，完成后通知 HUD 控制器。
- 挂载对象：`Slide06_RobotReplay` 页面里的 `HoldToActButton`。
- 必需组件：同对象需要 `RectTransform`、`Image`、`Button`；子对象建议有一个 `Image` 作为进度填充，一个 `TMP_Text` 作为进度文字。
- Inspector 重要字段：`Hold Key`、`Hold Seconds`、`Allow Keyboard Hold`、`Allow Pointer Hold`、`Progress Fill Image`、`Progress Label Text`、`Controller`、`Notify Controller On Completed`、`On Hold Completed`。
- 公开接口：`Configure(...)`、`ResetHold()`、`Progress`。
- 引用关系：完成后调用 `HearthHudController.CompleteRobotReplay()`；也可通过 `On Hold Completed` 接其他剧情事件。
- Unity 使用步骤：正常由生成器自动创建；若手动复用，给按钮对象添加该脚本，并拖入进度条 Image 和 TMP 文本。
- 测试方法和验收标准：进入 Slide 6 后长按 E，进度填满并显示 COMPLETE，随后回到调用 `ShowRobotReplay` 时记录的返回页。
- 后续扩展：可改成长按鼠标、长按手柄键，或接入正式输入系统。
- 最近更新：新增脚本。

## HearthHudButtonAction.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthHudButtonAction.cs`
- 脚本作用：给生成出来的透明点击区域提供 Inspector 可配置动作，包括跳页、回放、Tab、HUD 状态、字幕、信任度反馈和 TV 终端关闭。
- 挂载对象：所有自动生成的 `Button_*` 透明热区对象。
- 必需组件：同对象需要 `Button`，脚本带 `RequireComponent(typeof(Button))`。
- Inspector 重要字段：`Controller`、`Tv Terminal Controller`、`Action Type`、`Target Page`、`Robot Replay Return Page`、`Target Tab`、`Target Hud State`、`Subtitle Text`、`Show Trust Delta Before Action`、`Trust Delta`、`On Click Extra`。
- 公开接口：`Configure(...)`、`InvokeAction()`。
- 引用关系：优先查找父级 `HearthTvTerminalController`；如果不在 TV 终端下，再调用 `HearthHudController` 的公开接口。
- Unity 使用步骤：选中任意 `Button_*` 透明热区，在 Inspector 调整 `Action Type` 和目标页；TV 终端里的按钮不需要手动拖全局 `HearthHudRoot`，会自动找本终端控制器；需要接剧情事件时，把额外逻辑放到 `On Click Extra`。
- 测试方法和验收标准：屏幕 HUD 中 Slide 3/4 的 Tab 仍能切换页面；TV 终端中 Tab 只切换本 TV 的 6 页；`CloseTerminal` 能关闭当前 TV 终端焦点。
- 后续扩展：新增按钮动作时，先扩展 `HearthHudButtonActionType`，再在 `InvokeAction` 中加分支。
- 最近更新：使用方式有变化。新增 TV 终端本地路由和 `CloseTerminal` 动作，旧 HUD 按钮配置继续可用。

## HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`
- 脚本作用：世界空间 TV 终端的本地页面控制器。它管理一台 TV 内的 6 个 `HearthHudPage`，保证同一时间只显示一页，支持终端固定相机、键盘导航、机器人回放入口、打开/关闭终端焦点和整体放大。
- 挂载对象：自动挂在 `Assets/Prefabs/UI/HearthHud/Terminals/Terminal_17F01.prefab`、`Terminal_17F02.prefab`、`Terminal_17F03_Alert.prefab` 根对象。
- 必需组件：同对象需要 `RectTransform`、`CanvasGroup`；父级通常是 `MonitorCanvas`，需要 `Canvas`、`CanvasScaler`、`GraphicRaycaster`。
- Inspector 重要字段：`Canvas`、`Canvas Scaler`、`Content Root`、`Canvas Group`、`World Camera`、`Pages`、`First Slide Number`、`Starting Page`、`Show Starting Page On Start`、`Switch Camera While Open`、`Player Camera`、`Terminal Camera`、`Gameplay Behaviours To Disable`、`Player Interaction`、`Player Rigidbody`、`Zoom`、`Keyboard Navigation Enabled`、`Close Key`、`Cycle Page Key`、`Previous Selection Key`、`Next Selection Key`、`Submit Key`、`Keyboard Cycle Page Count`、`Keyboard Hint Text`、`Keyboard Focus Text`、`Min Loop Flow Controller`、`View Switch Controller`、`On Robot Replay Requested`、`On Post Replay Choice Shown`。
- 公开接口：`OpenTerminal()`、`CloseTerminal()`、`ShowPage(HearthHudPageId/int)`、`ShowNextPage()`、`ShowPreviousPage()`、`SelectDoorwayTab(...)`、`SetZoom(float)`、`SetWorldCamera(Camera)`、`SetTerminalCamera(Camera)`、`SetPlayerCamera(Camera)`、`SetSwitchCameraWhileOpen(bool)`、`SetMinLoopFlowController(...)`、`SetViewSwitchController(...)`、`RequestRobotReplay()`、`ShowPostReplayChoicePage()`、`RefreshPageListFromChildren()`。
- 引用关系：由 `HearthHudButtonAction` 本地调用；由 `HearthTvTerminalInteractable` 在玩家按 E 时打开；可调用 `MinLoopFlowController.RequestReplayFromTerminal()` 或 `ViewSwitchController.SwitchToCompanion()` 进入机器人回放；页面对象使用 `HearthHudPage`。
- Unity 使用步骤：优先通过 `Tools/Hearth HUD/Rebuild TV Terminal UI From Master PPT` 生成终端组 Prefab，再把对应 `Terminal_17Fxx` 放到 TV 的 `MonitorCanvas` 下；或选中 TV 后运行标准化菜单自动完成。
- 电脑键盘版操作：玩家看向 TV 按 `E` 进入；进入后切到该 TV 的 `Terminal Camera`；`Esc` 退出；`Tab` 只在前 5 张普通信息页循环；`Left/Right` 在 5 张普通信息页和 `RECALL EVENT` 焦点之间移动；焦点在 `RECALL EVENT` 时按 `Space` 请求机器人回放；最后第 6 张选择页不参与 Tab 循环，回放结束后由剧情调用 `ShowPostReplayChoicePage()` 显示。
- 测试方法和验收标准：Play 或编辑器核对时，`Pages` 数量为 6，默认只有 1 页 active；按 `Tab` 从第 1-5 页循环，不会进入第 6 页；按右键移动到 `RECALL EVENT | SPACE` 后按空格，会触发 `RequestRobotReplay()`；调 `Zoom` 到 1.2 后页面和按钮区域一起变大且仍对齐。
- 后续扩展：VR 版只需要把手柄/射线确认接到 `RequestRobotReplay()`，把回放完成事件接到 `ShowPostReplayChoicePage()`；如果某台 TV 的相机没有自动找到，手动把该 TV 子物体下的固定视角 Camera 拖到 `Terminal Camera`。
- 最近更新：使用方式有变化。新增电脑键盘版终端操作系统、固定相机切换、回放请求入口和回放后选择页入口。

## HearthTvTerminalInteractable.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalInteractable.cs`
- 脚本作用：让 TV 物体成为 `PlayerInteraction` 可射线命中的交互对象。玩家看向 TV 按 E 时，打开或关闭该 TV 的 `HearthTvTerminalController`。
- 挂载对象：TV 模型或带 Collider 的 TV 父物体，例如当前场景 `17F/ROOM2/TV (2)`。
- 必需组件：同对象或子对象需要 `Collider`，子对象需要有 `HearthTvTerminalController`。
- Inspector 重要字段：`Terminal Controller`、`Find Controller In Children`、`Toggle When Already Open`、`Open Description`、`Close Description`。
- 公开接口：`Interact()`、`GetDescription()`、`SetTerminalController(...)`。
- 引用关系：实现 `IInteractable`，由 `PlayerInteraction` 调用；内部打开/关闭 `HearthTvTerminalController`。
- Unity 使用步骤：通过 TV 标准化菜单自动添加；若手动添加，把当前 TV 下面的 `Terminal_17Fxx` 根对象拖到 `Terminal Controller`。
- 测试方法和验收标准：玩家视线对准 TV 时出现 `E 查看终端`；按 E 后进入该 TV 的固定 Camera 视角；如果 `Toggle When Already Open` 开启，再按 E 或按 Esc 可退出。
- 后续扩展：如果某台 TV 需要特殊剧情入口，可在 `Interact()` 外层接剧情判断，或把 `OpenTerminal()` 放到剧情脚本中手动调用。
- 最近更新：使用方式无变化。终端打开后的键盘操作由 `HearthTvTerminalController` 处理。

## HearthHudPreviewInput.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthHudPreviewInput.cs`
- 脚本作用：仅用于 Play 模式快速预览 UI，不参与正式剧情逻辑。
- 挂载对象：`HearthHudRoot.prefab` 根对象。
- 必需组件：同对象或父级需要 `HearthHudController`。
- Inspector 重要字段：`Controller`、`Enable Preview Input`、`Previous Page Key`、`Next Page Key`、`Home Key`、`Tab Key`。
- 公开接口：`SetPreviewInputEnabled(bool)`。
- 引用关系：调用 `HearthHudController.ShowPage`、`ShowNextPage`、`ShowPreviousPage`、`ShowRobotReplay`、`SelectDoorwayTab`。
- Unity 使用步骤：把 `HearthHudRoot.prefab` 放入测试场景；Play 后用 `[` / `]` 翻页，数字键 1-9 快速跳关键页面；正式接剧情时，可以关闭 `Enable Preview Input`。
- 测试方法和验收标准：键盘翻页和数字跳页正常；关闭 `Enable Preview Input` 后按键不再改变 HUD。
- 后续扩展：可改成只在 Debug Build 或开发者模式下启用。
- 最近更新：新增脚本。

## HearthHudPrefabBuilder.cs

- 路径：`Assets/Editor/HearthHudPrefabBuilder.cs`
- 脚本作用：Unity Editor 菜单生成器，从 `HEARTH-HUD.pptx` 自动读取 Slides 1-24 的 PPT 坐标、形状、颜色和英文文字，生成正式 HUD 根 Prefab 和 24 个页面 Prefab，并批量设置组件 PNG 为 Sprite。
- 挂载对象：不需要挂载；这是 Editor 工具脚本。
- 必需组件：文件必须位于 `Assets/Editor` 下；项目中需要 TextMesh Pro；项目根目录下需要能找到 `UI参考资料/HEARTH-HUD.pptx`；组件 PNG 位于 `Assets/Resources/UI/HearthHudComponents`。
- Inspector 字段：无。
- 菜单入口：`Tools/Hearth HUD/Rebuild Formal HUD Prefabs`
- 公开接口：`RebuildFormalHudPrefabs()`，菜单调用的一键生成入口，也可由 Unity MCP 或 Editor 代码直接调用。
- 自动生成内容：Screen Space Overlay Canvas、1920 x 1080 CanvasScaler、五层 UI 结构、24 个 `HearthHudPage`、透明按钮热区、`HearthHudButtonAction` 和 Slide 6 的 `HearthHoldToActButton`。
- 引用关系：生成并配置 `HearthHudController`、`HearthHudPersistentView`、`HearthHudPage`、`HearthHudButtonAction`、`HearthHoldToActButton`。
- Unity 使用步骤：确认 PPT 在 `UI参考资料/HEARTH-HUD.pptx` 后，点击菜单，等待 Console 出现 `[HearthHudPrefabBuilder] Rebuilt formal Hearth HUD prefabs...`，再把 `HearthHudRoot.prefab` 拖入场景测试。
- 测试方法和验收标准：菜单执行后生成 1 个根 Prefab 和 24 个页面 Prefab；PNG `.meta` 中 `textureType: 8`、`spriteMode: 1`、`alphaIsTransparency: 1`、`enableMipMap: 0`；Unity Console 没有新增 HEARTH HUD 相关错误。
- 后续扩展：如果 PPT 位置或页面数量变化，优先改构建器；如果需要更精细的按钮热区，在 `BuildPageInteractions` 中补充；若要把 13-17 页合并成同一个终端 Prefab，可在生成器里绑定 `HearthDoorwayTerminalPanel`。
- 最近更新：新增脚本。

## HearthTvTerminalPrefabBuilder.cs

- 路径：`Assets/Editor/HearthTvTerminalPrefabBuilder.cs`
- 脚本作用：Unity Editor 工具，从 `HEARTH-Night-Rounds-Master.pptx` 读取 18 页 TV 终端 UI，生成世界空间终端页面 Prefab、三套终端组 Prefab，并提供选中 TV 后一键标准化 `MonitorCanvas` 的菜单。
- 挂载对象：不需要挂载；这是 Editor 工具脚本。
- 必需组件：文件必须位于 `Assets/Editor` 下；项目中需要 TextMesh Pro；项目中需要 `UI参考资料/HEARTH-Night-Rounds-Master.pptx`。
- Inspector 字段：无。
- 菜单入口：
  - `Tools/Hearth HUD/Rebuild TV Terminal UI From Master PPT`
  - `Tools/Hearth HUD/TV Terminal/Apply 17F-01 To Selected TV`
  - `Tools/Hearth HUD/TV Terminal/Apply 17F-02 To Selected TV`
  - `Tools/Hearth HUD/TV Terminal/Apply 17F-03 Alert To Selected TV`
- 公开接口：`RebuildTvTerminalPrefabs()`、`StandardizeTvByHierarchyPath(...)`、`StandardizeTvTerminal(...)`。
- 自动生成内容：18 个 `TerminalSlide*.prefab`、3 个 `Terminal_17Fxx.prefab`、半透明 `TerminalScreenGlass`、`TerminalContentRoot`、键盘提示条 `KeyboardNavigationRoot`、透明按钮热区、`HearthTvTerminalController` 和 TV 交互入口所需结构。
- 引用关系：生成并配置 `HearthHudPage`、`HearthHudButtonAction`、`HearthTvTerminalController`；标准化 TV 时会添加 `HearthTvTerminalInteractable`，自动尝试绑定 TV 子物体或同房间层级下的终端 Camera。旧 `TerminalInteractable` 已删除；如果某台 TV 同时存在 `ResidentTerminalFlow`，需要手动确认入口职责，避免按 E 时两个流程同时响应。
- Unity 使用步骤：确认 PPT 位于 `UI参考资料/HEARTH-Night-Rounds-Master.pptx`；点击重建菜单；然后选中场景里的 TV 物体，点击对应 `Apply 17F-xx` 菜单。当前场景已检测到 3 个 TV 终端：`17F/ROOM1/TV (3)`、`17F/ROOM2/TV (4)`、`17F/ROOM3/TV (2)`，分别绑定了各自子物体下的 `Camera`。
- 测试方法和验收标准：菜单执行后生成 18 个页面 Prefab 和 3 个终端组 Prefab；标准化 TV 后，`MonitorCanvas` 为 World Space、尺寸 1920 x 1080、只有一个 `Terminal_17Fxx` 子物体；终端组内 6 页默认只显示 1 页；终端 Camera 默认 disabled，按 E 进入终端时才启用。
- 后续扩展：如果老师继续改 PPT，只需要替换 `UI参考资料/HEARTH-Night-Rounds-Master.pptx` 后重跑菜单；如果某个 TV 需要不同默认放大倍数，优先调该终端组的 `HearthTvTerminalController.Zoom`，不要单独拉伸子页面。
- 最近更新：使用方式有变化。生成的终端组新增键盘提示条，`RECALL EVENT` 热区会触发机器人回放请求，标准化工具会尝试自动绑定每台 TV 的固定相机。

## HearthHudDemoController.cs（旧预览）

- 删除状态：已于 2026-07-05 删除。
- 原路径：`Assets/Scripts/UI/HearthHudDemoController.cs`
- 说明：旧预览脚本不再保留，也不再通过 `HEARTH_HUD_DEMO_LEGACY_ENABLE` 启用。后续如果需要对照旧 UI，只看 Git 历史或 PPT/截图资料，不恢复运行时代码。
- 替代入口：正式 UI 请优先使用 `HearthHudRoot.prefab`、`HearthTvTerminalController`、`HearthCompanionHudController`。
- 最近更新：高置信度旧脚本清理；旧 Demo 完全退役。
## 2026-06-27 TV 终端沉浸式进入升级记录

本次修改围绕 TV 终端进入体验完成：玩家看向 TV 按 `E` 后，终端从暗屏进入开机闪烁状态，随后显示页面；终端页内保留 `Tab` 循环前 5 页、`Left/Right` 移动选择、高亮框显示当前选择、`Space` 请求机器人回放、`Esc` 退出。TV 终端结构仍然是 `TV -> MonitorCanvas -> Terminal_17Fxx`，不替换 `HearthHudRoot`。

### HearthTerminalCameraTransition.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTerminalCameraTransition.cs`
- 功能：处理玩家相机和 TV 固定终端相机之间的过渡表现。默认进入/退出时长为 `0.5` 秒，使用 `AnimationCurve` 控制缓动；如果过渡被中断，提供 `CancelTransition()` 关闭临时过渡相机。
- 挂载对象：自动挂在 `Terminal_17F01`、`Terminal_17F02`、`Terminal_17F03_Alert` 根对象上。
- 需要组件：同对象需要 `HearthTvTerminalController`；运行时会按需创建 `Terminal Transition Camera` 子物体。
- Inspector 重要字段：`Smooth Transition Enabled`、`Enter Duration`、`Exit Duration`、`Smooth Exit`、`Transition Curve`、`Use Unscaled Time`、`Transition Camera`。
- 公开接口：`TransitionToTerminal(...)`、`TransitionToPlayer(...)`、`CanRunEnterTransition(...)`、`CanRunExitTransition(...)`、`CancelTransition()`。
- 引用关系：由 `HearthTvTerminalController` 调用；不直接响应玩家输入。
- 最小测试：Play Mode 中打开任意 TV 终端，终端相机应在约 0.5 秒内接管视角；退出时恢复玩家相机。
- 后续扩展：如果之后安装 Cinemachine，可以保留本脚本作为无 Cinemachine fallback，或替换为 Cinemachine Brain/Virtual Camera 方案。

### HearthTerminalBootSequence.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTerminalBootSequence.cs`
- 功能：控制 TV 终端关闭暗屏、开机闪烁、扫描线、内容淡入和关闭淡出。默认开机动画约 `0.6` 秒，关闭淡出约 `0.18` 秒。
- 挂载对象：自动挂在每个 `Terminal_17Fxx` 根对象上。
- 需要组件：`TerminalContentRoot` 需要 `CanvasGroup`；终端根下需要 `TerminalOffOverlay` 和 `TerminalBootOverlay`。
- Inspector 重要字段：`Content Group`、`Off Overlay Group`、`Boot Overlay Group`、`Boot Duration`、`Close Fade Duration`、`Off Color`、`Boot Flash Color`、`Max Flicker Alpha`、`Scanline Max Alpha`、`Boot Scale Range`。
- 公开接口：`PlayOpenSequence()`、`PlayCloseSequence()`、`ApplyClosedInstant()`、`ApplyOpenInstant()`、`BootDuration`、`CloseFadeDuration`。
- 引用关系：由 `HearthTvTerminalController` 启动和强制收尾；由 `HearthTvTerminalPrefabBuilder` 自动创建并绑定 overlay。
- 最小测试：未打开终端时只看到暗屏；打开终端后暗屏消失，内容淡入；关闭后暗屏恢复。
- 后续扩展：如果要真实模糊，可从这里接入 URP Full Screen Pass 或自定义 Renderer Feature。

### HearthTerminalSelectionHighlighter.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTerminalSelectionHighlighter.cs`
- 功能：显示键盘选择焦点的低亮度矩形高亮框，支持平滑跟随目标、padding 和导航栏范围限制。
- 挂载对象：自动挂在 `TerminalContentRoot/TerminalSelectionRoot`。
- 需要组件：`KeyboardSelectionHighlight` 需要 `Image`；`SelectionTargets` 下需要 6 个 `RectTransform` 目标；`KeyboardSelectionBounds` 用于限制不越界。
- Inspector 重要字段：`Highlight Rect`、`Highlight Fill`、`Navigation Bounds`、`Targets`、`Padding`、`Fill Color`、`Border Color`、`Animate Movement`、`Follow Speed`、`Clamp To Navigation Bounds`。
- 公开接口：`Configure(...)`、`SetTargets(...)`、`SetFocus(int, bool)`、`SetVisible(bool)`、`TargetCount`。
- 引用关系：由 `HearthTvTerminalController` 在 `Tab`、`Left/Right`、页面切换时刷新；目标坐标由 `HearthTvTerminalPrefabBuilder` 从 PPT 文本位置生成。
- 最小测试：进入终端后高亮框应框住当前导航文字；按左右键时移动到相邻目标；第 6 个目标为 `RECALL EVENT`/机器人回放入口；高亮不应超出导航栏范围。
- 后续扩展：如果以后改成 VR 射线/手柄选择，仍可调用 `SetFocus` 更新当前视觉焦点。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`
- 本次更新：使用方式有变化。`OpenTerminal()` 现在会锁玩家、启动终端开机动画、启动相机过渡、开启键盘输入；`CloseTerminal()` 会关闭输入、恢复暗屏并回到玩家相机。新增音效预留字段：打开、关闭、开机、切页、左右移动、确认、回放请求、视角切换。
- 挂载对象：仍挂在每个 `Terminal_17Fxx` 根对象上。
- 新增依赖组件：同对象自动带 `AudioSource`、`HearthTerminalCameraTransition`、`HearthTerminalBootSequence`；子对象带 `HearthTerminalSelectionHighlighter`。
- Inspector 新增字段：`Camera Transition`、`Boot Sequence`、`Selection Highlighter`、`Audio Source`、`Open Clip`、`Close Clip`、`Boot Clip`、`Page Switch Clip`、`Focus Move Clip`、`Submit Clip`、`Replay Request Clip`、`View Switch Clip`、`Audio Volume`。
- 公开接口：原有 `OpenTerminal()`、`CloseTerminal()`、`ShowPage(...)`、`SelectDoorwayTab(...)`、`RequestRobotReplay()`、`ShowPostReplayChoicePage()` 继续可用；外部调用方式保持兼容。
- 引用关系：`HearthTvTerminalInteractable` 仍负责玩家按 `E` 调用；`HearthHudButtonAction` 仍优先路由到本地 TV 终端；机器人回放仍优先调用 `MinLoopFlowController`，当前场景没有该组件时走 `ViewSwitchController` 兜底。
- 最小测试：看向 TV 按 `E` 打开；开机后 `Tab` 在前 5 页循环；`Left/Right` 移动高亮；高亮到第 6 个目标按 `Space` 请求回放；`Esc` 退出并恢复暗屏。
- 后续扩展：音效素材准备好后直接拖到对应 Clip；如果某台 TV 需要特殊流程，可监听 `onOpened`、`onClosed`、`onRobotReplayRequested`。

### HearthTvTerminalPrefabBuilder.cs

- 路径：`Assets/Editor/HearthTvTerminalPrefabBuilder.cs`
- 本次更新：使用方式有变化。重新从 `HEARTH-Night-Rounds-Master.pptx` 生成 TV 终端时，会自动生成暗屏层、开机层、扫描线、高亮层、6 个键盘选择目标、`AudioSource`、`HearthTerminalCameraTransition`、`HearthTerminalBootSequence` 和 `HearthTerminalSelectionHighlighter`。
- 挂载对象：不需要挂载，这是 Editor 工具脚本。
- 菜单入口不变：`Tools/Hearth HUD/Rebuild TV Terminal UI From Master PPT`，以及 3 个 `Apply 17F-xx To Selected TV` 菜单。
- 公开接口不变：`RebuildTvTerminalPrefabs()`、`StandardizeTvByHierarchyPath(...)`、`StandardizeTvTerminal(...)`。
- 最小测试：重建后 3 个终端组 Prefab 都应包含 `TerminalOffOverlay`、`TerminalBootOverlay`、`TerminalSelectionRoot`；标准化三台 TV 后场景里每台 TV 都只保留一个对应终端组。
- 本次已应用到当前场景：`17F/ROOM1/TV (3)` -> `Terminal_17F01`，`17F/ROOM3/TV (2)` -> `Terminal_17F02`，`17F/ROOM2/TV (4)` -> `Terminal_17F03_Alert`。

## 2026-06-27 TV 终端相机过渡起点修正记录

本次修改用于修复 TV 终端进入动画从错误位置开始的问题。检查发现当前场景里三台 TV 终端的 `HearthTvTerminalController.Player Camera` 都绑定到了旧的 `Main Camera`，该相机位于场景原点附近；玩家实际使用的是 `Player/Person Controller/First Person Camera`，所以过渡视觉上会像从很远处飘过来。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`
- 本次修改：使用方式有变化。`OpenTerminal()` 和进入相机过渡前会重新解析玩家当前真实相机，优先使用 `PlayerInteraction.mainCamera`，其次查找场景中更像玩家第一人称的相机，最后才兜底使用 `Camera.main`。
- 挂载对象：仍然挂在每个 `Terminal_17Fxx` 根对象上，当前场景路径包括 `17F/ROOM1/TV (3)/MonitorCanvas/Terminal_17F01`、`17F/ROOM2/TV (4)/MonitorCanvas/Terminal_17F02`、`17F/ROOM3/TV (2)/MonitorCanvas/Terminal_17F03_Alert`。
- 重要 Inspector 字段：`Player Interaction` 建议指向玩家身上的 `PlayerInteraction`，当前场景为 `Player/Person Controller`；`Player Camera` 建议指向玩家正在看的第一人称相机，当前场景为 `Player/Person Controller/First Person Camera`；`Terminal Camera` 仍然指向每台 TV 子物体下自己的固定终端相机。
- 新增公开接口：`SetPlayerInteraction(PlayerInteraction interaction)`，供标准化工具或其他流程在运行前主动写入玩家交互脚本；写入后会同步读取该交互脚本的 `mainCamera`。
- 引用关系：仍由 `HearthTvTerminalInteractable` 调用 `OpenTerminal()`；相机过渡仍由 `HearthTerminalCameraTransition` 执行；玩家输入冻结仍通过 `PlayerInteraction.SetInteractionEnabled(false)`。
- 最小测试方法：Play 后看向任意 TV 按 `E`，过渡起点应从当前玩家第一人称视角开始，而不是从场景原点的旧 `Main Camera` 开始；Inspector 中三台终端的 `Player Camera` 应显示为玩家的 `First Person Camera`。
- 后续扩展：如果后面接 VR 或切换到机器人视角，需要确保当前控制视角的 `PlayerInteraction.mainCamera` 总是指向正在使用的相机，终端会自动跟随该引用。

### HearthTvTerminalPrefabBuilder.cs

- 路径：`Assets/Editor/HearthTvTerminalPrefabBuilder.cs`
- 本次修改：使用方式有变化。以后通过 `Tools/Hearth HUD/TV Terminal/Apply 17F-xx To Selected TV` 标准化 TV 时，不再直接使用 `Camera.main` 作为玩家相机，而是优先绑定场景中可用的 `PlayerInteraction` 和它的 `mainCamera`。
- 挂载对象：不需要挂载，仍然是 Unity Editor 菜单工具。
- 公开接口：`StandardizeTvTerminal(...)`、`StandardizeTvByHierarchyPath(...)` 使用方式不变，但自动绑定结果更准确。
- 最小测试方法：重新对某台 TV 执行 Apply 菜单后，`Terminal_17Fxx` 的 `Player Interaction` 应指向玩家控制器，`Player Camera` 应指向玩家当前第一人称相机，而不是旧 `Main Camera`。

## 2026-07-10 17F02 / 17F03 TV 终端过渡起点绑定修正

### HearthTvTerminalPrefabBuilder.cs

- 路径：`Assets/Editor/HearthTvTerminalPrefabBuilder.cs`。
- 本次修改：使用方式无变化。`StandardizeTvTerminal(...)` 在场景同时存在人类和机器人 `PlayerInteraction` 时，不再直接采用枚举到的第一个组件；它会给候选项评分，并优先选择层级路径中的 `Player/Person Controller`。`Player/Robot Controller` 不再被门口 TV 的标准化流程误选为过渡起点。
- 挂载对象：不需要挂载，是 Unity Editor 工具脚本。
- 依赖：场景中需要 `Player/Person Controller`、其 `PlayerInteraction`，以及子物体 `First Person Camera`。
- Inspector 结果：标准化完成后，终端 `HearthTvTerminalController / Player Interaction` 应指向 `Player/Person Controller`，`Player Camera` 应指向 `Player/Person Controller/First Person Camera`；`Terminal Camera` 仍指向对应 TV 自己的 Camera。
- 公开接口：`StandardizeTvTerminal(...)`、`StandardizeTvByHierarchyPath(...)` 保持不变。
- 引用关系：由 TV 终端 Apply 菜单和 `HearthTvTerminalSlideImageBuilder` 调用；运行时仍由 `HearthTvTerminalController` 执行 0.5 秒过渡。
- 最小测试：重新标准化 ROOM2 或 ROOM3 的 TV 后检查 Inspector，不应再绑定到 `Player/Robot Controller`；Play Mode 从人类视角按 E，过渡应从玩家当前画面开始。
- 后续扩展：如果未来正式改为 VR 或允许机器人直接操作门口终端，应增加明确的“交互发起者”参数，不要靠场景枚举顺序判断相机。

### HearthTvTerminalSlideImageBuilder.cs

- 路径：`Assets/Editor/HearthTvTerminalSlideImageBuilder.cs`。
- 本次修改：使用方式有变化。菜单 `Tools / Hearth HUD / TV Terminal / Rebuild PPT Image Terminals And Apply Current Scene` 现在固定应用为：ROOM1 -> 17F01、ROOM3 -> 17F02、ROOM2 -> 17F03 Alert。
- 挂载对象：不需要挂载，是 Unity Editor 工具脚本。
- 自动设置：应用 Prefab 后会显式设置三个终端的 `replayResidentId` 为 `17F01 / 17F02 / 17F03`，避免 ROOM2/ROOM3 层级名称对调后推断错误。
- 公开接口：`RebuildTvTerminalImagePrefabs()`、`RebuildAndApplyCurrentScene()` 保持不变。
- 最小测试：运行重建并应用菜单后，`ROOM3/TV (2)` 只包含 `Terminal_17F02` 且 resident 为 `17F02`；`ROOM2/TV (4)` 只包含 `Terminal_17F03_Alert` 且 resident 为 `17F03`；两台终端的 Player Camera 都必须是人类 First Person Camera。
- 当前场景已通过 Unity MCP 修正：三台 TV 终端均已绑定 `Player/Person Controller` 和 `Player/Person Controller/First Person Camera`。

## 2026-06-27 TV 终端 PPT 图片状态页接入记录

本次修改把 TV 终端 UI 改成“PPT 原样视觉 + Unity 状态切换”的结构。新版 `HEARTH-Night-Rounds-Master.pptx` 共 24 页，已同步到项目目录 `UI参考资料/HEARTH-Night-Rounds-Master.pptx`；前 8 页为 17F-01，中间 8 页为 17F-02，后 8 页为 17F-03 Alert。每组前 6 页为体验前导航/进入体验的选中态，最后 2 页为体验后 A/B 选择态。

### HearthTvTerminalSlideImageBuilder.cs

- 路径：`Assets/Editor/HearthTvTerminalSlideImageBuilder.cs`
- 脚本作用：Unity Editor 工具脚本，用 PowerPoint 把 `HEARTH-Night-Rounds-Master.pptx` 导出为 24 张 1920x1080 PNG，自动把接近黑底的像素转成透明，再生成 24 个图片页 Prefab 和 3 个终端 Prefab。
- 挂载对象：不需要挂载；这是 Editor 菜单工具。
- 菜单入口：
  - `Tools/Hearth HUD/Rebuild TV Terminal UI From PPT Images`：只重建图片页和终端 Prefab。
  - `Tools/Hearth HUD/TV Terminal/Rebuild PPT Image Terminals And Apply Current Scene`：重建后自动应用到当前场景三台 TV。
- 生成内容：
  - 透明 PNG：`Assets/Resources/UI/HearthTerminalSlideImages/TerminalImageSlide01.png` 到 `TerminalImageSlide24.png`
  - 图片页 Prefab：`Assets/Prefabs/UI/HearthHud/TerminalImagePages/TerminalImageSlide*.prefab`
  - 终端 Prefab：`Assets/Prefabs/UI/HearthHud/Terminals/Terminal_17F01.prefab`、`Terminal_17F02.prefab`、`Terminal_17F03_Alert.prefab`
- 依赖组件和环境：电脑需要安装 PowerPoint；Unity 会启动外部 PowerShell 脚本调用 PowerPoint 导出图片。项目需要 `HearthTvTerminalController`、`HearthTerminalCameraTransition`、`HearthTerminalBootSequence`、`HearthHudPage`。
- 重要规则：工具会优先使用最新修改时间的同名 PPT，因此桌面和项目目录都有 `HEARTH-Night-Rounds-Master.pptx` 时，会选最新的一份；本次已把桌面新版复制到 `UI参考资料`。
- 使用步骤：
  1. 在 PowerPoint 中修改 24 页终端 UI，保存 `HEARTH-Night-Rounds-Master.pptx`。
  2. 回到 Unity，点击 `Tools/Hearth HUD/TV Terminal/Rebuild PPT Image Terminals And Apply Current Scene`。
  3. 等待 Console 输出重建和应用完成。
  4. 检查三台 TV 下的 `Terminal_17Fxx` 是否各有 8 个 `HearthHudPage`。
- 最小测试方法和验收标准：菜单执行后应生成 24 张 PNG、24 个图片页 Prefab、3 个终端 Prefab；当前场景三台 TV 都应显示新版 PPT 视觉，并继续保留暗屏、开机闪烁和相机过渡。
- 后续扩展：如果黑底透明处理过强或过弱，调整 `ProcessSlideImageToTransparentPng` 里的亮度/饱和度阈值；如果以后每组页数变化，调整 `SlidesPerTerminal`、`PreChoiceSelectionPages` 和 `ChoicePages`。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`
- 本次修改：使用方式有变化。新增“PPT 图片状态页驱动模式”，由 `Page Driven Selection States` 控制。开启后，Unity 不再绘制额外高亮框，而是直接切换 PPT 里已经做好的选中态页面。
- 新增/调整 Inspector 字段：
  - `Page Driven Selection States`：开启 PPT 状态页模式。
  - `Pre Choice Selection Page Count`：体验机器人视角前可选页数，当前为 6，对应 5 个导航页 + RECALL/ENTER 入口页。
  - `Post Replay Navigation Page Count`：体验完成后还能返回查看的导航页数，当前为 5。
  - `Post Replay Choice Page Count`：体验完成后出现的选择页数，当前为 2，对应 A/B。
  - `Up Selection Key` / `Down Selection Key`：当前为上/下方向键；体验后，上键从 A/B 回到概要页，下键从导航回到 A/B。
  - `Close Terminal When Choice Submitted`：可选，勾选后按空格确认 A/B 会关闭终端。
  - `On Choice A Selected` / `On Choice B Selected`：A/B 被确认后的剧情事件接口。
- 当前键盘规则：
  - 打开终端后默认回到第 1 页概要。
  - 体验机器人视角前：`Left/Right` 或 `Tab` 在前 6 张 PPT 状态页之间切换；在第 6 页按 `Space` 触发机器人体验/进入单元。
  - 体验完成后：显示第 7 页 A 选中态；`Left/Right` 或 `Tab` 在第 7/8 页 A/B 之间切换；`Space` 确认当前 A/B。
  - 体验完成后：在 A/B 页按 `Up` 回到第 1 页概要；在 5 个导航页之间按 `Left/Right` 切换；按 `Down` 回到 A/B。此时第 6 页 RECALL/ENTER 不再可选。
- 公开接口：新增或继续使用 `SetPageDrivenSelectionStates(...)`、`ShowPostReplayChoicePage()`、`RequestRobotReplay()`、`OpenTerminal()`、`CloseTerminal()`。
- 引用关系：`HearthTvTerminalSlideImageBuilder` 会自动配置该模式；`HearthTvTerminalInteractable` 仍负责玩家看向 TV 按 `E` 打开；`HearthTerminalCameraTransition` 仍负责 0.5 秒相机过渡；`HearthTerminalBootSequence` 仍负责暗屏和开机闪烁。
- 最小测试方法：看向任意 TV 按 `E`，终端应先暗屏闪烁再显示 PPT 图片页；体验前只能在 1-6 页间切换；触发体验并调用 `ShowPostReplayChoicePage()` 后，应进入 A/B 选择态，并支持上/下切换导航区和选择区。
- 后续扩展：把最小循环或机器人回放流程完成事件接到 `ShowPostReplayChoicePage()`；把 A/B 的剧情结果接到 `On Choice A Selected` 和 `On Choice B Selected`。
## 2026-06-29 HEARTH 第一人称眼镜 HUD 重建

本次更新把旧的玩家第一人称 `HearthHudRoot` 替换为新 PPT `HEARTH-HUD改(3).pptx` 驱动的眼镜 HUD。TV 终端系统、机器人视角 HUD、终端 0.5 秒镜头过渡和终端屏幕开机闪烁没有重做，仍然使用原来的 `HearthTvTerminalController` 等脚本。

### 已实现

- 新 `HearthHudRoot` 已挂到当前场景，组件为 `Canvas / CanvasScaler / GraphicRaycaster / AudioSource / HearthFirstPersonHudController / HearthFirstPersonHudInput / HearthDispositionHistoryView / HearthSettingsView / HearthFirstPersonHudFlowBinder`。
- 新 PPT 的 24 页已解析为布局数据 `Assets/Art/UI/HearthHud/FirstPersonLayout/HearthFirstPersonHudLayout.json`。当前第一人称 HUD 不再使用整页截图贴图，页面由 TextMeshPro 文本、Unity UI 矩形、细线和边框重构。
- 旧第一人称页面 prefab 文件夹 `Assets/Prefabs/UI/HearthHud/Pages` 已删除；上一版截图式资源 `Assets/Art/UI/HearthHud/FirstPersonSlides` 也已删除；新页面 prefab 位于 `Assets/Prefabs/UI/HearthHud/FirstPersonPages`。
- `Tab / Esc / 上下左右 / Space / A / B` 已集中到新输入脚本；最终 A/B、警告链、退出确认、设置焦点、信任浮字都有可调用方法。
- 信任度规则通过新 HUD 绑定器配置为 A `+1`、B `-1`，最终 A 结局阈值为 `3`。

### 已预留但暂未真正接入的接口

- 音量设置：`HearthSettingsView.VolumeChanged` 会抛出 `Master / Dialogue / Ambient / SFX` 和数值，但还没有接到正式 `AudioMixer`。
- 退出游戏：Slide 24 的 Confirm 会触发 `HearthFirstPersonHudController.OnExitConfirmed`，目前只预留事件，不直接调用 `Application.Quit()` 或主菜单。
- 音效：HUD 控制器里预留了打开菜单、关闭菜单、切页、移动焦点、确认、取消、警告、信任浮字的 `AudioClip` 字段，素材为空时静默。
- 存档/读档：处置历史、信任分数、音量数值都有公开 setter/getter 或事件，后续可接正式存档系统。
- VR 输入：当前是 PC 键盘/鼠标；VR 射线、手柄按钮后续直接调用 `HearthFirstPersonHudController` 的公开方法即可。

### HearthFirstPersonHudTypes.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudTypes.cs`
- 脚本作用：定义第一人称 HUD 专用页码、HUD 状态、结局类型、按钮动作类型、处置历史记录和 UnityEvent 类型。
- 挂载对象：不用挂载，其他 HUD 脚本自动引用。
- 依赖组件：无。
- 重要接口：
  - `HearthFirstPersonHudPageId`：Slide 1-24 的新页码，不影响 TV 终端使用的旧 `HearthHudPageId`。
  - `HearthDispositionRecord`：处置历史列表的数据结构。
  - `HearthSettingsVolumeEvent`：设置音量预留事件，参数为通道名和数值。
- 后续扩展：如果新 PPT 增加页面，只扩展这里的 page id，再让 Builder 生成对应页面即可。

### HearthFirstPersonHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudController.cs`
- 脚本作用：新第一人称 HUD 总控制器，负责页面显隐、常驻 HUD、信任浮字、菜单焦点、最终 A/B、警告链、三个结局和退出确认。
- 挂载对象：`HearthHudRoot`。
- 必需组件：同物体需要 `Canvas`、`CanvasScaler`、`GraphicRaycaster`；音效预留需要 `AudioSource`。
- Inspector 重点字段：
  - `Pages`：由 Builder 自动填入 Slides 3-24 页面。
  - `Persistent Hud Root`：Slide 1 常驻 HUD 图层。
  - `Final Choice Trust Threshold`：默认 `3`。
  - `Trust Delta Canvas Group / Trust Delta Text`：右上角 `+1 TRUST` / `-1 TRUST` 浮字。
  - `Menu Focus Rect / Final Choice Focus Rect`：键盘选择高亮框。
  - `Disposition History View / Settings View`：历史和设置子视图。
  - Audio Clip 字段：后续直接拖入音效素材。
  - Events：`On Sync Confirmed`、`On Exit Confirmed`、结局事件等后续接剧情/存档/主菜单。
- 公开方法：
  - `ShowPage(pageId)`、`HideOverlay()`、`OpenMainMenu()`。
  - `OpenTodayRounds()`、`OpenDispositionHistory()`、`OpenSettings()`、`OpenExitConfirm()`。
  - `ShowFinalChoice(returnState)`、`ChooseFinalA()`、`ChooseFinalB()`。
  - `ContinueWarning()`、`CancelWarning()`、`ConfirmGracefulShutdown()`。
  - `SetHudState(state)`、`SetRoundsProgress(completed,total)`、`SetTrustScore(value)`、`ShowTrustDelta(delta)`。
  - `RecordDisposition(choice)` 和 `RecordDisposition(unitId, actionLabel, statusLabel, trustDelta)`。
- 关联脚本：被 `HearthFirstPersonHudInput`、`HearthFirstPersonHudButtonAction`、`HearthFirstPersonHudFlowBinder` 调用。
- 最小测试：Play 后按 `Tab` 打开 Slide 3，方向键移动焦点，`Space` 进入子页，`Esc` 关闭；调用 `ShowFinalChoice(false)` 后 A/B 能分流。
- 后续扩展：真实剧情系统只需要调用这些公开方法，不要直接开关页面 GameObject。

### HearthFirstPersonHudInput.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudInput.cs`
- 脚本作用：处理 PC 键盘输入，让 HUD 可交互。
- 挂载对象：`HearthHudRoot`。
- 必需组件：同物体需要 `HearthFirstPersonHudController`；设置页需要 `HearthSettingsView`。
- Inspector 重点字段：
  - `Enable Keyboard Input`：是否启用键盘测试。
  - `Menu Key` 默认 `Tab`，`Cancel Key` 默认 `Esc`，`Submit Key` 默认 `Space`。
  - `Settings Volume Step`：设置音量左右调整的步长，默认 `5`。
- 公开方法：
  - `SetKeyboardInputEnabled(bool)`：进入过场或 VR 接管输入时可关闭 PC 键盘输入。
- 使用步骤：随 `HearthHudRoot` 自动生成，无需手动挂。
- 最小测试：`Tab` 打开菜单，`↑↓` 选项，`←→` 在最终 A/B 或设置页内移动/调整。
- 后续扩展：VR 输入不要改这里，直接调用 `HearthFirstPersonHudController` 的公开方法。

### HearthFirstPersonHudPage.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudPage.cs`
- 脚本作用：每个 HUD 页面 prefab 的基础显隐控制。
- 挂载对象：`Assets/Prefabs/UI/HearthHud/FirstPersonPages` 下各页 prefab，以及 `HearthHudRoot` 内的页面子物体。
- 必需组件：`RectTransform`、`CanvasGroup`。
- Inspector 重点字段：
  - `Page Id`：对应 Slide 3-24。
  - `Fullscreen Takeover`：Slides 15-17 为 true，会隐藏常驻 HUD。
  - `Keep Persistent Hud Visible`：除全屏结局外通常为 true。
- 公开方法：`Show()`、`Hide()`、`SetVisible(bool)`。
- 使用方式：由 Builder 自动配置，不建议手动改页码。
- 最小测试：通过 Controller `ShowPage` 切换页面时，只应有一个 overlay 页面可见。

### HearthFirstPersonHudButtonAction.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudButtonAction.cs`
- 脚本作用：挂在透明 Button 热区上，把点击转换为 HUD 动作。
- 挂载对象：页面 prefab 内的 `Button_*` 子物体。
- 必需组件：`Button`、`Image`。
- Inspector 重点字段：
  - `Controller`：通常自动找父级 `HearthFirstPersonHudController`。
  - `Action Type`：例如 `OpenSettings`、`ChooseFinalA`、`ContinueWarning`。
  - `Target Page`：当动作是 `ShowPage` 时使用。
- 公开方法：`InvokeAction()`。
- 最小测试：鼠标点击 Slide 9 的 A/B 热区，应和键盘 A/B 一样分流。
- 后续扩展：如果 PPT 新增可点击区域，用 Builder 或手动加透明 Button，再配置本脚本。

### HearthDispositionHistoryView.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthDispositionHistoryView.cs`
- 脚本作用：管理处置历史记录，Slides 18-21 按记录数显示 0/1/2/3 户状态。
- 挂载对象：`HearthHudRoot`。
- 必需组件：无；如果未来要把行文字完全改成 TMP 动态列表，可把 TMP 字段拖入对应数组。
- Inspector 重点字段：
  - `Records`：当前班次的处置记录。
  - `Optional Row Bindings`：预留 TMP 行文本。
  - `Optional Footer Bindings`：预留信任变化和当前信任显示。
- 公开方法：
  - `AddRecord(record)`、`SetRecords(records)`、`ClearRecords()`。
  - `SetCurrentTrustScore(value)`、`GetPageForCurrentRecordCount()`。
- 当前实现说明：首版主要通过 Slide 18-21 的 PPT 图像状态显示历史；TMP 行字段已经预留，后续要接真实数据列表时直接拖文本。
- 最小测试：调用 `RecordDisposition` 三次后，再打开历史页，应进入 3 条记录版本。

### HearthSettingsView.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthSettingsView.cs`
- 脚本作用：管理 Slides 22-24 设置页焦点、四路音量数值和退出确认入口。
- 挂载对象：`HearthHudRoot`。
- 必需组件：无；如果接真实滑块，可拖 `Slider` 和 TMP 数值文本。
- Inspector 重点字段：
  - `Master / Dialogue / Ambient / SFX Volume`：默认 80/100/70/80。
  - `Focus Rect / Focus Targets`：设置页高亮框和焦点行。
  - `Volume Changed`：预留给 AudioMixer。
  - `On Exit Requested`：已由 Builder 接到 `HearthFirstPersonHudController.OpenExitConfirm()`。
- 公开方法：
  - `MoveFocus(direction)`、`AdjustFocusedVolume(delta)`、`ConfirmFocusedItem()`。
  - `SetMasterVolume`、`SetDialogueVolume`、`SetAmbientVolume`、`SetSfxVolume`。
  - `GetVolume(channel)`、`SetFocusVisible(bool)`。
- 当前实现说明：数值和事件已生效，但不会真正改变游戏音量；需要后续把 `VolumeChanged` 接入正式 AudioMixer。
- 最小测试：打开 Settings 后按 `↑↓` 移动焦点，`←→` 改数值，焦点在 EXIT GAME 上按 `Space` 出现 Slide 24。

### HearthFirstPersonHudFlowBinder.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudFlowBinder.cs`
- 脚本作用：把现有最小循环 `MinLoopFlowController`、`TrustStateController` 同步到新 HUD。
- 挂载对象：`HearthHudRoot`。
- 必需组件：场景里应有 `MinLoopFlowController` 和 `TrustStateController`；没有时 HUD 仍可用，只是不自动监听流程。
- Inspector 重点字段：
  - `Configure Trust Controller On Awake`：默认开启。
  - `Starting Trust Score`：默认 `0`。
  - `Min Trust Score / Max Trust Score`：默认 `-3 / 3`。
  - `Option A Delta / Option B Delta`：默认 `+1 / -1`。
  - `Show Sync Terminal On Access Card`：默认关闭，避免和 TV 终端入口抢显示；需要开场自动显示 Slide 4 时可打开。
- 公开方法：`SetReferences(hud, flow, trust)`。
- 关联脚本：
  - 监听 `MinLoopFlowController.StageChanged` 和 `DispositionApplied`。
  - 调用 `TrustStateController.ConfigureRules(...)` 设置新规则。
- 最小测试：最小循环提交 A/B 后，HUD 信任浮字和历史记录应按 `+1/-1` 更新。
- 后续扩展：多户数据正式化后，可把 `RecordDisposition` 的 unitId/actionLabel 改为从数据表传入。

### TrustStateController.cs

- 路径：`Assets/Scripts/MinLoop/TrustStateController.cs`
- 本次修改：使用方式无变化，仍然挂在原来的最小循环管理对象上。
- 新增公开方法：
  - `SetChoiceDeltas(optionA, optionB)`：运行时修改 A/B 信任变化量。
  - `ConfigureRules(startingTrust, minTrust, maxTrust, optionADelta, optionBDelta, resetCurrentTrust)`：一次性配置起始值、上下限和 A/B 增减。
- 本次用途：`HearthFirstPersonHudFlowBinder` 在 Awake 时把规则设为初始 `0`、范围 `-3..3`、A `+1`、B `-1`。
- 最小测试：绑定新 HUD 后完成一次 A 处置，`TrustStateController.CurrentTrust` 应加 1；完成一次 B 处置应减 1。
- 后续扩展：如果之后信任规则改成按住户/剧情表驱动，只需要由流程脚本调用 `ConfigureRules` 或 `SetChoiceDeltas`。

### HearthFirstPersonHudBuilder.cs

- 路径：`Assets/Editor/HearthFirstPersonHudBuilder.cs`
- 脚本作用：Editor 工具，读取 `HearthFirstPersonHudLayout.json`，把 PPT 中的文字实际边界、形状位置、线条和颜色重构成 Unity uGUI/TMP HUD prefab，并可应用到当前打开场景。它不再把整页 PPT 截图作为 Image。
- 挂载对象：不用挂载，只在 Unity Editor 菜单使用。
- 菜单入口：
  - `Tools / Hearth / HUD / Rebuild First Person HUD Prefab`
  - `Tools / Hearth / HUD / Rebuild And Apply First Person HUD To Scene`
- 输入资源：
  - `Assets/Art/UI/HearthHud/FirstPersonLayout/HearthFirstPersonHudLayout.json`。
  - JSON 由 PowerPoint 保存后的 PPT 解析生成，包含每个对象的位置、线条、填充、文字内容和 PowerPoint 计算出的文字实际边界。
- 输出资源：
  - `Assets/Prefabs/UI/HearthHud/HearthHudRoot.prefab`
  - `Assets/Prefabs/UI/HearthHud/FirstPersonPages/*.prefab`
- 使用步骤：
  1. 修改并保存 `HEARTH-HUD改(3).pptx`。
  2. 重新生成 `HearthFirstPersonHudLayout.json`。
  3. 在 Unity 菜单运行 `Tools / Hearth / HUD / Rebuild And Apply First Person HUD To Scene`。
  4. Play Mode 检查 Tab、A/B、Settings、History、Warning、Ending。
- 视觉检查：当前 prefab 校验应为 `spriteImages=0`，表示页面不是整页截图；如果某页视觉偏亮或偏暗，优先调整 Builder 中的 `ToFillColor`、`ToLineColor`、`ToTextColor` 风格归一化逻辑。
- 后续扩展：如果 PPT 页面布局大改，优先重新生成 layout JSON；如果按钮热区需要更贴合文字，修改 Builder 里的 `CreateButtonFromText` 和焦点目标生成逻辑。
## 2026-06-29 第一人称 HUD 输入锁和高亮框修正

本次修正解决三个问题：第一，玩家按 `Space` 不再触发跳跃；第二，`Tab` 打开左上角 HUD 菜单及其子页面后，玩家移动、视角、蹲伏和交互输入会被锁住；第三，菜单、最终选择和系统设置页的高亮框改为围绕文字范围生成，设置页不再出现整条超宽框。

### HearthPlayerControlLock.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthPlayerControlLock.cs`
- 脚本作用：集中锁定和恢复玩家控制输入，用于 HUD 菜单、设置页、历史页、警告页和结局页打开时阻止角色继续移动或转头。
- 挂载对象：随 `HearthHudRoot` 自动生成，挂在 `HearthHudRoot` 根物体上。
- 必需组件：无硬性依赖；默认会自动查找场景里的 `FirstPersonMovement`、`FirstPersonLook`、`Crouch`、`Jump`、`PlayerInteraction` 和相关 `Rigidbody`。
- Inspector 重点字段：
  - `Auto Find Scene Controllers`：默认开启，自动绑定当前场景的 Mia/Person 和 Robot 两套第一人称控制器。
  - `Movement Components / Look Components / Crouch Components / Jump Components / Interaction Components`：可手动指定要锁定的组件，后续若新增玩家控制器可拖入这里。
  - `Extra Behaviours To Disable`：预留给未来其他会读取键盘/鼠标的控制脚本。
  - `Disable Jump Always`：默认开启，保证跳跃组件即使在 HUD 关闭时也不响应 `Space`。
  - `Clear Rigidbody Velocity On Lock / Rigidbodies To Clear`：HUD 打开瞬间清掉残余速度，避免角色继续滑动。
- 公开方法：`SetControlsLocked(bool)`、`LockControls()`、`UnlockControls()`、`SetDisableJumpAlways(bool)`、`ResolveReferences()`。
- 引用关系：由 `HearthFirstPersonHudController` 在页面打开/关闭时调用；同时会调用 `Jump.SetJumpEnabled(false)`。
- Unity 使用步骤：通常无需手动处理；如果自动查找不符合预期，就在 `HearthHudRoot` 上展开本组件，把当前玩家控制器的移动、视角、蹲伏、交互组件拖进对应数组。
- 最小测试方法：编辑器验证结果为菜单打开时 `FirstPersonMovement / FirstPersonLook / Crouch / PlayerInteraction` 都变为 disabled，关闭菜单后恢复；`Jump` 始终保持 disabled。
- 后续扩展：VR 输入接入后，可以把 VR 射线或手柄控制脚本加入 `Extra Behaviours To Disable`，或直接调用 `SetControlsLocked`。

### HearthFirstPersonHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudController.cs`
- 本次修改：使用方式有变化。新增 `Player Control Lock` 区域，HUD 从常驻页切到任意 overlay 页面时会自动锁玩家控制，回到 Slide 1 常驻 HUD 时自动解锁。
- 挂载对象：仍然是 `HearthHudRoot`。
- 新增 Inspector 字段：
  - `Lock Player Controls While Overlay Open`：默认开启；若临时想让 HUD 打开时仍能走动，可关闭此项。
  - `Player Control Lock`：自动绑定同物体上的 `HearthPlayerControlLock`，也可手动拖入其他控制锁组件。
- 公开接口：原有 `ShowPage`、`HideOverlay`、`OpenMainMenu`、设置页、历史页、最终选择和警告链接口保持兼容。
- 最小测试方法：按 `Tab` 打开菜单后，上下左右和 `Space` 只操作 HUD，不再驱动人物；按 `Esc` 或再次关闭 overlay 后，人物移动恢复。
- 后续扩展：如果某些剧情页面不想锁玩家，可在 `ShouldLockPlayerControls` 逻辑里为特定页面做例外，或在 Inspector 关闭总开关。

### Jump.cs

- 路径：`Assets/Mini First Person Controller/Scripts/Components/Jump.cs`
- 本次修改：新增 `Jump Enabled` 开关和公开方法 `SetJumpEnabled(bool)`；默认关闭，避免 `Space` 被旧第一人称控制器当作跳跃。
- 挂载对象：仍然是 `Player/Person Controller` 和 `Player/Robot Controller` 上的原 `Jump` 组件。
- Inspector 重点字段：
  - `Jump Enabled`：当前保持关闭。
  - `Jump Strength`：保留旧字段，只有 `Jump Enabled` 打开时才生效。
  - `Ground Check`：沿用旧检测方式。
- 当前场景配置：两套控制器的 `Jump` 组件已经 disabled，并且 `Jump Enabled` 为 false。
- 最小测试方法：Play Mode 中按 `Space` 不应再跳起；HUD 页面打开时 `Space` 只触发 HUD 确认。
- 后续扩展：如果以后某段流程真的需要跳跃，先启用 `Jump` 组件，再把 `Jump Enabled` 打开。

### HearthFirstPersonHudBuilder.cs

- 路径：`Assets/Editor/HearthFirstPersonHudBuilder.cs`
- 本次修改：重建 `HearthHudRoot` 时会自动添加 `HearthPlayerControlLock`，并把它绑定给 `HearthFirstPersonHudController`。
- 高亮框规则：菜单、最终 A/B、系统设置页的 focus target 现在只基于文字区域加小 padding 生成；真正外扩量由 Inspector 中的 `Menu Focus Padding`、`Final Choice Focus Padding`、`HearthSettingsView.Focus Padding` 控制。
- 当前默认值：菜单 `8 x 4`，最终选择 `10 x 6`，设置页 `8 x 4`。
- 验证记录：当前场景里 `SettingsTarget_Master` 尺寸约为 `(142.36, 25.60)`，不再是整条长框；`HearthHudRoot` 已包含 `HearthPlayerControlLock`。
- 使用步骤：修改 PPT 或布局后，继续运行 `Tools / Hearth / HUD / Rebuild And Apply First Person HUD To Scene`。如果只是想微调高亮框大小，可以直接在 Inspector 调整 focus padding，不必重写脚本。
- 后续扩展：如果某个文本框的 PowerPoint 文字边界仍然不准，优先在 Builder 的 focus target 生成逻辑中给该项单独修正，而不是把整页做成截图。

### HearthSettingsView.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthSettingsView.cs`
- 本次修改：使用方式无变化；默认 `Focus Padding` 已从旧的大框改为 `8 x 4`，Builder 也会写入同样的小值，设置页高亮框会围绕当前文字项，不再覆盖整行。
- 可维护接口：`Focus Rect / Focus Targets / Focus Padding` 都保留在 Inspector，可手动拖目标或调整 padding。
- 最小测试方法：打开 Settings 后按上下键，焦点框应只包裹 `Master Volume`、`Dialogue Volume`、`Ambient Volume`、`SFX Volume` 或 `EXIT GAME` 的文字附近。

### 当前玩家移动参数记录

- 场景对象：`Player/Person Controller` 和 `Player/Robot Controller`。
- 调整位置：选中对应对象，在 Inspector 的 `FirstPersonMovement` 组件中调 `Speed` 和 `Run Speed`；在 `Crouch` 组件中调 `Movement Speed`。
- 当前数值：当前场景读取到 `Speed = 2.5`、`Run Speed = 6.3`、`Crouch Movement Speed = 1.4`；如需继续统一人类和机器人速度，优先改 `Person Controller` 后再同步给 `Robot Controller`。
- 使用方式：如果以后觉得走得还是快，直接在这两个控制器的 Inspector 里改这些数值即可，不需要改 HUD 脚本。

## 2026-06-29 Robot Controller 场景绑定修复

本次修复当前场景中新复制出来的 `Player/Robot Controller`。该对象现在作为唯一正式机器人控制器使用，`ViewSwitchController` 的 `Companion` rig、地点检测的 `companionProbeRoot`、HUD 输入锁和最小循环流程都已重新指向它。机器人默认处于待机状态：移动、视角、机器人相机、机器人 AudioListener、跳跃和蹲伏均关闭；切换到 Companion/Robot 视角时由 `ViewSwitchController` 再启用对应控制。

### 当前场景 Robot Controller 配置

- 场景对象：`Player/Robot Controller`。
- 子对象：沿用 `First Person Camera`、`Ground Check`、`First Person Audio` 等结构；这些子对象的引用已全部指回机器人自己，而不是 `Person Controller`。
- 当前 Inspector 关键状态：
  - `FirstPersonMovement.enabled = false`，速度数值已同步当前 `Person Controller`。
  - `FirstPersonLook.enabled = false`。
  - `PlayerInteraction.enabled = true`，但 `InteractionEnabled = false`，等待视角切换后启用。
  - `Jump.enabled = false` 且 `Jump Enabled = false`。
  - `Crouch.enabled = false` 且 `Crouch Enabled = false`。
  - 机器人相机 `Camera.enabled = false`，`AudioListener.enabled = false`，Tag 为 `Untagged`。
- 当前主相机：旧场景根部的 `Main Camera` 已禁用 Camera/AudioListener 并取消 `MainCamera` 标签；`Player/Person Controller/First Person Camera` 是当前唯一启用的主相机和 AudioListener。
- 最小测试方法：Play Mode 中默认从人类相机开始，Console 不应再出现两个 AudioListener 的新警告；触发机器人回放/切换时，`ViewSwitchController` 应启用 `Player/Robot Controller` 的相机、移动、视角和交互。
- 后续扩展：如果以后误删或重建 `Robot Controller`，先保证新对象名字包含 `Robot Controller`，再运行 `Tools / Hearth / HUD / Apply Location Detection To Open Scene` 和 `Tools / Min Loop / Auto Bind 17F-01 Scene References`，自动绑定工具现在能识别 `Person Controller / Robot Controller` 命名。

### HearthLocationDetectionSceneBinder.cs

- 路径：`Assets/Editor/HearthLocationDetectionSceneBinder.cs`
- 本次修改：使用方式有变化。自动绑定 `humanProbeRoot / companionProbeRoot` 时不再只依赖完整路径，而是识别 `Person Controller`、`Robot Controller`、`Companion_Controller`、`Robot_Controller` 等命名。
- 挂载对象：不需要挂载，仍是 Editor 菜单工具。
- 菜单入口：`Tools / Hearth / HUD / Apply Location Detection To Open Scene`。
- 最小测试方法：运行菜单后，`HEARTH_LOCATION_SYSTEM` 上的 `HearthLocationProbe.companionProbeRoot` 应指向 `Player/Robot Controller`。
- 后续扩展：如果每户机器人以后命名为 `Robot Controller 1/2/3`，只要名字包含 `Robot Controller`，地点系统就能识别；但正式可控视角仍建议只保留一个当前激活的 Robot Controller。

### MinLoopSceneAutoBinder.cs

- 路径：`Assets/Editor/MinLoopSceneAutoBinder.cs`
- 本次修改：使用方式有变化。最小循环自动绑定工具现在能识别 `Person Controller` 和 `Robot Controller` 这两个当前场景命名，不再只识别旧的 `Mia_Controller / Companion_Controller`。
- 挂载对象：不需要挂载，仍是 Editor 菜单工具。
- 菜单入口：`Tools / Min Loop / Auto Bind 17F-01 Scene References`。
- 最小测试方法：运行菜单后，`ViewSwitchController.human.rootObject` 应为 `Player/Person Controller`，`ViewSwitchController.companion.rootObject` 应为 `Player/Robot Controller`。
- 后续扩展：如果之后把 17F-02/17F-03 也接入最小循环，可以继续复用这套命名；每个剧情段只需要把唯一 Robot Controller 移动到对应锚点。

### ViewSwitchController.cs

- 路径：`Assets/Scripts/MinLoop/ViewSwitchController.cs`
- 本次修改：使用方式有变化。切换 Human/Companion 视角时，会自动把当前激活视角的相机设为 `MainCamera` Tag，把另一个相机设为 `Untagged`，避免后续脚本通过 `Camera.main` 取到错误相机。
- 挂载对象：仍为当前场景里的 `ViewSwitchController` 对象，当前路径是 `1F (1)/GameObject (1)`。
- Inspector 重点字段：`Human.View Camera` 当前指向 `Player/Person Controller/First Person Camera`；`Companion.View Camera` 当前指向 `Player/Robot Controller/Robot First Person Camera`。
- 当前场景命名：机器人高层级子对象已改为 `Robot First Person Camera`、`Robot Ground Check`、`Robot First Person Audio`。细分音频子对象如 `Step Audio / Running Audio` 保留原名，避免旧 First Person Audio 脚本 Reset 时重复创建同名音源。
- 最小测试方法：默认 Human 模式时，人类相机 Tag 为 `MainCamera`，机器人相机 Tag 为 `Untagged`；调用 `SwitchToCompanion()` 后应反过来。
- 后续扩展：如果以后机器人 UI 需要读取当前相机，优先从 `ViewSwitchController` 当前 rig 或 `Camera.main` 获取，不要硬编码子物体名字。

## 2026-06-29 HEARTH 地点 HUD 与蹲伏禁用

本次修改完成两件事：第一，人类和机器人控制器都禁止蹲伏，并和跳跃一样保留 Inspector/公开方法开关；第二，新增右下角 `LOCATION` 地点 HUD，根据当前人类或机器人控制器中心点向下检测脚下地面，显示 `17F-01 / 17F-02 / 17F-03 / 17F-04 / 17F CORRIDOR / 1F LOBBY`。右下角 `LOCATION` 区域常驻显示，不会整体隐藏；只有地点文字在切换时使用约 `0.5` 秒淡入和轻微重影模拟模糊感。当前场景已通过 Unity MCP 应用绑定：共 15 个地点判定面，人类与机器人 `Crouch` 组件均为 disabled 且 `Crouch Enabled = false`。

### Crouch.cs

- 路径：`Assets/Mini First Person Controller/Scripts/Components/Crouch.cs`
- 本次修改：使用方式有变化。新增 `Crouch Enabled` 字段和 `SetCrouchEnabled(bool)` 方法，默认关闭；如果角色已经蹲下时被关闭，会先恢复头部高度、Collider 高度和移动速度 override。
- 挂载对象：仍然是 `Player/Person Controller` 和 `Player/Robot Controller` 上的原 `Crouch` 组件。
- 需要组件：同对象需要 `FirstPersonMovement`，子物体需要第一人称相机，角色需要 `CapsuleCollider`。
- Inspector 重点字段：`Crouch Enabled` 当前保持关闭；`Key` 仍可保留 `LeftControl`，但开关关闭时不会响应；`Movement Speed / Height / Camera Height` 仅在重新启用蹲伏后生效。
- 公开接口：`SetCrouchEnabled(bool value)`。
- 最小测试方法：Play Mode 中按 `LeftControl`，人类和机器人镜头高度、Collider 高度、移动速度都不应变化。
- 后续扩展：如果以后某段流程真的需要蹲伏，必须同时启用组件并调用 `SetCrouchEnabled(true)`，否则按键仍不会生效。

### HearthPlayerControlLock.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthPlayerControlLock.cs`
- 本次修改：使用方式有变化。新增 `Disable Crouch Always`，默认开启；无论 HUD 是否打开，都会强制关闭人类和机器人控制器上的 `Crouch`。
- 挂载对象：随 `HearthHudRoot` 自动生成，挂在 `HearthHudRoot` 根物体。
- Inspector 重点字段：`Disable Jump Always` 和 `Disable Crouch Always` 都应保持开启；`Crouch Components` 可手动拖入新角色控制器的 `Crouch`。
- 公开接口：新增 `SetDisableCrouchAlways(bool value)`；原 `SetControlsLocked / LockControls / UnlockControls / SetDisableJumpAlways / ResolveReferences` 保持可用。
- 引用关系：由 `HearthFirstPersonHudController` 在 HUD overlay 打开/关闭时调用；同时会调用 `Jump.SetJumpEnabled(false)` 和 `Crouch.SetCrouchEnabled(false)`。
- 最小测试方法：Tab 打开菜单时移动/视角/交互被锁定；关闭菜单后移动恢复；`Jump` 和 `Crouch` 始终不恢复。

### HearthLocationSurface.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthLocationSurface.cs`
- 脚本作用：地点判定标签，挂在可被脚下射线命中的 Mesh/Collider 上，提供地点 id、显示文字、优先级和是否可触发欢迎回家。
- 挂载对象：当前自动挂在指定 17F Mesh 上，以及 `HEARTH_LOCATION_SYSTEM/GeneratedLocationColliders/Location_1F_LOBBY`。
- 需要组件：同对象需要 `Collider`；17F 使用已有 `MeshCollider`，1F 大堂使用自动生成的不可见 `BoxCollider`。
- Inspector 重点字段：`Location Id`、`Display Label`、`Priority`、`Can Trigger Home Welcome`。当前 `17F-04` 的四块地面允许触发欢迎回家。
- 公开接口：`Configure(string id, string label, int priority, bool triggerHomeWelcome)`；只读属性 `LocationId / DisplayLabel / Priority / CanTriggerHomeWelcome`。
- 最小测试方法：角色站在对应地面上，右下角地点 HUD 应显示该 `Display Label`。
- 后续扩展：新增楼层或房间时，在对应地面 Collider 上挂本脚本并填写显示文字即可。

### HearthLocationHudView.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthLocationHudView.cs`
- 脚本作用：控制右下角地点 HUD 的常驻显示、地点文字切换、淡入和“轻微模糊”重影效果。
- 挂载对象：`HearthHudRoot/PersistentHudLayer/PersistentHud/LocationHud`。
- 需要组件：同对象需要 `CanvasGroup`；子物体需要 `TMP_Text` 标题、主文本和 glow 重影文本。
- Inspector 重点字段：`Fade Duration` 当前为 `0.5`；`Keep Visible When Hidden` 当前为开启，表示右下角区域常亮；`Hold When Lost` 当前为 `0`；`Blur Start Scale`、`Glow Alpha`、`Title Color`、`Text Color`、`Glow Color` 可调视觉质感。
- 公开接口：`ShowLocation(string label)`、`HideLocation()`、`HideImmediate()`、`Configure(...)`。
- 引用关系：由 `HearthLocationProbe` 调用；由 `HearthFirstPersonHudBuilder` 自动生成。
- 最小测试方法：从一个已标记地面走到另一个已标记地面，右下角 `LOCATION` 标题应保持可见，只有地点值重新淡入并带轻微重影；如果暂时没有命中地点，保留上一次地点而不是把整个区域关掉。
- 后续扩展：如果以后要改成真正的后处理模糊，可以保留本脚本作为地点文字切换入口，只替换视觉层。

### HearthLocationProbe.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthLocationProbe.cs`
- 脚本作用：地点检测系统。按当前 `ViewSwitchController.CurrentMode` 选择人类或机器人控制器 Transform，从控制器中心点向下射线检测 `HearthLocationSurface`，并控制地点 HUD 与欢迎回家弹窗。
- 挂载对象：当前自动挂在场景根物体 `HEARTH_LOCATION_SYSTEM`。
- 需要组件：场景中需要 `ViewSwitchController`、人类控制器 Transform、机器人控制器 Transform、`HearthLocationHudView`；如果要触发欢迎页，需要 `HearthFirstPersonHudController`。
- Inspector 重点字段：`Location Enabled`、`Probe Height`、`Probe Distance`、`Refresh Interval`、`Surface Layers`、`Show Home Welcome Once`、`Home Welcome Location Id`、`Home Welcome Auto Close Seconds`、`Wait For Persistent Hud`。
- 公开接口：`SetLocationEnabled(bool enabled)`、`RefreshCurrentLocation()`、`ResetHomeWelcomeOnce()`。
- 引用关系：读取 `ViewSwitchController.CurrentMode`；调用 `HearthLocationHudView.ShowLocation/HideLocation`；第一次检测到 `17F-04` 时调用 `HearthFirstPersonHudController.ShowPage(Slide06HomeWelcome)`。
- 本次修正：即使脚下地点没有变化，也会持续确认 HUD 上已经写入当前地点，避免 `HearthHudRoot` 重建或显示层重置后右下角区域为空。
- 最小测试方法：人类和机器人视角分别站到不同标记地面上，地点 HUD 应跟随当前控制器，而不是固定跟随人类。
- 后续扩展：如果 VR 或剧情系统有自己的“当前操控对象”，可直接替换 `humanProbeRoot / companionProbeRoot`，或在外部调用 `SetLocationEnabled(false)` 暂停检测。

### HearthLocationDetectionSceneBinder.cs

- 路径：`Assets/Editor/HearthLocationDetectionSceneBinder.cs`
- 脚本作用：Unity Editor 自动绑定工具，用于给当前打开场景添加/修正地点系统、地点面、1F 大堂不可见判定盒，并关闭两套控制器的蹲伏。
- 挂载对象：不需要挂载，这是 Editor 菜单工具。
- 菜单入口：`Tools / Hearth / HUD / Apply Location Detection To Open Scene`。
- 自动处理内容：
  - 给指定 17F Mesh 添加 `HearthLocationSurface` 并确认有 Collider。
  - 为 `Mesh2491` 创建 `HEARTH_LOCATION_SYSTEM/GeneratedLocationColliders/Location_1F_LOBBY` 不可见 BoxCollider。
  - 创建或修正 `HEARTH_LOCATION_SYSTEM`，并绑定 `HearthLocationProbe`、`ViewSwitchController`、人类/机器人控制器和地点 HUD。
  - 调用 `Crouch.SetCrouchEnabled(false)` 并禁用场景中的 `Crouch` 组件。
- 当前验证记录：Unity MCP 射线检查已确认 15 个判定面均能命中对应地点；Console 无新增 Error。
- 后续使用步骤：如果之后更换场景、重新导入楼层模型或地面 Mesh 路径变化，重新运行本菜单；如果路径大改，需要先在该脚本的路径映射表里更新 Mesh 路径。

### HearthFirstPersonHudBuilder.cs

- 路径：`Assets/Editor/HearthFirstPersonHudBuilder.cs`
- 本次修改：使用方式有变化。重建 `HearthHudRoot` 时，会自动在常驻 HUD 右下角生成 `LocationHud`，包含 `LOCATION` 标题、地点主文本、重影文本和 `HearthLocationHudView`。
- 菜单入口：`Tools / Hearth / HUD / Rebuild And Apply First Person HUD To Scene`。
- 视觉位置：右下角，与左下角 `CURRENT TASK` 区域视觉对称；默认只显示地点两行文字，不抢 HUD 焦点。
- 最小测试方法：执行重建菜单后，场景中应存在 `HearthHudRoot/PersistentHudLayer/PersistentHud/LocationHud`，并带有 `HearthLocationHudView`。
- 后续扩展：如果想微调右下角位置或字号，优先改 Builder 中 `CreateLocationHudView` 的 Rect 数据；如果只改动画和颜色，可直接在 Inspector 上调 `HearthLocationHudView`。

## 2026-06-29 陪伴单元机器人视角 HUD

### HearthCompanionHudTypes.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudTypes.cs`
- 脚本作用：定义机器人 HUD 的模板类型、特殊效果类型、状态行数据和场景事件。
- 挂载对象：不需要挂载，这是共享类型文件。
- 公开接口：`HearthCompanionHudTemplate`、`HearthCompanionSpecialEffect`、`HearthCompanionMetricLine`、`HearthCompanionHudSceneEvent`。
- 引用关系：被 `HearthCompanionHudSceneData`、`HearthCompanionHudController`、`HearthCompanionHudBuilder` 使用。
- 后续扩展：新增机器人 HUD 模板或特殊效果时，先在这里加枚举，再让 Controller 和 Builder 识别。

### HearthCompanionHudSceneData.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudSceneData.cs`
- 脚本作用：机器人视角每一页的 ScriptableObject 数据，保存 scene id、住户、模式名、左上状态、右上决策、左下数据流、长按按钮、投影、黑场和特殊效果文案。
- 挂载对象：不挂载到 GameObject；生成在 `Assets/Data/HearthHud/Companion/CompanionScene_*.asset`。
- Inspector 重点字段：`Scene Id`、`Slide Number`、`Template`、`Special Effect`、`Mode Label`、`Status Lines`、`Decision Title/Body`、`Data Stream Lines`、`Show Hold Prompt`、`Auto Play Special Effect`。
- 公开接口：只读属性供 Controller 读取；`Configure(...)` 供 Editor 构建器生成默认数据。
- 引用关系：由 `HearthCompanionHudController` 读取；由 `HearthCompanionHudBuilder` 首次创建或按菜单重置。
- 使用步骤：平时直接在 Inspector 打开对应 `CompanionScene_*.asset` 改文字；不要改 PPT 后期待自动同步，除非后续再做 PPT 导入器。
- 最小测试方法：修改某个 asset 的 `Decision Title`，Play Mode 中 `HearthCompanionHudRoot` 显示该页时右上标题应跟着变化。
- 后续扩展：正式剧情、存档或音频系统都应通过 scene id 找到这些数据，而不是在脚本里硬编码文案。

### HearthCompanionHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudController.cs`
- 脚本作用：机器人 HUD 总控，管理 14 个场景数据、显隐、切页、长按确认、黑场音频、强制关机、深眠退化和剧情事件。
- 挂载对象：`HearthCompanionHudRoot` 根物体。
- 需要组件：同对象需要 `Canvas`、`CanvasScaler`、`GraphicRaycaster`、`CanvasGroup`、`AudioSource`；子物体需要各个 Companion View 脚本。
- Inspector 重点字段：`Scenes` 拖入 14 个 `CompanionScene_*.asset`；`Starting Scene Id` 默认 `17F01_01`；`Visible Only In Companion View` 默认开启；音效 Clip 可后续拖入。
- 公开接口：`ShowScene(string sceneId)`、`ShowScene(int slideNumber)`、`AdvanceScene()`、`ShowPreviousScene()`、`ConfirmCurrentPrompt()`、`ShowBlackAudio()`、`PlayShutdownGlitch()`、`PlayDeepSleep()`、`SetDirectionTarget(Transform, Camera)`、`SetVisible(bool)`、`NotifyReplayCompleted()`。
- 引用关系：调用 `HearthCompanionStatusPanelView`、`HearthCompanionDecisionPanelView`、`HearthCompanionDataStreamView`、`HearthCompanionHoldPrompt`、`HearthCompanionProjectionPanelView`、`HearthCompanionDirectionGuideView`、`HearthCompanionSpecialEffectsView`；可读取 `ViewSwitchController.CurrentMode`。
- 使用步骤：TV 或剧情流程进入机器人回放时调用 `ShowScene("17F01_01")`；长按完成后默认进入下一页或触发当前页特殊效果。
- 最小测试方法：Play Mode 中调用 `ShowScene(1)` 到 `ShowScene(14)`，每页内容应切换；第 8 页长按完成后出现强制关机故障，第 14 页长按完成后出现深眠黑场。
- 后续扩展：17F-02/17F-03 正式剧情只需要按剧情节点调用 `ShowScene(sceneId)`，并把角色 Transform 传给 `SetDirectionTarget`。

### HearthCompanionHudExclusiveMode.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudExclusiveMode.cs`
- 脚本作用：机器人 HUD 独占显示控制。进入 Companion 视角或机器人 HUD 自身可见时，自动压掉人类主角 HUD 的 CanvasGroup，避免机器人界面叠在人类 `HearthHudRoot` 常驻 UI 上。
- 挂载对象：`HearthCompanionHudRoot` 根物体，由 `HearthCompanionHudBuilder` 自动添加。
- 需要组件：同对象需要 `CanvasGroup`；场景中如果存在 `ViewSwitchController` 会自动绑定；默认自动查找名为 `HearthHudRoot` 的人类 HUD 根物体。
- Inspector 重点字段：`Suppress In Companion View`、`Suppress When This Hud Visible`、`Auto Suppress Root Names`、`Canvas Groups To Suppress`。如果以后人类 HUD 改名，把新名字加入 `Auto Suppress Root Names`。
- 公开接口：`SetViewSwitchController(ViewSwitchController controller)`、`RefreshTargets()`。
- 引用关系：读取 `ViewSwitchController.CurrentMode`；不会删除或 SetActive 关闭人类 HUD，只临时修改目标 CanvasGroup 的 `alpha / interactable / blocksRaycasts`，退出后恢复。
- 最小测试方法：Play Mode 中强制显示 `HearthCompanionHudRoot`，人类 HUD 左上工号、左下任务、右下地点都应消失；隐藏机器人 HUD 或回到 Human 视角后人类 HUD 恢复。
- 后续扩展：如果机器人视角后续改成独立 RenderTexture 或 VR Canvas，本脚本仍可作为“互斥 HUD 显示”入口。

### HearthCompanionStatusPanelView.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionStatusPanelView.cs`
- 脚本作用：显示左上状态面板，包括标题、状态行和页脚判断。
- 挂载对象：`HearthCompanionHudRoot/PersistentInfoLayer/StatusPanel`。
- 需要组件：子物体需要 `TMP_Text` 标题、状态行、页脚文本；需要一个 `Image` 作为 accent。
- Inspector 重点字段：`Title Text`、`Rows Text`、`Footer Text`、`Accent Image` 由 Builder 自动绑定。
- 公开接口：`Apply(HearthCompanionHudSceneData scene)`、`Clear()`、`Configure(...)`。
- 最小测试方法：切换不同 scene asset，左上状态行应同步变化。
- 后续扩展：如果要做逐行扫描动画，可在本脚本的 `Apply` 中接入，不需要改 Controller。

### HearthCompanionDecisionPanelView.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionDecisionPanelView.cs`
- 脚本作用：显示右上白色斜向信息区上方的系统决策面板，并做轻微淡入。
- 挂载对象：`HearthCompanionHudRoot/PersistentInfoLayer/DecisionPanel`。
- 需要组件：同对象需要 `CanvasGroup`；子物体需要 `TMP_Text` kicker、标题、正文和 accent `Image`。
- Inspector 重点字段：`Fade Seconds` 控制切页时的淡入速度。
- 公开接口：`Apply(HearthCompanionHudSceneData scene)`、`HideImmediate()`、`Configure(...)`。
- 最小测试方法：切换第 1 页和第 8 页，右上面板标题、正文和颜色应变化，文字不应被底图白色区域遮挡。
- 后续扩展：如果要改面板位置，优先改 `HearthCompanionHudBuilder.BuildDecisionPanel` 的 Rect；如果只改动画，调本脚本。

### HearthCompanionDataStreamView.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionDataStreamView.cs`
- 脚本作用：显示左下数据流标题和日志行。
- 挂载对象：`HearthCompanionHudRoot/PersistentInfoLayer/DataStreamView`。
- 需要组件：子物体需要 `TMP_Text` 标题和正文。
- 公开接口：`Apply(HearthCompanionHudSceneData scene)`、`SetLines(string[] lines)`、`AppendLine(string line)`、`Clear()`。
- 最小测试方法：调用 `AppendLine("0xTEST")` 后左下数据流应增加一行。
- 后续扩展：正式回放如果要逐条打字或滚动，只改本脚本，不改场景数据格式。

### HearthCompanionTriggerCardView.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionTriggerCardView.cs`
- 脚本作用：显示短暂触发卡片，支持延迟、停留和淡出。
- 挂载对象：`HearthCompanionHudRoot/TimedCardLayer/TriggerCardView`。
- 需要组件：同对象需要 `CanvasGroup`；子物体需要标题、正文和 accent。
- Inspector 重点字段：`Fade Seconds`；每页是否显示由 `HearthCompanionHudSceneData.Show Trigger Card` 控制。
- 公开接口：`Apply(scene)`、`ShowCard(title, body, accentColor, delay, seconds)`、`HideImmediate()`。
- 最小测试方法：在某个 scene asset 勾选 `Show Trigger Card` 并填写文字，显示该页后卡片应自动淡入、停留、淡出。
- 后续扩展：监控卡片、合成语音短句、临时异常提示都可以复用这个组件。

### HearthCompanionHoldPrompt.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHoldPrompt.cs`
- 脚本作用：机器人视角中央长按交互，默认按住 `E` 1.5 秒完成。
- 挂载对象：`HearthCompanionHudRoot/InteractionLayer/HoldPrompt`。
- 需要组件：同对象需要 `CanvasGroup`；子物体需要 prompt、按键、进度文字和 `Image` 进度条。
- Inspector 重点字段：`Hold Key`、`Hold Seconds` 由当前 scene asset 覆盖；`Allow Pointer Hold` 默认关闭，方便 PC 键盘优先。
- 公开接口：`Apply(scene)`、`SetVisible(bool)`、`ResetHold()`、`ForceComplete()`、`SetController(...)`；事件 `OnHoldCompleted`。
- 引用关系：完成后调用 `HearthCompanionHudController.ConfirmCurrentPrompt()`。
- 最小测试方法：第 1、2、5、8、11、12、14 页显示按钮；按住 E 进度条填满后触发下一页或特殊效果；其他页不显示按钮。
- 后续扩展：VR 射线或手柄确认可直接调用 `ForceComplete()`。

### HearthCompanionProjectionPanelView.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionProjectionPanelView.cs`
- 脚本作用：显示第 7 页家庭日志投影面板。
- 挂载对象：`HearthCompanionHudRoot/ProjectionLayer/ProjectionPanel`。
- 需要组件：同对象需要 `CanvasGroup`；子物体需要标题、正文和 accent。
- 公开接口：`Apply(scene)`、`SetVisible(bool)`、`Configure(...)`。
- 最小测试方法：显示 `17F02_04` 时投影面板出现；切到其他页时隐藏。
- 后续扩展：后续如果要把投影改成世界空间 UI，可保留本脚本作为数据入口，只换挂载位置和 Canvas。

### HearthCompanionDirectionGuideView.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionDirectionGuideView.cs`
- 脚本作用：第 11/12 页视线引导，当前先显示可移动 marker；绑定目标 Transform 后可跟随目标在屏幕上的方向。
- 挂载对象：`HearthCompanionHudRoot/InteractionLayer/DirectionGuide`。
- 需要组件：同对象需要 `CanvasGroup`；子物体需要 `TMP_Text` 和 marker `Image`。
- Inspector 重点字段：`View Camera`、`Target` 后续由剧情或机器人镜头系统注入；`Fallback Screen Position` 默认居中。
- 公开接口：`Apply(scene)`、`SetTarget(Transform newTarget, Camera newViewCamera)`、`SetVisible(bool)`。
- 最小测试方法：显示第 11/12 页时出现视线提示；调用 `SetTarget` 后 marker 应跟随目标屏幕位置。
- 后续扩展：如果要强制“看准目标才能按 E”，可在本脚本加入 dot product 判定，再通知 `HearthCompanionHoldPrompt` 是否可用。

### HearthCompanionSpecialEffectsView.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionSpecialEffectsView.cs`
- 脚本作用：控制机器人 HUD 的黑场音频、强制关机闪烁、深眠退化覆盖层。
- 挂载对象：`HearthCompanionHudRoot/SpecialEffectsLayer/SpecialEffectsView`。
- 需要组件：同对象需要 `CanvasGroup`；子物体需要黑色 overlay `Image`、标题、正文、状态文本和 pulse `Image`。
- Inspector 重点字段：`Fade Seconds`、`Use Unscaled Time`；特殊文案和持续时间来自 scene asset。
- 公开接口：`Apply(scene)`、`ShowBlackAudio(title, body, status)`、`PlayShutdownGlitch(title, body, status, duration)`、`PlayDeepSleep(title, body, status, duration)`、`HideImmediate()`。
- 最小测试方法：显示第 9 页自动进入黑场音频；第 8 页长按完成后闪烁至黑场；第 14 页长按完成后深眠退化。
- 后续扩展：如果后续接 URP 真后处理故障/模糊，保留这些公开方法作为剧情触发入口。

### HearthCompanionHudPreviewInput.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudPreviewInput.cs`
- 脚本作用：测试用输入，不参与正式剧情。
- 挂载对象：`HearthCompanionHudRoot` 根物体。
- Inspector 重点字段：`Preview Input Enabled`；`[` / `]` 或 `PageUp` / `PageDown` 切页，数字 1-9 直达前 9 页，`F10-F12` 直达第 10-12 页，`Enter` 模拟确认，反引号切换显隐。
- 公开接口：`SetPreviewInputEnabled(bool enabled)`。
- 最小测试方法：Play Mode 中用 `]` 从第 1 页循环浏览到第 14 页；按 `Enter` 可触发当前页确认逻辑。
- 后续扩展：正式构建或正式流程中可关闭 `Preview Input Enabled`，剧情系统改调 Controller 公开方法。

### HearthCompanionHudFlowBinder.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudFlowBinder.cs`
- 脚本作用：把现有 `MinLoopFlowController` 和 `ViewSwitchController` 连接到新机器人 HUD。
- 挂载对象：`HearthCompanionHudRoot` 根物体。
- 需要组件：同对象需要 `HearthCompanionHudController`；场景中需要 `MinLoopFlowController` 和 `ViewSwitchController`。
- Inspector 重点字段：`First Replay Scene Id` 默认 `17F01_01`；`Show Hud When Companion Replay Starts` 默认开启；`Hide Hud When Returning To Human` 默认开启。
- 公开接口：`BindReplayScene(string sceneId)`、`ShowScene(string sceneId)`、`NotifyReplayCompleted()`。
- 引用关系：监听 `MinLoopFlowController.StageChanged`；进入 `CompanionReplay` 时显示第一页；回到终端时隐藏；调用 `MinLoopFlowController.NotifyReplayCompleted()` 可回到处置选择。
- 最小测试方法：TV 终端触发当前最小循环回放并切到机器人视角时，HUD 自动显示 `17F01_01`。
- 后续扩展：三户正式剧情时，可由每户自己的流程脚本调用 `BindReplayScene` 或直接调用 `ShowScene`。

### HearthCompanionHudBuilder.cs

- 路径：`Assets/Editor/HearthCompanionHudBuilder.cs`
- 脚本作用：Editor 构建工具，生成机器人 HUD 数据资产、导入底图 Sprite、创建 `HearthCompanionHudRoot.prefab`，并可应用到当前场景。
- 挂载对象：不需要挂载，这是 Editor 菜单工具。
- 菜单入口：`Tools / Hearth / HUD / Rebuild Companion Unit HUD Prefab`、`Tools / Hearth / HUD / Rebuild And Apply Companion Unit HUD To Scene`、`Tools / Hearth / HUD / Regenerate Companion Unit Scene Data Defaults`。
- 自动生成内容：
  - `Assets/Data/HearthHud/Companion/CompanionScene_01_17F01_01.asset` 到 `CompanionScene_14_17F03_05.asset`。
  - `Assets/Prefabs/UI/HearthHud/Companion/HearthCompanionHudRoot.prefab`。
  - `Assets/Art/UI/HearthHud/Companion/CompanionRobotFrame.png` 设置为 UI Sprite。
- 维护规则：普通重建 Prefab 不覆盖已存在的 scene asset；只有执行 `Regenerate Companion Unit Scene Data Defaults` 才重置默认文案。
- 本次修正：布局坐标按 PPT 20 x 11.25 inch 的点坐标换算到 1920x1080，统一乘以 `4/3`；右上决策区和左上状态区不再额外绘制矩形面板，而是作为透明文字层落在底图框架内；中间长按按钮拆成提示框、按键文字和进度条，避免文字裁切。
- 最小测试方法：执行 `Rebuild Companion Unit HUD Prefab` 后，Console 无 Error，Prefab 和 14 个数据资产存在。
- 当前验证记录：已通过 Unity MCP 执行构建菜单；编译/重建 Console 新增 Error 为 0。视觉截图检查确认人类 HUD 已被压掉，右上决策文字位于切角框内，中间按钮不再裁切。进入/退出 Play Mode 时仍会显示场景旧问题：`Door` missing script 和 `BoxCollider does not support negative scale or size`。
- 后续扩展：如果要根据新版 PPT 自动导入文字，可在本 Builder 增加 PPT/XML 解析步骤，但保留现有 ScriptableObject 作为最终可维护数据层。

## 2026-06-30：17F01 最小游戏循环正式接入

本次更新把 17F01 回放从旧的固定热区/调试浏览流程，接入为“看向 17F01 TV 终端 -> 进入终端 -> 请求机器人回放 -> 机器人视角播放卧室字幕 -> 字幕后等待 1.5 秒 -> 看向小男孩并在极近距离长按 E -> 当前位置锁住移动并进入安抚 -> 客厅观察父母 -> 返回终端 A/B 处置”的正式链路。

### HearthCompanion17F01ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F01ReplayController.cs`
- 脚本作用：17F01 专用机器人回放流程导演，负责三段机器人视角：儿童房监控、床边安抚、客厅观察。
- 挂载对象：`MIN_LOOP_ROOT/FlowManagers/HearthCompanion17F01ReplayController`。
- 需要组件：场景中需要正式的 `Player/Robot Controller`、`HearthCompanionHudRoot`、`MinLoopFlowController`、`ViewSwitchController`、字幕播放器；小男孩对象上需要 `HearthCompanionReplayInteractable`。
- Inspector 重点字段：
  - `Flow Controller`：拖 `MIN_LOOP_ROOT/FlowManagers/MinLoopFlowController`。
  - `View Switch Controller`：拖 `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`。
  - `Companion Hud`：拖 `HearthCompanionHudRoot`。
  - `Robot Root/Camera/Movement/Look/Interaction/Rigidbody`：都使用唯一正式对象 `Player/Robot Controller` 上的对应组件。
  - `Child Room Start Anchor`、`Living Room Start Anchor`：分别是从正式 `Robot Controller`、`Robot Controller (1)` 复制出来的锚点。
  - `Bedside Interact Anchor`、`Bedside Path Points`：当前原地安抚流程不再使用，保留为以后如果恢复自动移动时的预留字段。
  - `Prompt Delay After Bedroom Prelude`：卧室字幕全部播放完后，再等待多久才允许显示小男孩交互提示；当前默认 1.5 秒。
  - `Bedroom/Bedside/Living Room Scene Id`：默认 `17F01_01`、`17F01_02`、`17F01_03`，对应机器人 HUD 的三页正式场景。
  - `Subtitle Lines`：英文对白和字幕 cue，可直接改文本、等待时间，后续可扩展 AudioClip。
- 公开接口：`BeginReplay()`、`BeginReplay(MinLoopFlowController owner)`、`CompleteCurrentStep()`、`ReturnToTerminalForDisposition()`、`CancelReplay()`、`SetReferences(...)`、`SetRobotRig(...)`、`SetAnchors(...)`、`SetApproachInteractable(...)`。
- 引用关系：由 `MinLoopFlowController.RequestReplayFromTerminal()` 启动；通过 `HearthCompanionHudController.ShowScene()` 控制机器人 HUD；回放结束时调用 `MinLoopFlowController.NotifyReplayCompleted()` 返回终端处置页。
- 使用步骤：
  1. 保留一个正式 `Player/Robot Controller`；`Robot Controller (1)` 只作为客厅位置参考，不作为可控制角色；`Robot Controller (2)` 已不再必需。
  2. 执行菜单 `Tools / Hearth / Replay / Apply 17F01 Minimal Loop Setup` 自动生成/绑定锚点。
  3. 在 Play Mode 中从人类视角打开 17F01 终端并请求回放。
- 最小测试方法：进入回放后，机器人 HUD 只显示 `17F01_01`；卧室字幕播放完并等待约 1.5 秒后，看向小男孩且距离/角度满足才出现中央长按提示；长按后机器人停在当前位置，不能移动但可以转视角；安抚完成后跳到客厅并播放父母对白；对白结束后回到终端 A/B 页。
- 后续扩展：如果导入正式动画，可把本脚本里的 `HearthActorPosePreset` 替换为 Animator Clip 或 Timeline；如果要更真实移动，可把路径点移动替换为 NavMeshAgent，但保留当前公开方法不变。

### HearthCompanionReplayInteractable.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanionReplayInteractable.cs`
- 脚本作用：17F01 回放中的条件交互判定，不走普通 `IInteractable`，而是由回放流程主动查询“机器人是否正在看向小男孩、距离是否够近、是否站在床的正确一侧”。
- 挂载对象：当前挂在 `Capsule Mesh (1)`，这个物体作为覆盖小男孩身体的隐形交互胶囊体。
- 需要组件：`Capsule Mesh (1)` 需要 Collider；当前由绑定工具自动添加 `CapsuleCollider`，设为 `Is Trigger`，并隐藏 Renderer。
- Inspector 重点字段：
  - `Focus Target` / `Raycast Target Root`：拖 `Capsule Mesh (1)`。
  - `Require Center Ray Hit`：开启后必须由机器人相机屏幕中心射线命中胶囊体才显示交互提示。
  - `Max Distance`：允许交互射线长度，当前自动绑定为 `0.71`，约等于旧版 `1.42` 的二分之一、上一版 `0.355` 的两倍。
  - `Max View Angle`：关闭中心射线模式时才作为角度兜底；当前保留为 8 度。
  - `Use Allowed Side Gate`：限制只能在床的一侧触发，避免床尾或另一边误触发。
  - `Allowed Side Local Normal` 和 `Min Allowed Side Dot`：用来微调“哪一侧算正确的一侧”。
  - `Replay Controller`：拖 `HearthCompanion17F01ReplayController`。
- 公开接口：`SetAvailable(bool)`、`CanInteract(Transform actor, Camera camera)`、`NotifyConfirmed()`、`Configure(...)`。
- 引用关系：`HearthCompanion17F01ReplayController` 在 `LookAtBoyPrompt` 阶段每隔短时间调用 `CanInteract`，只有返回 true 才显示 HUD 长按提示。
- 最小测试方法：回放开始后不看小男孩不显示提示；卧室字幕未播完时也不显示提示；字幕结束后等待约 1.5 秒，看向小男孩并站在极近距离允许侧才显示提示；离开目标或转开视角提示消失。
- 后续扩展：17F02/17F03 可以复用这个脚本作为“看向某个 NPC/物件才显示交互”的基础。

### HearthActorPosePreset.cs

- 路径：`Assets/Scripts/MinLoop/HearthActorPosePreset.cs`
- 脚本作用：在没有正式睡姿/坐姿动画资源时，用可维护的静态姿势 preset 临时控制 NPC 根物体位置、角度和缩放。
- 挂载对象：当前自动挂到小男孩、母亲、父亲模型根物体上。
- 需要组件：不需要额外组件；它只改指定 `Transform` 的 local position / rotation / scale。
- Inspector 重点字段：
  - `Default Pose Root`：默认是脚本所在对象，也可以拖某个骨骼或模型子节点。
  - `Poses`：每个 pose 有 `Id`、`Root`、`Local Position`、`Local Euler Angles`、`Local Scale`。
  - `Smooth Apply`、`Default Duration`、`Curve`：控制姿势切换是否有平滑过渡。
  - `Pause Animators On Apply`：默认开启，应用姿势时会暂停该角色下的 Animator，避免资产包默认跑步/跳跃动作抢走姿势。
  - `Animators To Pause`：可手动拖该角色身上的 Animator；为空时运行时会自动查找子物体 Animator。
- 公开接口：`ApplyPose(string poseId)`、`ApplyPose(string poseId, float duration)`、`CaptureCurrentPose(string poseId)`、`CaptureFirstPoseFromCurrent()`、`SetDefaultPoseRoot(Transform root)`、`SetAnimatorsPaused(bool paused)`。
- 引用关系：由 `HearthCompanion17F01ReplayController` 调用；当前预设 id 使用 `Sleep`、`Awake`、`Comforted`、`Sitting`。
- 使用步骤：在 Scene 里手动摆好 NPC 姿势后，选中对象，在组件菜单里用 `Capture Current Pose As First Pose` 或从代码调用 `CaptureCurrentPose` 保存。
- 最小测试方法：调用 `ApplyPose("Sleep")` 小男孩应回到保存的睡姿；调用 `ApplyPose("Sitting")` 父母应回到当前保存的坐姿。
- 当前限制：本脚本当前主要记录根物体 Transform，并负责暂停 Animator；如果你手动调整的是骨骼姿势，最稳的方法是直接在 Scene 里保存这些骨骼 Transform。后续如果需要运行时切换多套完整骨骼姿势，应扩展为骨骼 Pose 记录，或改用正式 AnimationClip。
- 后续扩展：导入正式骨骼动画后，可以保留本脚本做临时占位，也可以由流程脚本改为触发 Animator 参数。

### Hearth17F01MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F01MinimalLoopBinder.cs`
- 脚本作用：Editor 自动绑定工具，把当前场景整理成 17F01 最小游戏循环所需结构。
- 挂载对象：不需要挂载，是 Unity Editor 菜单工具。
- 菜单入口：`Tools / Hearth / Replay / Apply 17F01 Minimal Loop Setup`。
- 自动执行内容：
  - 创建或更新 `MIN_LOOP_ROOT/Anchors/Anchor_Robot_17F01_ChildRoomStart`、`Anchor_Robot_17F01_LivingRoomStart`；只有场景中仍存在 `Robot Controller (2)` 时，才更新床边旧锚点和旧路径点。
  - 把 `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController` 绑定到 `Player/Person Controller` 和唯一正式 `Player/Robot Controller`。
  - 把 `Terminal_17F01` 的 `Min Loop Flow Controller` 和 `View Switch Controller` 统一指向正式流程管理器。
  - 关闭 `HearthCompanionHudPreviewInput.previewInputEnabled` 和 `HearthCompanionHudController.showStartingSceneOnStart`，避免正式流程里浏览全部机器人 UI。
  - 把 `Robot Controller (1)` 的 Camera、AudioListener、移动、视角、交互等运行时组件禁用，让它只作为客厅锚点来源；`Robot Controller (2)` 已不再要求存在。
  - 给小男孩、父母补 `HearthActorPosePreset`；把 `Capsule Mesh (1)` 配置为隐形交互胶囊体，并补 `HearthCompanionReplayInteractable`。
  - 禁用 `MinLoopObjectivePresenter`、`MinLoopTrustPresenter`、`MinLoopRobotHudPresenter`，避免旧占位 UI 的黑块、信任条、机器人状态面板盖住正式 HUD。
  - 配置 `MinLoopSubtitlePlayer` 为纯白居中字幕样式：speaker 居中在上，字幕居中在下，宽度约为屏幕 2/3，不再生成黑色对话框。
  - 把 17F01 小男孩交互距离设为 `0.71`，即上一版距离的两倍；机器人不再自动走向床边。
  - 禁用 `little_boy_B`、`casual_Female_K/G`、`casual_Male_K/G` 上的 Animator，避免默认动作库自动跑步/跳跃；后续导入正式动画时再手动开启。
  - 配置信任度规则：初始 0，A +1，B -1，范围 -3 到 3。
  - 标记并保存当前打开的场景，避免自动绑定只停留在临时 Inspector 状态。
- 使用步骤：当你调整了 `Robot Controller (1)` 的位置/朝向后，再执行一次这个菜单，它会重新复制客厅锚点。`Robot Controller (2)` 删除后不需要补回。
- 最小测试方法：执行菜单后 Console 出现 `[Hearth17F01MinimalLoopBinder] 17F01 minimal loop setup applied.`；Unity Console 无新增 Error。
- 后续扩展：17F02/17F03 建议新建对应 Binder，复用“正式机器人 + 多个锚点 + 专用 replay controller”的结构。

### MinLoopFlowController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopFlowController.cs`
- 本次修改：增加 `Tv Terminal Controller`、`Companion 17F01 Replay Controller`、`Use Companion 17F01 Replay Controller` 字段，让 TV 终端请求回放时优先进入 17F01 新机器人流程，而不是旧 `ReplaySequenceController`。
- 使用方式变化：
  - `RequestReplayFromTerminal()` 仍然是终端触发回放的统一入口。
  - `ReturnToTerminalForDisposition()` 现在会优先调用 `HearthTvTerminalController.ShowPostReplayChoicePage()` 显示 TV 终端 A/B 页。
  - `ChooseDispositionA()` / `ChooseDispositionB()` 仍是信任度 A/B 处置入口。
  - 当 `Tv Terminal Controller` 存在时，A/B 选择后只更新正式 TV 流程阶段和信任度，不再弹出旧 `MinLoopTerminalPresenter` 的占位结果页。
- Inspector 重点字段：`Tv Terminal Controller` 拖 `17F/ROOM1/TV (3)/MonitorCanvas/Terminal_17F01`；`Companion 17F01 Replay Controller` 拖 `MIN_LOOP_ROOT/FlowManagers/HearthCompanion17F01ReplayController`。
- 最小测试方法：TV 终端选择回放后，`Current Stage` 应进入 `CompanionReplay`，然后由 17F01 新流程接管；回放结束后进入 `DispositionChoice`。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`
- 本次修改：增加 `Route Choices To Min Loop`，终端 A/B 页按 Space 确认后会直接调用 `MinLoopFlowController.ChooseDispositionA/B()`，不再只触发 UI 自己的 UnityEvent。
- 使用方式变化：17F01 终端的 `Min Loop Flow Controller` 必须拖正式 `MinLoopFlowController`；`Route Choices To Min Loop` 保持开启。
- 保留功能：0.5 秒终端视角过渡、开机闪烁、Tab/左右/上下/Space/Esc 导航、回放完成后 A/B 页逻辑不变。
- 最小测试方法：机器人回放结束后终端显示 A/B 页；选择 A 后信任度 +1，选择 B 后信任度 -1。

### HearthCompanionHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudController.cs`
- 本次修改：增加 `Auto Advance On Hold Prompt` 和公开方法 `SetAutoAdvanceOnHoldPrompt(bool)`、`SetHoldPromptVisible(bool)`、`ResetHoldPrompt()`。
- 使用方式变化：正式 17F01 回放中该字段应关闭，由 `HearthCompanion17F01ReplayController` 决定长按完成后推进到哪一步；预览模式或独立测试时可以打开自动推进。
- Inspector 重点字段：`Show Starting Scene On Start` 和 `Auto Advance On Hold Prompt` 在正式流程里都应关闭。
- 最小测试方法：正式回放中长按不会让机器人 HUD 自己乱跳下一页，而是只触发当前流程阶段。

### MinLoopSubtitlePlayer.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopSubtitlePlayer.cs`
- 本次修改：fallback 字幕从“底部黑色对话框”改为“屏幕中部纯白居中字幕”。
- 挂载对象：`MIN_LOOP_ROOT/UI/MinLoopSubtitlePlayer`。
- Inspector 重点字段：`Use Clean Centered Style` 保持开启；`Subtitle Width Fraction` 当前为 `0.66`；`Subtitle Center Y` 当前为 `0.46`；`Clean Speaker Font Size` / `Clean Body Font Size` 可调整文字大小。
- 显示规则：说话人名称在上方居中，正文在下方居中；正文超过 2/3 屏幕宽度会自动换行，换行后仍居中；面板 Image 透明且不拦截鼠标/射线。
- 公开接口不变：`PlaySequence(...)`、`PlayLines(...)`、`ShowLine(...)`、`Hide()`、`Stop()`。
- 最小测试方法：播放任意字幕 cue 时，屏幕中间只有白色文字，没有黑色底框或左侧 speaker 对话框。

### InteractionFeedbackController.cs

- 路径：`Assets/Scripts/Interactions/InteractionFeedbackController.cs`
- 本次修改：内部修复。`MaterialPropertyBlock` 不再在字段初始化时创建，改为 `Awake`/播放反馈时按需创建，避免 Unity 报 `CreateImpl is not allowed to be called from a MonoBehaviour constructor`。
- 使用方式无变化：原来挂在反馈物体上的灯光、材质、音效、事件引用都不用重拖。
- 最小测试方法：进入 Unity 后 Console 不再因为 `Feedback_NextResidentGuide`、`Feedback_ReplayRequest`、`Feedback_AccessCard`、`Feedback_TerminalOpen` 等对象连续报该异常。
## 2026-06-30 黑框清理、机器人 HUD 时序与 17F01 交互修正

本节覆盖前面所有关于 `MinLoopObjectivePresenter`、`MinLoopRobotHudPresenter`、`MinLoopTrustPresenter` 的旧说明：这三个旧占位 UI 已退役并从项目中删除，不再创建、不再挂载，也不再作为 17F01 最小游戏循环的可选 UI。玩家第一人称信息由 `HearthFirstPersonHudController` / `HearthHudRoot` 负责；机器人陪伴单元信息由 `HearthCompanionHudController` / `HearthCompanionHudRoot` 负责；信任度由正式 HUD 和 TV 终端流程显示。

### 已退役脚本

- `Assets/Scripts/MinLoop/MinLoopObjectivePresenter.cs`：旧左上目标黑底提示，已删除。以后不要再挂到 `MIN_LOOP_ROOT/UI`，也不要用它显示当前目标。
- `Assets/Scripts/MinLoop/MinLoopRobotHudPresenter.cs`：旧机器人视角占位 HUD，已删除。机器人视角统一使用 `HearthCompanionHudRoot`。
- `Assets/Scripts/MinLoop/MinLoopTrustPresenter.cs`：旧信任度黑底/条状占位 UI，已删除。信任变化由 `TrustStateController` 继续计算，并交给正式 HUD 或终端页面呈现。

### HearthCompanionHudTypes.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudTypes.cs`
- 本次修改：新增 `HearthCompanionTimedCue` 数据结构，用于机器人 HUD 上短时间出现的监控卡片。
- 挂载对象：不用挂载，作为数据类型被其他脚本引用。
- 公开能力：`delaySeconds`、`visibleSeconds`、`title`、`body` 可在 `HearthCompanionHudSceneData` 中配置。
- 使用方式：无独立 Inspector；通过机器人 HUD 场景数据 asset 间接维护。
- 最小测试：17F01 机器人 HUD 第 1 页左上监控卡显示约 3.5 秒后淡出。
- 后续扩展：17F02/17F03 如果需要心率、异常行为、声纹等短时弹出信息，继续往 timed cue 列表里加条目。

### HearthCompanionHudSceneData.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudSceneData.cs`
- 本次修改：新增 `Timed Cues` 列表，并在 `Configure(...)` 中支持写入。
- 挂载对象：不用挂载，是 ScriptableObject 数据资产，位置在 `Assets/Data/HearthHud/Companion/`。
- Inspector 重点字段：`Timed Cues` 每项填写延迟、持续时间、标题和正文。
- 关联脚本：由 `HearthCompanionHudController` 读取，由 `HearthCompanionTriggerCardView` 播放，由 `HearthCompanionHudBuilder` 生成默认数据。
- 使用步骤：执行 `Tools / Hearth / HUD / Regenerate Companion Unit Scene Data Defaults` 可重置默认 timed cue；手改数据资产则可微调每页时序。
- 最小测试：`CompanionScene_01_17F01_01` 有 1 条 timed cue；`CompanionScene_02_17F01_02` 有 4 条 timed cue；`CompanionScene_03_17F01_03` 有 1 条 timed cue。
- 后续扩展：如果接入正式剧情系统，可以只改数据资产，不改 HUD 脚本。

### HearthCompanionTriggerCardView.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionTriggerCardView.cs`
- 本次修改：从单张常驻/一次性卡片升级为 timed cue 序列播放器；没有 timed cue 时仍兼容旧的 `Show Trigger Card` 字段。
- 挂载对象：`HearthCompanionHudRoot/TimedCardLayer/TriggerCardView`，由 Builder 自动创建。
- 必需组件：`CanvasGroup`、两个 TMP Text、可选强调线 Image。
- Inspector 重点字段：由 Builder 自动绑定；一般不用手拖。
- 公开方法：`Apply(HearthCompanionHudSceneData scene)`、`ShowCueSequence(...)`、`HideImmediate()`。
- 关联脚本：由 `HearthCompanionHudController.ApplyScene()` 调用。
- 使用步骤：在对应 `HearthCompanionHudSceneData` 中配置 `Timed Cues`，切换到该机器人 HUD 场景后自动播放。
- 最小测试：Play Mode 显示 `17F01_01` 后等待 4-5 秒，`TriggerCardView` 的 CanvasGroup alpha 应为 0。
- 后续扩展：可把 `visibleSeconds` 调大用于长时间诊断信息，但默认监控卡不应常驻。

### HearthCompanionHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudController.cs`
- 历史阶段曾在应用场景时清空旧 StatusPanel；该行为已被 2026-07-27 V2 最终实现覆盖：
  当前 V2 Status Panel 显示 SceneData 常驻状态，并在 Trigger Card 可见期间临时隐藏。
  机器人 HUD 仍通过 `visibleOnlyInCompanionView` 限制只在 Companion 视角显示。
- 挂载对象：`HearthCompanionHudRoot`。
- 必需组件：`Canvas`、`CanvasScaler`、`GraphicRaycaster`、`CanvasGroup`、`AudioSource`。
- Inspector 重点字段：`Scenes`、`Root Canvas Group`、`View Switch Controller`、`Decision Panel View`、`Data Stream View`、`Trigger Card View`、`Hold Prompt`、`Projection Panel View`、音效 Clip。
- 公开方法：`ShowScene(string)`、`ShowScene(int)`、`AdvanceScene()`、`SetVisible(bool)`、`SetHoldPromptVisible(bool)`、`ConfirmCurrentPrompt()`、`SetDirectionTarget(...)`。
- 关联脚本：由 `HearthCompanion17F01ReplayController` 和 `HearthCompanionHudFlowBinder` 调用。
- 使用步骤：正式流程中不要打开 `Show Starting Scene On Start`；由剧情流程调用 `ShowScene("17F01_01")` 等。
- 最小测试：Human 视角下 root alpha 应为 0；Companion 视角或回放流程中才显示。
- 后续扩展：VR 输入或 Timeline 可以直接调用公开方法，不需要重写 HUD。

### HearthCompanionHudBuilder.cs

- 路径：`Assets/Editor/HearthCompanionHudBuilder.cs`
- 本次修改：重建机器人 HUD prefab 时不再生成左上常驻 `StatusPanel`；右上决策区、左下数据流、左上 timed card 去掉大面积半透明黑底，只保留文字和细线；默认场景数据会生成 timed cue。
- 挂载对象：不用挂载，是 Unity Editor 工具。
- 菜单入口：`Tools / Hearth / HUD / Rebuild Companion Unit HUD Prefab`、`Rebuild And Apply Companion Unit HUD To Scene`、`Regenerate Companion Unit Scene Data Defaults`。
- 自动生成内容：`HearthCompanionHudRoot.prefab`、14 个 `HearthCompanionHudSceneData` 资产、场景中的 `HearthCompanionHudRoot` 实例。
- 关联脚本：生成并绑定 `HearthCompanionHudController`、`HearthCompanionTriggerCardView`、`HearthCompanionHoldPrompt`、`HearthCompanionHudExclusiveMode` 等。
- 使用步骤：改了 Builder 或底图后，先执行重建 prefab，再执行应用到场景；若想重置默认文案，再执行 Regenerate Scene Data。
- 最小测试：重建后 `HearthCompanionHudRoot/PersistentInfoLayer/StatusPanel` 不存在，`TriggerCardView` 自身没有 Image 黑底。
- 后续扩展：如果以后重新调整 PPT 位置，优先改 Builder 中的 Rect，而不是在运行时脚本里硬写。

### HearthFirstPersonHudBuilder.cs

- 路径：`Assets/Editor/HearthFirstPersonHudBuilder.cs`
- 本次修改：常驻玩家 HUD 生成时会过滤大面积深色填充，避免 PPT 中用于黑背景的形状被带进游戏视野；警告、设置、确认框等真正需要底板的页面保留。
- 挂载对象：不用挂载，是 Unity Editor 工具。
- 菜单入口：`Tools / Hearth / HUD / Rebuild First Person HUD From Layout` 或当前项目使用的第一人称 HUD 重建入口。
- 关联脚本：生成 `HearthHudRoot` 和 `HearthFirstPersonHudController` 使用的页面 prefab。
- 使用步骤：更新 PPT/layout 后重建玩家 HUD；若某个页面确实需要深色面板，应让该页不是常驻 HUD，或在 Builder 的过滤规则里单独保留。
- 最小测试：常驻 HUD 左上/右上/左下不再出现截图式深色大块，只剩文字、线框和几何细节。
- 后续扩展：如要精修某个形状，改 `ShouldSkipFill(...)` 或对应页面模板。

### MinLoopSubtitlePlayer.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopSubtitlePlayer.cs`
- 本次修改：字幕从黑色对话框改为纯白居中字幕；说话人名称和正文分成两个独立锚区。
- 挂载对象：`MIN_LOOP_ROOT/UI/MinLoopSubtitlePlayer`。
- 必需组件：可自动创建 fallback UI；如果已有 UI，可手动拖 `Subtitle Panel`、`Speaker Text`、`Body Text`、`CanvasGroup`。
- Inspector 重点字段：`Use Clean Centered Style` 开启；`Subtitle Width Fraction` 默认 0.66；`Speaker Center Y` 默认 0.31；`Body Center Y` 默认 0.22；字体大小和颜色可调。数值越小越靠近屏幕下方。
- 公开方法：`PlaySequence(...)`、`PlayLines(...)`、`ShowLine(...)`、`Hide()`、`Stop()`，使用方式无变化。
- 关联脚本：由 `HearthCompanion17F01ReplayController`、旧 `ReplaySequenceController` 调用。
- 使用步骤：在字幕 cue 中写 speaker 和 text；播放时屏幕中部显示 speaker，偏下显示正文。
- 最小测试：播放字幕时没有任何黑底 Image，正文自动在约屏幕 2/3 宽度内居中换行。
- 后续扩展：接语音后，把 AudioClip 加到字幕 cue 播放逻辑即可。

### HearthCompanionReplayInteractable.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanionReplayInteractable.cs`
- 本次修改：增加 Scene 视图 Gizmo，可在编辑器里看到交互胶囊范围；Game 里 Renderer 仍关闭，保持隐形判定；中心射线现在会检查射线路径上的所有命中，只要命中 `Capsule Mesh (1)` 就通过，不再因为床/被子等碰撞体先被打到而失败。
- 挂载对象：`Capsule Mesh (1)`，当前只保留这一处；`little_boy_B` 上重复脚本已移除。
- 必需组件：`CapsuleCollider`，`enabled = true`，`Is Trigger = true`；Renderer 可存在但应关闭。
- Inspector 重点字段：`Require Center Ray Hit` 开启；`Max Distance` 当前 0.71；`Replay Controller` 指向 `HearthCompanion17F01ReplayController`；Gizmo 颜色可调。
- 公开方法：`SetAvailable(bool)`、`CanInteract(Transform actor, Camera camera)`、`NotifyConfirmed()`、`Configure(...)`。
- 关联脚本：由 `HearthCompanion17F01ReplayController` 在看向小男孩阶段查询。
- 使用步骤：调整胶囊体大小和位置，让它覆盖小男孩身体；玩家中心射线打到胶囊且距离/侧面条件满足时才显示 E 提示。
- 最小测试：看向 `Capsule Mesh (1)` 且距离合适才出现交互提示；看偏、过远或站错侧不出现。
- 后续扩展：其他 NPC 或物体也可以复用这个脚本做“必须看准目标才触发”的交互。

### Hearth17F01MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F01MinimalLoopBinder.cs`
- 本次修改：重新应用 17F01 最小循环时会删除旧三件套场景对象，移除小男孩身上重复的交互脚本，只保留 `Capsule Mesh (1)`；如果 `Capsule Mesh (1)` 缺失会自动创建，如果它不在 `little_boy_B` 层级下会自动归到小男孩下面；正式 `Player/Robot Controller` 和两个参考点 `Robot Controller (1)` / `(2)` 的移动数值都会统一写为 speed 1.625、runSpeed 4.095、`canRun=false`，但运行时只把正式 `Player/Robot Controller` 接到流程；自动走床边速度写为 0.45；字幕位置写入新的纯白居中样式。
- 挂载对象：不用挂载，是 Unity Editor 工具。
- 菜单入口：`Tools / Hearth / Replay / Apply 17F01 Minimal Loop Setup`。
- 自动处理对象：`Player/Robot Controller`、`Robot Controller (1)`、`Robot Controller (2)`、`Capsule Mesh (1)`、`little_boy_B`、`HearthCompanionHudRoot`、`MIN_LOOP_ROOT/FlowManagers/HearthCompanion17F01ReplayController`、`MIN_LOOP_ROOT/UI/MinLoopSubtitlePlayer`。
- 关联脚本：配置 `HearthCompanion17F01ReplayController`、`HearthCompanionReplayInteractable`、`HearthCompanionHudController`、`HearthCompanionHudFlowBinder`、`HearthCompanionHudExclusiveMode`、`TrustStateController`。
- 使用步骤：每次你调整 `Robot Controller (1)` / `(2)` 作为锚点位置后，重新执行该菜单；它会复制锚点并重绑正式机器人。以后不要直接切换三个可操作机器人，只用一份正式机器人，另外两个保留为点位模板。
- 最小测试：菜单执行后 Console 输出 setup applied；`Capsule Mesh (1)` active、位于 `little_boy_B` 层级下、Collider trigger、Renderer off；`little_boy_B` 上交互脚本数量为 0；三份 Robot Controller 的 movement 数值都显示为 1.625/4.095，但只有正式 `Player/Robot Controller` 参与流程。
- 后续扩展：17F02/17F03 建议复制该思路做专用 Binder，不要再复活旧 Presenter。

### MinLoopSceneSkeletonCreator.cs / MinLoopSceneAutoBinder.cs / MinLoopSceneValidator.cs / MinLoopSetupChecklistWindow.cs

- 路径：
  - `Assets/Editor/MinLoopSceneSkeletonCreator.cs`
  - `Assets/Editor/MinLoopSceneAutoBinder.cs`
  - `Assets/Scripts/MinLoop/MinLoopSceneValidator.cs`
  - `Assets/Editor/MinLoopSetupChecklistWindow.cs`
- 本次修改：工具链不再创建、自动绑定或要求 `MinLoopObjectivePresenter`、`MinLoopRobotHudPresenter`、`MinLoopTrustPresenter`；检查项改为推荐正式 `HearthFirstPersonHudController` 和 `HearthCompanionHudController`。
- 挂载对象：Skeleton/AutoBinder/Checklist 是 Editor 工具；Validator 仍可挂在 `MIN_LOOP_ROOT/FlowManagers/MinLoopSceneValidator`。
- 使用方式：旧 UI 三件套相关步骤作废；创建骨架后需要放入正式 HUD root。
- 最小测试：运行自动绑定或检查窗口时，不应再提示缺少旧三件套 Presenter。
- 后续扩展：后续如果新增正式 UI 检查项，继续在 Validator 里检查正式脚本，不回到占位 Presenter。

### 当前场景验证记录

- 已通过 Unity MCP 重新刷新脚本、重建 `HearthCompanionHudRoot` / `HearthHudRoot`，并执行 `Tools / Hearth / Replay / Apply 17F01 Minimal Loop Setup` 对当前场景重新绑定。
- 场景中旧对象 `MinLoopObjectivePresenter`、`MinLoopTrustPresenter`、`MinLoopRobotHudPresenter`、`Min Loop Objective`、`Min Loop Trust`、`Min Loop Robot HUD Panel`、`Robot HUD Panel` 均已不存在。
- `Capsule Mesh (1)` 当前 active，`CapsuleCollider enabled=true / trigger=true`，Renderer 关闭，`HearthCompanionReplayInteractable` 存在且启用；`little_boy_B` 上重复的 `HearthCompanionReplayInteractable` 为 0。
- `Player/Robot Controller` 当前 `FirstPersonMovement.speed = 1.625`，`runSpeed = 4.095`，`canRun = false`；`HearthCompanion17F01ReplayController.autoMoveSpeed = 0.45`。
- `HearthCompanionHudRoot` 当前没有 `PersistentInfoLayer/StatusPanel`，`TriggerCardView` 本体没有 Image 黑底；17F01 三个机器人场景数据 timed cue 数量分别为 1 / 4 / 1。
- Play Mode 下显示 `17F01_01` 后等待 5 秒，`TriggerCardView alpha = 0`，HUD/MinLoop 根节点下可见深色 UI Image 数量为 0。
- 视觉截图已保存：`Assets/Screenshots/hearth_hud_blackbox_check.png`，用于确认之前圈出的黑框不再出现。
- Unity Console 没有新增 C# 编译错误；仍存在旧场景问题：`Door` missing script 和 `BoxCollider does not support negative scale or size`。

### 2026-07-01 文件级补充修正

- 本次修改脚本：`Hearth17F01MinimalLoopBinder.cs`、`HearthCompanionReplayInteractable.cs`、`MinLoopSubtitlePlayer.cs`。
- 使用方式变化：
  - `Tools / Hearth / Replay / Apply 17F01 Minimal Loop Setup` 会把 `Capsule Mesh (1)` 自动归到 `little_boy_B` 下；若场景里找不到该对象，会自动新建一个隐形 Capsule 判定体。
  - 三个机器人对象的移动数值会统一写入，避免 Inspector 中看起来速度不一致；正式流程仍只使用 `Player/Robot Controller`，`Robot Controller (1)` 和 `Robot Controller (2)` 只作为点位/视角模板。
  - 字幕位置默认进一步下移：`Speaker Center Y = 0.31`，`Body Center Y = 0.22`。如果要手调，选中 `MIN_LOOP_ROOT/UI/MinLoopSubtitlePlayer` 后改这两个字段；数值越小越往下。
- 交互判定变化：`HearthCompanionReplayInteractable` 的中心射线会检查全部命中，不再只看第一下命中，因此床、被子或其他碰撞体在前面时，只要射线路径上命中了 `Capsule Mesh (1)`，仍可出现交互提示。
- 本次验证：已从 `Assets/Scenes/SampleScene.unity` 文件确认 `Capsule Mesh (1)` 尚存在且 active，Collider 为 trigger，但 Renderer 关闭并且当前是根物体；需要在 Unity 中执行一次 17F01 绑定菜单让层级和引用实际落到场景。

### 2026-07-01：17F01 原地安抚流程修正

- 本次修改脚本：`HearthCompanion17F01ReplayController.cs`、`Hearth17F01MinimalLoopBinder.cs`、`HearthCompanionHudBuilder.cs`、`脚本使用说明总表.md`。
- 使用方式变化：
  - `Robot Controller (2)` 不再是必需对象；删除后不需要补回。
  - 小男孩卧室段会先播放 `Bedroom Prelude Lines`，字幕全部结束后再等待 `Prompt Delay After Bedroom Prelude = 1.5` 秒，然后才允许显示 E 交互提示。
  - 玩家对准 `Capsule Mesh (1)` 并在极近距离长按 E 后，机器人不会自动移动到床边锚点，而是停在当前位置，禁用移动、保留视角旋转，并进入 `17F01_02` 安抚界面。
  - 小男孩交互射线距离当前为 `0.71`，约为旧版 `1.42` 的二分之一，也是上一版 `0.355` 的两倍。
  - 机器人左上短时监控卡回到旧左上监控区坐标：`TriggerCardView` 面板为 `PptRect(72, 75.5, 410, 214)`，标题基准对齐旧 `SUBJECT - MONITORING` 区域。
- Inspector 可调字段：
  - `HearthCompanion17F01ReplayController / Prompt Delay After Bedroom Prelude`：控制字幕结束后多久才出现 E。
  - `Capsule Mesh (1) / HearthCompanionReplayInteractable / Max Distance`：控制交互射线长度，绑定工具默认写入 `0.71`。
- HUD 重建方式：如修改 `HearthCompanionHudBuilder` 后需要落到场景，执行 `Tools / Hearth / HUD / Rebuild And Apply Companion Unit HUD To Scene`，再执行 `Tools / Hearth / Replay / Apply 17F01 Minimal Loop Setup` 重新绑定流程引用。
- 最小测试：进入 17F01 机器人回放后，字幕未播放完时不能触发 E；字幕结束后等约 1.5 秒，在极近距离看准小男孩才出现 E；确认后机器人原地不动但仍可转头。

## 2026-07-01 剧情字幕接口、A/B 防重复与历史记录修正

本节为最新说明，优先级高于前面所有关于 17F01 字幕、A/B 处置、信任度和历史记录页面的旧描述。后续做剧情或 UI 流程任务前，先读根目录 `HEARTH_剧情接口与可接入点.md`。

### HearthDialogueSequence.cs

- 路径：`Assets/Scripts/MinLoop/HearthDialogueSequence.cs`
- 脚本作用：新增可编辑剧情段落数据资产。每个资产保存一组字幕行、每句字幕的延迟、停留时间和可选语音。
- 挂载对象：不挂载，是 `ScriptableObject` 数据资产。
- 资产位置：当前 17F01 已自动生成 `Assets/Data/MinLoop/Dialogues/17F01_BedroomPrelude.asset`、`17F01_BedsideSoothing.asset`、`17F01_LivingRoomObservation.asset`。
- Inspector 重要字段：`Lines` 列表中的 `Speaker`、`Text`、`Start Delay`、`Hold Seconds`、`Voice Clip`；`Post Sequence Delay` 控制整段播完后的额外等待。
- 公开接口：`SequenceId`、`Notes`、`Lines`、`PostSequenceDelay`、`HasLines`。
- 关联脚本：由 `HearthCompanion17F01ReplayController` 引用，由 `MinLoopSubtitlePlayer` 播放。
- 使用步骤：在 Project 面板打开对应 asset，直接增删字幕行、修改时间、拖入语音 `AudioClip`。如果拖入语音，字幕会至少停留到音频播放完。
- 最小测试：修改 `17F01_BedroomPrelude.asset` 的任意一句文本后进入 17F01 回放，卧室阶段字幕应显示修改后的文本。
- 后续扩展：17F02/17F03 每个剧情段落都建议新建独立 `HearthDialogueSequence`，流程脚本只负责等待该段播放完成后开放下一步交互。

### MinLoopSubtitlePlayer.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopSubtitlePlayer.cs`
- 本次修改：新增 `PlaySequence(HearthDialogueSequence sequence)` 和 `PlaySequenceAsset(HearthDialogueSequence sequence)`，保留原有 `PlaySequence(IList<MinLoopSubtitleLine>)`、`PlayLines(...)`、`ShowLine(...)`、`Hide()`、`Stop()`。
- 挂载对象：推荐 `MIN_LOOP_ROOT/UI/MinLoopSubtitlePlayer`。
- 必需组件：可自动创建 fallback UI；若要播放声音，需要绑定 `AudioSource`。
- Inspector 重要字段：`Audio Source` 用于语音播放；`Use Clean Centered Style`、`Speaker Center Y`、`Body Center Y`、字体大小仍可调整字幕位置和样式。
- 使用方式变化：如果字幕行有 `Voice Clip`，实际停留时间会取 `Hold Seconds` 和 `Voice Clip.length` 中较大的值。
- 最小测试：给任意字幕行拖入一段音频，播放时字幕不应早于音频结束消失。

### HearthCompanion17F01ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F01ReplayController.cs`
- 本次修改：17F01 三段台词从脚本内固定列表升级为可拖拽的 `HearthDialogueSequence` 数据资产；旧的 `Bedroom Prelude Lines`、`Soothing Lines`、`Living Room Lines` 保留为 fallback。
- 挂载对象：`MIN_LOOP_ROOT/ReplayRoom_17F01/HearthCompanion17F01ReplayController`。
- Inspector 重要字段：`Prefer Dialogue Sequence Assets` 正式流程保持开启；`Bedroom Prelude Sequence`、`Soothing Sequence`、`Living Room Sequence` 分别拖入三份 17F01 dialogue asset；`Prompt Delay After Bedroom Prelude` 当前为 1.5 秒。
- 公开接口：`BeginReplay()`、`CompleteCurrentStep()`、`ReturnToTerminalForDisposition()`、`SetReferences(...)`、`SetRobotRig(...)`、`SetAnchors(...)`、`SetApproachInteractable(...)`。
- 使用方式变化：以后增删 17F01 台词优先改三个 dialogue asset，不再改脚本。
- 最小测试：卧室段字幕未播完时不会显示 E；字幕播完并等待 1.5 秒后，看向小男孩胶囊体才显示 E。

### MinLoopFlowController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopFlowController.cs`
- 本次修改：新增 `DispositionSubmitted` 运行时状态，同一次 A/B 处置只能提交一次。`ApplyDispositionChoice(...)` 只有在 `CurrentStage == DispositionChoice` 且未提交时才生效。
- 挂载对象：`MIN_LOOP_ROOT/FlowManagers/MinLoopFlowController`。
- 公开接口：`ChooseDispositionA()`、`ChooseDispositionB()` 使用方式不变；新增只读属性 `DispositionSubmitted`。
- 使用方式变化：玩家在 A/B 页重复按 Space 不会重复改变真实信任度。
- 最小测试：回放结束后选 A，一次后 `DispositionSubmitted = true`；继续按 Space 不再触发第二次 `+1 TRUST`。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`
- 本次修改：`Close Terminal When Choice Submitted` 默认开启；新增 `Prevent Repeated Choice Submission`。A/B 提交后会立即关闭终端输入并开始关闭终端。
- 挂载对象：每台 TV 的 `MonitorCanvas/Terminal_17Fxx`。
- Inspector 重要字段：`Route Choices To Min Loop`、`Close Terminal When Choice Submitted`、`Prevent Repeated Choice Submission` 正式流程保持开启；`Min Loop Flow Controller` 拖正式 `MinLoopFlowController`。
- 使用方式变化：17F01、17F02、17F03 三个终端场景实例和 prefab 都已统一为提交后关闭、防重复提交。
- 最小测试：A/B 页按 Space 后终端应自动退出；同一页不能继续反复加减信任度。

### HearthFirstPersonHudFlowBinder.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudFlowBinder.cs`
- 本次修改：`Complete` 阶段不再自动打开历史页，只回到常驻 HUD；历史页由玩家从左上菜单主动打开。
- 挂载对象：`HearthHudRoot`。
- 使用方式变化：A/B 选择后只记录历史、显示信任浮字、回到常驻 HUD，不会强制弹出历史 UI。
- 最小测试：选完 A/B 后玩家视角回到正常 HUD；按 Tab 进入菜单再打开历史，才看到记录。

### HearthFirstPersonHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudController.cs`
- 本次修改：`RecordDisposition(choice, currentTrustAfter, trustDelta)` 使用真实 `TrustStateController` 传入的当前分数和变化量，不再自己假设百分制。`SetTrustScore(...)` 会同步刷新历史页 footer。
- 挂载对象：`HearthHudRoot`。
- 公开接口：新增重载 `RecordDisposition(MinLoopDispositionChoice choice, int currentTrustAfter, int trustDelta)`；原有记录接口保留。
- 使用方式变化：当前信任度显示为 -3 到 3 的分数，不再显示 `50 / 100` 或 `51 / 100`。
- 最小测试：初始历史为 0；完成第一户选 A 后历史当前信任显示 `1`，选 B 后显示 `-1`。

### HearthDispositionHistoryView.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthDispositionHistoryView.cs`
- 本次修改：新增动态 `RowBinding`，历史页文本由真实 `Records` 列表驱动；未完成的行会隐藏或清空。新增 `shiftTrustDeltaTexts`、`currentTrustTexts` 支持多个历史页 footer 同步刷新。
- 挂载对象：`HearthHudRoot`。
- Inspector 重要字段：`Records`、`Row Bindings`、`Shift Trust Delta Texts`、`Current Trust Texts`。
- 公开接口：`SetRecords(...)`、`AddRecord(record)`、`AddRecord(record, currentTrustAfter)`、`ClearRecords()`、`SetCurrentTrustScore(...)`、`GetPageForCurrentRecordCount()`。
- 使用方式变化：历史页不再提前显示 17F-02/17F-03 示例记录；完成几户就显示几户。
- 最小测试：只完成第一户时，打开历史只应显示 17F-01 一条记录。

### HearthFirstPersonHudBuilder.cs

- 路径：`Assets/Editor/HearthFirstPersonHudBuilder.cs`
- 本次修改：重建 HUD 时过滤历史页中的静态示例记录、`50 / 100`、`51 / 100`、`view archive`、`EARLIER SHIFTS` 等占位文字，并按 PPT 原位置生成动态历史行绑定。
- 挂载对象：不挂载，是 Editor 菜单工具。
- 菜单入口：`Tools / Hearth / HUD / Rebuild And Apply First Person HUD To Scene`。
- 使用方式变化：以后从 PPT/layout 重建 HUD，也不会重新带回历史页示例数据和档案按钮。
- 最小测试：重建后打开 `Slide21_HistoryThree.prefab`，不应再看到静态 `view archive` 文本；运行时记录由 `HearthDispositionHistoryView` 填入。

### Hearth17F01MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F01MinimalLoopBinder.cs`
- 本次修改：执行 `Tools / Hearth / Replay / Apply 17F01 Minimal Loop Setup` 时会自动创建并绑定三份 17F01 dialogue asset；同时把 17F01 终端设置为 A/B 提交后关闭、防重复提交。
- 挂载对象：不挂载，是 Editor 菜单工具。
- 自动生成资产：`Assets/Data/MinLoop/Dialogues/17F01_BedroomPrelude.asset`、`17F01_BedsideSoothing.asset`、`17F01_LivingRoomObservation.asset`。
- 使用方式变化：如果这些 asset 已经存在，工具只会补引用，不会覆盖你手动改过的 `Lines`。
- 最小测试：执行菜单后，`HearthCompanion17F01ReplayController` 的三个 sequence 字段都应有引用；`Terminal_17F01` 的 `Close Terminal When Choice Submitted` 和 `Prevent Repeated Choice Submission` 都应开启。
## 2026-07-01 17F02 最小游戏循环接入

### HearthCompanion17F02ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F02ReplayController.cs`
- 脚本作用：第二户 `17F02` 的陪伴单元机器人回放流程控制器。它按当前新剧情拆成：房间黑屏唤醒、卧室倾诉、女主人离开时锁定机器人、餐桌观察、客厅固定视角调用日志、强制关闭、黑屏音频争吵、返回人类终端 A/B 处置。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/ReplayRoom_17F02/HearthCompanion17F02ReplayController`。
- 需要组件：本脚本自身不要求同物体额外组件；场景中需要正式 `Player/Robot Controller`、`HearthCompanionHudRoot`、`MIN_LOOP_ROOT/UI/MinLoopSubtitlePlayer`、`ViewSwitchController`、`MinLoopFlowController`。
- Inspector 关键字段：
  - `Flow Controller`：拖 `MIN_LOOP_ROOT/FlowManagers/MinLoopFlowController`。
  - `View Switch Controller`：拖 `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`。
  - `Companion Hud`：拖 `HearthCompanionHudRoot`。
  - `Subtitle Player`：拖 `MIN_LOOP_ROOT/UI/MinLoopSubtitlePlayer`。
  - `Robot Root / Robot Camera / Robot Movement / Robot Look / Robot Interaction / Robot Rigidbody`：绑定正式 `Player/Robot Controller` 及其子组件。
  - `Bedroom Start Anchor`：第二户卧室起始视角锚点，默认由 `Robot Controller2` 的位置生成。
  - `Living Room Terminal Anchor`：第二户客厅固定视角锚点，默认由 `Robot Controller3` 的位置生成。
  - `Living Room Terminal Camera Anchor`：第二户客厅固定视角的相机锚点，默认由 `Robot Controller3` 子物体里的 Camera 生成。第三幕会同时套用身体锚点和相机锚点，避免继承第二幕最后玩家低头/抬头的视角。
  - `Bedroom Wake / Confide / Comfort / Wife Exit / Dining / Log Access / Forced Shutdown / Black Audio Sequence`：对应 `Assets/Data/MinLoop/Dialogues/17F02_*.asset` 字幕资产。
  - `Timing`：控制黑屏、淡入淡出、女主人离开锁定时间、餐桌沉默、强制关闭等待等节奏。
  - `Interaction Gates`：控制卧室倾诉是否显示/等待长按 E，强制关闭阶段是否需要玩家按 E 确认。当前第二户默认是倾诉期间不显示 E，倾诉字幕播完后等待 `Bedroom Prompt Delay After Confide Seconds = 1.5` 秒，再显示 E；按 E 后播放 `17F02_BedroomComfort.asset`。
  - `Actor Visibility`：控制第二户不同段落的角色模型显示。当前已开启：卧室段显示 `Bedroom Wife Actor = casual_Female_K@Sitting_Disbelief`；第二幕餐桌段只显示 `Dining Wife Actor = casual_Female_K` 和 `Dining Husband Actor = casual_Male_K`；第三幕终端段只显示 `Terminal Husband Actor = casual_Male_K (1)`。
  - `Actor Animations`：控制第二户人物动作。`Bedroom Wife Animation` 绑定 `casual_Female_K@Sitting_Disbelief`；`Bedroom Talking Max Seconds` 默认 `10`，表示 `SittingTalking` 最多播放约 10 秒后继续流程，设为 `0` 或负数则等待完整动作/字幕。
  - `Wife Exit Blocking`：控制女主人从卧室走到门口、开门、走到门外的脚本位移。`Use Simple Wife Exit Route` 推荐开启；`Bedroom Wife Move Root` 拖运行时卧室女主 `casual_Female_K@Sitting_Disbelief`；`Wife Before Door Path Points` 只放开门前、房间内的移动点；`Wife Door Pause Anchor` 是门内停顿/开门点；`Wife After Door Path Points` 当前可为空；`Wife Exit Outside Anchor` 是门外终点；`Wife Exit Path Points` 保留为旧路线兜底；`Wife Exit Door` 拖 `Door_2_Brown (4)` 上的 `SmartDoorController`；`Wife Walk Speed / Wife Rotate Speed` 调移动和转身速度；`Keep Door Open After Wife Exit` 控制离开后门是否保持打开。
  - `Events`：预留角色动作、音效、特写等接口。当前门的基础开合由 `Wife Exit Door` 自动调用；如果后续换成完整 Animator 门动画，可接到 `On Wife Exit Started` / `On Wife Exit Finished`。
- 公开方法：
  - `BeginReplay()` / `BeginReplay(MinLoopFlowController owner)`：开始第二户回放。
  - `CancelReplay()`：取消回放并清理字幕、提示、黑屏和机器人控制。
  - `SetReferences(...)`：运行时重新绑定流程、视角、HUD、字幕。
  - `SetRobotRig(...)`：运行时重新绑定正式机器人控制器。
  - `SetAnchors(...)`：运行时替换卧室锚点、客厅身体锚点和客厅相机锚点；旧的两个参数版本仍可用。
- 引用关系：
  - 由 `MinLoopFlowController` 在当前终端识别为 `17F02` 时调用。
  - 调用 `HearthCompanionHudController.ShowScene("17F02_01" ... "17F02_06")` 显示第二户机器人 HUD。
  - 调用 `MinLoopSubtitlePlayer.PlaySequenceAsset()` 播放可编辑字幕资产。
  - 结束后调用 `MinLoopFlowController.NotifyReplayCompleted()`，回到终端 A/B 处置。
- Unity 使用步骤：
  1. 把 `Robot Controller2` 放在第二户卧室内，作为机器人开场位置和朝向参考。
  2. 把 `Robot Controller3` 放在客厅男主面对机器人时的位置和朝向参考。
  3. 运行菜单 `Tools / Hearth / Replay / Apply 17F02 Minimal Loop Setup`。
  4. 检查生成的 `MIN_LOOP_ROOT/Anchors/Anchor_Robot_17F02_BedroomStart`、`Anchor_Robot_17F02_LivingRoomTerminal` 和 `Anchor_Robot_17F02_LivingRoomTerminalCamera`。
  5. 如果要细调女主离开卧室，保留 `casual_Female_K@Sitting_Disbelief` 作为真实卧室女主；只移动 `REF_Wife_17F02_*` 参考模型，再运行 `Tools / Hearth / Replay / Build 17F02 Wife Route From Female References`。
  6. 检查 `HearthCompanion17F02ReplayController` 上的字幕资产、HUD 引用、`Wife Exit Door` 和路径点。
  7. 从 17F02 TV 终端进入机器人回放测试。
- 最小测试方法：
  - 进入 17F02 终端，触发机器人回放。
  - 开始时应先黑屏，女主人唤醒后淡入机器人 HUD。
  - 卧室倾诉阶段机器人可以移动和转视角。
  - 女主人离开阶段机器人移动被锁定。
  - 餐桌观察阶段机器人恢复移动，可以听对话。
  - 餐桌沉默后黑屏切到客厅固定视角，机器人不能移动和转视角；即使第二幕最后玩家看向地面，第三幕也应切到 `Anchor_Robot_17F02_LivingRoomTerminalCamera` 记录的固定相机视角。
  - 强制关闭后进入黑屏音频，最后返回人类终端 A/B 处置。
- 后续扩展入口：
  - 女主人出门路线优先改 `MIN_LOOP_ROOT/ReplayRoom_17F02/WifeRouteReferenceModels` 下的 `REF_Wife_17F02_*` 参考模型，然后运行 `Tools / Hearth / Replay / Build 17F02 Wife Route From Female References` 同步；旧 `Wife Exit Path Points` 只作为兜底，不再建议手动维护。
  - 如果看空锚点，`MIN_LOOP_ROOT/Anchors` 下的自动锚点会挂 `HearthRouteAnchorGizmo`，但推荐调参考模型，因为它能直接看见女主身体和朝向。
  - 本次内部逻辑补充：`ClearRobotVelocity()` 会跳过 kinematic Rigidbody，避免播放或绑定时出现“Setting velocity of a kinematic body is not supported”警告；挂载方式无变化。
  - 本次流程补充：新增 `Bedroom Comfort Sequence`，用于玩家在卧室倾诉结束后按 E 触发陪伴单元安慰字幕。默认资产为 `Assets/Data/MinLoop/Dialogues/17F02_BedroomComfort.asset`。
  - 门基础开合由 `SmartDoorController` 执行；如果门开错方向，把 `Door_2_Brown (4)` 上 `SmartDoorController` 的 `Open Local Euler Offset.y` 从 `90` 改成 `-90`。
  - 门音效拖到 `SmartDoorController` 的 `Open Clip / Close Clip`；如果门 prefab 自带 AudioSource 但没有 AudioClip，不会自动有声音。
  - 男主操作 UI、愤怒动作接 `On Living Room Terminal Started` / `On Forced Shutdown Started`。
  - 字幕和语音改 `Assets/Data/MinLoop/Dialogues/17F02_*.asset`。
  - 机器人 HUD 文字和短时卡改 `Assets/Data/HearthHud/Companion/CompanionScene_04_17F02_01.asset` 到 `CompanionScene_09_17F02_06.asset`。
  - 关卡 02 的 UI 统一调参入口见 `HEARTH_17F02最小循环接口说明.md / 关卡02 UI 统一调整入口`。

### Hearth17F02MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F02MinimalLoopBinder.cs`
- 脚本作用：Editor 自动绑定工具，用于把第二户流程接入当前打开场景。
- 挂载对象：Editor 工具脚本，不挂载到 GameObject。
- 菜单入口：`Tools / Hearth / Replay / Apply 17F02 Minimal Loop Setup`；女主参考模型路线同步入口是 `Tools / Hearth / Replay / Build 17F02 Wife Route From Female References`。
- 自动处理内容：
  - 创建/更新 `MIN_LOOP_ROOT/ReplayRoom_17F02/HearthCompanion17F02ReplayController`。
  - 从 `Robot Controller2` 复制卧室起始锚点。
  - 从 `Robot Controller3` 复制客厅固定视角身体锚点，并从它的子 Camera 复制 `Anchor_Robot_17F02_LivingRoomTerminalCamera`。
  - 复制锚点后会把 `Robot Controller (2)` 和 `Robot Controller (3)` 设为 inactive，并禁用它们的 Camera、AudioListener、移动、视角和交互组件。它们只作为摆点参考，不会在游戏画面里显示。
  - 自动查找 `Door_2_Brown (4)`，添加/配置 `SmartDoorController`，默认绑定门子物体 `Door` 作为旋转对象。
  - 创建默认女主路线锚点；如果已运行 `Build 17F02 Wife Route From Female References`，则优先使用 `Anchor_Wife_17F02_BeforeDoor_01` 到 `05`、`Anchor_Wife_17F02_DoorPause`、`Anchor_Wife_17F02_ExitOutside`，不会再用旧数字拆分路线。
  - `Build 17F02 Wife Route From Female References` 会优先使用 `casual_Female_K@Sitting_Disbelief` 作为真实卧室女主，不再把它当源动作模型关闭；`casual_Female_K (3)-(9)` 或已改名的 `REF_Wife_17F02_*` 只作为路线参考模型，再从这些模型复制位置/朝向生成程序锚点。
  - 同步参考模型路线时会删除已淘汰的旧路线锚点：`Anchor_Wife_17F02_Path01`、`Path01 (1)-(5)`、`Path02`；流程只保留 `Anchor_Wife_17F02_BeforeDoor_*`、`DoorPause`、`ExitOutside`。
  - 隐藏并禁用 `Robot Controller2/3` 上的 Camera、AudioListener、移动、视角、交互组件，使它们只作为参考锚点，不作为运行时机器人。
  - 创建默认字幕资产 `Assets/Data/MinLoop/Dialogues/17F02_*.asset`，包括 `17F02_BedroomComfort.asset`。
  - 绑定 `MinLoopFlowController`、`ViewSwitchController`、`HearthCompanionHudRoot`、`MinLoopSubtitlePlayer` 和 17F02 TV 终端。
- 重要字段：无运行时 Inspector 字段。
- 最小测试方法：运行主菜单后，Hierarchy 应出现 `ReplayRoom_17F02`、两个 `Anchor_Robot_17F02_*` 和女主离开路线锚点；运行参考模型路线菜单后，`Wife Before Door Path Points` 应包含 5 个 `Anchor_Wife_17F02_BeforeDoor_*`，`Wife After Door Path Points` 应为空，`Wife Door Pause Anchor` 和 `Wife Exit Outside Anchor` 应分别来自 `(8)` 和 `(9)`；`Door_2_Brown (4)` 应有 `SmartDoorController`；17F02 TV 终端的 `Replay Resident Id` 应为 `17F02`。
- 后续扩展：如果你改了第二户参考对象命名，优先扩展本工具里的 `Find(...)` 名称列表；如果要增加门后中间点，可以新增参考模型并扩展 `BuildWifeRouteFromFemaleReferences()` 的数组映射。

### MinLoopFlowController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopFlowController.cs`
- 本次修改：新增第二户回放控制器引用和当前回放住户字段。
- 新增/调整字段：
  - `Companion 17F02 Replay Controller`：拖 `HearthCompanion17F02ReplayController`。
  - `Use Resident Specific Replay Controllers`：开启后根据终端传入的住户编号启动不同回放控制器。
  - `Active Replay Resident Id`：当前准备启动的住户编号，例如 `17F01`、`17F02`。
- 新增公开方法：
  - `SetCompanion17F02ReplayController(HearthCompanion17F02ReplayController controller)`。
  - `SetActiveReplayResident(string residentId)`。
  - `SetActiveReplayResident(string residentId, HearthTvTerminalController terminal)`。
- 使用方式变化：17F01 仍走 `HearthCompanion17F01ReplayController`；17F02 由终端传入 `17F02` 后走 `HearthCompanion17F02ReplayController`。其他旧使用方式无变化。

## 2026-07-02 17F02 门时机与角色显隐修正

### HearthCompanion17F02ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F02ReplayController.cs`
- 本次修改：女主离开卧室路线从旧的“一个数组 + 开门插入点”升级为更直观的三段式：`Wife Before Door Path Points`、`Wife Door Pause Anchor`、`Wife After Door Path Points`、`Wife Exit Outside Anchor`。旧 `Open Door After Path Point Count` 只在关闭 `Use Simple Wife Exit Route` 时作为备用。
- 运行时兜底：如果当前场景还没填新的门前/门后数组，但旧 `Wife Exit Path Points` 有内容，脚本会自动把旧数组前 4 个点当门前、后续点当门后，避免旧场景直接失效。
- 使用方式变化：挂载方式无变化。若门时机不对，优先把点拖到门前/门后两个数组，而不是再调数字。
- 本次场景绑定：`Manage Actor Visibility` 已开启；卧室段、餐桌段、终端段会自动切换角色模型显隐。

### Hearth17F02MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F02MinimalLoopBinder.cs`
- 本次修改：自动绑定时优先把第三幕男主识别为 `casual_Male_K (1)`；同时默认开启 `Manage Actor Visibility`，并把女主路径拆成门前/门后两段。
- 新增菜单：`Tools / Hearth / Replay / Migrate 17F02 Wife Exit Route To Simple Segments`。它只迁移当前场景的女主出门路径数组并添加锚点可视化，不会重新生成或覆盖你手动摆好的锚点位置。
- 新增菜单：`Tools / Hearth / Replay / Build 17F02 Wife Route From Female References`。现在优先使用 `casual_Female_K@Sitting_Disbelief` 作为真实卧室女主；`casual_Female_K (2)` 只作为旧场景兼容查找名，不再推荐作为运行时女主；`casual_Female_K (3)-(9)` 或已改名的 `REF_Wife_17F02_*` 作为参考模型，用位置/朝向重建女主出门锚点。
- 使用方式变化：重跑 `Tools / Hearth / Replay / Apply 17F02 Minimal Loop Setup` 后，会保留这个新逻辑，不再默认让餐桌男女和第三幕男主一起显示。

### HearthEditorOnlyReferenceModel.cs

- 路径：`Assets/Scripts/MinLoop/HearthEditorOnlyReferenceModel.cs`
- 脚本作用：标记“只给编辑器摆位看的参考模型”。编辑状态下模型保持可见，但自身和子物体 Collider 会保持关闭，Rigidbody 不参与碰撞；进入 Play Mode 后自动关闭 Renderer、Collider、Animator、AudioSource、导航组件和额外 MonoBehaviour，避免参考女主模型在游戏里显示、挡路、碰撞或播放动作。
- 挂载对象：自动挂在 `MIN_LOOP_ROOT/ReplayRoom_17F02/WifeRouteReferenceModels` 下的 `REF_Wife_17F02_*` 参考模型。
- 需要组件：无。
- Inspector 关键字段：`Disable Colliders Always`、`Make Rigidbodies Kinematic Always`、`Hide Renderers In Play`、`Disable Animators In Play`、`Disable Audio Sources In Play`、`Disable Navigation In Play`、`Disable Mono Behaviours In Play`，默认都开启。
- 公开方法：`ApplyReferenceState()` 会立即应用参考模型的可视/碰撞状态；`ApplyPlayModeState()` 保留为兼容旧调用的包装方法。进入 Play Mode 时由 `Awake / OnEnable / Start` 自动调用，一般不需要手动调用。
- 使用步骤：移动或旋转参考模型后，执行 `Tools / Hearth / Replay / Build 17F02 Wife Route From Female References` 同步锚点；不要把参考模型拖进剧情显隐字段，也不要手动重新开启它们的 Collider。
- 最小测试：Play Mode 中房间里只应看到 `casual_Female_K@Sitting_Disbelief` 这一个卧室女主，旧 `Actor_Wife_17F02_Bedroom`、`REF_Wife_17F02_*` 不应显示或参与碰撞。

### HearthRouteAnchorGizmo.cs

- 路径：`Assets/Scripts/MinLoop/HearthRouteAnchorGizmo.cs`
- 脚本作用：给路径锚点在 Scene 视图里绘制线框小人、朝向箭头和标签，帮助判断女主人走到该点时的位置和面朝方向。它只画 Gizmo，不会在 Game 画面中显示。
- 挂载对象：推荐挂在 `MIN_LOOP_ROOT/Anchors` 下的女主走位锚点，例如 `Anchor_Wife_17F02_Path01`、`Anchor_Wife_17F02_DoorPause`、`Anchor_Wife_17F02_ExitOutside`。
- 需要组件：无。
- Inspector 关键字段：`Label Override` 改标签；`Draw Always` 控制未选中时是否也显示；`Body Color / Forward Color` 改线框和箭头颜色；`Body Height / Body Radius / Arrow Length / Label Height` 改预览大小。
- 公开方法：`Configure(string newLabel, Color newBodyColor, Color newForwardColor)`，供 Editor 绑定工具自动写标签和颜色。
- 使用步骤：执行 `Tools / Hearth / Replay / Migrate 17F02 Wife Exit Route To Simple Segments` 自动添加；也可以手动给任意锚点 Add Component。
- 最小测试：Scene 视图中应能看到锚点位置上的线框小人和黄色朝向箭头；旋转锚点 Y 轴，箭头方向会跟着变。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`
- 本次修改：新增 `Replay Resident Id`，终端请求机器人回放前会告诉 `MinLoopFlowController` 当前是哪一户。
- 新增/调整字段：
  - `Replay Resident Id`：建议 17F01 终端填 `17F01`，17F02 终端填 `17F02`，17F03 终端后续填 `17F03`。
- 新增公开方法：
  - `SetReplayResidentId(string residentId)`。
  - `GetReplayResidentId()`。
- 使用方式变化：如果 `Replay Resident Id` 留空，脚本会尝试从父物体名字里的 `ROOM1/ROOM2/ROOM3` 或 `17F01/17F02/17F03` 推断；也会按 `First Slide Number` 推断。为了稳定，建议手动填。
## 2026-07-02 17F02 回放切入预定位修复

### MinLoopFlowController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopFlowController.cs`
- 本次修改：进入机器人回放时，先调用当前住户回放控制器的 `PrepareReplayStart()`，再执行 `ViewSwitchController.SwitchToCompanion()`。这样 17F02 从 TV 终端进入回放时，会先把正式 `Player/Robot Controller` 放到 17F02 卧室起点并盖上黑屏，再切机器人相机，不会先露出 17F01 的机器人旧视角。
- 使用方式变化：Inspector 挂载方式无变化。`Companion 17F02 Replay Controller` 仍拖 `MIN_LOOP_ROOT/ReplayRoom_17F02/HearthCompanion17F02ReplayController`；`Use Resident Specific Replay Controllers` 保持开启。
- 公开接口变化：无新增公开方法；内部新增 `PrepareActiveResidentReplayStart()`，由 `RequestReplayFromTerminal()` 流程自动调用。
- 最小测试：从 `17F/ROOM2/TV (4)` 触发机器人回放，切换期间应直接黑屏/进入 17F02 卧室起点，不应再出现 17F01 房间约 2 秒的画面。

### HearthCompanion17F02ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F02ReplayController.cs`
- 本次修改：新增公开方法 `PrepareReplayStart()`。它会停止旧回放协程、清空字幕/长按提示、显示 17F02 回放黑屏层、把正式机器人传送到 `Bedroom Start Anchor`、关闭机器人移动/转向/交互，并把卧室女主人状态准备好。
- 挂载对象：仍是 `MIN_LOOP_ROOT/ReplayRoom_17F02/HearthCompanion17F02ReplayController`。
- 使用方式变化：无需手动调用。总流程会在切到机器人视角前自动调用；如果以后做 17F03 专用回放，也建议实现同名预备入口。
- 最小测试：Play Mode 下调用 17F02 终端回放后，正式 `Player/Robot Controller` 的位置应立刻贴近 `Anchor_Robot_17F02_BedroomStart`，同时 `Hearth17F02ReplayBlackout` alpha 为 1。

### HearthCompanion17F01ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F01ReplayController.cs`
- 本次修改：新增公开方法 `PrepareReplayStart()`，用于在切机器人相机前把正式机器人预定位到 17F01 儿童房起点，并关闭旧交互提示。17F01 现有流程和 Inspector 用法无变化。
- 使用方式变化：无需手动调用；由 `MinLoopFlowController` 在当前住户为 17F01 时自动调用。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`
- 本次修改：`RequestRobotReplay()` 会先计算并写入当前终端的 `Replay Resident Id`，再触发 `On Robot Replay Requested` 和最小循环回放请求。这样后续如果你给 `On Robot Replay Requested` 加 UnityEvent，也能读到正确住户，不会再误用默认 17F01。
- 使用方式变化：Inspector 挂载方式无变化。17F02 终端的 `Replay Resident Id` 继续保持 `17F02`。

## 2026-07-06 17F01/17F02 人物动作接入

### HearthActorAnimationPlayer.cs

- 路径：`Assets/Scripts/MinLoop/HearthActorAnimationPlayer.cs`
- 脚本作用：通用人物动画播放组件。它用 Unity Playables 直接把 `AnimationClip` 播到指定 `Animator` 上，不要求你手动给每个角色做 Animator Controller 状态机。
- 挂载对象：挂在需要被剧情流程控制动作的角色根对象上，例如 `little_boy_B`、`casual_Female_G@Sitting_Idle`、`casual_Male_G@Sitting`、`casual_Female_K@Sitting_Disbelief`、`casual_Female_K`、`casual_Male_K`、`casual_Male_K (1)`。
- 需要组件：同物体或子物体需要 `Animator`。如果绑定工具发现对象没有 Animator，会自动补一个。
- Inspector 关键字段：
  - `Animator`：拖角色身上的 Animator。
  - `Clips`：每条包含 `Clip Id`、`Clip`、`Loop`、`Apply Root Motion`、`Apply Foot Ik`、`Fade Seconds`、`Playback Speed`。
  - `Play On Enable / Play On Enable Clip Id`：可选，仅用于单独预览；正式剧情通常由流程脚本调用。
- 公开方法：
  - `Play(string clipId)`：按该 Clip 自己的 Loop 设置播放。
  - `PlayLoop(string clipId)`：强制循环播放。
  - `PlayOnce(string clipId)`：播放一次并返回按速度折算后的秒数。
  - `StopAndHold()`：停在当前帧。
  - `StopPlayback()`：销毁播放图，停止该组件驱动。
  - `SetRootMotion(bool value)`：运行时开关 root motion。
  - `HasClip(string clipId)` / `GetClipLength(string clipId)`：供流程脚本判断和等待。
- 使用方式：以后你从 Mixamo 导入新动作后，把 FBX 里的 `AnimationClip` 拖进某个 Slot，给它一个稳定的 `Clip Id`，再让剧情脚本调用这个 ID。
- 最小测试：Play Mode 下调用 `PlayLoop("SittingTalking")` 时，目标角色应循环播放对应动作；Slot 为空时脚本静默跳过，不会中断剧情。

### HearthCompanion17F01ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F01ReplayController.cs`
- 本次修改：新增 17F01 角色动作引用。进入 17F01 机器人回放后，优先播放已绑定的循环动作；如果没有绑定动画，才回退到原来的 `HearthActorPosePreset` 静态姿势。
- 新增 Inspector 字段：
  - `Boy Animation / Boy Sleep Animation Id`：默认 `LayingSleeping`。
  - `Mother Animation / Mother Sitting Animation Id`：默认 `SittingIdle`。
  - `Father Animation / Father Sitting Animation Id`：默认 `Sitting`。
- 当前场景绑定：
  - `little_boy_B` -> `LayingSleeping`，Clip 来自 `Assets/Animations/Hearth/17F01/Clips/Laying_Sleeping.fbx / mixamo.com`。
  - `casual_Female_G@Sitting_Idle` -> `SittingIdle`，Clip 来自 `Assets/Animations/Hearth/17F01/Clips/casual_Female_G@Sitting_Idle.fbx / mixamo.com`。
  - `casual_Male_G@Sitting` -> `Sitting`，Clip 来自 `Assets/Animations/Hearth/17F01/Clips/casual_Male_G@Sitting.fbx / mixamo.com`。
- 使用方式变化：17F01 人物位置和模型不再由脚本重摆；绑定工具只补动画播放器和 Clip 引用。原交互、字幕和终端流程不变。
- 最小测试：从 17F01 终端进入机器人回放，三名角色应在当前摆好的位置播放各自循环动作。
- 最近更新：17F01 的小男孩 E 交互判定已从可见小男孩模型中拆出来，`Capsule Mesh (1)` 现在挂在 `MIN_LOOP_ROOT/ReplayRoom_17F01/RuntimeInteractables`。以后删除或替换小男孩可见模型，不会再把交互判定一起删掉。
- 最近更新：绑定工具现在优先识别 `Laying_Sleeping` / `little_boy_B-Laying_Sleeping` 作为正式睡姿模型；如果旧的独立 `little_boy_B` 根对象还在场景里，会自动关闭它，避免旧模型和新睡姿模型重叠或播放错误动作。

### HearthCompanion17F02ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F02ReplayController.cs`
- 本次修改：新增 17F02 演员动画播放链路，并把女主出门动作接到原有字幕、E 交互、门和锚点流程中。
- 新增 Inspector 字段：
  - `Bedroom Wife Animation`：卧室女主 `casual_Female_K@Sitting_Disbelief` 的动画播放器。
  - `Bedroom Talking Max Seconds`：默认 `10`，控制 `SittingTalking` 一次性动作最多播放多久；设为 `0` 或负数时等待完整动作/字幕。
  - `Bedroom Wife Idle / Talking / Sit To Stand / Walk Loop / Open Door Animation Id`：默认分别为 `SittingDisbelief`、`SittingTalking`、`SitToStand`、`WalkLoop`、`OpenDoorOutwards`。旧 `StartWalking` 已不再作为默认流程动作。
  - `Door Open Delay After Animation Start Seconds`：默认 `1`，表示 `OpenDoorOutwards` 开始约 1 秒后调用门的 `Open()`。
  - `Dining Wife Animation / Dining Husband Animation / Terminal Husband Animation`：第二幕和第三幕演员动作播放器。
- 当前场景绑定：
  - 卧室女主 `casual_Female_K@Sitting_Disbelief`：`SittingDisbelief`、`SittingTalking`、`SitToStand`、`WalkLoop`、`OpenDoorOutwards`。
  - 餐桌女主：`Sitting`。
  - 餐桌男主：`SittingIdle`。
  - 第三幕男主：`ButtonPushing`。
- 女主离开卧室顺序：
  1. 黑屏恢复后，女主循环 `SittingDisbelief`。
  2. 倾诉结束并完成 E 安慰后，女主播放一次 `SittingTalking`，最多等待 `Bedroom Talking Max Seconds`。
  3. 男主喊吃饭、女主回应后，播放 `SitToStand` 一次。
  4. 循环 `WalkLoop`，使用 `Assets/Animations/Hearth/17F02/Clips/casual_Female_K@Walking.fbx / Walking`，同时沿 `Wife Before Door Path Points` 到 `Wife Door Pause Anchor`。
  5. 停住并播放 `OpenDoorOutwards`，该 Slot 开启 `Apply Root Motion`。
  6. 动画开始约 1 秒后调用 `Door_2_Brown (4)` 的 `SmartDoorController.Open()`。
  7. 动作结束后关闭 root motion，再直接移动/校正到 `Wife Exit Outside Anchor`。
- 使用方式变化：如果某个动作暂时没拖 Clip，流程会跳过该动作但继续剧情；挂载对象和终端触发方式无变化。
- 最小测试：17F02 回放中，黑屏结束后只看到 `casual_Female_K@Sitting_Disbelief`，并循环 `SittingDisbelief`；按 E 后播放一次 `SittingTalking`；离开时按起身、Walking 走路、开门、出门顺序执行。
- 最近更新：如果某个阶段已经有 `HearthActorAnimationPlayer` 和有效 Clip，流程不会再调用旧 `HearthActorPosePreset`。旧 PosePreset 只作为“没有动画 Clip 时的兜底”，避免它暂停 Animator、覆盖 Mixamo 动作或导致女主不播放起身动作。
- 最近更新：17F02 的真实演员逻辑固定为：卧室阶段只用 `casual_Female_K@Sitting_Disbelief`，餐桌阶段只用 `casual_Female_K` 和 `casual_Male_K`，第三幕只用 `casual_Male_K (1)`。旧 `Actor_Wife_17F02_Bedroom`、`Sitting_Idle`、`Sitting`、`Button_Pushing`、`Female_Start_Walking`、`Open_Door_Outwards`、`X_Bot@Sit_To_Stand` 这类旧演员/源动作对象不参与游戏显示。

## 2026-07-07 17F02 女主交互后消失与流程中断修复

本节是 17F02 女主卧室演员、动作和开门流程的最新规则，优先级高于前面旧的 17F02 动作说明。

### HearthActorAnimationPlayer.cs

- 路径：`Assets/Scripts/MinLoop/HearthActorAnimationPlayer.cs`
- 本次修改：`ClipSlot` 新增 `Stabilize Animator Transform`，组件新增 `Use Unscaled Time`。
- 用途：当 Mixamo 动画会改写角色根节点位置时，可让 Animator 子物体每帧恢复到原本的本地位置、旋转和缩放，世界位移只交给剧情脚本移动根控制。
- 17F02 当前用法：`Actor_Wife_17F02_BedroomRuntimeRoot` 上挂本脚本，`Animator` 指向子物体 `casual_Female_K@Sitting_Disbelief`。女主的 `SittingDisbelief / SittingTalking / SitToStand / WalkLoop / OpenDoorOutwards` 五个 Slot 都开启 `Stabilize Animator Transform`，并关闭 `Apply Root Motion`。
- 使用方式变化：以后给 17F02 女主换动作时，优先只替换 Slot 里的 `Clip`，不要改 `Clip Id`；如果新动作又把人物拉走，保持 `Stabilize Animator Transform` 开启。

### HearthCompanion17F02ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F02ReplayController.cs`
- 本次修改：17F02 女主可见模型和移动根分离。`Bedroom Wife Actor` 与 `Bedroom Wife Move Root` 现在都应绑定到 `Actor_Wife_17F02_BedroomRuntimeRoot`，而不是直接绑定可见模型。
- 可见模型：`casual_Female_K@Sitting_Disbelief` 必须作为 `Actor_Wife_17F02_BedroomRuntimeRoot` 的子物体存在并保持可见。
- 动作播放：E 安慰后继续在同一个子模型上播放 `SittingTalking`，不会切换到别的女主，也不会隐藏女主。
- 出门流程：`SittingTalking` 最多播放 `Bedroom Talking Max Seconds`，然后 `SitToStand` -> `WalkLoop` -> `Wife Before Door Path Points` -> `Wife Door Pause Anchor` -> `OpenDoorOutwards` -> 约 1 秒后开 `Door_2_Brown (4)` -> `Wife Exit Outside Anchor`。
- 路线规则：`Wife Before Door Path Points` 固定为 `REF_Wife_17F02_BeforeDoor_01` 到 `05`；`Wife After Door Path Points` 当前应为空；门后不再做复杂路径，直接到 `REF_Wife_17F02_ExitOutside`。
- 防卡死：女主移动时如果连续一段时间没有靠近目标锚点，会输出 Warning 并吸附到目标点继续流程；动作 Clip 或门引用缺失时也会跳过并继续，不应再中断整段回放。
- 时间规则：本脚本新增 `Use Unscaled Replay Time`，17F02 默认开启，避免回放锁控制或暂停状态让字幕、女主移动、开门延迟停住。

### SmartDoorController.cs

- 路径：`Assets/Scripts/Interactions/SmartDoorController.cs`
- 本次修改：新增 `Use Unscaled Time`。开启后门的移动和自动关闭计时使用 `Time.unscaledDeltaTime`。
- 17F02 当前用法：`Door_2_Brown (4)` 的 `SmartDoorController / Use Unscaled Time` 由绑定工具自动开启，避免女主开门阶段因为普通游戏时间暂停而卡住。
- 使用方式变化：普通可交互门仍可保持关闭；剧情门、黑屏中会动的门、被回放流程驱动的门建议开启。

### Hearth17F02MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F02MinimalLoopBinder.cs`
- 本次修改：`Tools / Hearth / Replay / Build 17F02 Wife Route From Female References` 和 `Tools / Hearth / Replay / Apply 17F02 Minimal Loop Setup` 会自动维护 `Actor_Wife_17F02_BedroomRuntimeRoot`。
- 绑定结果应为：
  - `Bedroom Wife Actor = Actor_Wife_17F02_BedroomRuntimeRoot`
  - `Bedroom Wife Move Root = Actor_Wife_17F02_BedroomRuntimeRoot`
  - `Bedroom Wife Animation = Actor_Wife_17F02_BedroomRuntimeRoot` 上的 `HearthActorAnimationPlayer`
  - 子模型 `casual_Female_K@Sitting_Disbelief` 仍是卧室可见女主
  - 旧 `Actor_Wife_17F02_Bedroom` 保持 inactive
  - `BeforeDoor` 数组 5 个点，`AfterDoor` 数组 0 个点
- 调整方法：你移动 `REF_Wife_17F02_*` 参考模型后，先运行 `Build 17F02 Wife Route From Female References` 同步路线，再运行 `Apply 17F02 Minimal Loop Setup` 刷新总绑定。

### Hearth17F01MinimalLoopBinder.cs / Hearth17F02MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F01MinimalLoopBinder.cs`、`Assets/Editor/Hearth17F02MinimalLoopBinder.cs`
- 本次修改：绑定工具会自动添加/维护 `HearthActorAnimationPlayer`，并按真实资产名绑定 Clip。
- 17F01 菜单：`Tools / Hearth / Replay / Apply 17F01 Minimal Loop Setup`。
- 17F02 菜单：`Tools / Hearth / Replay / Apply 17F02 Minimal Loop Setup`；女主路线同步仍用 `Tools / Hearth / Replay / Build 17F02 Wife Route From Female References`。
- 重要修正：同名 FBX 子网格很多，绑定工具现在会优先识别带 Animator 的演员根对象，避免把餐桌人物误绑到 `Sitting_Idle/casual_Male_K` 这类子网格上。
- 最近更新：使用方式无变化；演员查找进一步加固为优先选择自身或子物体带 `Animator` 的对象，方便以后重跑绑定菜单时继续避开同名子网格。
- 最近更新：17F01 菜单会把 `Capsule Mesh (1)` 放到独立 `RuntimeInteractables` 父级，并关闭被替换掉的旧 `little_boy_B` 根对象；17F02 菜单会关闭旧 `Actor_Wife_17F02_Bedroom` 和源动作模型，但会保留 `casual_Female_K@Sitting_Disbelief` 作为真实卧室女主。
- 使用方式变化：重跑菜单会保留并刷新这些动画 Slot；如果你换了动作 FBX，重跑菜单前请确认代码里写的资产路径或手动在 Inspector 替换 Clip。
## 2026-07-07 17F02 Humanoid Animator 规范化修复

### HearthActorAnimatorDriver.cs

- 路径：`Assets/Scripts/MinLoop/HearthActorAnimatorDriver.cs`
- 脚本作用：17F02 新的标准人物动画播放入口。它不再使用 Playables 直接把 Clip 打到角色身上，而是通过角色自己的 `Animator` 和 `AnimatorController` 播放状态，符合 Unity Humanoid / Animator Controller 的常规流程。
- 挂载对象：需要被剧情流程控制动作的运行时演员根对象。目前 17F02 为：
  - `Actor_Wife_17F02_BedroomRuntimeRoot`
  - `casual_Female_K`
  - `casual_Male_K`
  - `casual_Male_K (1)`
- 需要组件：目标对象或子对象必须有 `Animator`；该 `Animator` 必须有有效 Humanoid `Avatar` 和对应 `RuntimeAnimatorController`。
- Inspector 关键字段：
  - `Animator`：真正驱动骨骼的 Animator。卧室女主指向子物体 `casual_Female_K@Sitting_Disbelief` 的 Animator。
  - `States`：每条包含 `State Id`、`State Name`、`Clip`、`Loop`、`Apply Root Motion`、`Fade Seconds`、`Playback Speed`。
- 公开方法：
  - `Play(id)`：按 Slot 设置播放。
  - `PlayLoop(id)`：播放循环状态。
  - `PlayOnce(id)`：播放一次并返回动作长度。
  - `StopAndHold()`：停在当前动画姿势。
  - `SetRootMotion(bool)`：运行时开关 root motion。
  - `HasState(id)` / `GetStateLength(id)`：供流程脚本检查和等待。
- 使用方式：17F02 不再手动维护旧 `HearthActorAnimationPlayer`。运行 `Tools / Hearth / Replay / Apply 17F02 Minimal Loop Setup` 后，工具会自动生成/刷新 `Assets/Animation/Hearth/17F02/*.controller` 并绑定到这些 Driver。
- 最小测试：Play Mode 中调用 `PlayOnce("SittingTalking")`、`PlayOnce("SitToStand")`、`PlayLoop("WalkLoop")` 时，卧室女主应在同一可见模型上播放对应动作。

### HearthCompanion17F02ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F02ReplayController.cs`
- 本次修改：17F02 的 `Bedroom Wife Animation`、`Dining Wife Animation`、`Dining Husband Animation`、`Terminal Husband Animation` 字段类型改为 `HearthActorAnimatorDriver`。
- 使用方式变化：旧的 `HearthActorAnimationPlayer` 只保留给 17F01 等旧流程使用；17F02 不再依赖 Playables 播放器和 `Stabilize Animator Transform`。
- 当前动作顺序：
  - 卧室女主循环 `SittingDisbelief`。
  - 玩家 E 安慰后播放一次 `SittingTalking`。
  - 女主离开时播放 `SitToStand`，再循环 `WalkLoop` 沿参考锚点移动。
  - 到 `DoorPause` 后播放 `OpenDoorOutwards`，约 1 秒后调用 `Door_2_Brown (4).Open()`。
  - 第二幕餐桌女主 `Sitting`，餐桌男主 `SittingIdle`。
  - 第三幕男主 `ButtonPushing`。
- Inspector 检查重点：四个 animation 字段必须是 `HearthActorAnimatorDriver`；不是 `HearthActorAnimationPlayer`。若看到旧类型，重新运行 17F02 Setup。
- 最小测试：从 17F02 终端进入回放，完成 E 安慰后女主不应消失，并应依次进入 talking、起身、走路、开门流程。

### Hearth17F02MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F02MinimalLoopBinder.cs`
- 本次修改：绑定工具现在会自动维护 17F02 的 Humanoid 动作导入、Animator Controller、Animator Driver 和运行时演员引用。
- 菜单入口：
  - `Tools / Hearth / Replay / Apply 17F02 Minimal Loop Setup`
  - `Tools / Hearth / Replay / Build 17F02 Wife Route From Female References`
  - `Tools / Hearth / Replay / Validate 17F02 Animation Setup`
- 自动处理内容：
  - 将 17F02 使用的 Mixamo FBX 设为 Humanoid 动作导入。
  - 为卧室女主、餐桌女主、餐桌男主、第三幕男主生成 `Assets/Animation/Hearth/17F02/*.controller`。
  - 给真实演员绑定 `HearthActorAnimatorDriver`，并移除 17F02 运行链路上的旧 `HearthActorAnimationPlayer`。
  - 确保卧室女主 Animator 有 `casual_Female_KAvatar`、有效 Humanoid Avatar 和 `BedroomWife17F02.controller`。
- 当前 FBX 映射：
  - `SittingDisbelief` -> `Assets/Animations/Hearth/17F02/Clips/casual_Female_K@Sitting_Disbelief.fbx / mixamo.com`
  - `SittingTalking` -> `Assets/Animations/Hearth/17F02/Clips/Sitting_Talking.fbx / mixamo.com`
  - `SitToStand` -> `Assets/Animations/Hearth/17F02/Clips/X_Bot@Sit_To_Stand.fbx / Sit_To_Stand`
  - `WalkLoop` -> `Assets/Animations/Hearth/17F02/Clips/casual_Female_K@Walking.fbx / Walking`
  - `OpenDoorOutwards` -> `Assets/Animations/Hearth/17F02/Clips/Open_Door_Outwards.fbx / mixamo.com`
  - `Sitting` -> `Assets/Animations/Hearth/17F02/Clips/Sitting.fbx / mixamo.com`
  - `SittingIdle` -> `Assets/Animations/Hearth/17F02/Clips/Sitting_Idle.fbx / mixamo.com`
  - `ButtonPushing` -> `Assets/Animations/Hearth/17F02/Clips/Button_Pushing.fbx / mixamo.com`
- 最小测试：运行 `Validate 17F02 Animation Setup` 后，Console 中每个 17F02 Clip 应为 `human=True`，四个 Driver 都应显示有效 Avatar 和 Controller。

## 2026-07-12 17F03 人物摆演预览与锚点同步

### Hearth17F03StagingPoseProxy.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F03StagingPoseProxy.cs`
- 脚本作用：挂在第三户的每个 `Preview_*` 预览人物根节点上，把这个预览人物连接到一个正式剧情 Anchor。它只负责“编辑摆位 -> 写回 Anchor”，不参与 Play Mode 的剧情逻辑。
- 挂载对象：由 `Hearth17F03StagingPreviewTool` 自动添加；不需要手动挂。
- Inspector 关键字段：
  - `Target Anchor`：这个预览人物写回的位置和朝向目标。
  - `Stage Label`：当前预览属于哪一幕，仅供辨认。
- 公开方法：
  - `ApplyPreviewPoseToAnchor()`：将预览人物的世界位置和朝向写回正式 Anchor。
  - `ResetPreviewFromAnchor()`：将预览人物恢复到正式 Anchor 当前的位置和朝向。
- 使用方式无须变化：正常只通过下方 Editor 菜单批量调用，不建议逐个手动调用。
- 最近修改：预览根节点现在额外保存 `Preview Animator State` 和 `Preview Normalized Time`。Unity 编译、重载场景或重新打开项目后，会自动把预览人物恢复到指定动作帧，不会再全部退回 Animator 默认坐姿或原始展开手姿势。
- 位置生效规则：只移动/旋转名字以 `Preview_` 开头的根节点。按 Play 时工具会自动把所有 Preview 根节点写入对应 Anchor，因此不再要求每次手动点击 `Apply Preview Poses To Anchors`；该菜单仍保留用于立即同步和保存。

### Hearth17F03StagingPreviewTool.cs

- 路径：`Assets/Editor/Hearth17F03StagingPreviewTool.cs`
- 脚本作用：为 17F03 创建独立的编辑摆演层，让第一、二、三幕会出现的人物同时可见、可直接拖动和旋转；同时保证正式关卡仍由 `HearthCompanion17F03ReplayController` 按幕次显示和隐藏演员。
- 菜单入口：
  - `Tools / Hearth / Replay / 17F03 Staging / Create Or Update Preview`
  - `Tools / Hearth / Replay / 17F03 Staging / Apply Preview Poses To Anchors`
  - `Tools / Hearth / Replay / 17F03 Staging / Reset Preview Poses From Anchors`
  - `Tools / Hearth / Replay / 17F03 Staging / Reapply Animation Poses`
  - `Tools / Hearth / Replay / 17F03 Staging / Remove Preview`
- 自动生成位置：`MIN_LOOP_ROOT/ReplayRoom_17F03/StagingPreview_17F03`。
- 预览分组：
  - `Act01_HumanEntry`：第一幕母亲、父亲。
  - `Act02_MiddayMediation`：第二幕母亲、父亲、女儿。
  - `Act03_NightShutdown`：第三幕女儿的起点、中间路线点、陪伴单元前终点。
- 正确调整步骤：
  1. 确保当前不在 Play Mode。
  2. 运行 `Create Or Update Preview`。
  3. 在 Hierarchy 打开 `StagingPreview_17F03`，只移动或旋转需要调整的 `Preview_*` 人物；使用 Unity 的 Global 坐标系调整位置和朝向，不调整 Scale。
  4. 运行 `Apply Preview Poses To Anchors`。
  5. Play 测试第三户流程。需要放弃这次预览修改时，运行 `Reset Preview Poses From Anchors`。
- 正式运行规则：
  - 预览模型在 Scene 视图中可见，但 Play Mode 自动关闭 Renderer、Collider、Animator、声音和行为脚本，绝不会挡住玩家或重复出现在镜头中。
  - 正式演员 `RuntimeActors` 只在 Scene 视图临时隐藏，运行时不受此隐藏影响；它们仍由 `HearthCompanion17F03ReplayController` 的 `PrepareHumanActors / PrepareMiddayActors / PrepareNightActors` 控制显隐。
  - 不要直接调整 `RuntimeActors` 里的正式演员根节点。剧情开始时它们会被 Anchor 吸附，手调内容会被覆盖。
  - 不要移动 `Preview_*` 里面的模型子节点；只调整 `Preview_*` 根节点。新的预览组件会负责保持站姿、走路、说话或坐姿的指定动作帧，并在进入 Play Mode 前自动同步位置和朝向。
  - 日常运行 `Tools / Hearth / Replay / Apply 17F03 Minimal Loop Setup` 会保留已手调的 Anchor。只有 `Rebuild 17F03 Anchors From References` 才会用旧参考模型覆盖 Anchor，非必要不要运行。
- 关联关系：`Preview_* -> Hearth17F03StagingPoseProxy -> Anchors -> HearthCompanion17F03ReplayController -> RuntimeActors`。
- 扩展入口：若未来给女儿增加新的夜间路线点，先在 `Anchors` 下创建对应 Anchor，再在摆演预览工具中加入一个对应 `Preview_*` 项，并将该 Anchor 拖入 `HearthCompanion17F03ReplayController / Daughter Night Path Points` 数组。
- 最小测试：编辑模式下应看到 8 个预览人物且没有碰撞；Play Mode 中预览人物全部不可见、不可碰撞，正式第一/二/三幕演员按流程正常出现。
- 最近修复：预览层出现后重新运行 `Apply 17F03 Minimal Loop Setup`，不会再把 `Preview_*` 内的同名模型误认成正式演员。若历史场景中某个预览根节点意外缺少可视模型，重新运行 `Create Or Update Preview` 会只补回缺失的可视子模型，保留你已经摆好的 `Preview_*` 根节点位置和朝向。

### Hearth17F03MinimalLoopBinder.cs（17F03 演员去重保护）

- 路径：`Assets/Editor/Hearth17F03MinimalLoopBinder.cs`。
- 本次修改：正式演员来源会优先读取 `RuntimeActors` 下对应的正式模型，并明确排除 `StagingPreview_17F03` 内的编辑预览副本；不再使用全场景同名对象的随机第一项。
- 自动修复：重新运行 `Tools / Hearth / Replay / Apply 17F03 Minimal Loop Setup` 时，每个正式演员根节点只允许保留一份同名可视模型；多余副本会被清理，避免第一幕出现两个重叠母亲。
- 检查方式：`Tools / Hearth / Replay / Validate 17F03 Minimal Loop Setup` 现在会检查母亲、第一幕父亲、第二幕父亲和女儿的正式根节点是否各只有一份可视模型。发现重复或缺失会在 Console 报出明确对象名。
- 使用方式：无新增 Inspector 字段。摆位仍只调整 `StagingPreview_17F03/Preview_*`，然后执行 `Apply Preview Poses To Anchors`；正常重新绑定不会覆盖这些 Anchor。

## 2026-07-12 17F03 母亲起身动作根位移保留

### HearthActorRootMotionRelay.cs

- 路径：`Assets/Scripts/MinLoop/HearthActorRootMotionRelay.cs`
- 脚本作用：把 Animator 子模型产生的实际水平位移转存到外层正式演员根节点，随后把子模型恢复到本地基准位置。这样动作结束后切换到 Talking 不会把人物拉回起点。
- 挂载对象：由 `Hearth17F03MinimalLoopBinder` 自动挂到每个正式演员的 Animator 所在对象；当前母亲的实际引用为 `Actor_Mother_17F03_RuntimeRoot/casual_Female_G@Sit_To_Stand`。
- Inspector 关键字段：
  - `Actor Root`：接收世界位移的外层演员根节点。母亲应为 `Actor_Mother_17F03_RuntimeRoot`。
  - `Apply Horizontal Position`：默认开启，只转存 XZ 位移，不会让角色随动作上下漂浮。
  - `Apply Rotation`：默认关闭。之后若某个动作确实需要保留身体转向，可手动开启。
  - `Restore Animator Child Local Transform`：默认开启，确保骨骼模型的局部位移不会累积或在下一段动画中回弹。
- 公开方法：`Configure(actorRoot)` 自动绑定外层演员；`CaptureBaseline()` 重新记录本地基准；`RestoreAnimatorChildBaseline()` 将模型恢复到基准。
- 使用方式：通常不需要手动挂载或调用。给未来某条动作 State 打开 `Apply Root Motion` 后，组件会自动把该动作的水平移动保留到正式演员根节点。
- 最小测试：播放母亲 `SitToStand` 后，`Actor_Mother_17F03_RuntimeRoot` 应离开 `Anchor_Mother_17F03_Human` 约 `0.24m`；子模型 local position 保持基准；切换 `Talking` 后世界位置不回弹。

### HearthActorAnimatorDriver.cs / Hearth17F03MinimalLoopBinder.cs

- 本次修改：17F03 母亲 `SitToStand` 是唯一默认开启 `Apply Root Motion` 的 State；`Talking` 与 `StandingArguing` 仍保持原地。
- 导入规则：`Assets/Animations/Hearth/17F03/Clips/casual_Female_G@Sit_To_Stand.fbx` 的 XZ Root Motion 不再 Bake Into Pose。绑定工具会自动恢复该设置，并在 `Validate 17F03 Minimal Loop Setup` 中检查。
- 流程规则：第一幕中母亲起身后，位移会保留并延续到 Talking；进入第二幕时，流程会按设计把她吸附到 `Anchor_Mother_17F03_Midday`，这是刻意的幕次切换，不是回弹。
- 使用方式变化：如要更换母亲的起身动作，替换 `MotherBasePath` 对应 FBX 后运行 `Apply 17F03 Minimal Loop Setup`，再运行 `Validate 17F03 Minimal Loop Setup`。不要手动把 `SitToStand / Apply Root Motion` 关闭。

## 2026-07-14 17F03 门开启与实体检查镜头平移

### SmartDoorController.cs

- 路径：`Assets/Scripts/Interactions/SmartDoorController.cs`。
- 脚本作用：通用门的滑动、旋转或滑动加旋转控制，并提供 `Open()`、`Close()`、`Toggle()`、`SnapOpen()`、`SnapClosed()` 接口。
- 本次内部修正：门的动画结束帧会继续遵守 `Motion Mode`。当模式为 `Rotate` 时，只更新门扇旋转，不会再错误套用 `Open Local Position Offset` 并把门扇平移进墙体；滑动门和滑动加旋转门维持原有行为。
- 17F03 挂载对象：`17F/ROOM2/Door_2_Brown (7)/DoorHinge_17F03`。`Door_2_Brown (7)` 根节点只负责整体位置，真正转动的是侧边轴子对象。
- Inspector 关键字段：
  - `Moving Root`：保持为当前 `Door` 子对象。
  - `Motion Mode`：17F03 使用 `Rotate`。
  - `Closed Local Euler Angles`：关闭时角度，当前为 `(0, 0, 0)`。
  - `Open Local Euler Offset`：17F03 当前为 `(0, -90, 0)`，这是朝远离墙体的一侧打开的方向。若以后换了门的铰链方向，只改这里的 Y 值为 `90` 或 `-90` 后 Play 测试，不要移动门的旋转轴。
  - `Move Duration`：当前约 `0.55` 秒，可按演出节奏调整。
- 使用方式无变化：女儿第三幕仍由 `HearthCompanion17F03ReplayController` 在门口停顿后调用 `Open()`。
- 最小测试：在 Play Mode 调用 `Open()` 后，门扇应保持其关闭时 local position，只旋转到 `Y = 270`（即 `-90`）而不进入墙体。

### HearthCompanion17F03ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F03ReplayController.cs`。
- 挂载对象：`MIN_LOOP_ROOT/ReplayRoom_17F03`。
- 本次新增 Inspector 字段：
  - `Use Smooth Inspection Camera Transition`：默认开启。控制米娅从第一幕人类视角检查实体陪伴单元时，是否使用平移镜头。
  - `Inspection Camera Transition`：绑定 `UnitInspectionCameraTransition_17F03`。
- 行为：父母对话结束、玩家面向实体 `ROBOT` 按 E 后，流程从人类 Camera 平滑移动到 `ROBOT/Camera (1)`，完成后打开故障检查 UI；按 Esc 返回时使用反向平移。若组件或相机引用缺失，自动退回原有黑场切换，不会中断关卡。
- 公开接口：`OpenUnitInspection()`、`CancelFlow()` 的调用方式不变；VR 或其他输入仍可直接调用它们。
- 最小测试：进入检查后，`ROBOT/Camera (1)` 启用、人类 Camera 关闭、黑场透明度为 `0`；退出后状态反转。

### Hearth17F03MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F03MinimalLoopBinder.cs`。
- 脚本作用：17F03 场景的 Editor 自动绑定工具。
- 本次修改：运行 `Tools / Hearth / Replay / Apply 17F03 Minimal Loop Setup` 时，会把 `Door_2_Brown (7)/DoorHinge_17F03` 的默认开启方向重设为 `(0, -90, 0)`，并创建或复用 `MIN_LOOP_ROOT/ReplayRoom_17F03/UnitInspectionCameraTransition_17F03`。
- 新镜头组件默认值：进入 `0.5s`、退出 `0.5s`、使用非缩放时间、平滑退出开启。日常重新运行绑定菜单会保留同名组件；如要自定义速度，直接改该组件 Inspector 的 `Enter Duration` / `Exit Duration`。
- 最小测试：重跑绑定后，第三户门仍为向外旋转；实体 ROBOT 的 E 检查仍能正常进入和退出。

## 2026-07-14 17F03 交互限制与 17F04 自宅终局

### 一次性场景应用与检查

- 应用菜单：`Tools / Hearth / Finale / Apply 17F04 Home Finale Setup`。
- 验证菜单：`Tools / Hearth / Finale / Validate 17F04 Home Finale Setup`。
- 自动生成根节点：`MIN_LOOP_ROOT/Finale_17F04`，下含 `Anchors`、`UI` 和 `Hearth17F04FinaleController`。
- 自动生成资源：
  - `Assets/Prefabs/UI/HearthHud/V2/Terminals/Terminal_17F04_Home_V2.prefab`
    存在时作为正式 `TV (3)` 标准化来源；
    `Assets/Prefabs/UI/HearthHud/Terminals/Terminal_17F04_Home.prefab`
    只作为 V2 资产不存在时的 Legacy 回退。
  - `Assets/Data/MinLoop/Dialogues/17F04/*.asset`
  - `Assets/materials/Hearth/17F04_Photo_Unlit.mat`
- 菜单可安全重跑：保留已有同名 Anchor、TV 固定相机、女儿/机器人位置和用户手调 Transform；不会用 `Person Controller (2)/(3)` 再次覆盖已有 Anchor。
- 正式场景处于 V2 时，安全重跑还必须保持 Home Terminal 的 V2 Marker 与整场
  `7/7 V2`，并补齐共享控制锁、唯一 ViewSwitch、TV hardware root 和 World Camera。
- 只有需要首次接入或修复引用时运行 Apply；日常只改 Anchor、Dialogue Sequence 或 Inspector 字段后，不必重跑。

### IInteractable1.cs / IInteractionAvailability

- 路径：`Assets/Scripts/Interactions/IInteractable.cs`。
- 脚本作用：除原有 `IInteractable` 外，增加 `IInteractionAvailability.IsInteractionAvailable`，让对象在剧情阶段不正确时既不显示 E，也不能被执行。
- 挂载方式：接口本身不挂 GameObject；由门、相框、实体机器人等交互脚本实现。
- 公开接口：`bool IsInteractionAvailable { get; }`。
- 扩展方式：新增剧情交互时同时实现 `IInteractable` 与 `IInteractionAvailability`，把阶段判断写在可用性属性中，不在 `PlayerInteraction` 里硬编码关卡名。

### PlayerInteraction.cs

- 路径：`Assets/Scripts/Interactions/PlayerInteraction.cs`。
- 挂载对象：正式人类与机器人控制器上的原有 `PlayerInteraction`；挂载和 Inspector 使用方式无变化。
- 本次修改：射线会从命中 Collider 及其父级脚本中寻找第一项“当前可用”的交互，不会再被同一物体上的旧门脚本遮挡；不可用交互不显示提示也不执行。
- 公开接口仍为：`SetInteractionEnabled(bool)`、`SetInteractionCamera(Camera)`。
- 最小测试：剧情锁定前看向对象无提示；阶段开放后同一射线立即显示正确提示；同一 Collider 上旧交互不可用时仍能找到父级的新交互。

### SmartDoorController.cs

- 路径：`Assets/Scripts/Interactions/SmartDoorController.cs`。
- 新字段：`Allow Direct Player Interaction`。关闭后玩家 E 不再切换门，但剧情脚本仍可调用 `Open()`、`Close()`、`SnapOpen()`、`SnapClosed()`。
- 17F03 挂载对象：`17F/ROOM2/Door_2_Brown (7)/DoorHinge_17F03`。
- 17F03 当前值：`Motion Mode = Rotate`、`Open Local Euler Offset = (0,-90,0)`、`Move Duration = 0.55`、`Allow Direct Player Interaction = false`。
- 门轴说明：`DoorHinge_17F03` 就是资源原本位于门板侧边的真实旋转轴；门板在轴局部 X 方向距边缘约 `0.063m`，不再额外包父物体改变坐标。
- 公开接口：`SetDirectPlayerInteractionAllowed(bool)`；其他门默认仍允许直接 E，使用方式无变化。
- 最小测试：看向 Door7 不显示 E；剧情调用 `Open()` 后门只绕侧轴向外旋转，不平移进墙。

### Hearth17F03UnitInteractable.cs / ComfortActionInteractable.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F03UnitInteractable.cs`、`Assets/Scripts/MinLoop/ComfortActionInteractable.cs`。
- 本次修改：实现 `IInteractionAvailability`，把原有阶段开关同步给统一交互系统。
- 使用方式无变化：仍由各关卡流程控制 `SetAvailable` 或对应阶段状态；不需要新增 Inspector 引用。

### HearthTerminalCameraTransition（17F03 复用）

- 组件位置：`MIN_LOOP_ROOT/ReplayRoom_17F03/UnitInspectionCameraTransition_17F03`。
- 用途：米娅当前相机到 `ROBOT/Camera (1)` 的进入/退出平移，默认各 `0.5s`。
- 可调字段：`Enter Duration`、`Exit Duration`、缓动曲线、`Smooth Exit`。
- 关联：由 `HearthCompanion17F03ReplayController.OpenUnitInspection()` 调用；引用缺失才退回黑场。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`。
- 本次新增：`Primary Action = Custom`、`On Custom Primary Action`、`RequestCustomPrimaryAction()`，让 TV3 调用自宅流程而不冒充前三户回放。
- TV3 挂载位置：`17F/ROOM4/TV (3)/MonitorCanvas/Terminal_17F04_Home`。
- 关键值：`Replay Resident Id = 17F04`、`Primary Action = Custom`；事件绑定 `Hearth17F04FinaleController.BeginFromHomeTerminal()`。
- 原有终端的 Tab、左右、Space、Esc、0.5 秒镜头平移、开机闪烁和音效字段保持不变。
- 本次内部安全修正：只对活动且非 Kinematic 的玩家刚体清零速度；Inspector 使用方式无变化。

### HearthFirstPersonHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudController.cs`。
- 本次新增外部结局路由：`SetRouteFinalChoiceInternally(bool)`。17F04 设置为外部路由后，A/B 与关闭页面只发事件，不会自动跳入旧结局链。
- 17F04 使用事件：`OnFinalChoiceA`、`OnFinalChoiceB`、`OnGracefulShutdownConfirmed`、`OnForcedShutdownConfirmed`、`OnShutdownCancelled`。
- 公开方法：`ShowShutdownConfirmation(bool highTrust)`；高信任显示一次确认，低信任进入三段警告。
- 其他第一人称 HUD 页面与前三户使用方式无变化。

### HearthHouseholdProgressState.cs

- 路径：`Assets/Scripts/MinLoop/HearthHouseholdProgressState.cs`。
- 挂载对象：`MIN_LOOP_ROOT/FlowManagers/HearthHouseholdProgressState`。
- 作用：记录已完成住户编号，并提供前三户是否完成的统一查询。
- 公开接口：`MarkHouseholdCompleted(string)`、`HasHouseholdCompleted(string)`、`AreFirstThreeCompleted`、`CompletedHouseholds`、`ResetProgress()`、`HouseholdCompleted` 事件。
- 当前 17F04 的 `Require Previous Households` 默认关闭；以后开启后，TV3 会在前三户未完成时拒绝进入。
- 存档接入：存档读取后依次调用 `MarkHouseholdCompleted`；开始新档调用 `ResetProgress()`。

### Hearth17F04FinaleController.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F04FinaleController.cs`。
- 挂载对象：`MIN_LOOP_ROOT/Finale_17F04/Hearth17F04FinaleController`。
- 依赖：正式 `Player/Person Controller`、三个 Mia Anchor、TV4 相框、Door1 专用交互、`ROBOT (1)` 交互、第一人称 HUD、两个字幕播放器、黑幕 Canvas、14 个 Dialogue Sequence、`HearthShutdownChallenge`、信任度与住户进度。
- 状态：`Inactive / HomeTerminal / LivingRoom / Photo / DaughterRoom / Dialogue / FinalChoice / ApproachUnit / Shutdown / Epilogue / Complete`。所有 E、Space、A/B 都按当前状态过滤。
- 关键 Inspector 字段：
  - `Require Previous Households`：是否要求前三户完成，当前关闭。
  - `Trust Preview Override`：正常使用 `Use Runtime Trust`；测试可强制 High/Low。
  - `Fade Out / Black Hold / Fade In Seconds`：跨房间黑场节奏，当前 `0.5 / 0.1 / 0.5`。
  - `Sequences`：所有对白与四种结局文本，可直接替换 ScriptableObject。
- 公开接口：`BeginFromHomeTerminal()`、`BeginPhotoInspection()`、`CompletePhotoInspection()`、`EnterDaughterRoom()`、`ChooseAnswerSelf()`、`ChooseCompanionAnswer()`、`BeginUnitShutdown()`、`CompleteFinale()`、`ResetForPreview()`、`OnFinaleCompleted`。
- 控制规则：客厅、女儿房间 Dialogue、A 路线接近机器人阶段维持自由移动/转头；只有 FinalChoice、Shutdown、Epilogue 锁控制。
- 信任规则：只读取前三户分数，不在最终选择中加减；`>0` 高、`<0` 低、`0` 默认高信任预览并警告。
- 最小测试：按状态顺序完成 TV3、相框、Door1、对话、A/B、关闭/保留、黑幕并返回走廊；A/B 只能提交一次。

### HearthPhotoFrameInteractable.cs

- 路径：`Assets/Scripts/MinLoop/HearthPhotoFrameInteractable.cs`。
- 挂载对象：`17F/ROOM4/TV (4)`；需要交互 Collider、照片固定 Camera、`HearthTerminalCameraTransition`。
- Inspector 字段：`Player Camera`、`Photo Camera`、`Camera Transition`、三项玩家控制引用、`Player Rigidbody`、退出键 Space/Esc。
- 行为：仅在控制器 `CanInspectPhoto` 时显示 `E VIEW PHOTO`；0.5 秒平移到照片正面，锁移动/视角；对白完成后 Space/Esc 平滑返回。
- 公开接口：`OpenView()`、`CloseView()`、`NotifyDialogueComplete()`、`IsOpen`、`IsTransitioning`、`IsInteractionAvailable`、`Configure(...)`。
- 图片入口：TV4 子物体 `photo` 的材质使用 `17F04_Photo_Unlit.mat`；替换材质主纹理即可换图，不必改脚本。第二张照片可再建一个相同组件和 Dialogue Sequence。

### Hearth17F04RoomDoorInteractable.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F04RoomDoorInteractable.cs`。
- 挂载对象：`17F/ROOM4/Door_2_Brown (1)`，需要射线可命中的 Collider。
- 作用：相框及后续客厅对白完成前不可用；可用后 E 调用 `EnterDaughterRoom()`。门板不播放开门动画，流程以黑场传送到女儿房间 Anchor。
- 公开接口：`IsInteractionAvailable`、`SetController(...)`。日常无需手动调用。

### Hearth17F04HomeUnitInteractable.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F04HomeUnitInteractable.cs`。
- 挂载对象：`GameObject/ROBOT (1)/InteractionVolume_17F04`；实体阻挡由同级 `PhysicalBodyCollider_17F04` 负责。
- 作用：只有 A 路线对话结束并进入 `ApproachUnit` 后显示 `E SHUT DOWN UNIT`。
- 公开接口：`SetAvailable(bool)`、`SetController(...)`、`IsInteractionAvailable`。
- 扩展方式：以后改成机器人屏幕或独立按钮时，把同一组件移到新的交互 Collider，控制器流程无需改。

### HearthShutdownChallenge.cs / HearthSequentialShutdownChallenge.cs

- 路径：`Assets/Scripts/MinLoop/HearthShutdownChallenge.cs`、`Assets/Scripts/MinLoop/HearthSequentialShutdownChallenge.cs`。
- 挂载对象：具体实现与 17F04 控制器挂在同一 GameObject。
- 抽象接口：`BeginChallenge(bool highTrust)`、`Submit()`、`Cancel()`、`Completed`、`Cancelled`、`IsRunning`。
- 当前实现：高信任一次 Space；低信任三段警告各一次 Space。显示层复用第一人称 HUD。
- 后续滑块小游戏：新建继承 `HearthShutdownChallenge` 的组件，实现三个公开方法后拖到控制器 `Shutdown Challenge` 字段即可，不改终局状态机。

### Hearth17F04FinaleBinder.cs

- 路径：`Assets/Editor/Hearth17F04FinaleBinder.cs`，Editor 工具，不挂运行时 GameObject。
- 作用：建立 TV3 自宅终端、TV4 相框、三个 Mia Anchor、Door1、ROBOT1、黑幕、字幕、对白资产、进度状态及所有引用，并清理 TV3/TV4 上错误的 17F02 终端。
- 必需场景对象：`Player/Person Controller`、`Player/Person Controller (2)`、`Player/Person Controller (3)`、`17F/ROOM4/TV (3)`、`TV (4)`、`Door_2_Brown (1)`、`GameObject/ROBOT (1)`。
- 参考控制器规则：`Person Controller (2)/(3)` 只提供首次 Anchor 与 CameraPose，Play 时 Camera、AudioListener、Collider、移动组件被禁用；正式运行只移动 `Player/Person Controller`。
- 验证内容：错误终端残留、Door7 直接 E、缺失对象、重复 Camera/AudioListener。
- 最小验收：Validate 输出 `Validation passed`，编辑状态只有一个启用 Camera 与一个启用 AudioListener。

### HearthEditorOnlyReferenceModel.cs

- 路径：`Assets/Scripts/MinLoop/HearthEditorOnlyReferenceModel.cs`。
- 本次修改：除 Renderer、Collider、行为脚本外，Play 时也关闭参考对象上的 Camera 与 AudioListener，避免参考控制器成为重复视角/声音来源。
- 使用方式无变化：只挂参考模型或参考控制器，不挂正式运行演员。

### ViewSwitchController.cs / HearthPlayerControlLock.cs

- 路径：`Assets/Scripts/MinLoop/ViewSwitchController.cs`、`Assets/Scripts/UI/HearthHud/FirstPerson/HearthPlayerControlLock.cs`。
- 本次仅做内部安全修正：清理速度时跳过非活动或 Kinematic 刚体，避免关卡切换产生 Unity 警告。
- 挂载、Inspector 引用、公开接口和前三户使用方式无变化。
- 验收：进入 TV3、相框或最终选择时，Console 不再新增 `Setting velocity of a kinematic body is not supported`。

## 2026-07-15 17F04 终端黑场交接与玩家相机枢轴修正

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`。
- TV3 新字段：`Defer Custom Action Close Until External Fade = true`。Space 提交 Custom Action 后，终端停止接收重复输入，但继续保持 TV 固定相机与当前页面，直到外部剧情把黑幕盖满。
- 新公开接口：
  - `SetDeferCustomActionCloseUntilExternalFade(bool)`：为需要自行控制黑场的 Custom 终端启用/关闭延迟交接。
  - `CompleteCustomActionHandoff()`：黑幕完全遮住后关闭终端、恢复玩家相机和原有控制状态。
  - `CancelCustomActionHandoff()`：剧情入口被拒绝时恢复终端输入，不让页面卡死。
  - `IsCustomActionHandoffPending`：供剧情或测试查询当前是否等待交接。
- 挂载和其他终端使用方式无变化。17F01/02/03 仍按原逻辑立即进入各自流程；只有 TV3 的 17F04 Custom Action 开启该字段。
- `GetReplayResidentId()` 现在正式识别 `17F04 / ROOM4`，不会再把第四户回退推断为 17F01。
- 最小测试：进入 TV3 后按 Space，黑幕开始时活动相机仍应是 `17F/ROOM4/TV (3)/Camera`；黑幕完全盖住后才切回正式玩家相机。

### Hearth17F04FinaleController.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F04FinaleController.cs`。
- 新引用：`Home Terminal` 必须指向 `17F/ROOM4/TV (3)/MonitorCanvas/Terminal_17F04_Home`；引用缺失时脚本会按 `Resident Id = 17F04 + Primary Action = Custom` 自动查找。
- 进入自宅顺序改为：TV 固定视角开始渐黑 -> 黑幕完全遮住 -> `CompleteCustomActionHandoff()` -> 传送正式玩家 -> 从黑幕渐亮。不会再先回到按 E 前的人类视角。
- 玩家传送不再直接写子相机世界坐标。脚本会保存 `Player/Person Controller/First Person Camera` 的本地枢轴位置，并把控制器根节点对齐到 `CameraPose` 的世界朝向，防止物理更新延迟制造巨大的相机本地偏移。
- 新公开接口：`CaptureCurrentHumanCameraPivot()`。如果以后在 Edit Mode 主动修改了正式玩家相机的本地高度/偏移，正常重新进入 Play Mode 会自动重新读取；运行时动态改完后可调用此接口重新标定。
- Inspector 调整方式：到达位置改对应 `Anchor_Mia_17F04_*`，到达视线改其子物体 `CameraPose`；正式玩家相机的本地高度统一改 `Player/Person Controller/First Person Camera`。普通调整不需要重跑绑定菜单。
- 最小测试：进入客厅后相机 `localPosition` 保持 `(0, 1.488, 0)`；转动 45 度后相机相对控制器的水平半径仍为 `0`，移动和转头正常。

### Hearth17F04FinaleBinder.cs

- 路径：`Assets/Editor/Hearth17F04FinaleBinder.cs`。
- 本次更新：生成/修复 TV3 时自动开启延迟 Custom 交接，并把 `Home Terminal` 写入 17F04 控制器；
  当前最终实现还会在 V2 Prefab 可用时保持 V2 Wrapper。使用方式无变化。
- Validate 现在也会检查 TV3 的延迟交接开关、`Home Terminal` 引用和可用 V2 Marker，
  任一项被手动清空或降级都会明确报错。
- 后续重跑 `Tools / Hearth / Finale / Apply 17F04 Home Finale Setup` 不会取消这项设置，也不会覆盖已存在的 Mia Anchor 与 `CameraPose`。

## 2026-07-15 17F04 A/B 垂直输入、相框对白排队与猫咪引导

### Hearth17F04CatGuideController.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F04CatGuideController.cs`。
- 挂载对象：`MIN_LOOP_ROOT/Finale_17F04/CatGuide/CatMoveRoot`。`CatMoveRoot` 是唯一正式猫咪；`CatMoveRoot (1)-(7)` 只是编辑器中的位置与朝向参考。
- 依赖组件：同对象上的 `HearthActorAnimationPlayer`；正式猫模型子节点 `Cat_L_Red-D` 上的 `Animator`。
- `Route Steps` 固定七段，当前时长依次为 `1.5 / 1.5 / 1.5 / 1.5 / 7.5 / 0.5 / 0.5` 秒；相较旧配置整体加速为 2 倍。前六段使用 `Walk_F`，最后一段使用 `Run_F`。
- `Walk_F` 已在 FBX Import Settings 中单独启用 `Root Transform Position (XZ) / Bake Into Pose`，只保留原地走路肢体动作；世界位置完全由 `CatMoveRoot` 的路线插值控制，避免动画每次循环产生位移和回跳。
- `Path Smoothing` 默认 `0.75`。路线使用连续速度的 Hermite 曲线通过参考点，不再在每个点重新启动一段独立直线；参考点旋转也会平滑减速/起转。调到 `0` 可退回直线，通常不要低于 `0.5`。
- `Run_F` 保留原导入位移信息，但运行时世界落点仍由第 6 点到第 7 点的父级抛物线控制，不会修改现有跳上沙发的表现。
- `Jump Arc Height` 默认 `0.25`，控制第 6 点到第 7 点的额外抛物线高度。最后一帧会精确对齐第 7 点的位置与朝向，不依赖动画 Root Motion 决定落点。
- 到达第 7 点后先播放一次 `Lie_to`，再循环 `Lie_idle`。
- 公开接口：`BeginSequence()`、`ResetSequence()`、`StopSequence()`、`CaptureCurrentAsStartPose()`、`SnapToRoutePoint(int)`、`IsRunning`、`HasReachedPhoto`、`OnReachedPhoto`、`OnSequenceCompleted`。
- 调整路线：直接移动或旋转 `CatMoveRoot (1)-(7)`，再次 Play 即生效，不需要重跑绑定菜单。调整正式猫起点后，在组件 Inspector 点击 `Capture Current Pose As Start`。
- 最小测试：进入客厅渐亮后猫自动开始；Play Mode Inspector 可点击 `Begin Play Mode Preview`。最终应停在第 7 点并趴卧，参考猫在 Play Mode 中不可见、不可碰撞。

### Hearth17F04CatGuideBinder.cs

- 路径：`Assets/Editor/Hearth17F04CatGuideBinder.cs`，Editor 工具，不挂运行时对象。
- 应用菜单：`Tools / Hearth / Finale / Apply 17F04 Cat Guide Setup`。
- 验证菜单：`Tools / Hearth / Finale / Validate 17F04 Cat Guide Setup`。
- 功能：把八个猫对象归到 `CatGuide`，将 `(1)-(7)` 标记为编辑器参考，停用 Legacy `Animation`，为正式猫绑定 Generic `Walk_F / Run_F / Lie_to / Lie_idle`，并把猫咪控制器接到 17F04 状态机。应用时还会确保 `Walk_F` 使用 XZ 原地烘焙和 `Loop Pose`，并为旧场景补上路线平滑值。
- 安全重跑：不会覆盖八个猫对象的 Transform；已手动修改的非旧默认段时长和跳跃高度会保留。若检测到旧 `3 / 3 / 3 / 3 / 15 / 1 / 1` 配置，会只迁移为新的 2 倍速度配置。
- 验证菜单现在同时检查：`Walk_F` 是否为 XZ 原地动画、是否开启 `Loop Pose`、路线平滑是否启用，以及路线时长是否为 `1.5 / 1.5 / 1.5 / 1.5 / 7.5 / 0.5 / 0.5`。

### Hearth17F04CatGuideControllerEditor.cs

- 路径：`Assets/Editor/Hearth17F04CatGuideControllerEditor.cs`，自定义 Inspector，不挂 GameObject。
- 提供按钮：记录当前起点、复位到起点、吸附到指定路线点、Play Mode 开始/停止路线预览。
- Scene 视图选中正式猫时会显示路线连线、点位编号、动作类型和每段时长。

### HearthFirstPersonHudInput.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudInput.cs`；挂载与原来相同，仍在 `HearthHudRoot`。
- 新增 `HearthFinalChoiceInputProfile`：可指定最终选择使用水平或垂直方向键，并分别控制字母 A/B 与 Return 是否可提交。
- 新接口：`SetFinalChoiceInputProfile(...)`、`GetFinalChoiceInputProfile()`。
- 17F04 打开最终选择时临时切为 `Vertical / 禁止字母键 / 禁止 Return`，因此只接受 `↑/↓ + Space`；选择、重置或离开流程后恢复此前配置，前三户使用方式不变。

### Hearth17F04FinaleController.cs

- 使用方式变化：新增 `Cat Guide` 与 `First Person Hud Input` 引用，绑定工具会自动填写。
- 客厅渐亮后立即同时执行三件事：恢复玩家控制、开放相框、启动猫咪路线。猫咪到达沙发不是相框或房门的解锁条件。
- 欢迎对白与相框镜头已经解耦：欢迎对白期间可以立即按 E 进入相框固定视角；相框对白会等待欢迎对白结束后自动接续，公用字幕播放器不会重叠。
- 相框对白完成并退出后，才播放门内声音并开放女儿房间。
- 最终选择临时应用垂直输入配置；A/B 仍只能结算一次。

### HearthActorAnimationPlayer.cs

- 新接口：`PlayOnceForDuration(string clipId, float duration)`，通过调整播放速度让一次性动作匹配指定剧情段时长。
- 17F04 用它让 `Run_F` 与第 6→7 点的一秒跳跃段对齐；其他脚本原有使用方式不变。
- `Clip Slot` 新增 `Seamless Loop`。开启后，循环 Clip 使用 Unity 导入器的 `Loop Time / Loop Pose` 连续采样，不再由脚本在每轮结束时把时间硬跳回 `0`。17F04 的 `Walk_F` 和 `Lie_idle` 已开启；一次性动作保持关闭。
- 使用方式变化：新循环动作若要开启 `Seamless Loop`，必须同时在该 FBX Clip 的 Import Settings 开启 `Loop Time`，建议也开启 `Loop Pose`；现有其他关卡 Slot 默认关闭，行为不变。

### HearthEditorOnlyReferenceModel.cs

- 新增 `Disable Legacy Animations In Play`，运行时会停止并关闭子层级旧 `Animation`，避免参考猫播放旧动作或产生 Legacy Clip 警告。
- 现有参考角色、参考控制器的挂载方式无变化。

### 本次验收

- Unity 强制编译无新增 Error。
- 场景绑定为 1 只正式猫、7 只参考猫、7 段路线、4 个正式动作 Clip，旧 Legacy `Animation` 全部停用。
- 独立 Play Mode 路线完成后，正式猫与第 7 点位置误差 `0.00000m`、朝向误差 `0°`。
- 当前仍存在旧门 Missing Script、旧第三方 Animator、负缩放 BoxCollider 等历史警告；不是本次猫咪或 A/B 修改新增。

## 2026-07-16 Tab 菜单、英文交互、音频与对白维护接口

### HearthAudioSettingsController.cs

- 路径：`Assets/Scripts/Audio/HearthAudioSettingsController.cs`。
- 挂载对象：当前为 `HearthHudRoot`，同场景只保留一个。
- 作用：保存并应用 `Master / Dialogue / Ambient / SFX` 四个 0-100 音量；默认使用 PlayerPrefs 持久化。Master 直接控制 `AudioListener.volume`。
- Inspector：四个 Default Volume 是首次运行默认值；Runtime Values 是当前序列化值；`Load Saved Values On Awake` 与 `Save Changes Immediately` 默认开启。
- 公开接口：`GetVolume()`、`GetLinearVolume()`、`SetVolume()`、`ResetToDefaults()`、`SaveValues()`、`LoadSavedValues()`、`ApplyAll()`、`Resolve()`、`OnVolumeChanged`。
- 依赖：通过 `HearthAudioChannelSource.RefreshAll()` 更新已归类的 AudioSource。
- 最小测试：设置页把 Master 改为 62 后，验证 `AudioListener.volume = 0.62`；重新打开设置仍显示保存值。

### HearthAudioChannelSource.cs

- 路径：`Assets/Scripts/Audio/HearthAudioChannelSource.cs`。
- 挂载对象：需要纳入 Dialogue/Ambient/SFX 设置的每个 `AudioSource` 同物体。
- 必需组件：`AudioSource`。
- Inspector：`Channel` 选择 Dialogue、Ambient 或 SFX；`Base Volume` 是进入总线前的音量。
- 公开接口：`Configure()`、`SetChannel()`、`SetBaseVolume()`、`ApplyVolume()`、`RefreshAll()`。
- 后续接入：环境声添加本组件并选 Ambient；按钮/开机/交互短音效选 SFX；对白语音选 Dialogue。

### HearthFootstepAudioProfile.cs

- 路径：`Assets/Scripts/Audio/HearthFootstepAudioProfile.cs`。
- 正式挂载：人类 `Player/Person Controller/First Person Audio`；机器人 `Player/Robot Controller/Robot First Person Audio`。
- 必需组件：已有 `FirstPersonAudio`，并引用 `Step Audio` / `Running Audio`。
- 人类当前值：Walk `0.82`、Run `1.05`；机器人当前值：Walk `1.00`、Run `1.30`。
- Inspector：`Walk/Run Clip` 分别替换两种角色素材；`Playback Speed` 调当前循环录音步频；`Walk/Run Volume` 调进入 SFX 前音量。
- 公开接口：`Apply()`、`Configure()`、`SetPlaybackSpeeds()`、`SetFootstepClips()`。
- 当前限制：现有 `Steps` 是多脚步循环，改速度也会改音高；独立步频以后应换单步 One Shot 播放器。

### HearthSettingsView.cs / HearthFirstPersonHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/`。
- 使用方式变化：设置页不再只修改内存占位值；现在绑定 `HearthAudioSettingsController`，并同步更新 Slides 22/23/24 的 12 个音量数字文本。
- `HearthFirstPersonHudController` 会监听设置页 `OnExitRequested`，选择 EXIT GAME 后打开 Slide24；确认后 Editor 停止 Play，Build 调用 `Application.Quit()`。
- 音量 UI 操作：`↑/↓` 选择，`←/→` 每次调整 5，Space 在 EXIT GAME 上确认。
- 控制锁与 Inspector 挂载方式不变；Tab 打开页面时继续锁移动/视角/普通交互。

### HearthDispositionHistoryView.cs / HearthFirstPersonHudFlowBinder.cs

- 使用方式无变化。
- 内部修正：`SHIFT TRUST DELTA` 改为已显示记录的 `trustDelta` 总和；`CURRENT TRUST` 继续显示真实当前信任。
- Flow Binder 初始化时优先读取 `TrustStateController.CurrentTrust`，不再用旧 Inspector 起始占位覆盖真实分数。
- 运行验证：1/2 条记录分别打开 Slide19/20；`+1` 与 `-1` 合计显示 `+0`，当前信任显示 `0`。

### PlayerInteraction.cs 与单按交互说明

- 路径：`Assets/Scripts/Interactions/PlayerInteraction.cs`。
- 挂载方式和射线长度使用方式不变。
- 新字段：`English Prompts Only`、`Normalize Single Press Prompts`、`Interaction Key Label`。当前正式控制器统一为 `true / true / E`。
- 新接口：`SetSinglePressPromptPolicy(bool englishOnly, bool normalize, string keyLabel)`。
- 作用：单按交互统一输出 ASCII 英文，自动清理重复 `E` 前缀；若旧场景仍保存中文，会回退到 `E  INTERACT`，避免 TMP 方框。
- 长按 E 进度条没有改动，仍由关卡 Hold Prompt 负责。
- 同步改为英文默认值的脚本：`InteractionFeedbackController`、`SmartDoorController`、`HearthTvTerminalInteractable`、`ComfortActionInteractable`、`ResidentTerminalFlow`、`MinLoopFlowController`、`MinLoopTerminalPresenter`、`MinLoopWorldGuideMarker`、`ReplaySequenceController`。

### MinLoopSubtitleLine / MinLoopSubtitlePlayer.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopTypes.cs`、`Assets/Scripts/MinLoop/MinLoopSubtitlePlayer.cs`。
- 每句新增 `Duration Mode` 与 `Voice Tail Seconds`。
- `VoiceClipWhenAssigned`：有语音时按语音长度，无语音时按手动 Hold；`ManualHold`：只按手动值；`LongerOfVoiceAndManual`：取较长者。
- 字幕 AudioSource 已归类为 Dialogue；位置、字体、无黑底居中样式和原有调用接口不变。

### HearthDialogueSequenceEditor.cs

- 路径：`Assets/Editor/HearthDialogueSequenceEditor.cs`，Editor 工具，不挂 GameObject。
- 作用：Dialogue Sequence Inspector 可直接增删、拖动排序、修改说话人/正文/延迟/时长模式，并逐句拖入 AudioClip。
- 修改资产后直接生效，不需要重跑场景工具。

### HearthRuntimeInterfaceBinder.cs

- 路径：`Assets/Editor/HearthRuntimeInterfaceBinder.cs`，Editor 工具，不挂 GameObject。
- 应用菜单：`Tools / Hearth / Systems / Apply HUD Audio And English Prompt Fixes`。
- 验证菜单：`Tools / Hearth / Systems / Validate HUD Audio And English Prompts`。
- 作用：绑定唯一音频设置、三组字幕 Dialogue 音源、正式人类/机器人脚步配置，并把当前场景单按 E 序列化提示迁移为英文。
- 安全范围：不会改角色、相机、UI Transform 或剧情锚点；可安全重跑，不会重复压低 AudioSource 基础音量。
- 当前场景验证：1 个音频设置控制器、Human/Companion 两个脚步 Profile、3 个字幕播放器、3 个 Ambience 占位音源、11 个 PlayerInteraction；英文交互验证通过。

### 独立调整文档

- 完整入口见根目录 `HEARTH_UI音频与对白调整入口.md`。

## 2026-07-16 分关卡剧情音效空槽与猫咪 Walk_F 步频

### HearthSfxCuePlayer.cs

- 路径：`Assets/Scripts/Audio/HearthSfxCuePlayer.cs`。
- 挂载对象：当前场景为 `MIN_LOOP_ROOT/Audio/StorySFX_17F02`、`StorySFX_17F03`、`StorySFX_17F04`。
- 功能：用稳定 Cue ID 管理剧情一次性音效和移动循环音效；Clip 为空时静默继续剧情。
- 必需组件：播放器本体不要求同对象有 AudioSource；每个 `Cue Slot / Source` 引用独立子物体 AudioSource，子物体同时挂 `HearthAudioChannelSource` 并归类到 SFX。
- Inspector 重点字段：
  - `Cue Id`：剧情脚本调用名，完成绑定后不要随意改名。
  - `Placement Note`：发声时机和用途说明。
  - `Automatically Triggered`：勾选表示剧情已经自动调用；未勾选表示仅保留接口。
  - `Primary Clip`：正式首选素材；直接拖入即可。
  - `Alternate Clips`：多个随机候选；Primary 为空时使用。
  - `Loop`：人物走路等持续声开启；脚本在停下/结束时停止。
  - `Follow Target / Local Offset`：移动人物或发声物体；播放期间 AudioSource 跟随。
  - `Follow While Playing`：开启时只在音效实际播放期间追随目标；关闭时保持最近一次吸附位置。
  - `Spatial Blend / Min Distance / Max Distance`：空间音效范围；系统内声可降低 Spatial Blend。
  - `Base Volume / Pitch / Random Pitch Range`：进入 SFX 音量通道前的单 Cue 参数。
- 公开接口：`PlayCue()`、`PlayCueOneShot()`、`StartCueLoop()`、`StopCue()`、`StopAllCues()`、`HasCue()`、`HasAssignedClip()`、`AssignPrimaryClip()`、`SnapSourcesToTargets()`。
- 与其他脚本关系：17F02/03/04 控制器持有各自播放器引用；音量设置通过 `HearthAudioChannelSource` 接入 `HearthAudioSettingsController`。
- 使用步骤：选中相应 `StorySFX_17Fxx`，展开 Cues，把音效拖入 `Primary Clip`；无需修改剧情脚本、无需重跑菜单。
- 最小测试：Clip 为空时完整流程不报错；拖入测试 Clip 后，AUTO Cue 在对应动作出现且移动 Cue 跟随演员。
- 后续扩展：新建 Cue 后调用公开方法；若需要逐脚落地声，可从动画 Animation Event 调 `PlayCueOneShot()`，不必改移动状态机。

### HearthStorySfxBinder.cs

- 路径：`Assets/Editor/HearthStorySfxBinder.cs`，Editor 工具，不挂 GameObject。
- 应用菜单：`Tools / Hearth / Audio / Apply Story SFX Placeholder Setup`。
- 验证菜单：`Tools / Hearth / Audio / Validate Story SFX Placeholder Setup`。
- 功能：建立 17F02-04 的 15 个空音效槽、独立 AudioSource、空间跟随目标、控制器引用和两扇剧情门的 SFX 音源。
- 安全范围：不导入或选择 AudioClip；重跑时保留已有 `Primary Clip / Alternate Clips` 和用户调整的角色、猫咪及锚点 Transform。
- 当前布局：17F02 7 个、17F03 6 个、17F04 2 个；其中 `Wife.SitOnBed` 是唯一未自动触发的 TBD 槽。
- 17F01 使用方式无变化：继续使用 Room Tone、正式机器人脚步、Companion HUD 和 TV 终端已有字段。
- 验收：菜单验证通过，15 个 Cue、0 个已分配 Clip、1 个保留时机槽；空 Clip 是预期状态。

### HearthCompanion17F02ReplayController.cs

- 使用方式有变化：新增 `Story SFX / Sfx Cue Player` 及 Cue ID 字段；当前自动绑定 `StorySFX_17F02`。
- 自动时机：SitToStand 开始播放起身；走位期间开启并跟随 `Wife.Walk`，门口停下时停止，出门继续，终点停止；餐桌开场、日志调取、故障和关机各触发一次。
- `Wife.SitOnBed` 暂不自动调用，因为现有黑屏对白无法确定落座的精确时刻。
- 新公开接口：`SetSfxCuePlayer()`；其余挂载对象、演员、路径、字幕和交互方式不变。

### HearthCompanion17F03ReplayController.cs

- 使用方式有变化：新增 `Story SFX / Sfx Cue Player`、Cue ID 和 `Deep Sleep Power Off Delay Seconds`，当前自动绑定 `StorySFX_17F03`。
- 自动时机：母亲/女儿起身、女儿行走、EnteringCode、深眠故障和关机。
- 女儿移动音源跟随 `Daughter Move Root`，到最后路径点立即停止。
- 新公开接口：`SetSfxCuePlayer()`；其他场景显隐、相机、门、凝视交互和字幕方式不变。

### Hearth17F04FinaleController.cs

- 使用方式有变化：新增 `Story SFX / Sfx Cue Player`，当前绑定 `StorySFX_17F04`。
- 相框开始检查时调用 `Photo.Memory`；关闭挑战完成后调用 `Unit.PowerOff`。
- 猫咪没有任何音效调用；TV3/TV4 镜头与终端音效仍走各自原接口。
- 新公开接口：`SetSfxCuePlayer()`；A/B、信任分流、相框、房门和黑幕流程不变。

### HearthActorAnimationPlayer.cs / Hearth17F04CatGuideController.cs

- `HearthActorAnimationPlayer` 新增 `PlayLoopAtSpeed(string clipId, float playbackSpeed)`，只覆盖本次播放速度，不修改 Slot 默认值。
- 猫咪控制器新增 `Walk Playback Speed`，当前 `2.0`；只用于 `Walk_F`。
- `Run_F / Lie_to / Lie_idle` 继续使用各自 Slot 的 `1.0` 速度。
- 路线点仍在 `BeginSequence()` 时直接读取当前 `CatMoveRoot (1)-(7)` Transform；第 4、5、6 点无需同步。
- 绑定工具会移除正式猫和参考猫的旧 Legacy `Animation`，只保留 `HearthActorAnimationPlayer + Animator`，避免 `Drop_L_sit` 报错和动作争用。
- Play Mode 验收：完整路线到达第 7 点，`HasReachedPhoto = true`，最终位置与当前第 7 点一致。

## 2026-07-17 P0 稳定性任务 STAB-01 / 02 / 03 / 05

### HearthP0StabilityTools.cs

- 路径：`Assets/Editor/HearthP0StabilityTools.cs`。
- 类型与挂载：Editor 工具，不挂载到 GameObject，不参与 Build 运行时。
- 功能：集中修复并验证三类启动稳定性问题：Door 源 Prefab Missing Script、剧情演员随机 `CityPeople` 动作、猫咪 Legacy `Animation` 残留。
- 应用菜单：`Tools / Hearth / Stability / Apply P0 01-03 Repairs`。
- 只读验证菜单：`Tools / Hearth / Stability / Validate P0 01-03`。
- Apply 行为：
  - 检查 `Door_2_Brown / Milk / White` 三份源 Prefab，并移除其中 Missing Script。
  - 只停用由 HEARTH 剧情 Animator、预览代理或编辑器参考模型控制的 `CityPeople.CityPeople`；不会修改素材库原 Prefab 的通用随机行为。
  - 只从 `CatMoveRoot` 与 `(1)-(7)` 清理 Legacy `Animation`；不改变路线点位置、朝向、Animator 或动作素材。
- 公开维护接口：`ApplyP0Repairs()`、`ValidateP0Repairs()`、`DisableCityPeopleAutoplayInHierarchy(GameObject, bool)`。
- 依赖：UnityEditor、PrefabUtility、当前打开的 `SampleScene`；猫咪检查依赖现有 `HearthActorAnimationPlayer` 体系。
- 使用步骤：退出 Play Mode 后运行 Apply；完成后运行 Validate。日常检查只运行 Validate，避免不必要的场景写入。
- 最小验收：Validate 输出 passed；清空 Console 后 Play 10 秒，不再出现 Door Missing Script、`Drop_L_sit` Legacy Error、CityPeople State/Layer Error。
- 后续扩展：若新增剧情演员，需要让演员挂 `HearthActorAnimatorDriver / HearthActorAnimationPlayer`，或放在 `MIN_LOOP_ROOT/ReplayRoom_17Fxx` 下，统一工具即可识别为受控演员。

### HearthEarlyHouseholdValidationTools.cs

- 路径：`Assets/Editor/HearthEarlyHouseholdValidationTools.cs`。
- 类型与挂载：Editor 只读验证工具，不挂 GameObject，不修改演员、锚点、终端页面或剧情数据。
- 菜单：
  - `Tools / Hearth / Replay / Validate 17F01 Minimal Loop Setup`
  - `Tools / Hearth / Replay / Validate 17F02 Minimal Loop Setup`
- 17F01 检查：小男孩中心射线交互体与 Collider、正式人类/机器人控制器、Camera、AudioListener、唯一 ViewSwitch、三个对白资产、三个演员动作、HUD 和 17F01 终端绑定。
- 17F02 检查：卧室长按确认门槛、四个演员、女主门前路径/门口/门外锚点、门控制器、八个对白资产、Animator 状态、正式控制器、HUD 和 17F02 终端绑定。
- 公共检查：正式 `Player/Person Controller` 与 `Player/Robot Controller`、`MinLoopFlowController`、`TrustStateController`、`MinLoopSubtitlePlayer`、唯一启用 Camera/AudioListener/ViewSwitch。
- 当前特殊规则：17F01 采用原地交互，不再把已删除的 Bedside Controller/Anchor 当作必需项；`FirstPersonLook` 按正式相机子物体检查。
- 当前场景修复：`Terminal_17F02 / ViewSwitch Controller` 已从旧 `1F (1)/GameObject (1)` 改绑到 `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`。
- 最小验收：两个菜单都输出 validation passed。任何 Error 都应先修引用，不要直接重跑整套 Apply 绑定器覆盖用户手调位置。
- 后续扩展：新增必需对白、交互门槛或演员状态时，应同步加入对应住户验证函数。

### Hearth17F03StagingPreviewTool.cs

- 使用方式有变化：每次创建或更新 17F03 分幕预览时，会调用 `HearthP0StabilityTools.DisableCityPeopleAutoplayInHierarchy()`。
- 作用：预览人物仍可由 `Hearth17F03StagingPoseProxy` 显示指定姿势，但不会在 Play 启动时自行随机切动作或添加碰撞体。
- 位置、朝向、预览应用到 Anchor 的工作流不变。

### MinLoopSceneValidator.cs

- 使用方式有变化：`Validate On Start` 默认改为关闭，当前场景实例也已关闭。
- 原因：它检查的是早期占位最小循环结构，会对已退役的 Presenter/Cue 给出建议警告；正式 17F01/02 应使用新的住户验证菜单。
- 手动使用仍保留：选中 `MIN_LOOP_ROOT/MinLoopSceneValidator` 后可从 Inspector 运行旧结构检查。
- 公开方法和挂载位置无变化。

## 2026-07-17 四户共享字幕、交互反馈与 17F04 病毒弹窗关闭

### HearthSubtitleStyleProfile.cs

- 路径：`Assets/Scripts/MinLoop/HearthSubtitleStyleProfile.cs`。
- 资产：`Assets/Data/MinLoop/UI/Hearth_SubtitleStyle.asset`，不挂 GameObject。
- 功能：集中保存四户普通对白和最终黑幕对白的宽度、说话人位置、正文位置、区域高度、字号下限/上限、最大行数和行距。
- 维护入口：在 Project 中选中 `Hearth_SubtitleStyle`。修改 `Standard Dialogue` 会同步影响 17F01-04 的普通对白；修改 `Centered Epilogue` 只影响 17F04 最终黑幕。
- 最小测试：修改普通正文大小后 Play 任一住户对白；四户均应使用同一布局，长句在屏幕约三分之二宽度内自动居中换行。

### MinLoopSubtitlePlayer.cs

- 使用方式有变化：新增 `Style Profile` 与 `Presentation Mode`，普通对白统一为 `Standard Dialogue`，最终黑幕为 `Centered Epilogue`。
- 正式普通对白只使用场景中的 `MinLoopSubtitlePlayer`；17F04 不再创建自己的 `SceneDialogue_17F04`。
- 文本现在开启 Auto Size、Word Wrapping、Max Visible Lines 和 Truncate，避免长句越出 16:9 屏幕。
- 公开接口新增：`SetPresentation(profile, mode)`；原 `ShowLine`、`PlaySequence`、`PlaySequenceAsset` 和逐句 `Voice Clip` 用法不变。
- Canvas 排序改为提升根 Canvas，避免嵌套 Canvas 在不同关卡中出现位置或可见性差异。

### HearthDialoguePresentationBinder.cs

- 路径：`Assets/Editor/HearthDialoguePresentationBinder.cs`，Editor 工具，不挂 GameObject。
- 应用菜单：`Tools / Hearth / Dialogue / Apply Shared Subtitle Presentation`。
- 验证菜单：`Tools / Hearth / Dialogue / Validate Shared Subtitle Presentation`。
- 功能：把 17F01、17F02、17F03、17F04 场景对白和通用回放控制器统一绑定到一个普通字幕播放器；17F04 黑幕使用同一 Style Profile 的居中模式。
- 使用步骤：只有引用丢失或新建场景播放器时才需要运行 Apply；日常只改 `Hearth_SubtitleStyle.asset`，不需要同步菜单。
- 当前验收：四户绑定验证通过，17F03 不再误用 17F04 居中黑幕播放器。

### HearthUiPressFeedback.cs / HearthTvTerminalController.cs

- `HearthUiPressFeedback` 路径：`Assets/Scripts/UI/HearthHud/HearthUiPressFeedback.cs`。
- 功能：在确认键按下后，将指定 Image/TMP Graphic 短暂变灰，再自动恢复；使用 Unscaled Time，不受剧情暂停影响。
- 公开接口：`Configure(Graphic[])`、`PlayFeedback()`。
- 17F04 TV3 终端 Prefab 已绑定 `ConfirmBack + Confirm` 两个目标；按 Space 入户时会立即灰显。
- `HearthTvTerminalController` 新增 `Submit Feedback` 字段和 `SetSubmitFeedback()`；其他终端未绑定时行为不变。

### HearthVirusPopupShutdownChallenge.cs

- 路径：`Assets/Scripts/MinLoop/HearthVirusPopupShutdownChallenge.cs`。
- 挂载对象：`MIN_LOOP_ROOT/Finale_17F04/UI/ShutdownChallenge_17F04`。
- 必需组件：同对象需有 Canvas、CanvasScaler、CanvasGroup、Image、`HearthUiPressFeedback`；子物体需有 `PopupLayer` 和 inactive 的 `PopupTemplate`。
- 功能：High Trust 显示一次确认弹窗；Low Trust 按蓝色、橙色、红色三轮依次运行。每轮先显示一个居中的主警告，按 Space 后才从屏幕四边持续生成该轮警告窗；清空本轮后进入下一轮，三轮全部清空才完成关闭。
- Inspector 重点：`Low Trust Waves` 中每轮可分别调整标题、正文候选、背景色、强调色、总数量、初始爆发数量和生成间隔；公共参数为 `Wave Transition Seconds`、进入/关闭时长、边距和随机种子。
- 公开接口沿用抽象基类：`BeginChallenge(bool)`、`Submit()`、`Cancel()`、`Completed`、`Cancelled`。
- 文本安全：模板标题、正文、按键文字及底部计数都启用自动缩放、换行和截断，不能越出各自浮窗。
- 输入反馈：每轮标题和底部状态文字会同步使用当前蓝/橙/红强调色；弹窗标题不显示序号。若 UI 引用意外丢失，脚本会输出 Warning 并继续流程，不会永久卡住。
- 旧 `HearthSequentialShutdownChallenge` 不再挂到 17F04，但脚本文件保留作兼容参考。

### HearthCompanionSpecialEffectsView.cs / HearthCompanionHudBuilder.cs

- 使用方式有变化：关闭、故障、深眠画面的标题、正文、状态和脉冲线统一以 1920 宽度中心对齐。
- 所有文字启用自动字号、最大行数和截断，避免蓝字/红字中心不同或正文越界。
- 新公开接口：`ApplyLayoutNow()`。
- 定向修复菜单：`Tools / Hearth / HUD / Apply Companion Special Effect Alignment`；只调整 SpecialEffectsView，不重建整套机器人 HUD，也不覆盖其他用户手调 UI。

### HearthCompanion17F01ReplayController.cs

- 使用方式有变化：小男孩对白完成后只需要一次长按 E 到 100%。
- 第一次确认后直接进入安抚对白和客厅观察，不再显示第二个重复长按按钮。
- `CompleteCurrentStep()` 在协程运行期间会忽略重复完成事件，避免同一帧双触发。
- 其余模型、交互胶囊、射线距离、字幕资产和终端返回方式无变化。

### Hearth17F04FinaleBinder.cs

- 使用方式有变化：Apply 菜单会生成并绑定病毒弹窗关闭挑战、TV3 Space 灰显反馈和共享字幕引用，同时清理场景中的旧顺序关闭组件。
- 验证菜单新增检查：TV3 反馈组件、新病毒挑战、旧挑战残留、Camera/AudioListener 和既有终端引用。
- 当前视觉验收：1920x1080 正式对白最多两行且不截断；病毒弹窗文字不越框；机器人故障标题、正文、状态水平居中。

### Door_2 源 Prefab

- 修改资源：`Door_2_Brown.prefab`、`Door_2_Milk.prefab`、`Door_2_White.prefab`。
- 清理内容：移除无法加载且运行时从未生效的旧 `FreeWoodDoorPack.Door` 组件。
- 保留内容：门板模型、Transform、Collider、AudioSource、材质和所有场景实例。
- 正式剧情门继续使用 `SmartDoorController`；开关角度、时长、交互许可和音效仍从该组件维护。

## 2026-07-17 17F02 动作过渡、第三幕 UI 与 17F04 三轮警告/猫咪加速

### HearthActorAnimatorDriver.cs

- 路径：`Assets/Scripts/MinLoop/HearthActorAnimatorDriver.cs`；继续挂在各剧情演员的 RuntimeRoot，Animator 指向可见角色子模型。
- 功能变化：共享动作切换现在至少使用 `Minimum Transition Seconds`，当前正式值为 `0.32s`，避免 Sitting、Talking、Stand、Walk 之间一帧抽动。
- `State Slot / Stabilize Animator Transform` 用于阻止 Mixamo Clip 改写可见子模型的本地位置、旋转和缩放。17F02 卧室女主五个状态已开启，世界移动仍只由 RuntimeRoot 和路线锚点控制。
- 公开接口新增：`SetMinimumTransitionSeconds(float)`、`SetAllStateStabilization(bool)`、`CaptureAnimatorTransformBaseline()`、`RestoreAnimatorTransformNow()`。
- Inspector 调整：想让过渡更柔和可把最小时长调到 `0.35-0.5s`；不要把带路线移动的 RuntimeRoot 自己指定为 Animator，否则稳定功能会自动跳过。
- 最小测试：依次触发 SittingTalking、SitToStand、WalkLoop，角色不应瞬间抽动，也不应被动画拉离 RuntimeRoot。

### HearthCompanion17F02ReplayController.cs / Hearth17F02MinimalLoopBinder.cs

- 挂载和主要引用不变；使用方式有变化。
- `Door Open Delay After Animation Start Seconds` 当前为 `0.5s`：女主开始播放开门动作后半秒调用实体门，比旧值提前半秒。
- 女主到达 `ExitOutside` 后会停止动作、恢复 Animator 子模型基准并再次精确吸附最终锚点，修复出门后短暂闪回门后的问题。
- 开始回放时自动重新捕获女主动画基准；缺少动作或门引用时仍按既有防卡死逻辑继续剧情。
- 绑定工具默认写入 `0.5s` 和动作稳定设置；日常只改 Inspector 数值无需重跑菜单。

### CompanionScene_07_17F02_04.asset / HearthCompanionHudBuilder.cs

- 第三幕 `17F02_04` 的 `Center Message` 已清空，正式画面只保留中央 `FAMILY LOG - TODAY` 投影面板，不再叠加蓝色大字。
- 生成器默认值也同步清空，后续重建机器人 HUD 不会把旧大字重新生成。

### Hearth17F04CatGuideController.cs / Hearth17F04CatGuideBinder.cs

- `Walk Route Speed Multiplier` 当前为 `1.5`，只作用于 `Walk` 路段；`RunJump` 和到达后的 `Lie_to / Lie_idle` 不加速。
- 当前编辑时长仍保留 `1.5 / 1.5 / 1.5 / 1.5 / 7.5 / 0.5 / 0.5`，运行时前六个 Walk 段分别除以 `1.5`；最后 6→7 的 RunJump 仍为 `0.5s`。
- Walk 腿部动作不再叠加路线倍率，继续只使用 `Walk Playback Speed`（当前 `2.0`）；目标位置继续实时读取 `CatMoveRoot (1)-(7)`，不会覆盖用户新调的第 4-7 点。
- 公开方法 `BeginSequence()`、`ResetSequence()`、`StopSequence()` 无变化；修改参考点后重新进入 Play 即生效。

## 2026-07-18 1F 大堂开场、任务终端、电梯与最终对白同步

### HearthLobbyFlowController.cs

- 路径：`Assets/Scripts/MinLoop/HearthLobbyFlowController.cs`。
- 挂载对象：`MIN_LOOP_ROOT/LobbyOpening/HearthLobbyFlowController`。
- 功能：统一管理 `Opening -> FreeExploration -> OptionalConversation -> AssignmentTerminal -> Elevator -> ArrivedFloor17`，避免三组触发区、终端和电梯各自修改玩家控制状态。任务领取成功后终端本轮进入已消费状态，不可再次打开。
- 关键引用：正式 `Player/Person Controller`、Human Camera、Movement/Look/Interaction/Rigidbody、`Person Controller (4)` 大堂锚点、`Person Controller (5)` 电梯锚点、17 楼到达锚点、`MinLoopSubtitlePlayer`、`HearthScreenFader`、`HearthLobbyHudOverlay`、一楼终端和三个可选对白区。
- `Hud Canvases Hidden During Terminal`：终端打开时临时隐藏人类 HUD/字幕画布，关闭终端后按原状态恢复，避免画面重叠。
- 公开接口：`BeginOpening()`、`TryPlayOptionalConversation(...)`、`AcquireAssignmentFromTerminal()`、`BeginElevatorRide()`、`ResetLobbyFlowForPreview()`。
- 事件：`On Opening Completed`、`On Assignment Loaded`、`On Elevator Entered`、`On Floor17 Arrived`，可在 Inspector 接音效、任务系统、存档或后续场景加载。
- 使用方式：移动 `Person Controller (4)/(5)` 后运行时直接生效；修改 17 楼正式落点时使用 Lobby Capture 菜单。
- 最小测试：开始游戏时米娅在大堂且完全锁定；开场结束可移动；可选对白只锁移动；Space 领取任务后终端关闭且不可再次打开；任务说明期间米娅可移动和转头但不能按 E；说明结束后电梯才显示 E；电梯结束抵达 17 楼并恢复控制。

### HearthLobbyConversationZone.cs

- 路径：`Assets/Scripts/MinLoop/HearthLobbyConversationZone.cs`。
- 挂载对象：`Girl_A_Rigged (2)/space`、年轻男人组的 `space1`、老奶奶组的 `space2`。
- 依赖：启用且勾选 `Is Trigger` 的 Collider、正式玩家根 Transform、Lobby Flow 和对应 Dialogue Sequence。
- 功能：进入范围后播放一次 NPC 世界观对白；对话期间禁止移动、保留视角；NPC 对话结束后恢复移动，玩家离开 Trigger 时再播放对应的 Mia 感想。
- Inspector：`Exchange Sequence` 接区域内 NPC 对话，`Exit Commentary Sequence` 接离区后的 Mia 感想；`Play Once` 控制一轮是否只播一次。
- 公开接口：`Configure(...)`、`MarkExchangeCompleted()`、`MarkExitCommentaryCompleted()`、`ResetConversation()`。
- 并发规则：若上一段 Mia 感想仍在播放，下一组 NPC 对话会等待，不会把两段字幕或未来语音叠在一起。
- 维护入口：直接移动/缩放 Trigger 即可调整范围，不会移动 NPC，也不要求三组全部完成。

### HearthLobbyTaskTerminalInteractable.cs

- 路径：`Assets/Scripts/MinLoop/HearthLobbyTaskTerminalInteractable.cs`。
- 挂载对象：一楼 `TvUnitSet5` 的终端交互根。
- 依赖：Collider、`HearthTvTerminalController`、Lobby Flow、终端固定 Camera、Boot Sequence 与 Camera Transition。
- 功能：提供 `ACCESS ASSIGNMENT TERMINAL` 蓝色 E 提示；E 打开终端，Space 通过 Custom 主操作领取任务。成功领取后 `IsInteractionAvailable` 永久为 false，不再进入 Review 状态；确认前用 Esc 取消仍可重新打开，防止误操作导致流程卡死。
- 公开接口：`Configure(...)`、`Interact()`、`GetDescription()`、`IsInteractionAvailable`。
- Prefab：`Assets/Prefabs/UI/HearthHud/Terminals/Terminal_Lobby_Assignment.prefab`。
- 最小测试：从不同角度进入终端时约 0.5 秒平移；页面完整显示；Space 后任务状态变为已领取并关闭终端；任务说明期间可走动但电梯无 E；说明结束后电梯开放；再次看向任务终端不再出现 E。

### HearthLobbyElevatorInteractable.cs

- 路径：`Assets/Scripts/MinLoop/HearthLobbyElevatorInteractable.cs`。
- 挂载对象：围绕 `DIKUAIunity/Group1/Group144/Rectangle2106772232/Mesh2643` 建立的电梯按钮交互代理。
- 依赖：Collider 和 Lobby Flow。
- 功能：领取任务前和任务说明播放期间都不显示 E；说明全部结束后显示 `CALL ELEVATOR`，并启动渐黑、电梯对白和 17 楼到达流程。
- 公开接口：`Configure(...)`、`Interact()`、`GetDescription()`、`IsInteractionAvailable`。

### HearthLobbyHudOverlay.cs

- 路径：`Assets/Scripts/MinLoop/HearthLobbyHudOverlay.cs`。
- 挂载对象：`MIN_LOOP_ROOT/LobbyOpening/LobbyHudOverlay`。
- 功能：控制开场激活信息、Lily 留言展开/收起和任务已加载状态，不负责对白正文。Lily 留言在 Mia 说完 `Okay.` 后永久隐藏本轮显示，终端与机器人视角不会再次把它打开。
- 公开接口：`HideAllImmediate()`、`ShowActivation()`、`ShowExpandedVoiceMessage()`、`CollapseVoiceMessage()`、`DismissVoiceMessage()`、`SetAssignmentLoaded(bool)`。

### HearthScreenFader.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthScreenFader.cs`。
- 挂载对象：Lobby Opening 的全屏 Fade Canvas。
- 依赖：全屏黑色 Image 与 CanvasGroup，Canvas 排序需高于普通 HUD。
- 功能：提供可复用的立即黑屏、淡入和淡出协程；默认使用 Unscaled Time，避免流程锁定时动画停止。
- 公开接口：`SetImmediate(float)`、`FadeTo(...)`、`FadeOut(...)`、`FadeIn(...)`。

### HearthLobbyOpeningBinder.cs

- 路径：`Assets/Editor/HearthLobbyOpeningBinder.cs`。
- 类型：Editor 工具，不挂载到 GameObject。
- 菜单：
  - `Tools > Hearth > Lobby > Apply Ground Floor Opening Setup`
  - `Capture Current Player Pose As 17F Arrival`
  - `Validate Ground Floor Opening Setup`
- 功能：建立或修复 Lobby Flow、终端、电梯、Trigger 绑定、画布与锚点；调用最终对白同步；检查重复 Camera/AudioListener。
- 重要规则：Apply 不覆盖三组 NPC、触发区、`Person Controller (4)/(5)` 和用户手调终端 Camera 的 Transform。只有显式 Capture 菜单会改 17 楼到达锚点。

### HearthFinalDialogueSync.cs

- 路径：`Assets/Editor/HearthFinalDialogueSync.cs`。
- 类型：Editor 数据同步工具，不挂载到 GameObject。
- 唯一来源：项目根目录 `HEARTH_Full_Game_Script_No_Audio_Tags_Native_English.md`。`HEARTH_Full_Game_Script_ElevenLabs_v3_Native_English.md` 只作为未来配音情绪与表演提示参考，不参与游戏字幕同步。
- 菜单：`Normalize Final Script To Two-Line Segments`、`Sync All Dialogue From Final Script`、`Validate Final Script Coverage`、`Validate Two-Line Subtitle Layout`。
- 功能：解析 Markdown 场景与对白，使用隐藏的稳定段落标记绑定资产；当前覆盖最终稿全部 `330` 个字幕段，同步到 `70` 个 Dialogue Asset。额外终端说明保存在 `FinalScriptSupplemental`。
- 分段规则：按真实 TMP 字体、字号、宽度和 1920x1080 布局检测，优先在句号、分号、冒号、逗号处自然拆分；不改写原句内容。每个拆分段都有独立 `Voice Clip`。
- 稳定映射：不再依赖数字下标。以后在正式稿中插入或删除一句，不会让后续所有 Dialogue Asset 整体错位。
- 音频规则：旧行的 Speaker 与 Text 完全一致时保留 `AudioClip`；文本变化时不冒险错配语音。所有同步行使用 `VoiceClipWhenAssigned`，有音频按 Clip 长度，无音频按回退时间。
- 公开接口：`SyncAllFromFinalScript(bool logSummary)`，供其他 Editor Binder 在重建前调用。

### HearthCompanion17F03ReplayController.cs / Hearth17F03MinimalLoopBinder.cs

- 使用方式有变化，挂载位置和其余引用不变。
- 新增 `Inspection Recall Prompt Sequence`：实体机器人检查页打开后先播放最终稿的本地记录提示，完成后才允许 Space 调取记录。
- 提示播放期间提前按 Space 会立即灰显并排队；提示结束后自动开始回放，重复 Space 不会重复触发。
- 回放后先返回米娅房内视角，只播放 Laura 询问与 Mia 的回应；随后开放实体机器人 E。再次进入固定检查视角时先以锁定状态显示 A/B，Field Unit 的措施说明与推荐全部播完后才开放 `↑/↓ + Space`；提交后平滑返回米娅视角并播放对应家庭回应。
- 第三户完成后渐黑移动到 `Anchor_Mia_17F03_DoorReturn`，恢复普通人类控制，不再误开 17F02/17F03 门口终端。
- 绑定工具会先同步最终稿，再自动绑定 `17F03_InspectionRecallPrompt.asset`。

### Hearth17F04FinaleController.cs

- 使用方式有变化，挂载位置和公开方法不变。
- `BeginFromHomeTerminal()` 后的自宅问候现在保持在终端固定摄像机画面中播放；对白完成后才渐黑、关闭终端并传送进客厅。
- 该调整保留终端开机、按钮反馈和 Deferred Camera Handoff，避免渐黑前闪回米娅原视角。

### 对白资产统一维护

- 资产位置：`Assets/Data/MinLoop/Dialogues/`。
- 每句可修改 Speaker、Text、Start Delay、Hold Seconds、Voice Clip、Duration Mode 和 Voice Tail。
- 正式文字不要只改 Asset；应先改最终 Markdown 后运行 Sync，避免下次重绑被覆盖。
- 语音接入：把对应 Clip 拖入每句 `Voice Clip`，保留 `Voice Clip When Assigned`。语音长度不同无需手工统一字幕时长。
- 本次新增或修改脚本均已记录；未改变的脚本挂载方式无变化。

## 2026-07-18 任务终端隐藏、两行字幕与前三户处置闭环

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`。
- 挂载对象：各终端页面根；一楼任务终端为 `1F (1)/TvUnitSet5/MonitorCanvas/Terminal_Lobby_Assignment`。
- 新增能力：`Hide Canvas When Closed` 只对一楼任务终端开启。关闭时父级 World Space Canvas 完全禁用，按 E 时先启用，再执行 0.5 秒镜头平移与开机闪烁；退出完成后重新隐藏。
- 一楼维护入口：`TaskTerminalScreenAnchor` 保存 Canvas 的位置、朝向与尺寸；日常移动该 Anchor 后重跑 Lobby Apply。绑定工具不会覆盖用户手调的终端 Camera Transform/FOV。
- 选择门控：`SetChoiceInputEnabled(bool)` 用于推荐对白期间锁住 A/B；`SetCloseTerminalWhenChoiceSubmitted(bool)` 控制提交后是否立即关闭。17F01/02 当前均为 `false`，会先播放评价和下一户指引。
- 公开接口新增：`SetHideCanvasWhenClosed(bool)`、`SetChoiceInputEnabled(bool)`、`SetCloseTerminalWhenChoiceSubmitted(bool)`、`IsPresentationReady`。
- 最小测试：Edit Mode 关闭状态下 `MonitorCanvas/Canvas.enabled = false`；E 后可见，退出后不可见；侧面观察时没有悬空黑布。

### HearthResidentDispositionDialogueSet.cs

- 路径：`Assets/Scripts/MinLoop/HearthResidentDispositionDialogueSet.cs`，为可序列化数据类，不单独挂 GameObject。
- 功能：按住户保存 `Pre Choice Briefing / Option A Result / Option B Result / Post Choice Common` 四类对白，避免把住户文本硬编码进流程脚本。
- 17F01/02 数据由 `HearthDispositionDialogueSetup` 自动绑定到 `MinLoopFlowController / Disposition Dialogue Sets`。
- 扩展方式：新增住户时增加一项，填住户 ID 和对应 `HearthDialogueSequence`；无需复制结算状态机。

### MinLoopFlowController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopFlowController.cs`；挂载位置仍为 `MIN_LOOP_ROOT/FlowManagers/MinLoopFlowController`。
- 新流程：回放结束后先打开 A/B 页面但关闭输入，播放 Field Unit 推荐；推荐结束才开放选择。提交会立即锁定，信任度与历史事件只触发一次；评价和下一户指引结束后才关闭终端。
- 外部处置：17F03 使用 `BeginExternalDispositionChoice(residentId)`、`SubmitDisposition(choice)`、`CompleteExternalDisposition()`，复用同一信任结算但由房内检查 UI 表现。
- 住户进度：新增 `Household Progress` 引用。17F01/02 的终端评价结束、17F03 的房内分支结束时调用 `MarkHouseholdCompleted`；该状态本身会去重。
- 公开接口新增：`BeginDispositionBriefing(string)`、`BeginExternalDispositionChoice(string)`、`SubmitDisposition(...)`、`CompleteExternalDisposition()`、`SetHouseholdProgressState(...)`。
- 使用方式变化：Inspector 需绑定 `HearthHouseholdProgressState`、共享 `MinLoopSubtitlePlayer` 和住户对白数据；当前场景已由三个 Binder 自动绑定。

### Hearth17F03InspectionPanel.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F03InspectionPanel.cs`；仍挂在第三户实体机器人检查 UI。
- 检查模式：`QueueRecallRequest()` 支持说明期间提前 Space，`RecallQueued` 可查询等待状态。
- 处置模式：`OpenDispositionChoice(bool inputEnabled)` 可用锁定状态打开纵向 A/B；`SetChoiceInputEnabled(bool)` 在推荐结束后统一开放/关闭上下与 Space；`MoveChoice(int)` 响应上下键；`SubmitChoice()` 只提交一次。A 默认绿色高亮并显示 `RECOMMENDED`。
- 锁定时状态显示 `INPUT LOCKED - FIELD REVIEW IN PROGRESS / PLEASE WAIT`；开放后显示 `INPUT ENABLED / UP / DOWN SELECT / SPACE CONFIRM`。Canvas、相机和挂载引用无变化。
- 事件：`RecallRequested`、`CloseRequested`、`ChoiceSubmitted`。使用方式有变化，但 Canvas、相机和实体机器人引用仍由 17F03 Binder 管理。

### HearthFinalDialogueSync.cs / HearthSubtitleStyleProfile.cs

- 普通字幕与居中黑幕都严格限制最多两行；说话人仍居中位于正文上方。
- 正式修改顺序：先改根目录最终稿，运行 `Normalize Final Script To Two-Line Segments`，再运行 Sync、Coverage 和 Two-Line Validation。
- 每个拆分段的 `AudioClip` 独立；有音频时跟随真实 `AudioClip.length`，无音频时使用本段 `Hold Seconds`。
- 只改位置、字号、宽度时继续改 `Assets/Data/MinLoop/UI/Hearth_SubtitleStyle.asset`，无需重跑同步；改文字或拆分结构时才运行上述菜单。

## 2026-07-19 任务终端编辑预览、全局音效接口与项目整理

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`；挂载方式无变化。
- 新增 `Show Canvas In Edit Mode`：编辑器非 Play 状态可显示任务终端 World Space Canvas，运行时仍由 `Hide Canvas When Closed` 决定。任务终端当前为“编辑可见、游戏关闭时隐藏”。
- 新增持续声接口：`Active Loop Cue Player / Active Loop Cue Id`。打开终端时调用统一 Cue Loop，关闭时停止；仍兼容独立 `Active Loop Source / Active Loop Clip`。
- 新增公开方法：`SetShowCanvasInEditMode(bool)`、`SetActiveLoopCue(HearthSfxCuePlayer, string)`；原公开接口保持不变。
- 最小测试：Edit Mode 可看见 `MonitorCanvas`；Play 后未按 E 时 `Canvas.enabled=false`；打开终端后循环声可播放，退出后停止。
- 运行时关闭判定在 `Awake()` 即执行，避免任务终端 Canvas 在场景加载首帧短暂闪现；使用方式无变化。

### HearthLobbyOpeningBinder.cs

- 路径：`Assets/Editor/HearthLobbyOpeningBinder.cs`；Editor 工具，不挂载。
- 新菜单 `Tools > Hearth > Lobby > Show And Align Task Terminal Canvas For Editing`：显示 Canvas 并贴到 `TaskTerminalScreenAnchor`，不会改终端 Camera。
- 新菜单 `Capture Task Terminal Canvas Placement`：当你直接移动/旋转/缩放 `MonitorCanvas` 后，把当前结果保存回 Anchor，防止以后重跑 Lobby Apply 时丢失。
- 当前验证仍报告 `MonitorCanvas` 与 Anchor 约 `0.0154m / 13.71°` 的差异。为保护用户手调结果，本次没有自动覆盖；需要完全贴合时可运行 Show And Align，或确认当前视觉正确后运行 Capture 把现状保存为新 Anchor。
- 2026-07-19 路径修正：三组大厅剧情区域的正式根为 `1F (1)`；自动绑定与验证不再查询已经淘汰的旧 `1F` 根。触发区位置、旋转、尺寸和现有组件均未改变。

### HearthSfxCuePlayer.cs / HearthStorySfxBinder.cs

- 路径：`Assets/Scripts/Audio/HearthSfxCuePlayer.cs`、`Assets/Editor/HearthStorySfxBinder.cs`。
- 场景挂载：`MIN_LOOP_ROOT/Audio/StorySFX_Global`、`StorySFX_Lobby`、`StorySFX_17F01`、`17F02`、`17F03`、`17F04`。
- 当前共 `45` 个 Cue。所有空槽都允许剧情继续静默运行；拖入 `Primary Clip` 后无需修改代码。
- 一楼任务终端 `AssignmentTerminal.Hum` 已绑定到终端 Active Loop；17F04 弹窗四类反馈已绑定到关卡控制器。
- 菜单：`Tools > Hearth > Audio > Apply Story SFX Placeholder Setup` 与 `Validate Story SFX Placeholder Setup`。
- 完整 Cue 位置、用途、脚步和逐句语音入口见 `HEARTH_音频资源需求与素材来源清单.md / 第 11 节`。

### HearthVirusPopupShutdownChallenge.cs

- 路径：`Assets/Scripts/MinLoop/HearthVirusPopupShutdownChallenge.cs`；仍挂在 `MIN_LOOP_ROOT/Finale_17F04/UI/ShutdownChallenge_17F04`。
- 新增可维护黑色遮罩：`Background Dimmer`、`High Trust Dim Color`、`Low Trust Dim Color`；低信任默认 Alpha `0.62`。
- 新增音效 Cue：Popup Spawn、Dismiss、Wave Escalate、Success；由 `SetSfxCuePlayer(...)` 绑定。
- 弹窗生成、Space 关闭、第二/三波升级、全部清空均有独立声音入口；没有素材时静默，不阻断流程。

### HearthLobbyFlowController.cs / HearthCompanion17F01ReplayController.cs

- 路径分别为 `Assets/Scripts/MinLoop/HearthLobbyFlowController.cs`、`Assets/Scripts/MinLoop/HearthCompanion17F01ReplayController.cs`。
- 使用方式无变化；新增 `Sfx Cue Player` 引用，由 `HearthStorySfxBinder` 自动绑定。
- Lobby 自动控制大厅、电梯、到达和走廊音效；17F01 自动控制卧室/客厅底噪、回放转场和安抚完成声。

### HearthProjectOrganizer.cs

- 路径：`Assets/Editor/HearthProjectOrganizer.cs`；Editor 工具，不挂载到 GameObject。
- `Organize SampleScene Hierarchy (Safe)`：只调整根对象顺序并生成七个 `EditorOnly` 分隔线，不改 Transform、Active 或运行路径。
- `Organize Assets Root (Preserve GUIDs)`：用 `AssetDatabase.MoveAsset` 整理散落资源，保留 `.meta` 和 GUID，不覆盖同名目标。
- `Validate Project Organization`：检查场景分区与资源目标位置。
- 当前场景和 Assets 已执行整理。完整去向表见 `HEARTH_场景与Assets整理清单.md`。

### 资源路径迁移

- 第一户动画：`Assets/Animations/Hearth/17F01/Clips/`。
- 第二户动画/Controller：`Assets/Animations/Hearth/17F02/Clips/` 与 `Controllers/`。
- 第三户动画/Controller：`Assets/Animations/Hearth/17F03/Clips/` 与 `Controllers/`。
- 正式交互脚本：`Assets/Scripts/Interactions/`；`ViewSwitchController` 位于 `Assets/Scripts/MinLoop/`。
- 城市脚本：`Assets/Scripts/Environment/City/`。
- 相关 Binder 的资源常量已经同步；挂载对象、公开方法和运行流程无变化。

## 2026-07-19 全局 HUD、终端引导与结局时间提示

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`；继续挂在每台终端的页面根对象上，挂载方式无变化。
- 新增 Inspector 引用 `Runtime Prompt Text`，目标为本终端 `KeyboardNavigationRoot/RuntimePromptText`；缺失时住户简介仍可播放，但不会显示等待或键位提示。
- 新增公开方法：`SetPrimaryActionInputEnabled(bool)`、`SetCloseInputEnabled(bool)`、`SetRuntimePrompt(string)`、`ClearRuntimePrompt()`；新增事件入口 `OnOpened`、`OnClosed`。
- 主操作关闭时仍允许 Tab 浏览终端资料；Space 不会提前启动回放、入户或提交选择。关闭键可单独锁定，供一楼任务终端前 5 秒使用。
- A/B 可操作时自动显示：17F01/02 为 `LEFT / RIGHT  SELECT     SPACE  CONFIRM`，17F03/04 为 `UP / DOWN  SELECT     SPACE  CONFIRM`；锁定时显示 `PLEASE WAIT`。
- 最小测试：打开三台住户终端，简介期间 Tab 可用但 Space 无效；简介结束后提示变为对应方向键说明。

### HearthTerminalOpeningBriefing.cs

- 路径：`Assets/Scripts/MinLoop/HearthTerminalOpeningBriefing.cs`。
- 挂载对象：17F01、17F02、17F03 各自的 `HearthTvTerminalController` 同一 GameObject；依赖该终端控制器、共享 `MinLoopSubtitlePlayer` 和一个 `HearthDialogueSequence`。
- Inspector：`Terminal` 拖本机终端；`Subtitle Player` 拖共享字幕播放器；`Briefing Sequence` 分别使用 `17F01_TerminalIntro`、`17F02_TerminalIntro`、`17F03_CorridorTerminal`。
- 公开方法：`BeginBriefing()`、`CancelBriefing()`、`ResetBriefing()`、`Configure(...)`；公开状态 `BriefingCompleted`、`BriefingPlaying`。
- 行为：终端开机完成后锁主操作并播放简介；Esc 提前退出会停止本次简介，重开后从头播放；完整播放后本轮不重复。
- 建立/修复菜单：`Tools > Hearth > Terminals > Apply Household Opening Briefings`。仅重建终端或引用丢失时运行，普通改文字或语音不需要运行。

### HearthLobbyFlowController.cs

- 路径和挂载位置不变：`Assets/Scripts/MinLoop/HearthLobbyFlowController.cs`，挂在 `MIN_LOOP_ROOT/LobbyOpening` 流程对象。
- 新增字段 `Assignment Terminal Minimum View Seconds`，当前默认 `5`；新增公开方法 `BeginAssignmentBriefingFromTerminal()`、`ConfirmAssignmentTerminalClose()`。
- E 打开终端后，开机完成立即播放任务简报。前 5 秒锁 Space/Esc；随后 Space 只关闭终端，简报继续。终端关闭后米娅可移动和转头，但所有 E 与电梯保持锁定，直到最后一句结束。
- 成功领取时立即写入一次性状态，终端本轮不可再次打开；`ResetLobbyFlowForPreview()` 可重置测试。
- 引用关系：`HearthLobbyTaskTerminalInteractable` 查询 `CanOpenAssignmentTerminal`；电梯交互查询 `CanUseElevator`，因此不会在简报结束前提前开放。

### HearthFirstPersonHudInput.cs / HearthFirstPersonHudBuilder.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudInput.cs` 与 `Assets/Editor/HearthFirstPersonHudBuilder.cs`。
- HUD Prefab 的 `FocusLayer/FinalChoiceInputHint` 由 Builder 创建并绑定。水平模式显示左右选择，垂直模式显示上下选择；只在最终选择页且键盘输入开放时可见。
- `SetFinalChoiceInputProfile(...)` 会立即刷新提示；使用方式无变化。重建第一人称 HUD 后该引用会自动恢复。

### HearthCompanionHudLayoutProfile.cs / HearthCompanionHudLayoutController.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/`；配置资产为 `Assets/Data/HearthHud/Companion/Hearth_CompanionHudLayout.asset`。
- `HearthCompanionHudLayoutController` 挂在 `HearthCompanionHudRoot`，引用右上 `DecisionPanel`、左下 `DataStreamView` 及两区 TMP 文本。
- 共享字段：`Global Region Scale`、`Global Text Scale`、`Shared Horizontal Inset`、`Shared Vertical Offset`；单独微调为 `Decision Offset`、`Data Stream Offset`。
- 公开方法：`ApplySharedLayout()`；编辑模式实时预览。保存 Layout Asset 后 17F01/02/03 全部机器人页面在下一次 Play 直接生效，不需要重跑菜单。
- Builder 会继续引用同一资产，不会把用户保存的共享参数重置。若 HUD 引用损坏，运行 `Tools > Hearth > HUD > Rebuild And Apply Companion Unit HUD To Scene`。

### MinLoopTypes.cs / HearthSubtitleStyleProfile.cs / MinLoopSubtitlePlayer.cs

- 路径均在 `Assets/Scripts/MinLoop/`；共享字幕播放器的挂载位置不变。
- `MinLoopSubtitleLine` 新增 `Presentation Kind = Dialogue / TimeCard`；普通对白行为不变。
- 共享样式 `Assets/Data/MinLoop/UI/Hearth_SubtitleStyle.asset` 新增 `Time Card` 布局和 `Time Card Fade Seconds`。时间卡居中显示、隐藏说话人栏，并独立淡入淡出。
- 17F04 黑幕中的时间提示使用 `TIME CARD` 段落自动同步；有语音仍可跟随 `AudioClip.length`，无语音使用本行回退时长。
- 最小测试：播放四条 17F04 结局之一，时间卡居中淡入淡出，下一句普通对白恢复对应黑幕字幕布局。

### HearthPhotoFrameInteractable.cs

- 路径：`Assets/Scripts/MinLoop/HearthPhotoFrameInteractable.cs`；仍挂在 17F04 相框交互对象，使用方式基本不变。
- 新增 `Exit Hint Group / Exit Hint Text / Exit Hint Label`，当前 Binder 绑定为 `SPACE  RETURN`。相框对白完成前隐藏，完成后才显示；Space 正式退出，Esc 只作无显示的安全退出。
- 公开方法新增 `SetExitHint(CanvasGroup, TMP_Text)`；其余公开接口不变。

### Hearth17F04FinaleController.cs

- 路径和挂载位置不变；使用方式有变化但公开剧情入口不变。
- 自宅终端第一次确认后立即锁主操作并显示 `PLEASE WAIT`，重复 Space 不会再次触发。外部渐黑接管完成或流程重置时恢复终端状态。
- 正式稿中的 Lily 完整留言与 Field Unit 说明播放完后才进入客厅。

### Editor 绑定与同步工具

- `Assets/Editor/HearthTerminalOpeningBriefingBinder.cs`：为三台住户终端创建简介组件和 `RuntimePromptText`；兼容旧的图片式终端层级，并能从层级推断 17F01/02/03。
- `HearthLobbyOpeningBinder.cs`：保留手调任务终端 Camera、ScreenAnchor 与现有 Canvas；只在旧默认位置时下移大厅激活卡，并绑定 5 秒任务简报时序。
- `HearthTvTerminalPrefabBuilder.cs`、`Hearth17F04FinaleBinder.cs`：新建终端时自动创建 Runtime Prompt；17F04 自动创建相框退出提示。
- `HearthCompanionHudBuilder.cs`：创建并引用共享机器人 HUD Layout Profile。
- `HearthFinalDialogueSync.cs`：识别正式稿中的 `TIME CARD`，同步为 `Presentation Kind = TimeCard`，并按稳定文本键保留未变化的语音。
- 本次所有 Builder/Binder 都是 Editor 工具，不挂运行时 GameObject。普通改文字、字号、位置、AudioClip 不运行；只有重建对象或引用丢失时才运行。

## 2026-07-19 全局 E 交互提示恢复与防丢失

### PlayerInteraction.cs

- 路径：`Assets/Scripts/Interactions/PlayerInteraction.cs`；继续挂在正式 `Player/Person Controller` 与 `Player/Robot Controller`，挂载方式和射线距离设置无变化。
- 本次修复原因：两套正式控制器的 `UI Interaction`、`UI Interaction Text` 引用在 HUD 重建后变成空值，导致所有关卡的单按 E 蓝色提示都无法显示；关卡交互体和射线不是本次全局消失的根因。
- 新增 Inspector 字段：`Auto Resolve Prompt If Missing` 默认开启；`Prompt Resolve Retry Interval` 默认 `0.5s`。正式控制器会在启动和引用丢失时自动查找对应提示层。
- 人类提示查找路径：`HearthHudRoot/InteractionPromptLayer/PlayerInteractionPrompt`。
- 机器人提示查找路径：`HearthCompanionHudRoot/InteractionLayer/PlayerInteractionPrompt`。
- 新增公开方法：`BindPromptUi(GameObject prompt, TMP_Text label)` 用于工具或其他系统显式绑定；`ResolvePromptUiNow()` 用于立即重新查找并恢复引用。
- 运行关系：`PlayerInteraction` 仍通过摄像机中心射线查找 `IInteractable`，命中且可用时打开提示对象并写入英文说明；长按剧情仍由 `HearthCompanionHoldPrompt` 处理，两者互不抢占。
- 最小测试：进入 Play Mode，看向任意当前阶段可用的 TV、门、NPC 或机器人交互体，确认蓝色 `E  INTERACT` 框出现；看开后隐藏。进入需要长按的机器人剧情，确认提示显示、按住 E 时百分比从 `00%` 增长到 `100%`。

### HearthRuntimeInterfaceBinder.cs

- 路径：`Assets/Editor/HearthRuntimeInterfaceBinder.cs`；Editor 工具，不挂到 GameObject。
- 新增菜单：`Tools > Hearth > Systems > Repair Interaction Prompt Bindings`。
- 菜单会原位修复两份 HUD Prefab、当前场景中的 HUD 实例，以及正式人类/机器人控制器引用；不会重建全部 HUD，也不会覆盖关卡物体、射线距离或用户手调摄像机。
- 提示视觉由 Unity Image/TMP 构成：半透明深蓝底、青色细边、英文提示文字。人类与机器人各有独立实例，避免两个控制器争用同一个显示对象。
- 依赖：HUD 根需要 `Canvas`；提示文字使用 TextMeshPro。工具缺少根对象时会在 Console 输出明确警告。
- 最小测试：运行菜单后查看 Console，应出现恢复完成与验证通过信息；检查正式两个 `PlayerInteraction` 的 `UI Interaction` 和 `UI Interaction Text` 均非空。

### HearthFirstPersonHudBuilder.cs / HearthCompanionHudBuilder.cs

- 路径：`Assets/Editor/HearthFirstPersonHudBuilder.cs`、`Assets/Editor/HearthCompanionHudBuilder.cs`；均为 Editor 生成工具，使用方式无变化。
- 人类 HUD 重建时会永久创建/保留 `InteractionPromptLayer/PlayerInteractionPrompt`；机器人 HUD 重建时会永久创建/保留 `InteractionLayer/PlayerInteractionPrompt`，并保留原有独立 `HoldPrompt` 长按控件。
- Builder 保存 Prefab 并应用到场景后，会重新绑定正式两个控制器，因此以后重建 HUD 不应再次让全局 E 提示消失。
- 调整位置或大小：直接修改对应 Prefab 内 `PlayerInteractionPrompt` 的 `RectTransform`；调整背景、边框、文字则修改其子级 Image/TMP。普通手调后不需要运行菜单；只有引用为空或重建异常时才运行修复菜单。
- 本次 Play Mode 验证：普通提示由真实中心射线目标成功打开；机器人长按提示成功显示 `HOLD E  OFFER REASSURANCE TO CLAIRE`，进度填充与文本同步到 `50%`。Unity 编译无新增 Error。

## 2026-07-21 新正式稿、17F03 处置顺序与 17F04 结局扩展

### HearthFinalDialogueSync.cs

- 路径：`Assets/Editor/HearthFinalDialogueSync.cs`；Editor 工具，不挂运行时 GameObject。
- 正式字幕唯一来源改为根目录 `HEARTH_Full_Game_Script_No_Audio_Tags_Native_English.md`；带情绪标签的 ElevenLabs v3 文档不参与同步。
- 当前同步结果：`330` 个带稳定 `HEARTH:SEQUENCES` 标记的字幕段，写入 `70` 个 `HearthDialogueSequence` 资产，共 `394` 个映射条目。
- 新增/调整序列包括：`17F01_CorridorArrival`、`17F01_TerminalIntro`、`17F03_CorridorTerminal`、`17F03_PostReplayQuestion`、`17F03_CorridorEvaluation_A/B`、`17F03_PositiveTrustShiftResult`、`17F04_SecondPhoto`、`17F04_PhotoCompletion`、`17F04_FinalChoiceAdvisory` 和两条保留陪伴单元后的信任评价。
- 使用步骤：先修改无音频标签正式稿，再运行 `Sync All Dialogue From Final Script`；随后必须运行 Coverage 与 Two-Line 两项验证。普通改 AudioClip 不需要重跑 Binder。
- 音频规则不变：说话人与正文完全一致时保留旧 Clip；正文变化后需重新拖入语音；有 Clip 时字幕时长跟随 `AudioClip.length`。

### HearthLobbyFlowController.cs / HearthLobbyOpeningBinder.cs

- 运行脚本路径：`Assets/Scripts/MinLoop/HearthLobbyFlowController.cs`，继续挂在 `MIN_LOOP_ROOT/LobbyOpening`；使用方式无变化。
- 新增 Inspector 字段 `Floor17 Arrival Dialogue`，绑定 `17F01_CorridorArrival.asset`。电梯到达 17 楼并渐亮后先播放该指引，期间锁移动和 E、保留视角，播放完成后才恢复正常控制。
- 大厅开场仍为“可转头、不可移动、不可交互”，直到 Mia 说完 `Okay.`；三个公共事件继续是一次性且可全部跳过。
- Binder 路径：`Assets/Editor/HearthLobbyOpeningBinder.cs`。Apply 保留 NPC、Trigger、任务终端 Camera 和参考控制器 Transform；`Capture Task Terminal Canvas Placement` 只把当前 Canvas 位置保存到 `TaskTerminalScreenAnchor`。
- 最小测试：从大厅开始等待到 `Okay.`，确认移动解锁；完成任务终端与电梯对白，确认 17 楼指引结束前 E 仍锁定，之后能打开 17F01 终端。

### HearthCompanion17F03ReplayController.cs / Hearth17F03MinimalLoopBinder.cs

- 运行脚本路径：`Assets/Scripts/MinLoop/HearthCompanion17F03ReplayController.cs`，仍挂在 17F03 流程控制对象；挂载方式无变化。
- 新增 Inspector 对白槽：`Terminal Entry Sequence`、`Post Replay Question Sequence`、`Corridor Evaluation A/B Sequence`、`Post Replay Positive Trust Result Sequence`。原技术解释、负信任主管复核和完成通知继续保留。
- 新顺序：入户对白 -> 回放 -> 回米娅房内 -> Laura 提问与技术说明 -> 再开放实体机器人 E -> 锁定 A/B 并播放建议 -> 提交一次 -> 房内父母反应 -> 渐黑到走廊 -> Field Unit 评价 -> 累计信任正/负结果 -> 三户完成通知。
- 公开入口不变：`BeginHumanEntry()`、`OpenUnitInspection()`、`BeginRecordedReplay()`、`ConfirmCurrentGazeTarget(...)`、`CancelFlow()`；信任仍由 `MinLoopFlowController` 一次性结算。
- Binder 路径：`Assets/Editor/Hearth17F03MinimalLoopBinder.cs`。菜单 `Apply 17F03 Minimal Loop Setup` 会同步正式稿并绑定上述资产，保留已存在的用户锚点；`Validate` 检查动画、相机、AudioListener、ViewSwitch 和出生碰撞。

### Hearth17F04FinaleController.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F04FinaleController.cs`；继续挂在 `MIN_LOOP_ROOT/ReplayRoom_17F04` 的流程对象，挂载方式无变化。
- 新增 Inspector 对白槽：`Second Photo Sequence`、`Photo Completion Sequence`、`Final Choice Advisory Sequence`、`Companion Answer Positive/Negative Rating Sequence`。
- 最终选择前先播放第四面墙式“better ending”建议，再开放上下键与 Space；A/B 和前三户信任结算规则不变。
- 选择让陪伴单元代答时，按累计信任正负播放对应绩效评价；选择 Mia 亲自回答后仍进入现有关闭/保留分支。
- 新增公开方法 `RequestPhotoPage(int pageIndex)`；其余公开流程入口保持不变。第二张照片未配置时自动走单照片流程，不显示空页。

### HearthPhotoFrameInteractable.cs

- 路径：`Assets/Scripts/MinLoop/HearthPhotoFrameInteractable.cs`；挂在 17F04 电子照片屏交互对象，依赖 Renderer、固定视角、相机过渡、退出提示和 `Hearth17F04FinaleController`。
- 新增 Inspector 字段：照片 Renderer、第一/第二张 Texture、左右翻页键和翻页提示。公开属性 `HasSecondPhoto`、`CurrentPageIndex`。
- 新增公开方法：`ConfigurePhotoPages(...)`、`NotifyPageReadyForNavigation()`、`SetPageNavigationEnabled(bool)`；原 `OpenView()`、`CloseView()`、`SetExitHint(...)` 继续可用。
- 第二张图片预留路径：`Assets/Art/UI/HearthHud/Finale/FamilyPhoto_Second.png`。文件不存在时功能隐藏；放入图片后运行 `Tools > Hearth > Finale > Apply 17F04 Home Finale Setup` 才需要重新绑定。
- 每张照片的说明只在首次查看时播放；两张说明完成后播放 `17F04_PhotoCompletion`，随后仍可左右浏览，并显示 `LEFT / RIGHT  SWITCH PHOTO     SPACE  RETURN`。单照片状态只显示 `SPACE  RETURN`。
- 2026-07-21 收口修复：两张说明均完成后切回任意照片只更换画面，不会再次触发对白，也不会关闭翻页/返回提示；挂载方式和 Inspector 引用无变化。

### HearthVirusPopupShutdownChallenge.cs

- 路径：`Assets/Scripts/MinLoop/HearthVirusPopupShutdownChallenge.cs`；继续挂在 17F04 低信任关闭 UI 根对象，依赖 CanvasGroup、三类 Popup Prefab、遮罩和 SFX Player。
- 第四关仍保留三阶段病毒弹窗玩法，不替换成文档中的三次静态确认。三波分别表达：授权不足、非标准操作与人工复核、最终强制关闭并跳过告别协议。
- 新增公开方法 `ApplyDefaultWaveContentPreservingTuning()`：只更新默认文案与颜色，不覆盖用户已经调好的数量、生成速度、初始爆发数和间隔。
- 最小测试：低信任选择关闭，确认半透明遮罩出现；清完第一类弹窗后才进入第二类，第三类为红色且更快；最终清空后只触发一次完成事件。

### Editor 绑定工具

- `Assets/Editor/Hearth17F04FinaleBinder.cs`：绑定新增照片与结局对白；只在 `FamilyPhoto_Second.png` 存在时启用第二页；保留 TV Camera、演员和已有锚点；保留病毒弹窗挑战及用户调参。
- `Assets/Editor/HearthTerminalOpeningBriefingBinder.cs`：三户简介改为 `17F01_TerminalIntro / 17F02_TerminalIntro / 17F03_CorridorTerminal`，只调整对白顺序与主操作门控，不改变 17F01/17F02 回放、场景或走位玩法。
- 本轮 MCP 验证：正式稿 Coverage 与 Two-Line 均通过；大厅、17F03、17F04 Binder 验证通过；启用 Camera 与 AudioListener 均各为一个。Play Mode 仅剩 ROOM2 书桌和 ROOM3 电视柜旧负缩放 BoxCollider 报错。

## 2026-07-21 三户陪伴单元长按 E 提示修复

### HearthCompanionHoldPrompt.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHoldPrompt.cs`；继续挂在 `HearthCompanionHudRoot/InteractionLayer/HoldPrompt`，挂载方式无变化。
- 新增只读属性 `IsVisible`。组件会在启动和显示前自动补齐 `CanvasGroup` 与父级 `HearthCompanionHudController` 引用。
- `SetVisible(true)` 会先激活提示对象，再设置透明度和射线状态，避免父节点曾被隐藏后只改 CanvasGroup 却仍不显示。

### HearthCompanionHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudController.cs`；继续挂在 `HearthCompanionHudRoot`。
- 运行时会从包含 inactive 对象的子级中自动修复 `HoldPrompt` 引用；引用缺失时只警告一次。
- `SetHoldPromptVisible(true)` 在从隐藏切到显示时重新应用当前剧情页的提示文字、按键和时长；持续瞄准期间不会每帧重置长按进度。
- 若序列化引用指向已停用的旧 `ViewSwitchController`，运行时会自动切换到正式的已启用控制器，避免整个机器人 HUD 根 Canvas 被错误设为透明。
- 17F01、17F02、17F03 共用同一个正式 HUD 和 HoldPrompt；不改变射线距离、Collider、人物位置或单按 E 交互。

### ViewSwitchController.cs

- 路径：`Assets/Scripts/MinLoop/ViewSwitchController.cs`；正式实例仍为 `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`，挂载和 Inspector 使用方式无变化。
- 新增公开静态方法 `FindPreferredController()`：优先选择已加载场景中启用、激活且位于 `MIN_LOOP_ROOT/FlowManagers` 下的正式控制器。
- 旧参考物体上的停用 `ViewSwitchController` 可以继续保留为锚点资料，但 HUD 和剧情脚本不得再自动绑定到它们。

### HearthCompanionHudFlowBinder.cs / HearthCompanionHudExclusiveMode.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudFlowBinder.cs`、`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudExclusiveMode.cs`；均继续挂在 `HearthCompanionHudRoot`，使用方式无变化。
- 两个组件在引用为空、停用或所在对象未激活时，都会通过 `FindPreferredController()` 恢复正式 ViewSwitch 引用。
- 前者决定机器人 HUD 是否随陪伴单元视角显示；后者决定何时隐藏人类 HUD。两者必须指向同一个正式控制器。

### HearthCompanionHudPreviewInput.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudPreviewInput.cs`；正式 Prefab 的 `Preview Input Enabled` 默认改为关闭。
- 该组件只用于编辑器预览 14 个机器人页面；正式剧情不得让数字键或翻页键抢占当前场景。

### HearthCompanionHoldInteractionRepair.cs

- 路径：`Assets/Editor/HearthCompanionHoldInteractionRepair.cs`；Editor 工具，不挂 GameObject。
- 修复菜单：`Tools > Hearth > HUD > Repair Companion Hold Interactions`。
- 验证菜单：`Tools > Hearth > HUD > Validate Companion Hold Interactions`。
- 修复内容：把 17F01/02/03 流程控制器显式绑定到同一个正式 `HearthCompanionHudRoot` 和 `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`，绑定 HUD 与 HoldPrompt，关闭正式预览输入，并保持 17F02 对话结束后才开放安慰交互。
- 重建机器人 HUD 后若引用异常才需要运行 Repair；普通调整提示大小、位置或文案不需要运行。
- 本轮 Play Mode 视觉验证：蓝色框、提示文字和进度条已真实绘制；`17F01_01`、`17F02_02`、`17F03_02`、`17F03_03` 的根 Canvas Alpha 均为 1，提示均为 visible；Unity Console 无新增 Error。

## 2026-07-25 HEARTH UI V2 与终端独占显示

### HearthUiV2Builder.cs

- 路径：`Assets/Editor/HearthUiV2Builder.cs`；Editor 工具，不挂载到 GameObject。
- 功能：从现有 Legacy Prefab 建立一套独立的 V2 人类 HUD、陪伴单元 HUD 和五类终端 Prefab；使用 `Assets/UI/HEARTH/GeneratedParts/` 中的透明 UI 部件重绘线框、半透明衬底、按钮、提示和终端页面。
- 生成目标：
  - `Assets/Prefabs/UI/HearthHud/V2/HearthHudRoot_V2.prefab`
  - `Assets/Prefabs/UI/HearthHud/V2/Companion/HearthCompanionHudRoot_V2.prefab`
  - `Assets/Prefabs/UI/HearthHud/V2/Terminals/Terminal_17F01_V2.prefab`
  - `Terminal_17F02_V2.prefab`
  - `Terminal_17F03_Alert_V2.prefab`
  - `Terminal_17F04_Home_V2.prefab`
  - `Terminal_Lobby_Assignment_V2.prefab`
- 菜单：
  - `Tools > Hearth > UI V2 > Rebuild All V2 UI Assets`：重新生成全部 V2 Prefab；不会覆盖 Legacy Prefab。
  - `Use V2 UI In Open Scene`：把当前场景的人类 HUD、陪伴单元 HUD 和所有终端替换成 V2，并重映射剧情脚本引用。
  - `Use Legacy UI In Open Scene`：一键切回原 UI，便于 A/B 对比或紧急回退。
  - `Validate Open Scene UI`：检查人类 HUD、陪伴单元 HUD 数量，以及每台终端是否启用“终端打开时隐藏第一人称 UI”。
  - `Preview/Open Human Tab Menu`、`Preview/Show Companion HUD`、`Preview/Open 17F01 Terminal` 等菜单只用于 Play Mode 视觉检查。
- 自动维护：
  - 切换 Prefab 后会修复 Tab 菜单和最终 A/B 的高亮目标。
  - 切入 V2 陪伴单元 HUD 后会重新采集右上决策区和左下数据流的新版基准坐标，防止旧版坐标覆盖新版布局。
  - 终端会重新绑定当前正式玩家相机，并保留场景内终端 Transform、Canvas 尺寸和用户手调的固定机位。
- 最小测试：重建 V2，应用到场景，运行 Validate；Play 后分别预览人类、Tab、陪伴单元和终端。终端画面中不得出现人类身份、任务、地点、Tab 菜单、教学 Footer 或陪伴单元 HUD。

### HearthUiThemeMarker.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthUiThemeMarker.cs`。
- 挂载对象：由生成器自动挂到每个 V2 Prefab 根节点，不需要手动添加。
- 字段：`Version` 标记 `Legacy/V2`；`Build Label` 用于调试识别当前生成版本。
- 公开接口：`Version`、`BuildLabel`、`Configure(...)`。
- 依赖：无。
- 用途：切换器与验证器据此区分旧版和新版，不依赖对象名字猜测。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`；继续挂在每台终端的终端内容根对象，挂载方式无变化。
- 新增 Inspector 区域 `First Person UI Visibility`：
  - `Hide First Person Ui While Open`：默认开启。进入固定终端视角时隐藏全部屏幕空间第一人称 UI，关闭终端后精确恢复各对象进入前的状态。
  - `First Person Ui Roots To Hide`：可选补充数组。一般留空即可；脚本会自动发现 `HearthHudRoot`、`HearthCompanionHudRoot`、`HumanCanvas` 和大厅叙事 HUD。
- 新增公开方法：`SetHideFirstPersonUiWhileOpen(bool value)`。
- 运行逻辑：
  - `OpenTerminal()` 锁定玩法后立即保存并隐藏第一人称 Canvas。
  - `CloseTerminal()`、自定义操作交接结束和组件停用时都会恢复，避免异常退出后 HUD 永久消失。
  - 终端自身的 World Space Canvas 不会被隐藏；终端内部字幕仍可正常显示。
- 相机引用修复：打开终端前优先从正式 `ViewSwitchController` 取得当前人类或机器人相机和交互器，避免 Prefab 切换后继续使用旧机器人或停用相机。
- 最小测试：人类视角打开任一终端，只看到终端页面；Esc 退出后恢复此前的人类 HUD。切到机器人再打开终端时同样不得残留机器人全屏框。

### ViewSwitchController.cs

- 路径：`Assets/Scripts/MinLoop/ViewSwitchController.cs`；正式挂载对象仍为 `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`，Inspector 使用方式无变化。
- 新增只读接口：
  - `CurrentViewCamera`：当前 Human/Companion 模式真实使用的 Camera。
  - `CurrentInteraction`：当前模式真实使用的 `PlayerInteraction`。
  - `CurrentViewRoot`：当前模式控制器根对象。
- 作用：终端、相框或后续固定视角系统应读取这些接口，不再从场景中按名字猜测当前玩家相机。

### HearthFirstPersonHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudController.cs`；继续挂在 `HearthHudRoot`。
- 使用方式无变化。
- V2 兼容修复：
  - Awake 时会从当前 Prefab 子级重新发现页面。
  - Tab 菜单高亮和最终 A/B 高亮引用缺失时，会按稳定对象名自动恢复。
  - 可维护对象名为 `MenuFocus`、`Button_TODAY`、`Button_DISPOSITION_HISTORY`、`Button_SYSTEM_SETTINGS`、`FinalChoiceFocus`、`FinalChoiceTarget_A/B`。
- 最小测试：V2 下按 Tab，上下移动时只有当前条目出现匹配文字范围的高亮；最终选择页同样能正确切换。

### HearthCompanionHudLayoutController.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudLayoutController.cs`；继续挂在 `HearthCompanionHudRoot`。
- 新增公开方法：`RecaptureBaselines()`。
- 功能：记录当前右上决策区、左下数据流和文字字号作为共享布局的新基准，再应用 `HearthCompanionHudLayoutProfile`。
- 使用时机：V2 生成器和场景切换器自动调用；普通用户不需要手动调用。
- 后续手调：仍在 `Assets/Data/HearthHud/Companion/Hearth_CompanionHudLayout.asset` 调统一缩放、文字缩放、内缩和偏移。若直接移动了 Prefab 内区域，可在代码或工具中调用 `RecaptureBaselines()` 后再保存。

### HearthHudPersistentView.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthHudPersistentView.cs`；挂载和 Inspector 使用方式无变化。
- 状态标签统一为 `FIELD UNIT · ACTIVE / ALERT / DORMANT`，不再使用旧的 `COMPANION UNIT | ...`，并使用英文中点避免乱码。

### UI V2 部件资产

- 路径：`Assets/UI/HEARTH/GeneratedParts/`。
- 分类：`Common`、`Companion`、`Terminal`、`Interaction`、`Feedback`、`Inspection`、`Finale`。
- 这些 PNG 只负责科技线框和透明衬底；正式文字、颜色、透明度和状态仍由 TMP、Image 与脚本维护。
- 9-Slice 文件可安全改变 RectTransform 宽高；全屏框、对白框和 Hold 进度条按用途使用固定比例，避免拉伸变形。
- 替换部件后需要运行 `Rebuild All V2 UI Assets`；只改场景中 TMP 文本或共享布局资产时不需要重建。

## 2026-07-26 P0 运行拓扑、终端会话与 V2 迁移边界修复

本轮先按代码执行顺序治理运行拓扑，没有重新生成 V2 视觉资产。当前场景已通过两个静态验证器；
Lobby、17F01、17F02、17F03、17F04 五台终端已分别完成打开、稳定、关闭的 Play Mode 检查，
17F01 额外完成控制器中途停用后的清理检查。以下条目覆盖本轮新增或修改的全部 C# 使用变化。

### HearthRuntimeTopologyTools.cs

- 路径：`Assets/Editor/HearthRuntimeTopologyTools.cs`；Editor 工具，不挂 GameObject。
- 功能：
  - 只读检查当前打开场景的 P0 运行拓扑。
  - 在完整预检通过后，把重复 `ViewSwitchController` 的场景引用重映射到
    `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`，再只删除重复组件。
  - 修复五台终端的自有 Camera、硬件根、Canvas World Camera、正式 ViewSwitch、
    共享 `HearthPlayerControlLock`、页面引用和转场 AudioListener 连续性。
  - 修复 Lobby 的 `OnOpened / OnCustomPrimaryAction` 和 17F04 Home 的
    `OnCustomPrimaryAction` 三个必要持久回调。
- 菜单：
  - `Tools > Hearth > Validation > Validate Runtime Topology`：只读验证，不保存场景。
  - `Tools > Hearth > Repair > Repair P0 Runtime Topology`：执行修复、再次验证并保存；
    若修复后验证失败，会撤销本次 Undo 组。
- 修复前置条件：当前场景必须能唯一找到正式 ViewSwitch、一个 Human HUD、五台终端、
  一个共享控制锁、一个 Lobby Flow、一个 17F04 Finale Controller，以及 Lobby/Home 唯一终端。
  任一条件含糊时会在修改前停止。
- 公开接口：`RepairOpenSceneP0Topology()`、`ValidateOpenSceneP0Topology(bool logSuccess)`，
  供其他 Editor 工具或验证流程调用。
- Unity 使用步骤：先运行只读 Validate；只有输出明确问题并确认当前场景是目标场景时才运行 Repair；
  Repair 后重新运行两个 Validator，并在 Play Mode 做终端矩阵。
- 最小验收：本轮 `SampleScene` 的 Runtime Topology Validation 已通过，场景只剩一个正式
  ViewSwitch，五台终端 Camera 都位于自身 TV 层级。
- 后续扩展：新增场景或终端类型时，应先增加显式 Identity/配置，不要继续依赖名称 Token；
  自动化测试只调用公开验证接口，不应通过 Repair 来“让测试通过”。

### HearthPlayerControlLock.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthPlayerControlLock.cs`。
- 挂载对象：仍挂在 `HearthHudRoot`；当前场景应只有一个被 Human HUD 和五台终端共享的正式实例。
- 需要组件：无强制同物体组件；自动或手动引用
  `FirstPersonMovement`、`FirstPersonLook`、`Crouch`、`Jump`、`PlayerInteraction`
  以及需要清速的 `Rigidbody`。
- Inspector 使用方式无变化：
  - `Auto Find Scene Controllers` 可自动收集当前场景控制器。
  - 各控制组件数组可显式覆盖自动结果。
  - `Extra Behaviours To Disable` 用于附加输入脚本。
  - `Disable Jump Always / Disable Crouch Always` 继续控制常驻禁用规则。
- 本轮逻辑：
  - 控制锁从单一 bool 改成按 Owner 申请和释放。
  - 第一个 Owner 进入时保存组件状态并锁定；最后一个 Owner 退出时才恢复。
  - 同一 Owner 重复申请不会重复计数；Owner 被销毁或组件停用时会清理。
  - 静态 `AnyControlsLocked` 供 ViewSwitch 等全局输入门禁读取。
- 新增/调整接口：
  - `SetControlsLocked(Object owner, bool locked)`：推荐的新接口。
  - `ReleaseOwner(Object owner)`：只释放指定 Owner。
  - `ActiveLockCount`、`AnyControlsLocked`：调试和门禁接口。
  - 旧 `SetControlsLocked(bool)`、`LockControls()`、`UnlockControls()` 保持兼容，
    旧调用会以本组件自身作为 Owner。
- 引用关系：`HearthFirstPersonHudController` 和 `HearthTvTerminalController`
  都以自身作为 Owner；`ViewSwitchController` 读取全局锁状态，防止切换完成后提前恢复控制。
- 最小测试：本轮双 Owner 计数按 `1 → 2 → 1 → 0` 变化；释放第一个 Owner 后玩家仍被锁，
  最后一个 Owner 释放后才恢复。
- 后续接入：新菜单、正式对白或剧情接管必须传入稳定 Owner 调用带 Owner 的重载；
  不要在不同系统里继续保存并恢复自己的移动组件 bool。

### ViewSwitchController.cs

- 路径：`Assets/Scripts/MinLoop/ViewSwitchController.cs`。
- 正式挂载对象：`MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`；场景不再保留其他正式副本。
- Inspector 使用方式无变化：Human/Companion 的 Root、Camera、Movement、Look、Interaction、
  Rigidbody、Canvas 和 Fade 设置继续按原说明绑定。
- 本轮逻辑：
  - `FindPreferredController(Scene)` 只在指定场景选择控制器，并把正式完整层级设为最高优先级。
  - 非正式副本不会在 `Awake` 主动控制 Rig；若旧引用调用副本的 `SwitchTo(...)`，
    会转发给同场景正式控制器。
  - 手动 `R` 只有在没有 Owner Blocker 且没有共享玩家控制锁时才响应。
  - 切换淡入结束时不会在其他 Owner 仍持锁的情况下重新启用玩法控制。
- 公开接口：
  - `FindPreferredController()`、`FindPreferredController(Scene)`。
  - `SetManualSwitchBlocked(Object owner, bool blocked)`、`IsManualSwitchBlocked`。
  - 原 `CurrentViewCamera / CurrentInteraction / CurrentViewRoot` 和切换方法保持可用。
- 引用关系：终端打开时以终端为 Owner 阻断手动切换；LocationProbe、Companion HUD、
  MinLoop 流程和 Binder 都改为取得当前场景正式实例。
- 最小测试：五台终端打开期间 `R` 无效，关闭并释放 Owner 后恢复；场景静态验证只找到一个正式组件。
- 后续接入：剧情若需要阻断 `R`，调用 Owner 接口并在正常退出、取消和 `OnDisable` 中成对释放，
  不要直接改 `switchKey` 或禁用正式 ViewSwitch 组件。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`；仍挂在每台终端内容根，
  原页面、输入、音频和剧情字段使用方式保持兼容。
- 新增重要 Inspector 引用：
  - `Terminal Hardware Root`：拖入拥有本终端 Camera 的 TV/Terminal 硬件根。
  - `Player Control Lock`：拖入场景共享 `HearthPlayerControlLock`。
  - `Terminal Camera`：只能拖当前硬件根内的自有 Camera。
  - `World Camera`：供 World Space Canvas 事件/渲染使用；不再被反向当成终端视角来源。
- 相机语义：
  - `SetTerminalCamera(Camera)` 只设置终端视角，不再同时覆盖 `worldCamera`。
  - 新增 `SetTerminalHardwareRoot(Transform)`。
  - 打开前会验证 Camera 确属当前终端硬件根，并拒绝 Human/Companion 第一人称相机；
    绑定错误时只在本终端层级内寻找可唯一确认的自有 Camera。
- 会话与控制逻辑：
  - 静态 `AnyTerminalOpen` 表示当前是否有正式打开的终端。
  - 同一场景同一时间只允许一个终端会话；打开另一台前先关闭已登记终端。
  - 打开时以自身 Owner 申请共享控制锁，并阻断 ViewSwitch 的手动 `R`。
  - 正常关闭、即时关闭和 `OnDisable` 都移除登记、释放自身锁和 `R` Blocker，
    恢复进入前 Camera、AudioListener、Cursor 与第一人称 UI。
  - 异常停用清理不触发正常 `OnClosed` 剧情回调，避免把中断误当成用户完成退出。
- 原有必需组件：终端 Canvas、CanvasScaler、CanvasGroup、页面、EventSystem；
  使用平滑切换时需要 `HearthTerminalCameraTransition`。终端 Camera 的 AudioListener
  可随 Camera 一起启停。
- 公开接口：原 Open/Close、页面、选择、剧情方法保持兼容；新增
  `SetTerminalHardwareRoot(Transform)` 和静态 `AnyTerminalOpen`。
- 引用关系：调用共享控制锁、正式 ViewSwitch 和 CameraTransition；Human/Companion HUD 输入
  读取 `AnyTerminalOpen`，防止终端期间出现 Tab 或预览输入穿透。
- Unity 使用步骤：选中终端内容根，核对 Hardware Root、Terminal Camera、Player Camera、
  Player Control Lock 和 ViewSwitch；运行 Runtime Topology Validate，不要只检查字段是否非空。
- 最小验收：
  - Lobby、17F01、17F02、17F03、17F04 均使用各自 TV Camera 打开并正常关闭。
  - 终端期间移动、视角、交互和 `R` 被阻断；关闭后 Owner 数回到 0。
  - 17F01 打开后停用本组件，Human Camera/Listener 和玩家控制会恢复。
- 后续扩展：终端剧情模块拆分时保留当前 Open/Close 生命周期为兼容入口；
  不要再次让 `worldCamera`、`terminalCamera` 和 `playerCamera` 共用同一含义。

### HearthTerminalCameraTransition.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTerminalCameraTransition.cs`；
  继续挂在终端根或终端控制器可引用的对象上，挂载方式无变化。
- Inspector：`Copy Audio Listener If Missing` 默认开启；旧场景即使序列化为关闭，
  当源 Camera 有 AudioListener 时，临时转场 Camera 仍会补齐并接管 Listener 连续性。
- 需要组件：可手动指定 Transition Camera；若为空且 `Create Transition Camera If Missing`
  开启，运行时会创建 `Terminal Transition Camera`。
- 公开协程和 `CancelTransition()` 使用方式无变化。
- 最小测试：进入/退出过渡期间检查到一个有效 Camera 和一个 AudioListener，
  不再出现转场中 0 Listener。
- 后续扩展：若改成 Timeline/Cinemachine，仍需保持同一条“源 Listener 关闭前，
  目标或转场 Listener 已接管”的验收规则。

### HearthFirstPersonHudController.cs / HearthFirstPersonHudInput.cs

- 路径：
  - `Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudController.cs`
  - `Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudInput.cs`
- 挂载对象与 Inspector：仍在 `HearthHudRoot`，使用方式无变化。
- Controller 本轮变化：
  - HUD 锁定玩家时改用 `SetControlsLocked(this, locked)`，只释放自己的 Owner。
  - V2 页面和高亮引用仍会从当前 Prefab 子级按稳定对象名重新解析。
- Input 本轮变化：任意终端打开时不处理 Tab、方向、Space、A/B 等 Human HUD 键盘输入。
- 公开接口：原页面、菜单、最终选择和 `SetKeyboardInputEnabled(...)` 接口保持不变。
- 引用关系：Controller 依赖共享 `HearthPlayerControlLock`；Input 读取
  `HearthTvTerminalController.AnyTerminalOpen`。
- 最小测试：终端打开时按 Tab/Space 不弹出 Human HUD 或触发其选择；
  关闭终端后按键恢复。HUD Overlay 与终端形成嵌套锁时，任一方先释放都不会提前解锁。
- 后续扩展：输入上下文服务建立后，可把静态终端门禁迁到 InputRouter，
  但必须保留当前行为兼容。

### HearthCompanionHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudController.cs`；
  继续挂在 `HearthCompanionHudRoot`，Inspector 使用方式无变化。
- 本轮变化：
  - 自动查找 ViewSwitch 时始终修正为当前场景的正式控制器，不再保留“非空但错误”的旧引用。
  - `ApplyScene(...)` 在 `statusPanelView` 有效时调用 `Apply(scene)`，不再错误地 `Clear()`。
- 2026-07-27 最终现状：V2 Companion Prefab 的 `Status Panel View` 已由安全 Refresh
  自动绑定；Trigger Card 可见时 Controller 临时隐藏它，Card 淡出后按当前 SceneData
  是否有 Status 内容恢复。此前“序列化引用仍为空”的记录已失效。
- 需要组件与公开接口：沿用原 Status/Decision/DataStream/Trigger/Hold/Projection/Direction/SFX
  子视图和场景数据接口；没有新增 Inspector 字段或公开调用方式。
- 最小测试：验证正式 ViewSwitch 不指向旧副本；再用至少两个 SceneData 检查 Status 内容，
  并确认 Timed Trigger Card 从淡入到淡出期间面板隐藏、结束后自动恢复。
- 后续接入：不需要手工补挂或拖入 V2 Status Panel；若引用丢失，运行 V2 安全 Refresh，
  不运行 Legacy Companion Builder 或全量 Rebuild。

### HearthCompanionHudExclusiveMode.cs / HearthCompanionHudFlowBinder.cs / HearthCompanionHudPreviewInput.cs

- 路径：
  - `Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudExclusiveMode.cs`
  - `Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudFlowBinder.cs`
  - `Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudPreviewInput.cs`
- 挂载对象、所需 CanvasGroup、Companion HUD、MinLoop Flow 和 Inspector 使用方式无变化。
- 本轮变化：
  - ExclusiveMode 与 FlowBinder 始终把 ViewSwitch 修正为当前场景正式实例。
  - PreviewInput 在任意终端打开时停止处理方括号、PageUp/PageDown、Return 和反引号预览键。
- 公开接口：`SetViewSwitchController / RefreshTargets / BindReplayScene / ShowScene /
  NotifyReplayCompleted / SetPreviewInputEnabled` 均保持兼容。
- 最小测试：终端打开时 Companion 预览键不改变场景或可见性；关闭后按原开关恢复。
- 后续扩展：正式 InputRouter 接入后，预览输入应成为 Editor/Development Context，
  不应在 Release 流程中与终端输入竞争。

### HearthLocationProbe.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthLocationProbe.cs`；
  继续挂在地点探测对象，Probe Root、HUD View、Raycast 和 Welcome 字段使用方式无变化。
- 本轮变化：每次解析引用时都会选择当前场景正式 ViewSwitch；序列化字段即使非空，
  只要指向旧副本也会修正。
- 公开接口和 Collider 依赖无变化。
- 最小测试：Runtime Topology Validator 确认 LocationProbe 指向正式 ViewSwitch；
  Human/Companion 切换后仍以当前正式 Rig 进行地点探测。
- 后续扩展：若引入 `HearthRuntimeContext`，应由 Context 提供当前 Probe Root，
  不再由 Probe 自行查找控制器。

### HearthCompanion17F01ReplayController.cs / HearthCompanion17F02ReplayController.cs / MinLoopFlowController.cs / MinLoopSceneValidator.cs

- 路径：
  - `Assets/Scripts/MinLoop/HearthCompanion17F01ReplayController.cs`
  - `Assets/Scripts/MinLoop/HearthCompanion17F02ReplayController.cs`
  - `Assets/Scripts/MinLoop/MinLoopFlowController.cs`
  - `Assets/Scripts/MinLoop/MinLoopSceneValidator.cs`
- 挂载对象、住户锚点、演员、字幕、HUD、信任度和公开流程接口均无变化。
- 本轮仅改变引用安全：自动解析时不再只判断 ViewSwitch 是否为空，而是始终修正为
  当前场景的正式 `ViewSwitchController`。
- Inspector 使用：现有 `View Switch Controller` 应指向
  `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`；旧副本引用会在场景修复工具中重映射。
- 最小测试：场景只读验证通过，17F01/17F02 控制器和总 Flow 不再保留重复 ViewSwitch；
  本轮没有重新执行完整住户回放剧情，使用方式无变化。
- 后续扩展：新增住户 Replay Controller 应直接接收 Context/显式引用，
  不要复制 `FindObjectOfType<ViewSwitchController>()`。

### HearthLocationDetectionSceneBinder.cs

- 路径：`Assets/Editor/HearthLocationDetectionSceneBinder.cs`；Editor 工具，不挂 GameObject。
- 菜单：`Tools > Hearth > HUD > Apply Location Detection To Open Scene`。
- 本轮变化：写入 `HearthLocationProbe.viewSwitchController` 时使用当前 Probe 所在场景的正式
  ViewSwitch，不再用任意 `FindObjectOfType` 结果。
- 其他地点 Collider、Probe Root、HUD View 绑定方式无变化。
- 最小测试：运行 Binder 后再执行 Runtime Topology Validate，LocationProbe 必须仍指向正式对象。
- 后续扩展：Binder 新增任何 Scene Controller 引用时都应先按 Scene 和显式 Identity 选择。

### HearthTvTerminalPrefabBuilder.cs

- 路径：`Assets/Editor/HearthTvTerminalPrefabBuilder.cs`；Editor 工具，不挂 GameObject。
- 菜单与输出 Prefab 使用方式无变化；仍可从 Master PPT 重建终端 UI，或把指定终端类型应用到选中 TV。
- 本轮变化：
  - 只把当前 TV 根内部明确找到的 Camera 设置为 `Terminal Camera`。
  - 同步写入 `Terminal Hardware Root`，有有效终端 Camera 时才设置 Canvas `World Camera`。
  - 不再从 Canvas World Camera 或 TV 同级其他对象的 Camera 兜底。
  - ViewSwitch 使用当前场景正式实例。
- 需要场景条件：选中对象必须能唯一识别自身 TV Camera；若没有，应停止并人工修正硬件层级，
  不要把 Player Camera 拖入 Terminal Camera。
- 最小测试：应用后运行两个静态 Validator，并在 Play Mode 打开该终端确认 Camera/Listener 唯一。
- 后续扩展：终端 Prefab 正式收口后，应把硬件 Camera 槽位改成显式 Identity/Wrapper，
  减少名称查找。

### HearthLobbyOpeningBinder.cs

- 路径：`Assets/Editor/HearthLobbyOpeningBinder.cs`；Editor 工具，不挂 GameObject。
- 主要菜单：
  - `Tools > Hearth > Lobby > Apply Ground Floor Opening Setup`
  - `Tools > Hearth > Lobby > Validate Ground Floor Opening Setup`
- 本轮变化：绑定 Lobby 任务终端时分别写入 Player Camera、Terminal Camera、
  Terminal Hardware Root 和 Canvas World Camera；不再依赖终端控制器把两个相机字段混为一体。
- 其他 Lobby Flow、NPC、任务终端和电梯 Inspector 使用方式无变化。
- 最小测试：本轮 Lobby 终端使用 `1F (1)/TvUnitSet5/Camera` 打开和关闭通过，
  必要的 Open/Custom Action 回调也通过静态验证。
- Lobby `activeLoopCuePlayer` 已持久化指向 `MIN_LOOP_ROOT/Audio/StorySFX_Lobby`，
  并在 V2/Legacy 事务切换和保存重载后保持；`AssignmentTerminal.Hum` 当前仍没有正式
  `AudioClip`，所以这里只能确认播放路由，不能确认循环底噪已可听。
- 后续扩展：修改大厅层级或替换任务终端后，先运行 Lobby Validate，再运行 Runtime Topology Validate。

### HearthUiV2Builder.cs

- 路径：`Assets/Editor/HearthUiV2Builder.cs`；Editor 工具，不挂 GameObject。
- 原菜单保持：
  - `Rebuild All V2 UI Assets`
  - `Use V2 UI In Open Scene`
  - `Use Legacy UI In Open Scene`
  - `Validate Open Scene UI`
  - Play Mode Preview 子菜单。
- 本轮迁移边界：
  - 切入 V2 前要求 Human、Companion 和五类终端共七个 V2 Prefab 全部存在。
  - 修改场景前严格预检一个 Human、一个 Companion、五台可唯一识别的终端，
    并确认七个目标槽位不重复；不再把无法识别的终端静默当作 17F01。
  - Legacy → V2 只复制 Human/Companion/Terminal 根控制器的功能字段白名单。
  - TMP、Image、Canvas、CanvasGroup 和其他视觉 MonoBehaviour 不参与全量复制。
  - 只单独迁移 Lobby 的 `OnOpened / OnCustomPrimaryAction` 与 17F04 Home 的
    `OnCustomPrimaryAction` 三个必要无参回调。
  - 终端相机即使非空但属于玩家 Rig，也会按本终端硬件归属修复。
  - 七个新根会按原兄弟索引升序恢复层级顺序，避免共享父节点的多个 UI 根互相挤动。
- Validator 新覆盖：七个唯一槽位、一个正式 ViewSwitch、页面引用、五台终端自有 Camera、
  V2/Legacy Marker、终端独占显示和三个必要剧情回调。
- 使用步骤：
  1. 日常先运行 `Validate Open Scene UI`，不要用 Rebuild 代替诊断。
  2. 切换前保存/记录当前 Dirty Scene 和 Prefab Override。
  3. 切换后运行 UI Validator、Runtime Topology Validator 和五终端 Play Mode 矩阵。
- 当前限制：
  - 本轮没有运行 Rebuild；已在 `SampleScene` 隔离副本完成正式 V2/Legacy 双向切换、
    Undo、Redo 和保存重载验证，没有用正式场景承担破坏性测试。
  - Human 约 1435、Companion 约 200、七个 V2 实例合计约 2023 个 Scene Override 未清理。
  - V2 Prefab 与 Builder 固定规则谁是视觉正式源仍未决定。
  - Companion V2 的 `statusPanelView` 仍未绑定。
- 最小验收：当前 `Validate Open Scene UI` 已通过；这只证明结构规则通过，
  不证明 Scene Override 已清理或 V2 视觉已经最终交付。
- 事务验收：隔离场景的 V2 → Legacy、Legacy → V2、Undo、Redo 与保存后重载均通过；
  两个状态都通过 UI Validator 和 Runtime Topology Validator。切换中一旦映射、相机、
  Listener 或后置验证失败，会回滚整个 Undo 组且不保存半套 UI。
- 后续扩展：下一步应先建立显式 `UiSlotIdentity / TerminalIdentity / HearthUiPrefabSet`
  和 Override 白名单报告，再决定是否拆分 Builder、Migration、Binder、Validator。

### 本轮统一验证边界

- 已验证：
  - Unity 刷新编译无本轮新增编译错误。
  - Runtime Topology Validator 和 UI Open Scene Validator 通过。
  - 五台终端基础打开、稳定、关闭。
  - 转场、终端和 Human 视角阶段各保持一个有效 Camera 与 AudioListener。
  - 17F01 终端控制器中途停用能清理。
  - 两个 Owner 嵌套控制锁不会提前恢复。
  - 隔离场景完成 V2/Legacy 双向切换、Undo/Redo、保存后重载。
  - Lobby `activeLoopCuePlayer` 在切换和重载后保持正确引用。
- 未验证：
  - 五户完整剧情、Replay、Decision、Result、Return 的端到端链路。
  - Companion `statusPanelView` 的可见效果。
  - 清理 2023 个 V2 Scene Override 后的重开一致性。
  - EditMode/PlayMode 自动化测试和 CI。
  - `AssignmentTerminal.Hum` 正式 AudioClip 的实际播放效果。
- 旧场景仍会报告 ROOM2 书桌、ROOM3 木柜/电视柜相关的负缩放 BoxCollider 错误；
  该模型碰撞问题不属于本轮 UI/P0 脚本新增错误。

## 2026-07-26 UI V2 文字容量与 17F03 主动作修正

### HearthCompanionSpecialEffectsView.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionSpecialEffectsView.cs`。
- 使用方式无变化：仍挂在
  `HearthCompanionHudRoot/SpecialEffectsLayer/SpecialEffectsView`，所需
  `CanvasGroup`、overlay `Image`、标题、正文、状态和 pulse 引用及所有公开接口均不变。
- 本轮只修正文字容量：标题、正文和状态保留 Prefab 中配置的固定字号，允许完整换行，
  使用 `Overflow`，并清除人工字符、词和行数上限；不再自动缩字或截断。此规则取代上文
  “自动字号、最大行数和截断”的旧说明。
- 最小测试：依次触发 Black Audio、Shutdown Glitch、Deep Sleep，并用最长正式显示内容确认
  三个 TMP 均保持原字号、完整显示，没有 `Truncate` 或行数裁切。
- 后续替换视觉时只需调整 Prefab 的字号和 RectTransform 容量，不需要修改脚本公开调用。

### HearthUiV2Builder.cs

- 路径：`Assets/Editor/HearthUiV2Builder.cs`；Editor 工具，不挂 GameObject。
- 使用方式无变化：日常只使用
  `Tools > Hearth > UI V2 > Refresh Existing V2 Prefab Visuals` 安全刷新已有七套 V2 Prefab；
  不以 `Rebuild All` 或 `Use V2 UI In Open Scene` 代替现场刷新。
- 本轮刷新规则会同时写入视觉与真实动作：17F01/17F02 固定为
  `RequestReplay`，17F03 固定为 `EnterUnit`，因此 17F03 顶栏 `ENTER UNIT` 与 Space 执行逻辑
  不会在后续 restyle 后再次分离。
- 最小测试：安全刷新后读取
  `Assets/Prefabs/UI/HearthHud/V2/Terminals/Terminal_17F03_Alert_V2.prefab` 上的
  `HearthTvTerminalController.PrimaryAction`，应为 `EnterUnit`；Play Mode 中动作解锁后按
  Space 应走 `RequestEnterUnit`，而不是请求 archived replay。

## 2026-07-27 UI V2 三条真实剧情流程接入

### Hearth17F03InspectionPanel.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F03InspectionPanel.cs`。
- 挂载对象不变：仍挂在
  `MIN_LOOP_ROOT/ReplayRoom_17F03/UI/Hearth17F03InspectionCanvas`，由
  `HearthCompanion17F03ReplayController.inspectionPanel` 打开和关闭；Canvas、CanvasGroup、
  标题、状态、正文、Recall 区和 A/B 选择引用均不变。
- 本轮新增 `ConfigureSecondUiVisual(HearthUiThemeProfile, bool)` 和
  `ApplySecondUiVisual()`：在原有真实检查面板上应用第二套 UI 的 1920×1080 布局、固定字号、
  Overflow、深蓝黑半透明底板、冷白/灰蓝文字、信息规则线、物理画面标识和准星。
- 输入和剧情职责没有迁移：Space/Esc、Recall 排队、A/B 处置、`RecallRequested`、
  `CloseRequested`、`ChoiceSubmitted` 仍由本脚本与 17F03 Replay Controller 按原顺序处理；
  新视觉不读取额外按键、不推进剧情。
- Inspector 新字段：
  - `Use Second Ui Visual`：正式 V2 场景保持开启。
  - `Second Ui Theme`：拖入
    `Assets/UI/HEARTH/V2/Profiles/Hearth_UiV2Theme.asset`。
- 最小测试：进入 17F03 房内后对实体单元按 E，固定检查相机上应显示
  `ENTITY INSPECTION`；对白结束后 Space 可进入 Recall，回放后 A/B 仍按上下键和 Space
  提交；所有 TMP 固定字号且不截断。
- 后续替换模型或相机无需改脚本；替换色调从 Theme Profile 接入，调整检查面板专属位置时从
  `ApplySecondUiVisual()` 的 1920 坐标接入。

### Hearth17F03MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F03MinimalLoopBinder.cs`；Editor 工具，不挂 GameObject。
- 使用方式无变化；若未来明确需要重新运行 17F03 Apply，它会在创建/修复真实检查面板后同时
  绑定 `Hearth_UiV2Theme.asset` 并调用第二套视觉配置。
- 本轮正式场景接入不依赖重跑整个 Binder，避免覆盖用户已经调整的演员、锚点和相机。

### HearthCompanion17F03ReplayController.cs

- 路径：`Assets/Scripts/MinLoop/HearthCompanion17F03ReplayController.cs`；挂载对象、剧情阶段、
  演员、固定检查相机、实体单元和公开流程方法使用方式无变化。
- 本轮补齐 V2 迁移后的引用安全：`Awake` 和每次进入实体检查前会解析当前场景正在使用的
  `HearthFirstPersonHudController` 根 `CanvasGroup`；若旧字段为空或指向非活动 Legacy 根，
  会改用正式活动 HUD。
- 当前 `SampleScene` 已显式序列化
  `humanHudCanvasGroup = HearthHudRoot`。进入检查时 Human 身份、任务、地点和叙事卡会一起隐藏，
  字幕播放器仍保持独立可见；退出检查恢复原 HUD 状态。
- 最小测试：Play Mode 调用真实检查入口后，检查面板可见时正式 Human HUD 根 alpha 应为 0；
  退出后恢复为进入前数值。使用方式无变化，无新增按键或剧情事件。

### HearthPhotoFrameInteractable.cs

- 路径：`Assets/Scripts/MinLoop/HearthPhotoFrameInteractable.cs`。
- 挂载对象不变：仍挂在 17F04 电子照片屏 `TV (4)`，继续依赖
  `Hearth17F04FinaleController`、实体照片 Renderer、固定 Photo Camera、相机过渡、
  Player 控制组件和安全退出提示。
- 新增第二套照片档案引用：
  - `Use Second Ui Photo Archive`：正式 V2 场景开启。
  - `Photo Archive Hud`：拖入当前场景正式 `HearthFirstPersonHudController`。
  - `Photo Archive Hud Input`：拖入同一 HUD 根的 `HearthFirstPersonHudInput`。
  - `Archive Render Texture Size`：默认 `880×610`，对应 V2 `V2_PhotoViewport`。
- 实际运行链路：相框仍先切换到真实固定相机并把当前 Renderer 照片贴图显示出来；随后脚本把
  该相机实时输出写入临时 RenderTexture，并显示到 V2 `Slide07Photo2023` /
  `Slide08Photo2026` 的照片视口。左右键仍由相框脚本切换 Renderer 和剧情页；V2 HUD Input
  在查看期间临时停用，避免其“任意键关闭故事页”抢走照片流程按键。
- 当前动作提示会写入 V2 `V2_PhotoReturnHint`：对白期间为 `PLEASE WAIT`，可翻页时显示
  左右键，可退出后才显示 Space/安全 Esc；旧 ExitHint Canvas 在 V2 打开时不会重复显示。
- V2 照片页强制 `Keep Persistent HUD = false`，查看期间隐藏 Human 身份/任务；静态
  `V2_PhotoFieldUnit` 占位通道会暂时关闭，把同一位置留给正式字幕播放器。页码按实际素材
  计算：当前第二张贴图为空时显示 `PAGE 01 / 01`，不会误报 2 页。
- 公开接口新增
  `ConfigureSecondUiPhotoArchive(HearthFirstPersonHudController, HearthFirstPersonHudInput, bool)`；
  其余 `OpenView/CloseView/NotifyPageReadyForNavigation/NotifyDialogueComplete` 使用方式不变。
- 若 HUD 页或照片视口缺失，脚本会保留原实体固定相机流程并给出 Warning，不会阻断剧情。
- 最小测试：17F04 入户后对照片屏按 E；V2 档案页中间应显示真实 Photo Camera 画面；
  正式对白结束前 Space 不退出；有第二张贴图时左右键同步切换页码、Renderer 与对白；退出后
  恢复原相机、HUD 页和 HUD Input。

### HearthVirusPopupShutdownChallenge.cs

- 路径：`Assets/Scripts/MinLoop/HearthVirusPopupShutdownChallenge.cs`。
- 挂载对象不变：仍挂在
  `MIN_LOOP_ROOT/Finale_17F04/UI/ShutdownChallenge_17F04`，并继续作为
  `Hearth17F04FinaleController.shutdownChallenge` 的唯一剧情挑战引用。
- 新增 `Use Second Ui High Trust Confirmation` 与 `High Trust Hud Controller`：
  高信任分支改为调用正式 Human V2 `Slide10ShutdownConfirm`；HUD 的确认/取消事件回传本
  Challenge 的 `Completed/Cancelled`，17F04 Finale Controller 继续拥有剧情推进权。
- 高信任分支会再次确认 Slide10 为 Fullscreen Takeover 且
  `Keep Persistent HUD = false`，防止 Scene Override 让身份/任务 HUD 穿透关机确认。
- 低信任分支完全保留既有三波病毒弹窗、生成速度、Space 消除玩法、半透明遮罩和 SFX；
  本轮没有把低信任改成静态 Slide11–13。
- 公开接口新增
  `ConfigureSecondUiHighTrustConfirmation(HearthFirstPersonHudController, bool)`；
  `BeginChallenge/Submit/Cancel` 对外用法不变。若 Human HUD 缺失，高信任会自动回退到原单弹窗
  确认，避免流程中断。
- 最小测试：用高信任进入 17F04 关机阶段应显示 V2
  `SHUTDOWN COMPANION UNIT`，Space 完成、Esc 回到 ApproachUnit；低信任进入同一阶段仍显示
  动态病毒弹窗波次，并且 Human Slide10 不出现。

## 2026-07-27 UI V2 最终运行互斥、安全刷新与输入门控增量

> 本节是 2026-07-27 收口后的最新使用口径，覆盖前文中“大厅简报在终端内播放”、
> “视觉刷新后必须重装教程”以及“协调器只读但不主动处理外部显示”的旧描述。
> 本节没有改变正式英文对白、现有按键、住户剧情状态机或各功能 Wrapper 的所有权。

### HearthLobbyFlowController.cs

- 路径：`Assets/Scripts/MinLoop/HearthLobbyFlowController.cs`；继续挂在
  `MIN_LOOP_ROOT/LobbyOpening/HearthLobbyFlowController`。挂载方式、锚点和 Inspector
  使用方式无变化。
- 依赖组件和关键引用无变化：Human Root/Camera、Movement、Look、Interaction、
  Rigidbody、Lobby/Elevator/17F Arrival 锚点、正式 `MinLoopSubtitlePlayer`、
  `HearthScreenFader`、`HearthLobbyHudOverlay`、Lobby 任务终端、可选对白区、
  正式 `HearthDialogueSequence` 与共享 SFX Cue Player。
- 本轮内部流程变化：确认任务后，终端先保持至少 `5` 秒并显示 `PLEASE WAIT`；
  解锁后 Space 只请求关闭终端。`assignmentLoadedDialogue` 只有在终端真实关闭后才开始，
  因此世界字幕不会与全屏终端重叠。终端关闭后的简报期间保留移动和视角，
  但 E 交互与电梯继续锁定，最后一句完成后才恢复。
- 公开状态和接口：`CurrentStage`、`AssignmentLoaded`、`CanOpenAssignmentTerminal`、
  `CanUseElevator`、`BeginOpening()`、`TryPlayOptionalConversation(...)`、
  `TryPlayExitCommentary(...)`、`AcquireAssignmentFromTerminal()`、
  `BeginAssignmentBriefingFromTerminal()`、`ConfirmAssignmentTerminalClose()`、
  `BeginElevatorRide()`、`ResetLobbyFlowForPreview()`、`SetSfxCuePlayer(...)`。
  对外调用方式无变化。
- 引用关系：Lobby 终端的 Custom 主动作调用
  `BeginAssignmentBriefingFromTerminal/ConfirmAssignmentTerminalClose`；
  电梯交互只读取 `CanUseElevator`；字幕仍由正式播放器自动推进，UI Presenter 不参与流程。
- 最小测试：打开任务终端后前 5 秒 Space/Esc 均不关闭且无世界字幕；
  5 秒后出现 `SPACE CLOSE TERMINAL`，Space 关闭；关闭后正式路线简报才出现，
  可走动/转头但 E 和电梯无效；简报结束后电梯开放，终端本轮不能再次打开。
- 后续扩展：更改等待时间只调 `Assignment Terminal Minimum View Seconds`；
  更换终端视觉不得把 `assignmentLoadedDialogue` 移回终端打开阶段。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`；继续挂在 Lobby、
  17F01、17F02、17F03、17F04 各终端内容根。Canvas/CanvasScaler/页面、
  Player Camera、终端自有 Camera、Terminal Hardware Root、Camera Transition、
  共享 Control Lock、音频与剧情 UnityEvent 的挂载和 Inspector 使用方式无变化。
- 本轮内部收口：
  - `TerminalViewState` 持续输出页码、焦点、主动作、锁定和退出状态；
    Lobby Custom 动作为 `CLOSE TERMINAL`，17F01/02 为
    `REVIEW ARCHIVED EVENT`，17F03 为 `ENTER UNIT`，17F04 为 `ENTER HOME`。
  - 锁定时动作仍可见并显示 `PLEASE WAIT`，不会移动到最后一页或从顶栏消失。
  - `KeyboardNavigationRoot` 的嵌套 Canvas 会在引用解析时强制
    `Override Sorting=true / Sorting Order=20`，保证 Footer 位于页面 Canvas（Order 10）
    上方；旧 Scene Override 也会在运行时修正。
- 公开接口：除原有 `OpenTerminal/CloseTerminal/ShowPage/ShowNextPage/
  ShowPreviousPage/RequestRobotReplay/RequestEnterUnit/RequestCustomPrimaryAction/
  ShowPostReplayChoicePage` 外，继续提供 `TerminalViewState`、
  `TerminalViewStateChanged`、`SetPrimaryActionInputEnabled(...)`、
  `SetChoiceInputEnabled(...)`、`SetCloseInputEnabled(...)`、
  `SetRuntimePrompt(...)`、`ClearRuntimePrompt()` 和各 `Set...` 配置接口。
- 引用关系：本脚本仍唯一拥有终端 Tab、方向键、Space、Esc 的读取与剧情动作执行权；
  `HearthTerminalViewState` 和视觉层只是被动读取。Human HUD Input 通过
  `AnyTerminalOpen` 让出输入，`HearthUiStateCoordinator` 用同一状态做全局互斥。
- 最小测试：五台终端分别打开/关闭，检查只有实体电视外框；Footer 清晰位于页面之上；
  17F01/02/03/04 顶栏动作分别正确；锁定时 Space 无效、动作不消失；
  解锁后 Space 执行原剧情动作，Esc 恢复进入前相机、HUD 和控制。
- 后续扩展：新增动作先扩展 `HearthTerminalViewState` 映射和控制器动作枚举，
  不从 TMP 文本或页码猜剧情。使用方式无变化。

### HearthUiStateCoordinator.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthUiStateCoordinator.cs`；由
  `HearthUiV2SystemInstaller` 挂在 `HearthHudRoot_V2` 根，与
  `HearthFirstPersonHudController` 同对象；同一 HUD 根只保留一个。
- Inspector 正式值：`Automatically Resolve Runtime State=true`；
  `Apply Resolved State To Canvas Groups=false`；`Human Hud` 绑定同根控制器。
  ViewSwitch、字幕播放器、关机 Challenge 和 Lobby Overlay 可由运行时解析。
- 本轮职责扩展：除读取 Human 页、终端、字幕、视角、Control Lock、暂停与 Takeover 外，
  现在接受具名 Owner 的 `SetExternalModalRequest(owner, visible)`，并按
  `Takeover > Modal > Terminal > 普通 HUD` 解析唯一可见层。
  当 Terminal、Modal 或 Takeover 接管画面时，协调器会主动调用 Human HUD、
  字幕播放器和 Lobby Overlay 的外部抑制接口。Terminal 只放行显式 Terminal Context
  的所属对白；Modal/Takeover 抑制全部字幕视觉，但均不停止对白音频、计时或剧情协程。
- 公开接口新增/重点：`CanOpenHumanMenu`、`SetExternalModalRequest(...)`；
  其余 `RequestedState`、`ResolvedState`、`ResolvedStateChanged`、
  `ConfigureRuntimeSources(...)`、`SetRuntimeIntegration(...)`、
  `RefreshRuntimeState()`、`ResolveRuntimeSources()` 和静态 `Resolve(...)` 保持可用。
- 引用关系：`HearthFirstPersonHudInput` 查询 `CanOpenHumanMenu`；
  `Hearth17F03InspectionPanel` 以自身为 Owner 申请/释放 Modal；
  `MinLoopSubtitlePlayer` 与 `HearthLobbyHudOverlay` 接收显示抑制；
  初始教程读取 `ResolvedState`。协调器仍不读取按键、不推进剧情。
- 最小测试：普通 Human 为 Persistent+Interaction；正式对白时 Interaction 关闭；
  Tab/照片/检查为 Modal；任意终端为 Terminal；关机/结局为 Takeover。
  Terminal 中只有显式 Terminal Context 字幕可见，世界字幕和 Lobby 卡片不可见；
  Modal/Takeover 中全部字幕和 Lobby 卡片不可见。退出后按仍在运行的真实状态恢复，
  不应停止或重播对白。
- 后续扩展：新的场景模态 UI 应使用稳定 Owner 调用
  `SetExternalModalRequest(this, true/false)`；不要直接改字幕或常驻 HUD Alpha。

### HearthFirstPersonHudInput.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudInput.cs`；
  继续挂在 Human `HearthHudRoot`，与 `HearthFirstPersonHudController` 同对象。
  Controller、Settings View、最终选择提示和键位字段的使用方式无变化。
- 新增关键引用：`State Coordinator` 应指向同根
  `HearthUiStateCoordinator`；为空时运行时从 Controller 同对象解析。
- 本轮内部变化：当当前页为 Persistent/None 且协调器报告
  `CanOpenHumanMenu=false` 时，Tab 不再打开 Human Menu；因此对白、控制锁、
  终端、Modal 或 Takeover 中不会穿出第二层菜单。已经打开的菜单仍可用 Tab/Esc 关闭，
  原方向键、Space、A/B 和设置输入不变。
- 公开接口：`SetKeyboardInputEnabled(...)`、`GetFinalChoiceInputProfile()`、
  `SetFinalChoiceInputProfile(...)` 均无变化；没有把剧情推进权移入本脚本。
- 引用关系：先用 `HearthTvTerminalController.AnyTerminalOpen` 做终端输入让渡，
  再由 Coordinator 判断普通 Human 菜单是否可开；页面执行仍交给 HUD Controller。
- 最小测试：普通自由玩法 Tab 可开/关；字幕、锁控、终端、照片档案、实体检查和关机界面中
  Tab 均不能新开菜单；退出这些状态后 Tab 恢复。使用方式无变化。

### MinLoopSubtitlePlayer.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopSubtitlePlayer.cs`；继续使用场景中正式播放器，
  依赖 Style Profile、V2 VisualRoot、Speaker/Body TMP、CanvasGroup、
  AudioSource/Dialogue Channel Source。挂载和正式对白调用方式无变化。
- 本轮新增显示接口：`ExternalPresentationSuppressed` 和
  `SetExternalPresentationSuppressed(bool)`。抑制只把最终 Canvas Alpha 隐藏；
  `IsPlaying`、AudioClip、逐句时长、协程与完成事件继续运行，解除后按当前播放状态恢复，
  不从头重播也不提前完成。
- 其余公开接口 `PlaySequence(...)`、`PlayLines(...)`、`ShowLine(...)`、
  `SetPresentation(...)`、`SetSubtitleContext(...)`、`Hide()`、`Stop()` 保持不变；
  带 `HearthSubtitleContext` 的一次性播放重载见本文件后续“最终增量”条目。
- 引用关系：由 Lobby/住户/Replay/Finale 状态机播放正式
  `HearthDialogueSequence`；只有 `HearthUiStateCoordinator` 使用外部抑制接口。
  教程和操作提示仍禁止调用 `ShowLine/Stop`。
- 最小测试：世界对白播放中打开终端或任意对白播放中打开 Modal，字幕画面立即隐藏但
  `IsPlaying=true`、语音与时长继续；显式 Terminal Context 对白在终端内保持可见。
  关闭接管界面后若被抑制句尚未结束则恢复当前句，若已结束则正常保持结束。

### HearthLobbyHudOverlay.cs

- 路径：`Assets/Scripts/MinLoop/HearthLobbyHudOverlay.cs`；继续挂在
  `MIN_LOOP_ROOT/LobbyOpening/LobbyHudOverlay`。
- 组件和引用：原 Activation、Expanded Message、Pinned Message、
  Assignment Status CanvasGroup/TMP 保持；新增/自动解析的
  `External Presentation Group` 应为 Overlay 根 `CanvasGroup`，缺失时脚本自动补齐。
- 新公开接口：`SetExternalPresentationSuppressed(bool)`；它只控制 Overlay 根 Alpha，
  不改变 Lily 留言已展开/折叠/已读状态，也不重置任务加载文本。
  原 `HideAllImmediate/ShowActivation/ShowExpandedVoiceMessage/
  CollapseVoiceMessage/DismissVoiceMessage/SetAssignmentLoaded` 均不变。
- 引用关系：Lobby Flow 继续拥有叙事卡内容状态；Coordinator 只在
  Persistent 层不应显示时临时抑制整个 Overlay。
- 最小测试：正常 Lobby 开场卡片按既定顺序显示；打开终端、Human Modal 或 Takeover 后
  Overlay alpha 为 0；退出后恢复此前真实状态，不重复展开 Lily 留言。使用方式无变化。

### HearthInitialGameplayTutorialController.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthInitialGameplayTutorialController.cs`；
  由 System Installer 挂在
  `HearthHudRoot_V2/PersistentHudLayer/V2_InitialTutorialRoot`，同对象需要
  Canvas、CanvasGroup、Image 与 `HearthActionHintPresenter`。
- Inspector：Presenter、CanvasGroup、Human HUD、同根 Coordinator 为正式引用；
  `Effective Gameplay Seconds=10`、`Fade Out Seconds=0.35`。
- 本轮内部变化：只累计真正可交互的 Human Gameplay 时间；终端、Modal、Takeover、
  正式对白、控制锁、暂停、非 Human 视角和动态 E 提示出现时同时暂停计时并隐藏教程。
  返回普通玩法后继续剩余时间，满 10 秒后用 0.35 秒淡出。
- 公开接口：`ElapsedGameplaySeconds`、`IsRunning`、`UiStateCoordinator`、
  `Configure(...)`、`SetUiStateCoordinator(...)`、`RestartForPreview()`、
  `ResetCompletionForCurrentRun()`。
- 引用关系：优先读取 Coordinator；未安装时才回退读取终端、锁控、ViewSwitch、
  Human 页、动态交互提示与字幕。Presenter 只显示
  `WASD/MOUSE/E/TAB`，不使用 `SPACE CONTINUE`。
- 最小测试：自由玩法累计 10 秒后淡出；在字幕、E 提示、Tab、终端、照片/检查、
  关机或 Companion 视角中等待不消耗计时且教程不可见；返回后继续剩余秒数。
- 视觉刷新现已保留 `V2_InitialTutorialRoot`；日常 Refresh 后无需重复安装。
  只有首次缺少 Theme/Layout/Coordinator/教程根，或 Validator 报缺失时才运行
  `System > Install Profiles And Human Tutorial`。其余使用方式无变化。

### Hearth17F03InspectionPanel.cs

- 路径：`Assets/Scripts/MinLoop/Hearth17F03InspectionPanel.cs`；继续挂在
  `MIN_LOOP_ROOT/ReplayRoom_17F03/UI/Hearth17F03InspectionCanvas`。
  CanvasGroup、标题/状态/详情/Recall、A/B 选择、Highlight 与 UnityEvent 的挂载不变。
- 关键引用：`Second Ui Theme` 绑定
  `Assets/UI/HEARTH/V2/Profiles/Hearth_UiV2Theme.asset`；
  `Ui State Coordinator` 可显式拖入当前 Human V2 根实例，也可在运行时解析。
- 本轮运行互斥：`Open()` 时以本面板为 Owner 申请外部 Modal，
  `Close()`/停用时释放；因此 Human 常驻 HUD、Lobby 卡片和正式字幕视觉不会叠在检查画面上，
  但 Recall、对白计时、回放和选择流程仍由原控制器继续。
- 公开接口：`Configure(...)`、`ConfigureChoiceUi(...)`、
  `ConfigureSecondUiVisual(...)`、`ApplySecondUiVisual()`、`Open()`、
  `OpenDispositionChoice(...)`、`SetChoiceInputEnabled(...)`、`Close()`、
  `ConfirmRecall()`、`RequestClose()`、`SetRecallAvailable(...)`、
  `QueueRecallRequest()`、`MoveChoice(...)`、`SubmitChoice()`；
  事件 `RecallRequested/CloseRequested/ChoiceSubmitted` 保持原语义。
- 引用关系：`HearthCompanion17F03ReplayController` 仍拥有检查相机、Recall 和处置阶段；
  Coordinator 仅负责显示互斥；Theme 仅提供视觉令牌。
- 最小测试：实体单元 E 打开检查，面板为 `1320×840`，所有 TMP 固定字号、
  `Overflow`、无穿线/截断；Human/Lobby/字幕视觉隐藏；Space Recall、
  Up/Down+A/B 与 Esc 按原阶段工作；关闭后 HUD 恢复。

### HearthUiV2Builder.cs

- 路径：`Assets/Editor/HearthUiV2Builder.cs`；Editor 工具，不挂 GameObject。
- 当前唯一日常视觉入口：
  `Tools > Hearth > UI V2 > Refresh Existing V2 Prefab Visuals`。
  它只重建七套既有 V2 Prefab 中的 `V2_` 视觉树，保留功能 Wrapper、
  序列化玩法字段、剧情 UnityEvent 和 `V2_InitialTutorialRoot`。
- 依赖：七套既有 V2 Prefab、`HearthUiThemeProfile`、
  `HearthUiLayoutProfile`、Liberation Sans 和各 Human/Companion/Terminal 控制器。
  主题颜色、键帽尺寸及共享区域优先从 Theme/Layout Profile 读取。
- 输出仍为 Human、Companion、17F01、17F02、17F03、17F04、Lobby 七个 Prefab；
  覆盖 11 类运行界面。动态底板、键帽、按钮、2 px 规则线和文字使用 Unity Image/TMP；
  当前运行视觉对 `Assets/UI/HEARTH/GeneratedParts/` 的序列化引用审计为 `0`。
- 安全流程：退出 Play Mode并保存现场 → 运行 Refresh → 运行
  `Validate Open Scene UI`、System Validator、Subtitle Validator 与 Runtime Topology
  Validator → 进入 Play 做原生 1920×1080 截图和真实流程测试。
  若首次缺 Theme/Layout/Coordinator/教程才运行
  `System > Install Profiles And Human Tutorial`；普通 Refresh 后教程会保留。
- 严禁把 `Rebuild All V2 UI Assets` 或 `Use V2 UI In Open Scene`
  当日常刷新：前者会从 Legacy 全量克隆，后者是七根整体迁移事务，
  都可能覆盖现场 Override/引用，只有明确回退或迁移任务并建立备份后才可使用。
- 公开 Editor 接口/菜单：`RefreshExistingV2PrefabVisuals()`、
  `ValidateOpenSceneUi()` 与 Preview 菜单为日常入口；全量 Rebuild 和
  V2/Legacy Switch 仅保留作受控回退。
- 最小测试：刷新后教程根仍存在；七槽位、五终端自有 Camera、唯一 ViewSwitch、
  必要剧情回调全部通过；Human/Companion/终端真实流程没有双重大框、白底、穿字或
  GeneratedParts Sprite 引用。

### Hearth17F03MinimalLoopBinder.cs

- 路径：`Assets/Editor/Hearth17F03MinimalLoopBinder.cs`；Editor 工具，不挂 GameObject。
  菜单仍为 `Apply 17F03 Minimal Loop Setup`、`Rebuild 17F03 Anchors From References`
  和 `Validate 17F03 Minimal Loop Setup`，使用方式无变化。
- 本轮内部收口：创建/修复真实检查面板时继续绑定 V2 Theme 并调用
  `ConfigureSecondUiVisual(...)`；安全文字统一关闭 Auto Size、启用 Word Wrapping，
  使用 `TextOverflowModes.Overflow`，不再写回 `Truncate`。
- 依赖关系：仍负责 17F03 演员、锚点、终端、Replay Controller、检查面板和正式对白引用；
  运行时剧情所有权不迁入 Binder。现有正式场景不因 UI 微调而重跑整个 Apply。
- 公开 Editor 接口：`ApplySetup()`、`RebuildAnchorsFromReferences()`、
  `ValidateSetup()`；无新增运行时组件或 Inspector 槽位。
- 最小测试：仅在明确需要重建 17F03 时，于退出 Play Mode且已备份锚点后运行 Apply，
  再运行 Validate；检查面板所有 TMP 必须为固定字号+Overflow，主题引用有效，
  Recall/选择/相机和既有演员锚点不丢失。
- 后续扩展：优先调用真实面板的定向视觉接口；不得为了改颜色或字号重跑完整 17F03 Binder。

## 2026-07-27 Companion V2 状态面板补建与绑定

### HearthUiV2Builder.cs

- 路径：`Assets/Editor/HearthUiV2Builder.cs`；Editor 工具，不挂 GameObject。
- 本轮职责：`Refresh Existing V2 Prefab Visuals` 在既有
  `HearthCompanionHudRoot_V2.prefab/PersistentInfoLayer` 下定向重建
  `V2_StatusPanel`，并把其 `HearthCompanionStatusPanelView` 写入同根
  `HearthCompanionHudController.statusPanelView`。该流程不替换场景实例、不复制
  Legacy 根，也不改剧情输入或 UnityEvent。
- 生成组件：状态面板只使用 `RectTransform`、
  `HearthCompanionStatusPanelView`、平面 `Image`、TMP 和 2 px 规则线；
  不引用 `GeneratedParts`，不烘焙正式状态文字。Title、Rows、Footer 继续由
  `HearthCompanionHudSceneData` 在运行时写入，Accent 继续跟随场景状态色。
- Inspector/引用：刷新完成后不需要手工拖拽；Builder 使用 Editor 序列化接口把
  `V2_StatusPanel` 自动绑定到 Controller。Controller 与
  `HearthCompanionStatusPanelView` 的运行时公开接口、挂载方式和使用方式无变化。
- Unity 使用步骤：退出 Play Mode 后运行
  `Tools > Hearth > UI V2 > Refresh Existing V2 Prefab Visuals`，然后检查
  V2 Companion Prefab 的 `Status Panel View` 不为空；不要为本修复运行全量 Rebuild
  或场景 UI 切换。
- 最小测试：进入 Companion 视角并切换至少两个不同的
  `HearthCompanionHudSceneData`；标题、最多五行指标、Footer 和 Accent 应随数据更新，
  字号固定、文字完整。Trigger Card 淡入/显示期间 Status Panel 必须隐藏，
  Card 淡出完成后自动恢复，不与临时内容重复。
- 后续扩展：若增加超过五行或明显更长的指标，先调整
  `BuildCompanionStatusPanel(...)` 的文字容量和区域，不要把固定文字烘焙进底板。

## 2026-07-27 UI V2 最终增量（终端语境、常驻层与文字安全）

> 本节是 2026-07-27 的最终补充口径。若同日较早条目中存在“终端会隐藏全部字幕”、
> “终端底栏 Y=968”或“Companion 状态面板尚未绑定”等旧描述，以本节为准。
> 本次没有修改
> `HEARTH_Full_Game_Script_No_Audio_Tags_Native_English.md`，没有改写正式对白。

### HearthCompanionTriggerCardView.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionTriggerCardView.cs`；
  继续挂在
  `HearthCompanionHudRoot/TimedCardLayer/TriggerCardView`，同对象仍需要
  CanvasGroup，子物体仍为 Title/Body TMP 与可选 Accent Image。
  挂载位置和 Inspector 使用方式无变化。
- 新增只读属性 `IsVisible` 与事件
  `VisibilityChanged(bool)`。延迟阶段保持不可见；开始淡入前通知 `true`，
  完成停留并淡出后通知 `false`。`HideImmediate()`、新的 Card/Cue 抢占旧协程时也会
  先复位为不可见，避免订阅者残留错误状态。
- `OnDisable()` 会立即停止活动协程、清理可见状态并通知 Controller 恢复 Status Panel；
  `Timed Cues` 为空或 CanvasGroup 缺失时也会安全隐藏，不会在切换视角、停用对象或
  不完整绑定后把临时卡状态卡住。
- 原公开接口 `Configure(...)`、`Apply(...)`、`ShowCueSequence(...)`、
  `ShowCard(...)`、`HideImmediate()` 保持不变；Timed Cues 与旧
  `Show Trigger Card` 数据格式均不变。
- 引用关系：`HearthCompanionHudController` 订阅可见性事件，用它临时抑制
  `HearthCompanionStatusPanelView`；Trigger Card 仍不自行读取按键或推进剧情。
- Unity 使用步骤：无需新增 Inspector 拖拽；保持 Controller 的
  `Trigger Card View` 与 `Status Panel View` 指向同一 Companion HUD 内的正式实例。
- 最小测试：播放含延迟的 Cue 时，延迟前 Status Panel 正常；Card 开始淡入时
  `IsVisible=true` 且 Status Panel 隐藏；Card 淡出结束后 `IsVisible=false`
  且 Status Panel 自动恢复。
- 后续扩展：新的临时 Companion 卡片继续复用本事件，不要直接改 Status Panel Alpha。

### HearthCompanionHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/Companion/HearthCompanionHudController.cs`；
  继续挂在 `HearthCompanionHudRoot`，所需 Canvas、CanvasScaler、
  GraphicRaycaster、CanvasGroup、AudioSource 及 Scenes/View 引用不变。
  挂载和 Inspector 使用方式无变化。
- `ApplyScene(...)` 现在把当前 SceneData 写入 `Status Panel View`；
  仅在 SceneData 存在非空 StatusTitle、非空 StatusFooter 或至少一条非空 StatusLine
  时显示常驻面板，空字符串/空数组不会生成空状态卡。
  Trigger Card 淡入、停留和淡出期间临时关闭 Status Panel GameObject，
  淡出结束后按当前 SceneData 是否有内容自动恢复，避免临时卡与常驻卡重复显示同一信息。
- Controller 在 Awake/Configure 后绑定
  `HearthCompanionTriggerCardView.VisibilityChanged`，重新配置引用前与
  `OnDestroy()` 时解除旧订阅，避免重复回调或销毁后悬挂。
- 原公开方法 `Configure(...)`、`ShowScene(string/int)`、`AdvanceScene()`、
  `SetVisible(...)`、`SetHoldPromptVisible(...)`、`ConfirmCurrentPrompt()`、
  `SetDirectionTarget(...)` 保持不变；本轮没有新增 Inspector 字段。
- 引用关系：SceneData 仍是 Status/Trigger 内容来源，Controller 只协调两个 View 的互斥；
  `HearthCompanionHudFlowBinder` 与三户 Replay Controller 的调用方式无变化。
- 最小测试：选择一个同时有 Status 内容和 Timed Cue 的 SceneData；
  Card 可见全程 Status Panel inactive，Card 完成后面板恢复且仍显示当前 SceneData，
  切换到无 Status 内容的 SceneData 后不得错误恢复空面板。
- 后续扩展：若增加其他临时全屏/卡片提示，应接入统一可见状态或 Coordinator，
  不要在剧情脚本中手动启停 `V2_StatusPanel`。

### HearthTvTerminalController.cs

- 路径：`Assets/Scripts/UI/HearthHud/HearthTvTerminalController.cs`；继续挂在 Lobby、
  17F01、17F02、17F03、17F04 五个终端的内容根。目标对象仍需要 Canvas、
  CanvasScaler、CanvasGroup、页面根、键盘导航根，以及各自 TV 硬件内的终端 Camera；
  Player Camera、Terminal Hardware Root、共享 `HearthPlayerControlLock`、剧情事件和音频引用
  的 Inspector 使用方式无变化。
- 最终内部增量：`GetReplayResidentId()` 会先识别自身及父级名称中的
  `Lobby/Assignment`；大厅任务终端返回空住户 ID，不再由默认页码错误推断成 `17F01`。
  17F01–17F04 的显式住户 ID 和主动作映射不变。
- `KeyboardNavigationRoot` 的 Canvas 仍在运行时保证
  `Override Sorting=true / Sorting Order=20`。其布局由 Layout Profile 的
  `TerminalFooter=(96, 920, 1728, 64)` 决定，避免底栏被实体电视下边框裁切。
- 公开接口及关系：`GetReplayResidentId()`、`TerminalViewState`、
  `TerminalViewStateChanged`、`OpenTerminal()`、`CloseTerminal()` 和全部输入门控接口保持可用；
  Lobby 的 Custom 动作仍由 `HearthLobbyFlowController` 接收，住户 Replay/Enter 动作仍由原
  状态机接收。UI Presenter 不据空住户 ID自行推进剧情。
- Unity 使用步骤：不要手填 Lobby 为 `17F01`；运行 Lobby 定向 Apply/Validate 后确认
  `GetReplayResidentId()` 为空。仅改底栏视觉时运行 V2 安全 Refresh，不运行全量 Rebuild。
- 最小测试：大厅终端返回空住户 ID且右上仍为 `CLOSE TERMINAL`；17F01/02/03/04
  分别保持 Replay/Replay/Enter Unit/Enter Home；五台终端底栏在 1920×1080 实体电视内完整可见。
- 后续扩展：新增非住户终端时应加入明确的终端类型/动作映射，不得依赖页码或兜底住户 ID。
  除上述内部识别外，挂载、Inspector 和现有输入使用方式无变化。

### HearthLobbyOpeningBinder.cs

- 路径：`Assets/Editor/HearthLobbyOpeningBinder.cs`；Editor 工具，不挂 GameObject，
  不需要运行时组件。菜单仍为
  `Tools > Hearth > Lobby > Apply Ground Floor Opening Setup` 与
  `Validate Ground Floor Opening Setup`，使用方式无变化。
- Apply 会把 Lobby Overlay 的 Activation、Expanded Message、Pinned Message、
  Assignment Status 七处 TMP，以及 Lobby 终端下全部 TMP，统一为固定字号、
  `enableAutoSizing=false`、`TextOverflowModes.Overflow`；不会修改文字内容。
- Validate 现在同时检查大厅终端住户 ID必须为空，并覆盖上述 Overlay 与终端完整 TMP 树；
  任意自动缩字、Ellipsis 或 Truncate 都会报告问题。
- 依赖与引用：继续读取正式 Lobby Flow、Overlay、终端、相机、交互器和正式对白资产；
  不新增 Inspector 字段或运行时公开接口。Apply 仍会确认终端硬件根与 World Camera，
  但不改变剧情状态机所有权。
- 最小测试：退出 Play Mode 后运行 Apply，再运行 Validate；应无 Lobby 住户 ID和文字容量错误，
  最长 Lily 消息、任务状态和终端提示均完整显示。
- 后续扩展：新增 Lobby TMP 时应纳入对应路径表或终端子树验证；不得通过 Auto Size
  掩盖容量不足。正式文本有变更时仍从正式对白同步链路处理，而不是在 Binder 中改稿。

### MinLoopSubtitlePlayer.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopSubtitlePlayer.cs`；继续挂在场景正式字幕播放器，
  依赖字幕 CanvasGroup、Speaker/Body TMP、`Hearth_SubtitleStyle.asset`、
  AudioSource/Dialogue Channel。挂载与 Inspector 使用方式无变化。
- 新增一次性显式语境重载：
  `PlaySequence(lines, context)`、`PlaySequence(sequence, context)`、
  `PlayLines(lines, context)`、`PlaySequenceAsset(sequence, context)`。
  它们只为本次播放指定 `HearthSubtitleContext`，不会永久改写播放器的自动/固定语境配置。
- 原无语境重载、`ShowLine(...)`、`SetSubtitleContext(...)`、
  `SetExternalPresentationSuppressed(...)`、`Hide()`、`Stop()` 继续可用。
  正式顺序仍是 Markdown → 同步资产 → `HearthDialogueSequence` → 本播放器。
- 引用关系：`MinLoopFlowController` 通过显式 `Terminal` 语境播放终端内处置对白；
  `HearthUiStateCoordinator` 只读取 `IsPlaying/ActiveContext` 决定是否显示，
  不停止音频、协程或完成事件。
- 最小测试：终端内播放显式 Terminal 序列时字幕可见；同一终端期间启动 Human/Field Unit
  世界语境序列时视觉被抑制；退出终端后默认/自动语境仍按原配置工作。
- 后续扩展：任何必须留在终端画面中的新正式序列都显式传
  `HearthSubtitleContext.Terminal`；不要靠说话人字符串猜语境。使用方式除新增重载外无变化。

### MinLoopFlowController.cs

- 路径：`Assets/Scripts/MinLoop/MinLoopFlowController.cs`；继续挂在
  `MIN_LOOP_ROOT/FlowManagers` 的正式最小循环流程对象。终端、住户进度、信任状态、
  正式字幕播放器和处置对白集合的 Inspector 引用及挂载方式无变化。
- 本轮仅改变调用语境：17F01/17F02 终端处置的 Pre-Choice、Option A/B Result 和
  Post-Choice Common 均通过 `PlayDialogue(sequence, HearthSubtitleContext.Terminal)`
  播放。对白资产、逐句文本、时长、选择结果和流程顺序不变。
- 本脚本没有新增对外玩法接口；原处置选择、终端关闭、住户完成和信任结算公开能力保持不变。
  它继续拥有剧情推进权，字幕播放器和 Coordinator 只负责播放与显示。
- 最小测试：在 17F01/17F02 终端提交 A/B 后，结果对白留在终端内可见，
  播放完成才按原流程关闭并标记住户完成；正式对白 Coverage 不发生变化。
- 后续扩展：新增终端内对白必须在调用点显式传 Terminal 语境；世界对白继续使用原自动语境。
  使用方式无变化。

### HearthUiStateCoordinator.cs

- 路径：`Assets/Scripts/UI/HearthHud/V2/HearthUiStateCoordinator.cs`；仍由
  `HearthUiV2SystemInstaller` 挂在 Human `HearthHudRoot_V2` 根，与
  `HearthFirstPersonHudController` 同对象。所需组件和 Inspector 引用
  （Human HUD、ViewSwitch、字幕播放器、Lobby Overlay、Takeover Challenge）不变，
  也可继续运行时解析。
- 最终显示规则：Terminal 状态只允许 `IsPlaying=true` 且
  `ActiveContext=HearthSubtitleContext.Terminal` 的字幕显示；其他 Human、
  Field Unit 或自动解析为世界语境的字幕在终端期间隐藏。Modal 与 Takeover
  无条件抑制所有字幕视觉。以上均不停止对白、语音、计时或完成事件。
- Coordinator 同时调用 Human HUD 的外部 Persistent 抑制，确保
  Terminal/Modal/Takeover/最终选择期间身份、任务、地点不会穿透。
  `OnDisable()` 会解除 Human Persistent、所有字幕和 Lobby Overlay 的外部抑制，
  防止组件停用后残留 Alpha/可见性锁。
- 公开接口仍包括 `CanOpenHumanMenu`、`SetExternalModalRequest(...)`、
  `ConfigureRuntimeSources(...)`、`SetRuntimeIntegration(...)`、
  `RefreshRuntimeState()`、`ResolveRuntimeSources()`、`ResolvedStateChanged`；
  不读取按键、不推进剧情。
- 最小测试：终端内 Terminal 对白可见而世界对白不可见；Tab/照片/实体检查为 Modal，
  Shutdown/Low Trust 为 Takeover，后二者无任何字幕或 Persistent HUD；停用 Coordinator
  后外部抑制全部释放。
- 后续扩展：新的终端消息先定义正确 Subtitle Context；新的模态界面以稳定 Owner
  调用 `SetExternalModalRequest`。挂载和 Inspector 使用方式无变化。

### HearthFirstPersonHudController.cs

- 路径：`Assets/Scripts/UI/HearthHud/FirstPerson/HearthFirstPersonHudController.cs`；
  继续挂在 Human `HearthHudRoot_V2` 根，依赖 PersistentHudRoot/CanvasGroup、页面、
  Focus 与原剧情事件。挂载、所需组件和 Inspector 使用方式无变化。
- 新增公开接口 `SetExternalPersistentPresentationSuppressed(bool)`。
  Controller 分别保存页面真实请求的 Persistent 显隐和外部抑制状态；
  解除抑制后恢复当前页本来应有的状态，不会强制打开 Persistent HUD 或重置页面。
- 引用关系：只有 `HearthUiStateCoordinator` 调用该外部抑制接口；
  `HearthFirstPersonHudInput`、照片档案、最终选择和关机挑战继续使用原页面/输入接口。
- 最小测试：从普通 HUD 进入 Terminal、Entity Inspection、Final Choice 或 Takeover，
  身份/任务/地点立即隐藏；退出后按当前页真实设置恢复；停用 Coordinator 不会留下永久隐藏。
- 后续扩展：新增全屏页面应通过页面的 `Keep Persistent HUD` 和 Coordinator 层级表达，
  不直接改 Persistent Canvas Alpha。除新增接口外使用方式无变化。

### Hearth17F03InspectionPanel.cs

- 路径、挂载、Canvas/CanvasGroup、TMP、Recall 与 A/B Inspector 引用均沿用上文，
  继续位于
  `MIN_LOOP_ROOT/ReplayRoom_17F03/UI/Hearth17F03InspectionCanvas`。
- 最终生命周期增量：面板在 `Open/OpenDispositionChoice` 时申请 Modal，
  `Close()` 释放；若 GameObject 或组件在打开中途被停用，`OnDisable()` 也会清除
  `IsOpen` 并释放同一 Owner，避免 Coordinator 永久停留在 Modal。
- 公开接口、事件和 Replay Controller 引用关系不变。最小测试是在面板打开时直接停用对象，
  再启用/回到 Human；Modal 应已释放，Tab 和 Persistent HUD 按真实状态恢复。
- 后续扩展任何异常退出路径都必须调用同一释放逻辑。使用方式无变化。

### HearthUiV2SystemInstaller.cs

- 路径：`Assets/Editor/HearthUiV2SystemInstaller.cs`；Editor 工具，不挂 GameObject。
  菜单仍为 `Install Profiles And Human Tutorial` 与对应 Validate，使用方式无变化。
- 教程每个 Key 与 Action TMP 现统一 `enableWordWrapping=false`、固定字号和 Overflow，
  因此 `INTERACT`、`LOOK`、`MENU` 等不会在 1920×1080 或缩放分辨率中被拆成两行。
- 依赖仍为 Human V2 Prefab、Theme/Layout Profile、Coordinator、
  `HearthInitialGameplayTutorialController` 与 `HearthActionHintPresenter`；
  无新增运行时公开接口或 Inspector 槽位。
- 最小测试：运行 Installer/Validate 后在 1920×1080、1280×720、2560×1440
  查看 10 秒教程，四组 Key/Action 均为单行且无互相覆盖。
- 后续扩展更长动作词时先增加单项宽度或整体容量，不得重新开启自动换行/自动缩字。

### HearthUiV2Builder.cs / HearthUiLayoutProfile.cs

- 路径：
  `Assets/Editor/HearthUiV2Builder.cs` 与
  `Assets/Scripts/UI/HearthHud/V2/HearthUiLayoutProfile.cs`。
  Builder 是 Editor 工具，不挂 GameObject；Layout Profile 是 ScriptableObject，
  正式资产为
  `Assets/UI/HEARTH/V2/Profiles/Hearth_UiV2Layout_1920x1080.asset`。
- 最终共享坐标：`TerminalFooter=(96, 920, 1728, 64)`；Builder 的缺省值与 Profile
  资产一致。Footer Canvas 固定 Order 20，文字禁止换行。
- Human 最终选择不再生成第二个 `V2_FinalChoiceHint`。Builder 只定位和样式化真实
  `FocusLayer/FinalChoiceInputHint`；该提示继续由 `HearthFirstPersonHudInput`
  根据水平/垂直选择与键盘开放状态动态写入，画面中只保留一套真实提示。
- 在样式化 Final Choice（Slide09/14）、Shutdown Confirm（Slide10）和
  Low Trust Warning（Slide11–13）前，Builder 会显式遍历对应页面全部子 Image，
  将名称以 `Border_` 开头的旧边框设为透明、禁用并关闭 Raycast。
  这一步清理的是 Legacy/Scene Override 遗留细框，不删除真实按钮、V2 规则线或剧情组件。
- Companion 安全 Refresh 会生成/更新
  `PersistentInfoLayer/V2_StatusPanel` 并自动绑定
  `HearthCompanionHudController.statusPanelView`；Title、Rows、Footer、Accent
  继续来自 `HearthCompanionHudSceneData`，不烘焙固定文字。
- Builder 日常公开菜单仍只使用
  `Refresh Existing V2 Prefab Visuals`；Profile 公开查询
  `GetRect(HearthUiLayoutRegion.TerminalFooter)`。所有挂载与 Inspector 使用方式无变化。
- 最小测试：安全 Refresh 后终端 Footer 不被电视边框裁切；Final Choice 只出现一个动态提示；
  Slide09–14 的 Final Choice、Shutdown Confirm 与 Low Trust 页面均没有启用的
  `Border_*` Image；Companion 切换至少两个 SceneData 时 V2 状态面板内容和 Accent
  随数据变化，临时 Trigger Card 可见时 Status Panel 隐藏。
- 后续扩展：调底栏坐标只改 Layout Profile并保持 Builder fallback 同步；
  调最终选择提示只改真实 `FinalChoiceInputHint`；调 Companion 行数从状态面板容量接入；
  新增终局页时如继承旧 `Border_*`，必须在对应 Style 方法中调用同一清理规则。

### Hearth17F04FinaleBinder.cs

- 路径：`Assets/Editor/Hearth17F04FinaleBinder.cs`；Editor 工具，不挂 GameObject，
  Apply/Validate 17F04 Home Finale 菜单和使用方式无变化。
- Apply 标准化 `17F/ROOM4/TV (3)` 时会先检查
  `Assets/Prefabs/UI/HearthHud/V2/Terminals/Terminal_17F04_Home_V2.prefab`：
  该 Prefab 存在时必须使用 V2 Wrapper；只有 V2 资产确实不存在时才允许回退到
  `Assets/Prefabs/UI/HearthHud/Terminals/Terminal_17F04_Home.prefab`。
  因此对已经完成 V2 迁移的正式场景重跑 Finale Apply，不会再把 Home Terminal
  降级为 Legacy。
- 标准化后会显式补齐同一场景的统一 `ViewSwitchController`、
  `HearthPlayerControlLock`、`Terminal Hardware Root=TV (3)`、终端 World Camera，
  并把终端 Canvas 的 `worldCamera` 指向该 TV 自有 `TerminalCamera`。
  这些引用用于相机、输入锁与终端独占恢复，不改变 17F04 剧情事件。
- Apply 会把既有 `PhotoExitHintCanvas/HintPanel/HintText` 及新建 17F04 UI TMP
  统一为固定字号、`maxVisibleLines=int.MaxValue`、Overflow；
  Validate 新增照片退出提示的 Auto Size/Overflow 检查。
- 依赖和引用仍是 17F04 Finale Controller、Photo Frame、Home Terminal、
  Shutdown Challenge、Human HUD、V2 Home Terminal Prefab、共享控制锁、
  唯一 ViewSwitch 与正式对白资产；不新增运行时 Inspector 字段或公开接口。
- Validate 在 V2 Home Prefab 可用时要求 `TV (3)` 的终端具有可用
  `HearthUiThemeMarker`，其 `Version=V2` 且 `Build Label=HEARTH UI V2`；
  缺失或降级会明确报错。
- 最小测试：运行定向 Apply/Validate 后，照片页 `PLEASE WAIT`、翻页和退出长提示均完整；
  再运行 `Validate Open Scene UI`，Human、Companion、Lobby、17F01–17F04
  七个槽位应仍为 `7/7 V2`；Home Terminal 使用 TV (3) 自有 Camera，Space/Esc 门控、
  控制锁与相机恢复不变。
- 后续新增 17F04 UI TMP 时沿用固定字号+Overflow，并补进 Validator；
  调整 Finale Apply 时不得把 Home Terminal 路径重新硬编码为 Legacy。

### HearthRuntimeInterfaceBinder.cs

- 路径：`Assets/Editor/HearthRuntimeInterfaceBinder.cs`；Editor 工具，不挂 GameObject，
  `Repair Interaction Prompt Bindings` 与 HUD/Audio/English Prompt Validator
  的使用方式无变化。
- Human/Companion 的运行时 E 交互提示 TMP 现固定字号、禁止换行并使用 Overflow，
  不再使用 Ellipsis；Validator 会对活动交互器的提示引用检查 Auto Size 与 Overflow。
- 依赖仍为 Human/Companion `PlayerInteraction`、各自提示 GameObject/TMP 和现有交互体；
  不新增运行时公开方法或 Inspector 字段，提示内容仍由 `PlayerInteraction` 写入。
- 最小测试：定向 Repair/Validate 后看向可交互 TV、门或 NPC；
  `E  INTERACT` 单行完整显示，移开后隐藏，Human/Companion 不争用同一个提示实例。
- 后续扩展更长交互动词时先扩大 RectTransform；不得恢复 Ellipsis、Auto Size 或借用字幕。

### 本次分辨率与对白验收记录

- 已在真实 Game View 分别切换到 `1280×720` 与 `2560×1440`，验证
  `Scale With Screen Size / Reference Resolution 1920×1080` 下共享锚点、终端底栏和文字
  没有漂移；基线截图为
  `Assets/Screenshots/HEARTH_UI_V2_2026-07-26/HEARTH_UI_V2_Resolution_HD_1280x720.png`
  与
  `Assets/Screenshots/HEARTH_UI_V2_2026-07-26/HEARTH_UI_V2_Resolution_QHD_2560x1440.png`。
- 这两张图证明代表性界面与共享缩放链已经真实运行检查；仍不等同于 11 类界面在两个分辨率下
  各自保存了完整截图矩阵。
- 本次正式对白 Markdown 未改；终端语境变化只发生在播放调用与显示协调层。
