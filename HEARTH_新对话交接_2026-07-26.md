# HEARTH 新对话交接

> 检查日期：2026-07-26  
> 项目：`D:\UGit\GAME-CBY`  
> Unity 场景：`Assets/Scenes/SampleScene.unity`

## 1. 新对话先做什么

新对话开始后，先按以下顺序读取：

1. `AGENTS.md`
2. 本文档 `HEARTH_新对话交接_2026-07-26.md`
3. `HEARTH_UI_V2使用与切换说明.md`
4. `HEARTH_Full_Game_Script_No_Audio_Tags_Native_English.md`
5. `HEARTH_剧情变更记录.md`
6. `HEARTH_剧情接口与可接入点.md`
7. `脚本使用说明总表.md`

然后检查：

1. `git status --short`
2. Unity 是否打开、是否退出 Play Mode、是否编译完成
3. Unity MCP 当前实例是否为 `GAME-CBY`
4. Unity Console 是否有新增 Error
5. 本文档列出的 P0 验证菜单

不要以旧聊天摘要、旧截图或旧数量统计代替当前项目状态。

## 2. 当前工作区安全提示

当前工作区有大量尚未提交的有效制作内容：

- 29 个已跟踪文件有修改。
- 当前差异约为 12,487 行新增、2,418 行删除。
- `Assets/Scenes/SampleScene.unity` 单文件约有 13,389 行差异。
- V2 UI 脚本、Prefab、图片部件和截图中仍有未跟踪文件。

因此，在建立 Git 基线之前：

- 不要执行 `git reset --hard`。
- 不要使用 `git checkout --` 回退场景或脚本。
- 不要删除未跟踪的 V2 UI 文件。
- 不要为了“重新绑定”而重跑所有大型 Builder/Binder。
- 不要运行 `Rebuild All V2 UI Assets`，它可能覆盖当前视觉调整。
- 不要把 `SampleScene` 替换成旧版本。

## 3. 当前已完成的主要工作

### 3.1 游戏流程基础

项目已经建立以下主要流程和接口：

- 一楼大堂出生、开场对白、公共事件、任务终端和电梯转场。
- 17F01 小男孩回放、长按交互、返回终端和处置流程。
- 17F02 女主人倾诉、动作/走位、开门、餐桌、关闭记录和处置流程。
- 17F03 米娅入户、实体机器人检查、两段机器人回放、返回米娅和处置流程。
- 17F04 自宅、任务终端、相框、女儿房间、A/B、关闭挑战和结局接口。
- 信任度、住户完成记录、处置历史、设置入口和后续存档接口。

注意：这些模块“已有实现”不等于全部已经在当前 V2 场景中完整回归通过。后续必须按关卡重新试玩。

### 3.2 第一人称与终端 UI

- Legacy UI 仍保留，可用于回退比较。
- 当前 `SampleScene` 保存为 V2 UI 状态。
- 已生成 7 个 V2 Prefab：
  - 1 套 Human HUD
  - 1 套 Companion HUD
  - 5 套终端/特殊页面
- 已生成 25 个可复用 UI PNG 部件。
- 已保留 28 张 V2 阶段检查截图。
- 已实现进入终端时隐藏全部第一人称 UI。
- 已实现终端关闭后恢复正确第一人称 UI。
- 五台终端使用各自本地相机，并由 owner-based lock 防止互相抢占。
- V2/Legacy 切换曾通过隔离场景、Undo/Redo 和保存重载检查。

### 3.3 对白系统

正式游戏对白唯一来源：

`HEARTH_Full_Game_Script_No_Audio_Tags_Native_English.md`

当前资产统计：

- `HearthDialogueSequence` 资产：71 个。
- 序列化对白行：398 行。
- 正式稿覆盖验证：330 个稳定标记段全部有对应资产。
- 已绑定语音 `AudioClip`：0。

398 行包括补充分支、流程提示或非正式稿主段，不应简单与 330 个正式稿标记比较后删除。

当前验证已通过：

- 正式稿覆盖率验证。
- 共享字幕显示配置验证。
- 1920×1080 两行字幕布局验证。
- 英文交互提示和运行时音频接口验证。

### 3.4 当前 Unity 拓扑

2026-07-26 使用 Unity MCP 检查时：

- Unity 处于 Edit Mode。
- Unity 未编译、未运行 Play Mode。
- 活动场景为 `Assets/Scenes/SampleScene.unity`。
- 场景只有一个标准 `MIN_LOOP_ROOT/FlowManagers/ViewSwitchController`。
- UI V2 场景验证通过：
  - 7 个唯一 UI slot
  - 1 个标准 ViewSwitchController
  - 5 个终端本地相机
- Runtime Topology 验证通过。
- P0 01-03 稳定性验证通过。
- 大堂开场、17F03、17F04 Finale、17F04 Cat 验证通过。

这些是编辑器绑定和结构验证，不等同于完整 Play Mode 通关。

## 4. 当前确认未完成或存在故障

## P0：下一对话首先处理

### 4.1 17F01 V2 终端缺少 3 个绑定

`Tools > Hearth > Replay > Validate 17F01 Minimal Loop Setup` 当前报告：

1. `Terminal_17F01 / minLoopFlowController` 未绑定当前 `MinLoopFlowController`。
2. `Terminal_17F01 / playerCamera` 未绑定 `First Person Camera`。
3. `Terminal_17F01 / playerInteraction` 缺失。

### 4.2 17F02 V2 终端缺少 3 个绑定

`Tools > Hearth > Replay > Validate 17F02 Minimal Loop Setup` 当前报告：

1. `Terminal_17F02 / minLoopFlowController` 未绑定当前 `MinLoopFlowController`。
2. `Terminal_17F02 / playerCamera` 未绑定 `First Person Camera`。
3. `Terminal_17F02 / playerInteraction` 缺失。

### 4.3 陪伴单元长按交互有 3 个绑定警告

`Tools > Hearth > Validation > Validate Companion Hold Interactions` 当前报告：

1. Companion HUD 未绑定标准 `ViewSwitchController`。
2. Companion HUD flow binder 未绑定标准 `ViewSwitchController`。
3. Companion HUD exclusive mode 未绑定标准 `ViewSwitchController`。

这很可能影响 17F01、17F02、17F03 的长按 E 提示、UI 显隐或交互阶段判断。应在继续视觉制作前修复。

### 4.4 建立可恢复的 Git 基线

修复上述绑定并确认场景保存后，应：

1. 检查差异中没有误删用户模型、锚点和手调位置。
2. 保存 Unity 场景。
3. 建立一次明确的 Git commit 或外部备份。
4. 记录 V2 当前基线截图。

在此之前继续做大型 UI 重建或场景整理风险很高。

## P1：完成一个可交付的纵向流程

### 4.5 完整回归 17F01

先只跑通：

`1F Lobby -> Elevator -> 17F01 Terminal -> Robot Replay -> Hold E -> Return -> A/B -> Trust -> Exit`

需要检查：

- 单按 E 和长按 E 的蓝色提示都正常。
- 终端进入/退出镜头正确。
- 人类 HUD、机器人 HUD 和终端 UI 不同时叠加。
- 对白不重叠，最长两行。
- 处置只提交一次。
- 信任度只修改一次。
- 玩家控制锁定和恢复正确。
- Console 无新增 Error。

17F01 通过后，再用同一验收表依次跑 17F02、17F03、17F04。

### 4.6 增加最低限度自动化测试

当前自动测试文件数量为 0。

建议先补：

- EditMode：正式稿标记与 Dialogue Asset 对应。
- EditMode：每台终端 resident id、主操作和关键绑定。
- EditMode：单个标准 ViewSwitchController。
- PlayMode：终端进入/退出后 UI 和玩家控制状态。
- PlayMode：A/B 不能重复提交。
- PlayMode：长按 E 只能在正确阶段完成。

## P2：内容与交付

### 4.7 音频尚未正式接入

当前项目内约有 15 个音频文件，但 398 个对白行的语音槽均未分配。

仍需：

- 正式人声录制、命名和逐句绑定。
- 人类与机器人不同脚步声。
- 环境底噪、房间氛围和电梯环境。
- 门、终端、开机、切页、确认、警告和关闭音效。
- AudioMixer、分类音量和对白 ducking。
- 语音存在时以 `AudioClip.length` 驱动字幕时长。

### 4.8 存档与完整产品入口

已有接口但未形成完整产品流程：

- 新游戏 / 继续游戏 / 重置进度。
- 信任度、住户完成、处置历史和设置持久化。
- 17F04 是否要求前三户完成。
- 结局后进入主菜单、Credits 或正式结束页。
- 异常退出后恢复当前关卡。

### 4.9 独立构建验证

Build Settings 当前只有：

`Assets/Scenes/SampleScene.unity`

仍需：

- 明确启动场景和正式 Build Profile。
- Windows Standalone 构建。
- 1920×1080、其他 16:9 和窗口模式检查。
- 无 Unity Editor 环境下完整通关。
- 输入、音频、字体、材质和视频资源打包检查。

## P3：稳定后再做的整理

### 4.10 V2 UI 真相源尚未统一

当前 V2 同时存在 Prefab、Builder、场景 override 和图片部件：

- Human V2 场景实例约 1,435 个 Prefab override。
- Companion V2 场景实例约 200 个 Prefab override。
- 七个 V2 根合计约 2,023 个 override。
- Companion HUD 的 `statusPanelView` 仍未绑定或未决定是否保留。

需要在玩法稳定后选择：

- Prefab 为唯一视觉真相源；或
- Builder 为唯一生成真相源。

不要在流程仍有断绑时先清 override。

### 4.11 控制和大型脚本可继续拆分

目前 owner-based lock 已解决一部分相机/UI 抢占，但还没有完整统一成：

- Input Router
- Camera State
- Player Control Lock
- UI Visibility State
- Dialogue Playback State

终端、玩家锁定和流程控制器仍较大。建议先锁定四关流程，再按职责逐步拆分，不在当前故障修复阶段重构全部架构。

### 4.12 场景和资源整理仍有一个确认项

项目整理验证当前报告：

`Assets/Art/Environment/SourceModels/1地块.fbx` 缺失。

需要确认：

- 文件是否改名或迁移；
- 验证器路径是否过时；
- 该资源是否已被其他场景模型替代。

确认前不要随意删除现有环境模型。

## 5. 其他当前警告

### 5.1 大堂故事音效

故事音效验证报告：

`MIN_LOOP_ROOT/Audio/StorySFX_Lobby / AssignmentTerminal.Confirm`

被设置为空间/自动音效，但没有 follow target。需要绑定任务终端位置，或明确改成 2D UI 音效。

### 5.2 负缩放 Collider

既有文档记录 ROOM2 桌子和 ROOM3 TV 柜存在负缩放 BoxCollider 风险。当前轮未重新进入 Play Mode 复现，后续应在完整回归时确认。

### 5.3 兼容性警告

当前可见的非阻断警告包括：

- MCP WebSocket 未初始化提示。
- 旧查询尝试查找不存在的 `HearthUiV2Binder`。
- 音频序列化字段 `minVolume/maxVolume/rolloffFactor` 已过时。

不要把这些提示与新增游戏逻辑 Error 混在一起，但应在收口阶段清理。

## 6. 下一步推荐执行顺序

下一对话的第一个实际任务应严格限制为：

1. 只修复 17F01/17F02 V2 终端的 6 个缺失引用。
2. 只修复 Companion HUD 三处标准 ViewSwitchController 引用。
3. 保存场景。
4. 重跑：
   - `Validate 17F01 Minimal Loop Setup`
   - `Validate 17F02 Minimal Loop Setup`
   - `Validate Companion Hold Interactions`
   - `Validate Open Scene UI`
   - `Validate Runtime Topology`
5. 不进行 UI 重建。
6. 不修改剧情、动画或模型位置。
7. 验证通过后再完整试玩 17F01。

## 7. 常用 Unity 菜单

- `Tools > Hearth > UI V2 > Validate Open Scene UI`
- `Tools > Hearth > Validation > Validate Runtime Topology`
- `Tools > Hearth > Stability > Validate P0 01-03`
- `Tools > Hearth > Replay > Validate 17F01 Minimal Loop Setup`
- `Tools > Hearth > Replay > Validate 17F02 Minimal Loop Setup`
- `Tools > Hearth > Validation > Validate Companion Hold Interactions`
- `Tools > Hearth > Dialogue > Sync All Dialogue From Final Script`
- `Tools > Hearth > Dialogue > Validate Final Script Coverage`
- `Tools > Hearth > Dialogue > Validate Two-Line Subtitle Layout`

菜单名称若与 Unity 当前显示略有差异，应先搜索对应 Editor 脚本中的 `MenuItem`，不要猜测后直接运行其他 Builder。

## 8. 新对话接手口径

新对话收到本项目后，应先回复它已经确认以下边界：

- 当前场景是 V2 UI，但 Legacy 仍保留。
- 不重建全部 V2 UI。
- 不覆盖用户手调的模型、锚点、Camera 和 UI 位置。
- 不回退脏工作区。
- 正式对白只以根目录正式稿为准。
- 脚本修改后同步更新 `脚本使用说明总表.md`。
- 剧情/流程修改同步更新 `HEARTH_剧情变更记录.md`。
- 先修 P0 引用，再试玩 17F01。

## 9. 本次审计结论

项目已经不再是“功能尚未建立”的阶段，而是进入“整合、回归和交付”阶段。

当前最主要风险不是缺少更多功能，而是：

1. V2 UI 切换后的引用断开。
2. 缺少自动化和完整 Play Mode 回归。
3. 大量未提交场景修改没有稳定基线。
4. UI 存在多种真相源和大量 override。
5. 正式语音、存档和独立构建尚未完成。

下一阶段应停止横向增加新系统，先完成 P0 修复和一条可重复通过的 17F01 纵向流程。
