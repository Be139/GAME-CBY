# 17F-01 最小循环脚本挂载清单

这份清单只用于场景搭建时快速对照。更完整的脚本字段说明见 `脚本使用说明总表.md`。

## 0. 先创建标准骨架

在 Unity 顶部菜单执行：

```text
Tools / Min Loop / Create 17F-01 Scene Skeleton
```

执行后应出现：

```text
MIN_LOOP_ROOT
  FlowManagers
    MinLoopFlowController
    TrustStateController
    ViewSwitchController
    MinLoopStageObjectActivator
    MinLoopStageAnchorController
    MinLoopStageCueController
    MinLoopLightingStateController
    MinLoopAudioStateController
    MinLoopDebugHotkeys
  UI
    MinLoopTerminalPresenter
    MinLoopSubtitlePlayer
    MinLoopRobotHudPresenter
    MinLoopObjectivePresenter
    MinLoopTrustPresenter
  ReplayRoom_17F01
    ReplaySequenceController
    ComfortAction_Bedside
  Interactions
    Terminal_17F01_Interactable
    Feedback_TerminalOpen
    Feedback_AccessCard
    Feedback_ReplayRequest
    Feedback_DispositionSubmit
    Feedback_ComfortReady
    Feedback_MorningReview
    Feedback_NextResidentGuide
  Guides
    Guide_Terminal_17F01
    Guide_ComfortAction
    Guide_NextResident_17F02
  Anchors
    Anchor_Mia_CorridorStart
    Anchor_Companion_ReplayStart
    Anchor_Mia_TerminalReturn
    Anchor_NextResident_17F02
  Lighting
    Light_Corridor_Warm
    Light_Replay_Night
    Light_Morning_Soft
  Audio
    Audio_Corridor_Ambience
    Audio_Replay_Night_Ambience
    Audio_Morning_Ambience
  MinLoopSceneValidator
```

也可以打开清单窗口：

```text
Tools / Min Loop / Open Setup Checklist
```

摆好并命名关键对象后，执行自动绑定：

```text
Tools / Min Loop / Auto Bind 17F-01 Scene References
```

也可以在清单窗口点击：

```text
Auto Bind References
```

`MinLoopFlowController` 的核心引用会由骨架工具自动连好。`Stage Changed` 和 `Disposition Applied` 是给后续任务 UI、音效或日志用的事件，第一版可以先不拖。

`MinLoopStageObjectActivator` 也会由骨架工具自动配置 3 条规则：

| 指引父物体 | 自动显示阶段 |
|---|---|
| `Guide_Terminal_17F01` | `Corridor` |
| `Guide_ComfortAction` | `WaitingForComfort` |
| `Guide_NextResident_17F02` | `Complete` |

第一版即使还没有实际箭头、光圈资源也能跑。骨架会给这三个父物体自动挂 `MinLoopWorldGuideMarker`，Play 后显示发光点和文字。后续有正式箭头/光圈时，把正式视觉作为这些父物体的子物体即可。

`MinLoopStageAnchorController` 会由骨架工具自动绑定 `MinLoopFlowController`。骨架会在 `MIN_LOOP_ROOT/Anchors` 下创建四个 Anchor：Mia 开场走廊站位、陪伴单元复盘起点、Mia 回终端站位、下一户方向参考。运行自动绑定后，会把 Mia/Companion 根物体填进对应锚点规则。

`MinLoopStageCueController` 会由骨架工具自动绑定 `MinLoopFlowController`。骨架会创建 `Feedback_ComfortReady`、`Feedback_MorningReview`、`Feedback_NextResidentGuide`，并写入 3 条默认 Cue 规则：等待安抚、早晨回顾、下一户指引三个阶段各触发一次反馈。

`MinLoopLightingStateController` 会由骨架工具自动绑定 `MinLoopFlowController`。骨架会在 `MIN_LOOP_ROOT/Lighting` 下创建三盏占位灯：走廊/终端暖光、昨夜复盘冷光、早晨回顾柔光。后续放正式灯光时，把正式 Light 拖进对应规则即可。

`MinLoopAudioStateController` 会由骨架工具自动绑定 `MinLoopFlowController`。骨架会在 `MIN_LOOP_ROOT/Audio` 下创建三个占位 AudioSource：走廊/终端环境声、昨夜复盘环境声、早晨回顾环境声。后续有正式音频后，把 Clip 拖到对应 AudioSource 即可。

`MinLoopObjectivePresenter` 会由骨架工具自动绑定 `MinLoopFlowController`。第一版不拖正式 UI 也会自动生成左上角当前目标提示。

`MinLoopRobotHudPresenter` 会由骨架工具自动绑定 `MinLoopFlowController`。第一版不拖正式 UI 也会在陪伴单元复盘阶段自动生成机器 HUD。

`MinLoopTrustPresenter` 会由骨架工具自动绑定 `TrustStateController` 和 `MinLoopFlowController`。第一版不拖正式 UI 也会自动生成右上角信任度提示。

`MinLoopDebugHotkeys` 会由骨架工具自动绑定总流程和复盘控制器。它只用于 Play Mode 快速测试，默认只在 Editor 或 Development Build 生效。

| 热键 | 作用 |
|---|---|
| `F1` | 重置最小循环 |
| `F2` | 打开/触发 17F-01 终端入口 |
| `F3` | 模拟刷工牌 |
| `F4` | 调出昨夜事件复盘 |
| `F5` | 模拟执行安抚 |
| `F6` | 选择 A |
| `F7` | 选择 B |
| `F8` | 继续到下一户 |

## 0.1 自动绑定能帮你做什么

自动绑定工具认这些名字：

| 对象名 | 自动处理 |
|---|---|
| `Player_Mia_Controller` | 填到 `ViewSwitchController / Human`，并绑定 Mia 的 Camera、移动、视角、交互、Rigidbody |
| `Companion_Controller` | 填到 `ViewSwitchController / Companion`，并绑定陪伴单元的 Camera、移动、视角、交互、Rigidbody |
| `Terminal_17F01_Interactable` 或 `Terminal_17F01` | 绑定 `ResidentTerminalFlow` 到总流程；缺 Collider 时补 BoxCollider |
| `ComfortAction_Bedside` | 绑定到 `ReplaySequenceController`；缺 Collider 时补 BoxCollider |
| `Child_Actor` | 缺 `SimpleActorCueController` 时自动补，并填到复盘控制器 |
| `Mother_Actor` | 缺 `SimpleActorCueController` 时自动补，并填到复盘控制器 |
| `Father_Actor` | 缺 `SimpleActorCueController` 时自动补，并填到复盘控制器 |
| `DoorLook_Target` | 填到孩子看向门口的目标点 |
| `RobotCanvas` 或 `CompanionCanvas` | 填到 `MinLoopRobotHudPresenter / Fallback Parent Canvas` |
| `Guide_Terminal_17F01` | 走廊阶段显示 |
| `Guide_ComfortAction` | 等待安抚阶段显示 |
| `Guide_NextResident_17F02` | 完成阶段显示 |
| 上面 3 个 Guide 对象 | 缺 `MinLoopWorldGuideMarker` 时自动补，并写入默认文字 |
| `Anchor_Mia_CorridorStart`、`Anchor_Companion_ReplayStart`、`Anchor_Mia_TerminalReturn` | 填到 `MinLoopStageAnchorController` 三条默认站位规则 |
| `Feedback_ComfortReady`、`Feedback_MorningReview`、`Feedback_NextResidentGuide` | 填到 `MinLoopStageCueController` 三条默认阶段 Cue 规则 |
| `Light_Corridor_Warm`、`Light_Replay_Night`、`Light_Morning_Soft` | 填到 `MinLoopLightingStateController` 三条默认灯光规则 |
| `Audio_Corridor_Ambience`、`Audio_Replay_Night_Ambience`、`Audio_Morning_Ambience` | 填到 `MinLoopAudioStateController` 三条默认环境声规则 |
| `Feedback_TerminalOpen`、`Feedback_AccessCard`、`Feedback_ReplayRequest`、`Feedback_DispositionSubmit` | 填到总流程的对应反馈字段 |

推荐顺序：

1. `Create 17F-01 Scene Skeleton`
2. 把真实资源放进场景并按上表命名。
3. `Auto Bind 17F-01 Scene References`
4. `Validate Scene Setup`

Console 报告里：

| 标记 | 含义 |
|---|---|
| `[绑定]` | 这次已经自动填好 |
| `[已存在]` | 原来就已经填好 |
| `[需要处理]` | 必须补，不补最小循环可能跑不通 |
| `[可稍后]` | 第一版可先跳过，例如父母模型、正式 UI、音效等 |

## 1. 必须摆进场景的资源

| 场景对象名 | 放什么 | 用途 |
|---|---|---|
| `Player_Mia_Controller` | Mia / 玩家第一人称控制器 | 走廊、终端、A/B 处置视角 |
| `Companion_Controller` | 陪伴单元第一人称控制器 | 儿童房昨夜事件复盘视角 |
| `Terminal_17F01` | 17F-01 门口终端模型或占位物 | 刷工牌、摘要、调出事件、A/B |
| `ReplayRoom_17F01` | 儿童房空间 | 昨夜事件复盘 |
| `Child_Actor` | 孩子模型或占位物 | 噩梦醒来、说“妈妈”、看门口 |
| `DoorLook_Target` | 空物体，放在儿童房门口方向 | 孩子看向门外的目标 |
| `ComfortAction_Bedside` | 床边空物体或夜灯/按钮 | 唯一安抚操作 |
| `Mother_Actor` | 母亲模型或占位物 | 第二天早晨对话 |
| `Father_Actor` | 父亲模型或占位物 | 第二天早晨对话 |
| `RobotCanvas` 或 `CompanionCanvas` | 陪伴单元视角 UI Canvas，可选 | 机器 HUD 的正式或占位父 Canvas |
| `Guide_Terminal_17F01` | 终端方向箭头/光圈/提示模型的父物体 | 走廊阶段提示去终端 |
| `Guide_ComfortAction` | 安抚点箭头/光圈/提示模型的父物体 | 等待安抚阶段提示唯一操作 |
| `Guide_NextResident_17F02` | 下一户方向箭头/光圈/提示模型的父物体 | 完成后提示下一户 |
| `Anchor_Mia_CorridorStart` | 空物体 | Mia 开场走廊站位 |
| `Anchor_Companion_ReplayStart` | 空物体 | 陪伴单元进入儿童房复盘的起点 |
| `Anchor_Mia_TerminalReturn` | 空物体 | 复盘结束后 Mia 回到 17F-01 终端前的位置 |
| `Anchor_NextResident_17F02` | 空物体 | 下一户方向参考，默认不自动传送玩家 |
| `Light_Corridor_Warm` | 走廊/终端占位灯或正式灯 | 走廊、刷卡、摘要、处置阶段氛围 |
| `Light_Replay_Night` | 儿童房夜间占位灯或正式灯 | 昨夜复盘、噩梦、安抚阶段氛围 |
| `Light_Morning_Soft` | 早晨柔光占位灯或正式灯 | 第二天早晨父母对话氛围 |
| `Audio_Corridor_Ambience` | 走廊/终端环境声 AudioSource | 走廊、刷卡、摘要、处置阶段底噪 |
| `Audio_Replay_Night_Ambience` | 夜间儿童房环境声 AudioSource | 昨夜复盘、噩梦、安抚阶段底噪 |
| `Audio_Morning_Ambience` | 早晨环境声 AudioSource | 第二天早晨父母对话底噪 |
| `Feedback_ComfortReady` | 安抚点就绪提示音/发光对象 | 等待安抚阶段的一次性反馈 |
| `Feedback_MorningReview` | 早晨回顾提示音/发光对象 | 第二天早晨对话开始的一次性反馈 |
| `Feedback_NextResidentGuide` | 下一户指引提示音/发光对象 | 处置完成后出现下一户方向的一次性反馈 |

## 2. Mia / Companion 视角切换

对象：

```text
MIN_LOOP_ROOT/FlowManagers/ViewSwitchController
```

脚本：

```text
ViewSwitchController
```

如果已经按命名规则运行过 `Auto Bind 17F-01 Scene References`，以下字段会自动填；如果没自动填，再手动拖：

| 字段 | 拖什么 |
|---|---|
| `Human / Root Object` | `Player_Mia_Controller` |
| `Human / View Camera` | Mia 的 Camera |
| `Human / Movement` | Mia 的 `FirstPersonMovement` |
| `Human / Look` | Mia 的 `FirstPersonLook` |
| `Human / Interaction` | Mia 的 `PlayerInteraction` |
| `Human / Rigidbody` | Mia 的 Rigidbody，可选 |
| `Human / Canvas` | `HumanCanvas`，可选 |
| `Companion / Root Object` | `Companion_Controller` |
| `Companion / View Camera` | Companion 的 Camera |
| `Companion / Movement` | Companion 的 `FirstPersonMovement` |
| `Companion / Look` | Companion 的 `FirstPersonLook` |
| `Companion / Interaction` | Companion 的 `PlayerInteraction` |
| `Companion / Rigidbody` | Companion 的 Rigidbody，可选 |
| `Companion / Canvas` | `RobotCanvas`，可选 |

默认：

```text
Starting Mode = Human
Create Fade Overlay = 开启
```

## 2.1 玩家交互射线

Mia 和 Companion 如果都需要按 `E` 交互，各自控制器上都应有：

```text
PlayerInteraction
```

拖字段：

| 字段 | 拖什么 |
|---|---|
| `Main Camera` | 当前控制器自己的第一人称 Camera |
| `Interaction Range` | 交互距离，建议 3 到 4 |
| `Interact Key` | 默认 E |
| `Interaction Layers` | 第一版保持 Everything |
| `Trigger Interaction` | 第一版保持 Collide |
| `Ui Interaction` | 对应视角的交互提示 UI 根物体 |
| `Ui Interaction Text` | 对应 TMP 提示文字 |

保持：

```text
Search Parent For Interactable = 开启
Find Camera If Missing = 开启
Description Refresh Interval = 0.1
```

这样 Collider 可以放在模型子物体上，交互脚本挂父物体也能被射线找到。`ViewSwitchController` 会在切换视角时自动把 `PlayerInteraction` 的相机改成当前视角相机。

## 3. 17F-01 终端入口

推荐对象：

```text
MIN_LOOP_ROOT/Interactions/Terminal_17F01_Interactable
```

脚本：

```text
ResidentTerminalFlow
```

必须：

```text
BoxCollider 或其他 Collider
```

如果自动绑定没有填，再手动拖字段：

| 字段 | 拖什么 |
|---|---|
| `Flow Controller` | `MIN_LOOP_ROOT/FlowManagers/MinLoopFlowController` |

如果你使用正式终端模型 `Terminal_17F01`，可以把 `ResidentTerminalFlow` 和 Collider 挂到模型父物体上，或者让骨架中的 `Terminal_17F01_Interactable` 放在模型前方作为交互热区。

测试时如果你在刷工牌页或住户摘要页关掉终端，再看向终端按 `E`，终端会恢复当前阶段对应页面，不会重新把流程退回开头。

## 4. 终端 UI

对象：

```text
MIN_LOOP_ROOT/UI/MinLoopTerminalPresenter
```

脚本：

```text
MinLoopTerminalPresenter
```

第一版可以不拖正式 UI，脚本会自动生成占位 UI。没有 `TerminalUIController` 时，占位终端面板也会带右上角关闭按钮，避免测试时卡在终端里。

建议同时保留或配置 `TerminalUIController`：它负责打开终端时冻结玩家移动/视角并管理鼠标。

如果已经绑定 `TerminalUIController`，玩家冻结/恢复由 `TerminalUIController` 单独负责；`MinLoopTerminalPresenter` 不会再额外禁用这些控制脚本，避免关闭终端后 Mia 仍然不能动。

如果你暂时不用 `TerminalUIController`，至少这样拖：

| 字段 | 拖什么 |
|---|---|
| `Create Event System If Missing` | 保持开启 |
| `Disable Gameplay Behaviours When Open` | 保持开启 |
| `Gameplay Behaviours To Disable` | 拖 Mia 当前启用的 `FirstPersonMovement`、`FirstPersonLook`、`PlayerInteraction` |

这样占位 UI 打开时会释放鼠标、自动补 `EventSystem`，并临时关掉玩家移动/视角；关闭终端后恢复。

运行过自动绑定后，`Gameplay Behaviours To Disable` 通常会自动填入 Mia 的 `FirstPersonMovement`、`FirstPersonLook`、`PlayerInteraction`。有 `TerminalUIController` 时它只作为备用记录；没有 `TerminalUIController` 时才由 `MinLoopTerminalPresenter` 使用这份列表冻结玩家。

如果你后面上传正式 UI，拖这些字段：

| 字段 | 拖什么 |
|---|---|
| `Bound UI Root` | 正式终端 UI 根物体 |
| `Bound Title Text` | 标题 TMP 文本 |
| `Bound Body Text` | 正文 TMP 文本 |
| `Bound Primary Button` | 主按钮 |
| `Bound Primary Button Text` | 主按钮文字 |
| `Bound Secondary Button` | 第二按钮，用于 B 选项 |
| `Bound Secondary Button Text` | 第二按钮文字 |
| `Bound Close Button` | 关闭按钮，可选 |
| `Bound Close Button Text` | 关闭按钮文字，可选 |

保持：

```text
Use Bound UI When Assigned = 开启
```

想让 `Auto Bind References` 自动拖正式终端 UI，推荐这样命名：

| UI 对象 | 推荐名字 |
|---|---|
| 终端 UI 根物体 | `Terminal_BoundUI` |
| 面板 | `Terminal_Panel` |
| 内容根物体 | `Terminal_ContentRoot` |
| 标题 TMP | `Terminal_TitleText` |
| 正文 TMP | `Terminal_BodyText` |
| 主按钮 | `Terminal_PrimaryButton` |
| 主按钮文字 | `Terminal_PrimaryButtonText`，或按钮子物体 `Label` |
| 第二按钮 | `Terminal_SecondaryButton` |
| 第二按钮文字 | `Terminal_SecondaryButtonText`，或按钮子物体 `Label` |
| 关闭按钮 | `Terminal_CloseButton` |

## 4.1 当前目标提示 UI

对象：

```text
MIN_LOOP_ROOT/UI/MinLoopObjectivePresenter
```

脚本：

```text
MinLoopObjectivePresenter
```

第一版可以不拖正式 UI，脚本会自动生成左上角目标提示。

后续如果你上传正式目标提示 UI，拖这些字段：

| 字段 | 拖什么 |
|---|---|
| `Objective Root` | 当前目标 UI 根物体 |
| `Title Text` | 当前目标标题 TMP 文本 |
| `Body Text` | 当前目标正文 TMP 文本 |
| `Canvas Group` | 当前目标 UI 的 CanvasGroup，可选 |

想让 `Auto Bind References` 自动拖正式目标提示 UI，推荐命名：

```text
Objective_Root
Objective_TitleText
Objective_BodyText
```

`Objective Overrides` 可以改每个阶段显示的文字；不改也会使用默认文案。

## 4.2 机器视角 HUD

对象：

```text
MIN_LOOP_ROOT/UI/MinLoopRobotHudPresenter
```

脚本：

```text
MinLoopRobotHudPresenter
```

第一版可以不拖正式 UI，脚本会在陪伴单元复盘阶段自动生成左下角机器 HUD，显示：

```text
02:47
心率 124 / 132 / 104
噩梦可能性高
儿童呼唤家属
执行唯一安抚操作
```

如果场景中有 `RobotCanvas` 或 `CompanionCanvas`，运行 `Auto Bind 17F-01 Scene References` 后会自动填到 `Fallback Parent Canvas`。

后续如果你上传正式机器 UI，拖这些字段：

| 字段 | 拖什么 |
|---|---|
| `HUD Root` | 机器 HUD UI 根物体 |
| `Time Text` | 时间 TMP 文本，例如 02:47 |
| `Heart Rate Text` | 心率 TMP 文本 |
| `Status Text` | 系统判定 TMP 文本 |
| `Instruction Text` | 当前指令 TMP 文本 |
| `Accent Image` | 状态条 Image，可选 |
| `Canvas Group` | HUD 的 CanvasGroup，可选 |

想让 `Auto Bind References` 自动拖正式机器 HUD，推荐命名：

```text
RobotHUD_Root
RobotHUD_TimeText
RobotHUD_HeartRateText
RobotHUD_StatusText
RobotHUD_InstructionText
RobotHUD_Accent
```

`Stage Overrides` 可以改每个阶段的机器 HUD 文案；不改也会使用默认文案。

## 4.3 信任度 UI

对象：

```text
MIN_LOOP_ROOT/UI/MinLoopTrustPresenter
```

脚本：

```text
MinLoopTrustPresenter
```

第一版可以不拖正式 UI，脚本会自动生成右上角信任度提示。

后续如果你上传正式信任度 UI，拖这些字段：

| 字段 | 拖什么 |
|---|---|
| `Trust Root` | 信任度 UI 根物体 |
| `Value Text` | 当前信任度数值 TMP 文本 |
| `Delta Text` | 信任度变化量 TMP 文本 |
| `Label Text` | 标题 TMP 文本，可选 |
| `Trust Slider` | 信任度进度条 Slider，可选 |
| `Canvas Group` | 信任度 UI 的 CanvasGroup，可选 |

想让 `Auto Bind References` 自动拖正式信任度 UI，推荐命名：

```text
Trust_Root
Trust_ValueText
Trust_DeltaText
Trust_LabelText
Trust_Slider
```

`Show Only After Disposition` 默认关闭，表示一开始就显示当前信任度；如果只想 A/B 后显示，可以开启。

## 4.4 世界指引标记

对象：

```text
MIN_LOOP_ROOT/Guides/Guide_Terminal_17F01
MIN_LOOP_ROOT/Guides/Guide_ComfortAction
MIN_LOOP_ROOT/Guides/Guide_NextResident_17F02
```

脚本：

```text
MinLoopWorldGuideMarker
```

骨架工具会自动挂好，并写入默认文字：

| Guide 对象 | 默认文字 |
|---|---|
| `Guide_Terminal_17F01` | 前往 17F-01 终端 |
| `Guide_ComfortAction` | 床边安抚操作 |
| `Guide_NextResident_17F02` | 下一户 17F-02 |

第一版可以不放正式箭头，脚本会自动生成发光点、世界文字和轻微浮动。后续如果你有正式箭头/光圈资源：

| 字段 | 拖什么 |
|---|---|
| `Visual Root` | 正式箭头/光圈/提示牌父物体 |
| `Label Text` | 正式 3D TMP 文本，可选 |
| `Marker Renderer` | 要变色/发光的 Renderer，可选 |
| `Marker Light` | 指引灯光，可选 |
| `Target Camera` | 玩家相机，可选，不拖会自动找 |

保持：

```text
Show Distance = 开启
Distance Refresh Interval = 0.12 左右
```

Play 后默认文字下方会显示类似 `3.5 m` 的距离，方便在白盒场景里确认目标方向和远近。

`MinLoopStageObjectActivator` 会负责按阶段显示/隐藏这些 Guide 父物体。

## 4.5 阶段站位锚点

对象：

```text
MIN_LOOP_ROOT/FlowManagers/MinLoopStageAnchorController
MIN_LOOP_ROOT/Anchors/Anchor_Mia_CorridorStart
MIN_LOOP_ROOT/Anchors/Anchor_Companion_ReplayStart
MIN_LOOP_ROOT/Anchors/Anchor_Mia_TerminalReturn
MIN_LOOP_ROOT/Anchors/Anchor_NextResident_17F02
```

脚本：

```text
MinLoopStageAnchorController
```

骨架工具会自动挂好，并写入 3 条默认传送规则：

| 规则 | 默认阶段 | 目标 |
|---|---|---|
| `Mia 初始走廊站位` | `Corridor` | `Player_Mia_Controller` -> `Anchor_Mia_CorridorStart` |
| `陪伴单元复盘起点` | `SwitchingToCompanion` | `Companion_Controller` -> `Anchor_Companion_ReplayStart` |
| `Mia 回到终端站位` | `ReturningToTerminal` | `Player_Mia_Controller` -> `Anchor_Mia_TerminalReturn` |

第一版需要你把这些 Anchor 摆到正确位置：

| Anchor | 放哪里 |
|---|---|
| `Anchor_Mia_CorridorStart` | 17F 走廊开场位置 |
| `Anchor_Companion_ReplayStart` | 儿童房内陪伴单元第一人称复盘起点 |
| `Anchor_Mia_TerminalReturn` | 17F-01 终端前方，玩家看得到终端的位置 |
| `Anchor_NextResident_17F02` | 下一户方向或 17F-02 门口附近，默认只做参考 |

运行 `Auto Bind References` 后会自动把 Mia/Companion 根物体填到规则里。传送时会清空 Rigidbody 速度，并默认只对齐 Y 轴朝向。

## 4.6 阶段 Cue 表现触发

对象：

```text
MIN_LOOP_ROOT/FlowManagers/MinLoopStageCueController
MIN_LOOP_ROOT/Interactions/Feedback_ComfortReady
MIN_LOOP_ROOT/Interactions/Feedback_MorningReview
MIN_LOOP_ROOT/Interactions/Feedback_NextResidentGuide
```

脚本：

```text
MinLoopStageCueController
InteractionFeedbackController
```

骨架工具会自动挂好，并写入 3 条默认 Cue 规则：

| 规则 | 默认阶段 | 默认反馈 |
|---|---|---|
| `安抚点就绪反馈` | `WaitingForComfort` | `Feedback_ComfortReady` |
| `早晨回顾反馈` | `MorningReview` | `Feedback_MorningReview` |
| `下一户指引反馈` | `Complete` | `Feedback_NextResidentGuide` |

第一版可以只用这三个反馈对象做声音/发光。后续如果要加门、动画或更多声音：

| 字段 | 拖什么 |
|---|---|
| `Flow Controller` | `MinLoopFlowController` |
| `Rules / Feedback Objects` | 要播放的 `InteractionFeedbackController` |
| `Rules / Door Cues` | 要打开/关闭/锁定的 `SmartDoorController` |
| `Rules / Animator Cues` | 孩子、父母、门或终端的 Animator Trigger |
| `Rules / Audio Cues` | 一次性音效 `AudioSource` / `AudioClip` |
| `Rules / Cue Event` | 后续自定义脚本公开方法 |

如果只想让某个阶段开门，给该阶段规则加一条 `Door Cues`，拖门对象并选择 `Open`。如果只想让角色做动作，给该阶段规则加一条 `Animator Cues` 并填写 Trigger 名。

## 4.7 阶段灯光与氛围

对象：

```text
MIN_LOOP_ROOT/FlowManagers/MinLoopLightingStateController
MIN_LOOP_ROOT/Lighting/Light_Corridor_Warm
MIN_LOOP_ROOT/Lighting/Light_Replay_Night
MIN_LOOP_ROOT/Lighting/Light_Morning_Soft
```

脚本：

```text
MinLoopLightingStateController
```

骨架工具会自动挂好，并写入 3 条默认规则：

| 规则 | 默认阶段 | 默认灯 |
|---|---|---|
| `走廊/终端暖光` | `Corridor`、`AccessCard`、`ResidentSummary`、`ReturningToTerminal`、`DispositionChoice`、`Complete` | `Light_Corridor_Warm` |
| `昨夜复盘冷光` | `SwitchingToCompanion`、`CompanionReplay`、`WaitingForComfort`、`Comforting` | `Light_Replay_Night` |
| `早晨回顾柔光` | `MorningReview` | `Light_Morning_Soft` |

第一版可以直接用这三盏占位灯。后续如果你有正式灯光：

| 字段 | 拖什么 |
|---|---|
| `Flow Controller` | `MinLoopFlowController` |
| `Rules / Lights` | 对应阶段的一盏或多盏正式 Light |
| `Rules / Active Objects` | 可选，窗外晨光片、夜间雾效占位物等 |
| `Rules / Behaviours` | 可选，想随阶段开关的灯光脚本或特效脚本 |

如果你不希望脚本影响全局环境光或雾效，关闭对应规则里的 `Apply Ambient Color` 和 `Apply Fog`。

## 4.8 阶段环境声

对象：

```text
MIN_LOOP_ROOT/FlowManagers/MinLoopAudioStateController
MIN_LOOP_ROOT/Audio/Audio_Corridor_Ambience
MIN_LOOP_ROOT/Audio/Audio_Replay_Night_Ambience
MIN_LOOP_ROOT/Audio/Audio_Morning_Ambience
```

脚本：

```text
MinLoopAudioStateController
```

骨架工具会自动挂好，并写入 3 条默认规则：

| 规则 | 默认阶段 | 默认 AudioSource |
|---|---|---|
| `走廊/终端环境声` | `Corridor`、`AccessCard`、`ResidentSummary`、`ReturningToTerminal`、`DispositionChoice`、`Complete` | `Audio_Corridor_Ambience` |
| `昨夜复盘环境声` | `SwitchingToCompanion`、`CompanionReplay`、`WaitingForComfort`、`Comforting` | `Audio_Replay_Night_Ambience` |
| `早晨回顾环境声` | `MorningReview` | `Audio_Morning_Ambience` |

第一版可以没有 Clip，脚本不会报错，只是没有声音。后续如果你有正式环境声：

| 字段 | 拖什么 |
|---|---|
| `Flow Controller` | `MinLoopFlowController` |
| `Rules / Audio Sources` | 对应阶段的一条或多条 AudioSource |
| `Rules / Fallback Clip` | 可选，如果 AudioSource 本身没填 Clip，就用这里的 Clip |
| `Rules / Target Volume` | 该阶段目标音量 |
| `Rules / Fade In Seconds` / `Fade Out Seconds` | 淡入淡出时间 |

刷卡哔声、电梯叮咚、终端确认音继续用 `InteractionFeedbackController`，不要放到这里。

## 5. 儿童房复盘

对象：

```text
MIN_LOOP_ROOT/ReplayRoom_17F01/ReplaySequenceController
```

脚本：

```text
ReplaySequenceController
```

如果已经按命名规则运行过自动绑定，以下字段会自动填；如果没自动填，再手动拖：

| 字段 | 拖什么 |
|---|---|
| `Flow Controller` | `MinLoopFlowController` |
| `Subtitle Player` | `MinLoopSubtitlePlayer` |
| `Comfort Action` | `ComfortAction_Bedside` |
| `Child Actor` | 孩子身上的 `SimpleActorCueController` |
| `Mother Actor` | 母亲身上的 `SimpleActorCueController`，可选 |
| `Father Actor` | 父亲身上的 `SimpleActorCueController`，可选 |
| `Door Look Target` | `DoorLook_Target` |

保持：

```text
Seed Default Lines If Empty = 开启
Wait Before Door Look After Wake = 0.35 左右
Create Fallback Comfort Action If Missing = 开启
```

`Comfort Action` 推荐拖正式的 `ComfortAction_Bedside`。如果第一版还没摆这个对象，只要保持 `Create Fallback Comfort Action If Missing` 开启，运行时会自动生成 `Generated_ComfortAction_Bedside`，避免复盘卡在等待安抚阶段。

## 6. 唯一安抚操作

对象：

```text
MIN_LOOP_ROOT/ReplayRoom_17F01/ComfortAction_Bedside
```

脚本：

```text
ComfortActionInteractable
```

必须：

```text
BoxCollider 或其他 Collider
```

这是真正推荐的正式安抚点。若暂时没摆，`ReplaySequenceController` 可以运行时生成兜底安抚点；但正式演示仍建议使用这个对象，位置更可控。

如果自动绑定没有填，再手动拖字段：

| 字段 | 拖什么 |
|---|---|
| `Sequence Controller` | `ReplaySequenceController` |
| `Visual Root` | 床边提示图标/夜灯高亮，可选；不拖会自动生成小发光球 |
| `Create Fallback Visual If Missing` | 保持开启，第一版没有正式模型也能看到安抚点 |

保持：

```text
Available On Start = 关闭
```

进入等待安抚阶段后，Collider 和 `Visual Root` 会同时启用；执行安抚后会一起隐藏/禁用。

## 7. 孩子和父母

给 `Child_Actor`、`Mother_Actor`、`Father_Actor` 分别挂：

```text
SimpleActorCueController
```

孩子必须拖或确认：

| 字段 | 拖什么 |
|---|---|
| `Animator` | 孩子 Animator，可选 |
| `Look Root` | 孩子头部或根物体 |
| `Visible Root` | 孩子模型根物体 |
| `Pose Root` | 要做占位姿势的模型根或姿势父物体 |
| `Sleep Pose` | 没有 Animator 时的睡下姿势 |
| `Nightmare Wake Pose` | 没有 Animator 时的惊醒姿势 |
| `Comforted Pose` | 没有 Animator 时的重新睡下姿势 |
| `Morning Pose` | 没有 Animator 时的早晨/站立姿势，父母主要用这个 |

没有 Animator 也能跑；如果人物有 Animator 但没有对应 Trigger，也会自动走占位姿势。保持 `Use Fallback Poses When No Animator` 开启，脚本会用本地位移/旋转/缩放做睡下、惊醒、被安抚、早晨出现。你也可以先在 Scene 里手动摆好人物姿势，然后在组件右上角菜单里点 `Capture Sleep Pose From Current` 等命令记录姿势。

## 8. 门

大厅玻璃门或普通门使用：

```text
SmartDoorController
```

玻璃滑门推荐：

```text
Motion Mode = Slide
Moving Root = 玻璃门板
Open Local Position Offset = 根据方向填 X 或 Z，例如 X = 1.6
```

如果玻璃门要“玩家靠近自动开、离开自动关”，再建一个子物体：

```text
Lobby_GlassDoor/Lobby_GlassDoor_TriggerZone
```

挂：

```text
SmartDoorTriggerZone
```

保持：

```text
Target Door = Lobby_GlassDoor 上的 SmartDoorController
Open On Enter = 开启
Close On Exit = 开启
Close Delay = 0.25 到 0.6
Create Box Collider If Missing = 开启
Auto Mark Collider As Trigger = 开启
Fallback Box Size = 覆盖玩家经过玻璃门前的区域
```

如果 `Lobby_GlassDoor_TriggerZone` 是门的子物体，`Target Door` 可不拖，脚本和自动绑定工具都会尽量自动找父级门。

普通房门推荐：

```text
Motion Mode = Rotate
Moving Root = 门板或门轴父物体
Open Local Euler Offset = Y = 90
```

必须：

```text
门交互对象或子物体有 Collider
```

## 9. 电梯按钮 / 刷卡 / 提交反馈

使用：

```text
InteractionFeedbackController
```

推荐对象：

```text
Feedback_TerminalOpen
Feedback_AccessCard
Feedback_ReplayRequest
Feedback_DispositionSubmit
Feedback_ComfortReady
Feedback_MorningReview
Feedback_NextResidentGuide
Elevator_Button_17F
```

拖字段：

| 字段 | 拖什么 |
|---|---|
| `Audio Source` | 该对象上的 AudioSource |
| `Feedback Clip` | 叮咚/蜂鸣音效 |
| `Target Renderers` | 要发光的按钮或终端屏幕 Renderer |
| `Target Lights` | 要亮起的小灯，可选 |

如果只是被流程调用：

```text
Play Feedback On Interact = 关闭
```

如果玩家要直接按电梯按钮：

```text
Play Feedback On Interact = 开启
按钮对象需要 Collider
```

## 10. 场景检查

对象：

```text
MIN_LOOP_ROOT/MinLoopSceneValidator
```

步骤：

1. 先点击 `Auto Bind Scene References`。
2. 点击 `Resolve References`。
3. 点击 `Validate Scene Setup`。
4. Console 中没有 `[必须]` 错误，才进入 Play 测试。

## 11. Play Mode 最小测试

1. 玩家从走廊出生。
2. 看向 17F-01 终端，出现交互提示。
3. 按 `E`，出现刷工牌页。
4. 点击刷工牌，出现住户摘要。
5. 点击调出昨夜事件，黑场切到陪伴单元视角。
6. 陪伴单元视角左下角出现机器 HUD，显示 02:47、心率、噩梦判定和当前指令。
7. 儿童房中出现孩子噩梦醒来文本，“妈妈”必须出现。
8. 安抚点开放，玩家按 `E` 执行唯一安抚。
9. 孩子重新睡下，并再次看向门口方向。
10. 播放第二天早晨父母对话。
11. 黑场切回 Mia / 终端。
12. 选择 A/B 处置。
13. 显示信任度变化。
14. 等待安抚、早晨回顾、下一户完成阶段会分别触发对应 Feedback 对象。
15. 显示下一户指引。
